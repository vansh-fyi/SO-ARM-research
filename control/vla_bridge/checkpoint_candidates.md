# SmolVLA Checkpoint Candidates — Fetched Comparison

Generated by `control/vla_bridge/check_checkpoint.py` (Phase 11, Plan 11-03, Task 1).
Replaces the `[ASSUMED]` WebSearch-only candidate table in `11-RESEARCH.md` with real fetched `config.json` data (or an explicit fetch-failure note) for each candidate.

**Important caveat — the AR0144 "overhead" camera is one physical 2560x720 side-by-side stereo frame, not two independent camera feeds** (confirmed in `diagnostics/UAT/function/basic/UAT.md`, Step 4). Any candidate that expects exactly 2 named camera inputs could plausibly map to `wrist` + one-half-of-stereo (or the whole stereo frame passed as a single wide image) — a candidate expecting 3 named inputs would need a 3rd feed (a split of the stereo pair, or a dummy/empty camera) to fit this project's real 2-physical-camera rig.

| Repo ID | Fetch status | Camera keys found | Action dim found | Task description | 2-camera rig fit |
|---|---|---|---|---|---|
| `victorvanhalst/smolvla_so101_cube` | OK (config.json fetched to /Users/hp/.cache/huggingface/hub/models--victorvanhalst--smolvla_so101_cube/snapshots/0186f797cedaf0e13618e593dbdc2b2367543b45/config.json) | input_features image keys: ['observation.images.camera1', 'observation.images.camera2', 'observation.images.camera3'] (3 camera(s)); input_features state key `observation.state` shape=[6] | output_features `action` type=ACTION shape=[6] | not present in config.json | PARTIAL FIT — expects 3 camera inputs; needs a dummy/empty 3rd feed or a split of the AR0144 stereo frame into two halves |
| `majinwakeup30/smolvla_so101_stack_cube_v3_2_cameras` | OK (config.json fetched to /Users/hp/.cache/huggingface/hub/models--majinwakeup30--smolvla_so101_stack_cube_v3_2_cameras/snapshots/efd45e1e522ae86d9256921d06e52be977b8b5c1/config.json) | input_features image keys: ['observation.images.camera1', 'observation.images.camera2', 'observation.images.camera3'] (3 camera(s)); input_features state key `observation.state` shape=[6] | output_features `action` type=ACTION shape=[6] | not present in config.json | PARTIAL FIT — expects 3 camera inputs; needs a dummy/empty 3rd feed or a split of the AR0144 stereo frame into two halves |
| `lerobot/svla_so100_pickplace` | fetch failed: RepositoryNotFoundError: 401 Client Error. (Request ID: Root=1-6ab0551c-7c994f1266a3c2ec0f8fc541;fbe64a03-2ff9-49c9-8c92-539d0516de84) Repository Not Found for url: https://huggingface.co/lerobot/svla_so100_pickplace/resolve/main/config.json. Please make sure you specified the correct `repo_id` and `repo_type`. If you are trying to access a private or gated repo, make sure you are authenticated and your token has the required permissions. For more details, see https://huggingface.co/docs/huggingface_hub/authentication Invalid username or password.; file listing also failed: RepositoryNotFoundError: 401 Client Error. (Request ID: Root=1-6ab0551d-2828a4fc1f98556c57235c56;65888861-cc61-4f8e-b341-d97731847377) Repository Not Found for url: https://huggingface.co/api/models/lerobot/svla_so100_pickplace/tree/main?recursive=true&expand=false. Please make sure you specified the correct `repo_id` and `repo_type`. If you are trying to access a private or gated repo, make sure you are authenticated and your token has the required permissions. For more details, see https://huggingface.co/docs/huggingface_hub/authentication Invalid username or password. | n/a | n/a | n/a | UNKNOWN — could not parse camera count from config.json |
| `cn0303/smolvla-so101-strawberry-v3` | OK (config.json fetched to /Users/hp/.cache/huggingface/hub/models--cn0303--smolvla-so101-strawberry-v3/snapshots/36794faf1142e4db9be8b22f92c98beaf60193c0/config.json) | input_features image keys: ['observation.images.camera1', 'observation.images.camera2', 'observation.images.camera3', 'observation.images.empty_camera_0', 'observation.images.empty_camera_1'] (5 camera(s)); input_features state key `observation.state` shape=[6] | output_features `action` type=ACTION shape=[6] | not present in config.json | UNCLEAR FIT — expects 5 camera inputs, does not cleanly map to this project's 2-physical-camera rig |

**Note:** `config.json` schema is not standardized across community checkpoint authors — the camera-key/action-dim columns above report whatever was actually present in each fetched file, not a fixed assumed schema. Where a fetch failed, the fetch-failure reason (or the repo's raw file listing, if `config.json` itself was missing) is reported verbatim rather than omitting the candidate.
