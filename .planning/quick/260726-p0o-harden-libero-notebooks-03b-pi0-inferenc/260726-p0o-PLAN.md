---
phase: 03-vla-inference-loop
plan: 260726-p0o
type: execute
wave: 1
depends_on: []
files_modified:
  - libero/notebooks/03b-pi0-inference-smoketest.ipynb
autonomous: true
requirements: [VLA-04]
---

<objective>
Harden Notebook B against two live-confirmed failure modes before the next (L4) Colab run:

1. **Server stdout pipe deadlock (suspected cause of the 95-minute silent hang):** cell id `7a49e230` launches `serve_policy.py` with `stdout=subprocess.PIPE, stderr=subprocess.STDOUT` and nothing drains the pipe after startup — once the OS pipe buffer (~64KB) fills with the server's per-request/JAX logs mid-run, the server blocks on its next write and silently stops answering websocket requests. Fix: redirect server output to `/content/serve_policy.log`.

2. **openpi-client editable-install `.pth` activation (confirmed live, workaround tested by user):** openpi's `uv pip install -e .` side-effect-registers openpi-client as an editable install in the kernel's Python, making cell id `b8dacb5b`'s `pip install openpi-client -q` a no-op; the editable `__editable__*.pth` finder is only processed at interpreter startup, so the running kernel can't import it even though `pip show` succeeds. Fix: `site.addsitedir(sysconfig.get_paths()["purelib"])` + an import verification in the same cell.

Executed inline by the orchestrator (planner/executor subagents unavailable — spend limit); edits were fully specified before dispatch.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Patch cells b8dacb5b (openpi-client activation) and 7a49e230 (server log-file redirect)</name>
  <files>libero/notebooks/03b-pi0-inference-smoketest.ipynb</files>
  <action>
    Scripted JSON patch (object_pairs_hook=OrderedDict, ensure_ascii=True, indent=1, no sort_keys — per this repo's prior notebook-edit lessons). Replace cell b8dacb5b's source with the pip install + pip show + site.addsitedir + import-verification block. In cell 7a49e230: insert SERVE_LOG/_server_log setup before the Popen, change stdout=subprocess.PIPE to stdout=_server_log, replace the exited-early branch's server_proc.stdout.read() with a tail of the log file, and append a log-location hint to the success print. Clear outputs/execution_count on both edited cells. Touch nothing else.
  </action>
  <verify>
    <automated>python3 JSON validity + 24-cell count + greps: SERVE_LOG present, stdout=_server_log present, stdout=subprocess.PIPE absent, site.addsitedir present.</automated>
  </verify>
  <done>Both cells patched, all greps pass, notebook still 24 valid-JSON cells, no other cell changed.</done>
</task>

</tasks>
