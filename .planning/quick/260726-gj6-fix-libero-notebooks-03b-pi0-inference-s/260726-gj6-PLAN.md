---
phase: 03-vla-inference-loop
plan: 260726-gj6
type: execute
wave: 1
depends_on: []
files_modified:
  - libero/notebooks/03b-pi0-inference-smoketest.ipynb
  - .planning/phases/03-vla-inference-loop/03-03-PLAN.md
  - .planning/phases/03-vla-inference-loop/03-RESEARCH.md
autonomous: true
requirements: [VLA-04]

must_haves:
  truths:
    - "Notebook B's serve_policy.py launch cell sets OPENPI_DATA_HOME to local Colab disk (/content/openpi_data), not the Drive-mounted path, so gsutil -m cp -r no longer downloads pi05_libero's 11.6GB/16-sharded composite-object checkpoint through an unreliable Drive FUSE mount — the exact failure ('6 files/objects could not be transferred') the user hit live on Colab"
    - "A new cell force-reinstalls crcmod with its compiled C extension immediately before the serve_policy.py launch cell, per gsutil's own documented recommendation for composite-object downloads"
    - "03-03-PLAN.md's T-3-08 threat-model entry carries a dated 2026-07-26 amendment recording the Drive-to-local-disk revision and the explicitly accepted loss of restart-persistence; 03-RESEARCH.md's Pitfall 5 carries a matching cross-referenced resolution note"
  artifacts:
    - libero/notebooks/03b-pi0-inference-smoketest.ipynb
    - .planning/phases/03-vla-inference-loop/03-03-PLAN.md
  key_links:
    - "Notebook B's server-launch cell's os.environ[\"OPENPI_DATA_HOME\"] value is what serve_policy.py's download.maybe_download() reads before invoking gsutil -m cp -r against gs://openpi-assets/checkpoints/pi05_libero — repointing it to /content/openpi_data is the actual fix, not just documentation"
---

<objective>
Fix a confirmed real-world download-reliability failure in `libero/notebooks/03b-pi0-inference-smoketest.ipynb`: `serve_policy.py`'s first-run checkpoint download (`pi05_libero`, 11.6 GiB across 16 sharded/composite files) failed with `gsutil`'s `CommandException: 6 files/objects could not be transferred` when downloading into the Drive-mounted `OPENPI_DATA_HOME` path chosen in 03-03-PLAN.md's original T-3-08 mitigation. Switch `OPENPI_DATA_HOME` to local Colab disk (`/content/openpi_data`), add a `crcmod` C-extension reinstall cell per `gsutil`'s own documented recommendation, and record the accepted trade-off (loss of restart-persistence) in the planning docs.

Purpose: Drive FUSE mounts do not reliably support `gsutil -m cp -r`'s parallel writes for large composite objects at this scale — this is a well-known reliability gap, confirmed here by a real failing Colab run, not a hypothetical risk. The user was presented this exact trade-off (Drive persistence vs. reliable download) and chose local disk plus the crcmod fix.
Output: A corrected, JSON-valid `03b-pi0-inference-smoketest.ipynb` that downloads the checkpoint to local disk and reinstalls `crcmod`'s C extension first; a dated amendment to 03-03-PLAN.md's T-3-08 threat-model row; a matching dated resolution note on 03-RESEARCH.md's Pitfall 5 (its natural anchor for checkpoint-download mechanics).
</objective>

<execution_context>
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/workflows/execute-plan.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-vla-inference-loop/03-CONTEXT.md
@.planning/phases/03-vla-inference-loop/03-RESEARCH.md
@.planning/phases/03-vla-inference-loop/03-03-PLAN.md
</context>

<tasks>

<!-- planner-discipline-allow: /content/drive/MyDrive/SoARM-Research/openpi_data -->

<task type="auto">
  <name>Task 1: Notebook B — local-disk OPENPI_DATA_HOME + crcmod C-extension reinstall</name>
  <files>libero/notebooks/03b-pi0-inference-smoketest.ipynb</files>
  <read_first>
    libero/notebooks/03b-pi0-inference-smoketest.ipynb (the file being fixed — load with Python's `json` module, not a text editor, to avoid corrupting the notebook's JSON structure; edits below are keyed by each cell's stable `id` field, e.g. `"1b668f9c"` and `"7a49e230"`, which is more robust than positional index since a new cell is being inserted)
    .planning/quick/260726-epz-fix-libero-notebooks-03b-pi0-inference-s/260726-epz-SUMMARY.md (the prior quick fix to this same notebook earlier today — follow its exact pattern: re-serialize with `json.dump(nb, f, indent=1, ensure_ascii=True)` plus a trailing newline. That SUMMARY records that this file's original convention is `ensure_ascii=True`, NOT `ensure_ascii=False` — using `False` rewrites every existing em-dash/Greek-letter escape in the file and produces a diff many times larger than intended. Use `True` from the start this time.)
  </read_first>
  <action>
    Write a throwaway Python script (e.g. run via Bash as `python3 - <<'PY' ... PY`, or write it to a scratch path and run it, then discard the script — only the notebook diff should remain) that loads the notebook with `json.load`, applies exactly the three edits below keyed by cell `id`, and writes back with `json.dump(nb, f, indent=1, ensure_ascii=True)` followed by a trailing newline (`f.write("\n")`) to keep the git diff minimal and scoped. Do not hand-edit the raw JSON text with a text editor.

    Edit 1 — markdown cell `id="1b668f9c"` (the "### Start `serve_policy.py --env=LIBERO`" header, immediately before the server-launch code cell): its `source` list's last 4 entries are the paragraph beginning `` `OPENPI_DATA_HOME` is set to a Drive-backed path BEFORE starting the server `` and ending `` pattern for the HF token). `` — replace exactly those last 4 entries (keep the earlier `---`, heading, and T-3-06 localhost-bind paragraph unchanged) with new lines stating: (a) the cell below first force-reinstalls `crcmod` with its compiled C extension per gsutil's own documented recommendation for composite-object downloads (cite `"gsutil help crcmod"`), then starts the server; (b) an `**Amendment (2026-07-26):**` paragraph stating `OPENPI_DATA_HOME` now points to local Colab disk (`` `/content/openpi_data` ``) instead of the Drive-mounted path originally chosen for T-3-08's restart-persistence goal, because a real Colab run showed `gsutil -m cp -r` downloading `pi05_libero`'s 11.6 GiB, 16-sharded composite-object checkpoint to a Drive FUSE mount is unreliable (cite the exact observed error, `` `CommandException: 6 files/objects could not be transferred` ``) since Drive FUSE mounts do not reliably support gsutil's parallel composite-object writes at this scale; (c) an `**Accepted trade-off:**` paragraph stating T-3-08's original restart-persistence goal is no longer met — a Colab runtime restart now requires re-downloading the checkpoint from `gs://openpi-assets` again, accepted because local-disk GCS downloads are fast while Drive-mounted composite-object downloads are not reliable at this scale — and pointing to `03-03-PLAN.md`'s T-3-08 threat-model entry for the full amendment.

    Edit 2 — code cell `id="7a49e230"` (the server-launch cell containing `serve_cmd` and the port-polling loop): (a) replace its 3-line leading comment block (`` # T-3-08 mitigation: Drive-backed cache so a successful checkpoint download ``, `` # survives session restarts, mirroring Phase 1's Drive-based persistence ``, `` # pattern. ``) with a comment block dated `` # T-3-08 amendment (2026-07-26): `` explaining the same rationale as Edit 1(b)/(c) above in condensed form (Drive FUSE composite-object transfer failure observed on Colab; local disk chosen for reliability; restart-persistence knowingly dropped as the accepted trade-off; reference `03-03-PLAN.md`'s T-3-08 entry); (b) change the two lines `` os.makedirs("/content/drive/MyDrive/SoARM-Research/openpi_data", exist_ok=True) `` and `` os.environ["OPENPI_DATA_HOME"] = "/content/drive/MyDrive/SoARM-Research/openpi_data" `` so both occurrences of the Drive path string become `` /content/openpi_data `` — keep the `os.makedirs(...)` call and its `exist_ok=True` kwarg exactly as-is, only the path argument changes. Do not touch anything else in this cell: the T-3-06 bind-to-127.0.0.1 comment/lines, `SERVE_HOST`/`SERVE_PORT`, `serve_cmd`, the `Popen` call, the port-polling loop, or the final print/assert lines must remain byte-identical. (c) This cell's stored `outputs` (a `stdout` stream entry containing the full failing `gsutil` traceback plus an `error` entry) is the exact stale failure this plan fixes — set this cell's `outputs` to `[]` and `execution_count` to `null`.

    Edit 3 — insert one brand-new code cell immediately after cell `id="1b668f9c"` and before cell `id="7a49e230"` (i.e. it becomes the new cell directly beneath the just-updated markdown header, directly above the server-launch cell). Give it a fresh 8-hex-character `id` not colliding with any existing cell id in this notebook (e.g. derived from `uuid.uuid4().hex[:8]`), `cell_type: "code"`, `metadata: {}`, `execution_count: null`, `outputs: []`, and `source` lines: a short leading comment citing gsutil's own documented recommendation for composite-object/sliced downloads (`"gsutil help crcmod"`) and noting Colab already ships the compiler toolchain needed for the C extension so no `apt-get` step is required, followed by two shell lines: `` !pip uninstall -y crcmod -q `` then `` !pip install --no-cache-dir -U crcmod -q ``. Note in the comment that this alone does not guarantee the transfer succeeds — the primary fix is Edit 2's local-disk `OPENPI_DATA_HOME` change — but it removes one contributing factor gsutil itself flagged in the original failure output ("your crcmod installation isn't using the module's C extension").

    `crcmod` itself needs no Package Legitimacy checkpoint: it is a long-established, Google-authored CRC library that `gsutil` (already present in the Colab image) itself depends on for exactly this use case — the same "foundational infra, registry-metadata-gap is not a legitimacy signal" class 03-RESEARCH.md's audit already applied to `jax`/`flax`/`orbax-checkpoint`, not a novel or narrow-purpose package requiring a blocking human-verify step.

    Do not modify any other cell. Do not touch the localhost/127.0.0.1-only binding logic, `Pi0Backend` usage, `run_suite`/`eval_loop` calls, task/episode counts, the openpi-client legitimacy checkpoint cell, or the earlier `--env=LIBERO` fix from quick task `260726-epz` — that part is already correct and must not be reverted.
  </action>
  <acceptance_criteria>
    - `grep -c "/content/openpi_data" libero/notebooks/03b-pi0-inference-smoketest.ipynb` reports at least 2 occurrences (both `os.makedirs` and `os.environ` lines)
    - `grep -q "/content/drive/MyDrive/SoARM-Research/openpi_data" libero/notebooks/03b-pi0-inference-smoketest.ipynb` finds zero matches (old Drive path string fully absent, including from the cleared stale output)
    - `grep -q "pip uninstall -y crcmod" libero/notebooks/03b-pi0-inference-smoketest.ipynb` and `grep -q "pip install --no-cache-dir -U crcmod" libero/notebooks/03b-pi0-inference-smoketest.ipynb` both match
    - `python3 -c "import json; nb=json.load(open('libero/notebooks/03b-pi0-inference-smoketest.ipynb')); assert len(nb['cells'])==17"` confirms exactly one new cell was inserted
  </acceptance_criteria>
  <verify>
    <automated>grep -c "/content/openpi_data" libero/notebooks/03b-pi0-inference-smoketest.ipynb | awk '{exit ($1>=2)?0:1}' && ! grep -q "/content/drive/MyDrive/SoARM-Research/openpi_data" libero/notebooks/03b-pi0-inference-smoketest.ipynb && grep -q "pip uninstall -y crcmod" libero/notebooks/03b-pi0-inference-smoketest.ipynb && grep -q "pip install --no-cache-dir -U crcmod" libero/notebooks/03b-pi0-inference-smoketest.ipynb && python3 -c "
import json
nb = json.load(open('libero/notebooks/03b-pi0-inference-smoketest.ipynb'))
assert len(nb['cells']) == 17, f'expected 17 cells, got {len(nb[\"cells\"])}'
by_id = {c.get('id'): c for c in nb['cells']}
c11 = by_id['7a49e230']
assert c11['outputs'] == [], 'cell 7a49e230 outputs not cleared'
assert c11.get('execution_count') is None, 'cell 7a49e230 execution_count not cleared'
print('Notebook fix verified OK')
"</automated>
  </verify>
  <done>libero/notebooks/03b-pi0-inference-smoketest.ipynb downloads pi05_libero's checkpoint to local Colab disk (/content/openpi_data) instead of the unreliable Drive FUSE mount, force-reinstalls crcmod's compiled C extension immediately beforehand, stays valid JSON, and no longer carries the stale gsutil failure traceback in its server-launch cell's outputs.</done>
</task>

<task type="auto">
  <name>Task 2: Record the T-3-08 decision revision in 03-03-PLAN.md and 03-RESEARCH.md</name>
  <files>.planning/phases/03-vla-inference-loop/03-03-PLAN.md, .planning/phases/03-vla-inference-loop/03-RESEARCH.md</files>
  <read_first>
    .planning/phases/03-vla-inference-loop/03-03-PLAN.md (the `<threat_model>` STRIDE Threat Register table — locate the `T-3-08` row exactly)
    .planning/phases/03-vla-inference-loop/03-RESEARCH.md ("### Pitfall 5: openpi's checkpoint auto-download exhausting Colab session time/quota" under "## Common Pitfalls" — its natural anchor for checkpoint-download mechanics; the section immediately following it is "## Code Examples")
    .planning/quick/260726-epz-fix-libero-notebooks-03b-pi0-inference-s/260726-epz-SUMMARY.md (the established pattern for this repo: amend a decision/assumption record in place with a dated sub-note, cross-referencing the other file, without deleting or rewording the original text)
  </read_first>
  <action>
    In `03-03-PLAN.md`, within the STRIDE Threat Register table's `T-3-08` row, append (do not delete or reword the existing Mitigation Plan cell text — it remains the historical record) a new sentence to that same cell beginning `**Amendment (2026-07-26):**` stating: this mitigation was abandoned after a real Colab run demonstrated Drive FUSE composite-object downloads are unreliable at this scale (`pi05_libero`'s 11.6 GiB checkpoint, 16 sharded files) — the exact failure was `gsutil`'s `CommandException: 6 files/objects could not be transferred`. State the resolution: `OPENPI_DATA_HOME` now points to local Colab disk (`/content/openpi_data`), and the notebook additionally force-reinstalls `crcmod`'s compiled C extension per gsutil's own recommendation. State the accepted trade-off explicitly: restart-persistence is no longer provided — a session restart now re-downloads the checkpoint from `gs://openpi-assets`, accepted because local-disk downloads are fast and reliable while the Drive-mounted approach was not. Reference `libero/notebooks/03b-pi0-inference-smoketest.ipynb` as where this was implemented.

    In `03-RESEARCH.md`, directly beneath Pitfall 5's existing "**Warning signs:**" line (and before the "## Code Examples" heading that follows it), add a new paragraph beginning `**Resolved (2026-07-26):**` recording that this exact pitfall materialized on a real Colab run: the Drive-backed `OPENPI_DATA_HOME` path recommended in this pitfall's original "How to avoid" guidance failed with `gsutil`'s composite-object transfer error. State the resolution (local Colab disk, `/content/openpi_data`, plus the `crcmod` C-extension reinstall) and cross-reference `03-03-PLAN.md`'s T-3-08 amendment for the full threat-model-level rationale and accepted trade-off. Do not reword or delete the original Pitfall 5 text — it remains the historical record of the anticipated risk.
  </action>
  <acceptance_criteria>
    - The `T-3-08` row in `03-03-PLAN.md`'s STRIDE table still describes the original Drive-backed mitigation AND carries the new dated amendment sentence in the same cell
    - Pitfall 5 in `03-RESEARCH.md` still contains its original "What goes wrong"/"Why it happens"/"How to avoid"/"Warning signs" text AND carries the new dated "Resolved" paragraph immediately after it
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Amendment (2026-07-26)" .planning/phases/03-vla-inference-loop/03-03-PLAN.md && grep -q "/content/openpi_data" .planning/phases/03-vla-inference-loop/03-03-PLAN.md && grep -q "Resolved (2026-07-26)" .planning/phases/03-vla-inference-loop/03-RESEARCH.md && grep -q "/content/openpi_data" .planning/phases/03-vla-inference-loop/03-RESEARCH.md && echo OK</automated>
  </verify>
  <done>03-03-PLAN.md's T-3-08 threat-model row and 03-RESEARCH.md's Pitfall 5 both carry a dated 2026-07-26 amendment/resolution note recording the Drive-to-local-disk revision and the accepted loss of restart-persistence, cross-referencing each other, with all original text preserved as historical record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries are introduced by this plan. It repoints a local filesystem destination path (Drive-mounted FUSE mount -> local Colab VM disk) and reinstalls a foundational, already-present-in-the-dependency-chain package (`crcmod`, the same library `gsutil` itself already depends on); it does not add a new network endpoint, change any install source, or touch credential handling.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-10 | Denial of Service | `libero/notebooks/03b-pi0-inference-smoketest.ipynb` — `OPENPI_DATA_HOME` checkpoint download destination (supersedes T-3-08's original approach) | low | accept | This plan only relocates the checkpoint download destination from an unreliable Drive FUSE mount to local Colab disk and force-reinstalls `crcmod`'s compiled C extension; it does not change the install source, the localhost-only bind, or add any package requiring a new legitimacy review. Pre-existing threats from 03-03-PLAN.md's threat model (T-3-SC package legitimacy, T-3-06 localhost-only bind, T-3-07 pinned commit SHA) remain valid and unchanged. T-3-08's own mitigation text is amended in place (not replaced) to record this trade-off — see Task 2. |

</threat_model>

<verification>
- Task 1's automated check confirms `OPENPI_DATA_HOME`'s destination is now local Colab disk (`/content/openpi_data`, at least 2 occurrences), the old Drive-mounted path string is fully absent (including from the cleared stale output), the `crcmod` reinstall cell is present, the notebook remains valid JSON with exactly one new cell (17 total), and the server-launch cell's stale failing-run output/execution_count are cleared.
- Task 2's automated check confirms both `03-03-PLAN.md` (T-3-08 amendment) and `03-RESEARCH.md` (Pitfall 5 resolution note) carry the dated, cross-referenced revision record, with original text preserved.
- No live Colab re-run is part of this plan's scope — the user re-runs Notebook B on Colab as a follow-up to confirm the checkpoint now downloads successfully to local disk (this was already covered by 03-03-PLAN.md's Task 4 human-verify checkpoint, which this fix unblocks a second time).
</verification>

<success_criteria>
- `libero/notebooks/03b-pi0-inference-smoketest.ipynb` downloads `pi05_libero`'s checkpoint to local Colab disk instead of a Drive FUSE mount, force-reinstalls `crcmod`'s C extension first, stays valid JSON, and carries no stale error output.
- `03-03-PLAN.md`'s T-3-08 row and `03-RESEARCH.md`'s Pitfall 5 both carry a dated 2026-07-26 record of this real-world decision revision, cross-referencing each other, with the accepted restart-persistence trade-off stated explicitly.
- Nothing outside the notebook's `OPENPI_DATA_HOME`/crcmod cells and these two docs was touched (no changes to `Pi0Backend`, `run_suite`/`eval_loop`, the localhost-only bind, the openpi-client legitimacy checkpoint, or the `--env=LIBERO` fix from `260726-epz`).
</success_criteria>

<output>
Create `.planning/quick/260726-gj6-fix-libero-notebooks-03b-pi0-inference-s/260726-gj6-SUMMARY.md` when done
</output>