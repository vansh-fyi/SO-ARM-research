---
phase: 03-vla-inference-loop
plan: 260726-io0
type: execute
wave: 1
depends_on: []
files_modified:
  - libero/notebooks/03b-pi0-inference-smoketest.ipynb
  - .planning/phases/03-vla-inference-loop/03-CONTEXT.md
autonomous: true
requirements: [VLA-04]

must_haves:
  truths:
    - "Notebook B's kernel installs the LIBERO simulation stack (robosuite/mujoco/bddl/gym) before the VLA-04 proof cell runs, so `from libero.libero.envs import OffScreenRenderEnv` no longer raises ModuleNotFoundError: No module named 'robosuite'"
    - "The new install order (EGL apt packages -> robosuite/mujoco/bddl/gym pip install -> numpy purge+pin+ABI-probe as the LAST step, before restart) mirrors Notebook A's already-proven Block A pattern exactly, avoiding a third occurrence of the 'numpy.dtype size changed' ABI mismatch inside this same kernel"
    - "A mandatory runtime-restart step separates the new Block A2 installs from openpi's own existing Block A install and the VLA-04 proof cell that follow, consistent with Notebook A's own restart discipline"
    - "03-CONTEXT.md's D-06 records this was a genuine 03-03-PLAN.md planning gap (not a D-06 torch/transformers/jax conflict) and documents the fix, per the established same-day amendment format"
  artifacts:
    - libero/notebooks/03b-pi0-inference-smoketest.ipynb (24 cells: 19 original + 5 new Block A2 cells)
    - .planning/phases/03-vla-inference-loop/03-CONTEXT.md (D-06 amendment)
  key_links:
    - "The 5 new Block A2 cells sit between the existing GPU-assertion cell (index 3) and the existing 'BLOCK A: openpi install' markdown header (originally index 4, now index 9), so the LIBERO sim stack exists on-disk in this kernel before openpi's own Block A and before the VLA-04 proof cell (originally index 17, now index 22) import robosuite transitively"
    - "The new numpy==1.26.4 pin stays compatible with openpi's own numpy>=1.22.4,<2.0.0 pyproject.toml constraint, and does not reintroduce D-06's torch/transformers conflict because openpi's torch/transformers/jax stack lives in its own isolated uv venv at /content/openpi, never touching this kernel's numpy/robosuite/mujoco"
---

<objective>
Fix `libero/notebooks/03b-pi0-inference-smoketest.ipynb` (Notebook B), which crashes at its VLA-04 proof cell with `ModuleNotFoundError: No module named 'robosuite'` on live Colab. Root cause: Notebook B was only ever given cells that install `openpi`/`openpi_client` — it never installed the LIBERO simulation stack (`robosuite`, `mujoco`, `bddl`, `gym`) that `libero.libero.envs.OffScreenRenderEnv` hard-imports (`bddl_base_domain.py` does `import robosuite.utils.transform_utils as T`). This is a genuine planning gap in 03-03-PLAN.md's Notebook B task description, not a D-06 (Claude's-discretion) torch/transformers/jax conflict — robosuite/mujoco/bddl/gym participate in neither stack's version constraints.

Insert a new **Block A2** into Notebook B, mirroring Notebook A's already Colab-A100-sign-off-approved Block A pattern (`libero/notebooks/01-colab-env-setup.ipynb` cells 4/6/11/12) exactly: EGL apt packages -> robosuite/mujoco/bddl/gym pip install -> numpy purge+pin+ABI-probe as the LAST step -> mandatory restart. This order is required because Notebook A's own `01-DEBUG-HISTORY.md` already documented and fixed a "mixed numpy ABI" failure mode from pinning numpy before other installs settle; replicating the proven order avoids a third occurrence of that exact class of failure (already hit twice today: once fixed via 260726-hw8) inside this same kernel.

Then amend `03-CONTEXT.md`'s D-06 (per D-06) with a dated amendment documenting this was a planning gap, not a D-06 conflict, following the same "**Amendment (2026-07-26):**" nested-bullet format already used under D-07 two bullets below it in the same file.

Purpose: Unblock the VLA-04 live-Colab proof (the interface-swap smoke test for π0/openpi) without reintroducing D-06's torch/transformers/jax version conflict — robosuite/mujoco/bddl/gym are safe to add to this kernel because openpi's own `pyproject.toml` pins `numpy>=1.22.4,<2.0.0` (satisfied by the same `numpy==1.26.4` pin Notebook A already proved works with `robosuite==1.4.0`/`mujoco` on live Colab A100), and openpi's actual conflicting stack (`torch`==2.7.1/`transformers`==4.53.2/`jax`) lives in its own isolated `uv` venv at `/content/openpi`, never touching this kernel's `numpy`/`robosuite`/`mujoco`.
Output: `libero/notebooks/03b-pi0-inference-smoketest.ipynb` grows from 19 to 24 cells (5 new Block A2 cells inserted at index 4-8); all other existing cells preserved byte-for-byte, shifted down by 5. `03-CONTEXT.md`'s D-06 gains a dated amendment sub-bullet.
</objective>

<execution_context>
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/workflows/execute-plan.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-vla-inference-loop/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert Block A2 (LIBERO sim stack) into Notebook B, then amend D-06</name>
  <files>libero/notebooks/03b-pi0-inference-smoketest.ipynb, .planning/phases/03-vla-inference-loop/03-CONTEXT.md</files>
  <read_first>
    libero/notebooks/03b-pi0-inference-smoketest.ipynb (current 19-cell structure — cell index 3 is the GPU assertion ending "GPU OK — target A100 tier..."; cell index 4 is the "BLOCK A: openpi install" markdown header; cell index 17 is the VLA-04 proof cell that currently crashes on `from libero.libero.envs import OffScreenRenderEnv`)
    libero/notebooks/01-colab-env-setup.ipynb cells at index 4 (EGL apt packages), 6 (mujoco/robosuite/bddl/gym + numpy-last install), 11 (numpy purge+pin+ABI-probe final gate), 12 (restart markdown) — the already Colab-A100-sign-off-approved Block A pattern this task mirrors
    .planning/phases/03-vla-inference-loop/03-CONTEXT.md (D-06 and D-07's existing "**Amendment (2026-07-26):**" nested-bullet format, to match style)
  </read_first>
  <action>
    Part 1 — Notebook B structural edit. Do NOT hand-edit the raw notebook JSON with the Edit tool (unicode-escaped source lines and JSON quote-escaping make manual text matching unreliable). Instead write and run a short Python script via Bash that:

    1. Loads `libero/notebooks/03b-pi0-inference-smoketest.ipynb` with `json.load`.
    2. Builds 5 new cell dicts. For each cell, write its raw text as an ordinary Python multi-line string with NO trailing newline character at the very end, then derive its `"source"` list via `text.splitlines(keepends=True)` — this reproduces nbformat's own line-list convention (every line ends in `\n` except the final line, which has none) with no manual line-splitting. Assign each new cell a fresh unique id via `uuid.uuid4().hex[:8]` (do not reuse any of the 19 existing ids). Use `"metadata": {}` for all 5. Code cells get `"execution_count": None` and `"outputs": []`; markdown cells get neither key (matches every existing markdown cell in this file — verified via read_first).
    3. The 5 cells, IN ORDER, with EXACT verbatim content (transcribe precisely — do not paraphrase or "improve" wording; this content was derived by directly reading Notebook A's proven equivalent cells):

    CELL A (markdown) — the Block A2 explainer, replaces the gap explanation and mirrors Notebook A's own Block A framing prose:
    "---\n\n## BLOCK A2: LIBERO simulation stack (robosuite/mujoco/bddl)\n\nNotebook B's kernel was only ever set up with **openpi**'s own dependency\nstack — it never installed the LIBERO simulation stack (`robosuite`,\n`mujoco`, `bddl`, `gym`) that `libero.libero.envs.OffScreenRenderEnv` needs\nto actually run episodes. This gap was discovered live 2026-07-26 when the\nVLA-04 cell below crashed with `ModuleNotFoundError: No module named\n'robosuite'`.\n\nThis is safe to add alongside openpi: `openpi`'s own `pyproject.toml` pins\n`numpy>=1.22.4,<2.0.0`, which is satisfied by the same `numpy==1.26.4` pin\nNotebook A/Phase 1 already proved works with `robosuite==1.4.0`/`mujoco` on\nlive Colab A100 (sign-off 2026-07-18). openpi's `torch`/`transformers`/`jax`\nstack (the actual D-06 conflict) lives in its own isolated `uv` venv at\n`/content/openpi` and never touches this kernel's `numpy`/`robosuite`/\n`mujoco`, so there is no repeat of D-06's conflict here.\n\nFollowing Notebook A's proven pattern exactly: install the EGL system\npackages, then `robosuite`/`mujoco`/`bddl`/`gym`, then purge-and-pin `numpy`\nas the LAST step before a mandatory runtime restart — this order avoids the\n\"mixed numpy ABI\" failure mode Phase 1 already diagnosed and fixed\n(`01-DEBUG-HISTORY.md`).\n\nRun all 3 cells below top to bottom, then restart per the STOP cell that\nfollows.\n\n---"

    CELL B (code) — EGL apt packages:
    "# Step: EGL system packages (Block A2 — LIBERO simulation stack)\n# Must run BEFORE pip mujoco install.\n!apt-get update -qq\n!apt-get install -y -q --fix-missing \\\n    libglfw3 \\\n    libglew-dev \\\n    libosmesa6-dev \\\n    libgles2 \\\n    libglvnd0 \\\n    libegl-dev \\\n    libegl1 \\\n    libgl1-mesa-glx"

    CELL C (code) — robosuite/mujoco/bddl/gym pip install:
    "# Install LIBERO's simulation stack (robosuite/mujoco/bddl/gym) — never installed\n# in this kernel before (Notebook B only ever installed openpi's own stack).\n# Versions match Notebook A's Phase 1 EXPECTED table (already proven working\n# together on live Colab A100, sign-off 2026-07-18).\nimport subprocess, sys\n\npkgs = [\"mujoco==3.3.2\", \"robosuite==1.4.0\", \"bddl==1.0.1\", \"gym==0.25.2\"]\nsubprocess.run([sys.executable, \"-m\", \"pip\", \"install\", *pkgs, \"-q\"], check=True)\nprint(f\"Installed: {', '.join(pkgs)}\")"

    CELL D (code) — numpy final gate (must run LAST, before restart):
    "# Block A2 final gate — numpy ABI enforcement (run LAST, after the installs\n# above, before restart). Mirrors Notebook A/Phase 1's Block A final numpy\n# gate cell — same purged-reinstall-probe pattern, same exact pin, to avoid\n# repeating the \"numpy.dtype size changed\" ABI mismatch (discovered live\n# 2026-07-26, quick task 260726-hw8) inside THIS kernel's own cells this time.\nimport glob, os, shutil, subprocess, sys, sysconfig\nimport importlib.metadata\n\nPURELIB = sysconfig.get_paths()[\"purelib\"]\nNUMPY_PIN = \"numpy==1.26.4\"  # matches Notebook A's proven-working pin; also satisfies openpi's own numpy>=1.22.4,<2.0.0 constraint (though openpi's pin only applies inside its isolated `uv` venv at /content/openpi, not this kernel)\n\nfor _ in range(3):\n    try:\n        importlib.metadata.version(\"numpy\")\n    except importlib.metadata.PackageNotFoundError:\n        break\n    subprocess.run([sys.executable, \"-m\", \"pip\", \"uninstall\", \"-y\", \"numpy\"], capture_output=True)\n\nfor _pattern in (\"numpy\", \"numpy-*.dist-info\", \"numpy.libs\"):\n    for _path in glob.glob(os.path.join(PURELIB, _pattern)):\n        if os.path.isdir(_path):\n            shutil.rmtree(_path)\n        else:\n            os.remove(_path)\n        print(f\"Removed → {_path}\")\n\n_r = subprocess.run([sys.executable, \"-m\", \"pip\", \"install\", NUMPY_PIN, \"--no-cache-dir\", \"-q\"], capture_output=True, text=True)\nif _r.returncode != 0:\n    raise RuntimeError(f\"pip install {NUMPY_PIN} failed:\\n{_r.stderr}\")\n\n_probe = subprocess.run([sys.executable, \"-c\", \"import numpy; import numpy.random; numpy.random.rand(3); print('OK')\"], capture_output=True, text=True)\nif _probe.returncode != 0 or \"OK\" not in _probe.stdout:\n    raise RuntimeError(f\"numpy ABI probe FAILED after clean {NUMPY_PIN} install:\\n{_probe.stderr}\")\nprint(f\"numpy ABI gate: PASS — {NUMPY_PIN} installed cleanly, numpy.random C extension loads OK in a fresh subprocess.\")"

    CELL E (markdown) — mandatory restart stop:
    "---\n\n## *** STOP — Restart runtime now ***\n\nGo to: **Runtime > Restart session**, then continue below.\n\n**After restart, re-run cells 1–3 first** (Drive mount/unzip, path setup, GPU\nassertion) — the kernel restart wipes Python variables (`REPO_ROOT`,\n`LIBERO_PKG`, `TASKS`, etc.) even though Drive stays mounted and `/content`\nfiles persist. Do NOT re-run the Block A2 cells above (robosuite/mujoco/bddl\ninstall + numpy pin) — those already completed and are on-disk; re-running\nthem is redundant, not required.\n\nThen continue with **BLOCK A: openpi install** below.\n\n---"

    4. Insert the 5 cells at index 4 via `nb["cells"][4:4] = new_cells` — this pushes the current index-4 cell ("BLOCK A: openpi install" markdown header) and every cell after it down by 5 without modifying their content.
    5. Re-serialize with `json.dump(nb, f, indent=1, sort_keys=True, ensure_ascii=True)` followed by a single trailing `"\n"` — matches this file's existing serialization exactly (confirmed: every existing cell's dict keys are already alphabetically sorted, and existing em-dash/π characters are already `\uXXXX`-escaped, so `sort_keys=True, ensure_ascii=True` reproduces the current on-disk format with no incidental diff noise).
    6. Delete the throwaway script after running it; it is not a repo artifact.

    Do not modify any byte of the 19 pre-existing cells (0-3 and what becomes 9-23) — only their position in the `cells` array changes.

    Part 2 — D-06 amendment. In `.planning/phases/03-vla-inference-loop/03-CONTEXT.md`, immediately after the existing D-06 bullet's final sentence ("...same class of version-conflict reason."), append a nested sub-bullet matching D-07's own "**Amendment (2026-07-26):**" indentation/format two bullets below it. New sub-bullet content: state that Notebook B's kernel was missing the LIBERO simulation stack entirely (robosuite/mujoco/bddl/gym) — discovered live 2026-07-26 via the VLA-04 cell's `ModuleNotFoundError: No module named 'robosuite'` (`OffScreenRenderEnv`'s import chain hard-imports `robosuite.utils.transform_utils`); clarify this is NOT a D-06 conflict (D-06 is specifically about torch/transformers/jax version incompatibility between OFT and π0, which robosuite/mujoco/bddl/gym do not participate in) but a genuine planning gap in 03-03-PLAN.md's original Notebook B task description, which never specified installing the LIBERO sim stack; record the fix as a new Block A2 mirroring Notebook A's proven Block A pattern (EGL apt packages → robosuite/mujoco/bddl/gym pip install → numpy==1.26.4 purge+pin+ABI-gate → mandatory restart), confirmed compatible with openpi's own `numpy>=1.22.4,<2.0.0` pyproject.toml constraint (verified by fetching openpi's live pyproject.toml) and safe because openpi's torch/transformers/jax stack is isolated in its own uv venv, never touching this kernel's numpy/robosuite/mujoco.
  </action>
  <verify>
    <automated>cd /Users/hp/Desktop/Work/Repositories/SoARM-Research && python3 -c "import json; json.load(open('libero/notebooks/03b-pi0-inference-smoketest.ipynb'))" && python3 -c '
import json
nb = json.load(open("libero/notebooks/03b-pi0-inference-smoketest.ipynb"))
cells = nb["cells"]
assert len(cells) == 24, f"expected 24 cells, got {len(cells)}"
joined = "\n".join("".join(c["source"]) for c in cells)
needles = ["mujoco==3.3.2", "robosuite==1.4.0", "bddl==1.0.1", "gym==0.25.2", "NUMPY_PIN = \"numpy==1.26.4\"", "Restart runtime now"]
missing = [n for n in needles if n not in joined]
assert not missing, f"missing needles: {missing}"
vla04_src = "".join(cells[22]["source"])
assert "from libero.libero.envs import OffScreenRenderEnv" in vla04_src, "VLA-04 proof cell (index 22) content changed unexpectedly"
print("notebook OK")
' && grep -c "Amendment (2026-07-26)" .planning/phases/03-vla-inference-loop/03-CONTEXT.md | awk '{exit ($1>=2)?0:1}' && grep -q "Block A2" .planning/phases/03-vla-inference-loop/03-CONTEXT.md && echo ALL_OK</automated>
  </verify>
  <done>
    `libero/notebooks/03b-pi0-inference-smoketest.ipynb` is valid JSON with exactly 24 cells: the 19 original cells preserved byte-for-byte (now at indices 0-3 and 9-23), plus 5 new Block A2 cells at indices 4-8 (markdown explainer, EGL apt install, robosuite/mujoco/bddl/gym pip install, numpy purge+pin+ABI-gate, restart-stop markdown) in that order. The original VLA-04 proof cell (now index 22) is unchanged and still imports `OffScreenRenderEnv`. `03-CONTEXT.md`'s D-06 has a new "**Amendment (2026-07-26):**" sub-bullet documenting the planning-gap root cause and the Block A2 fix, matching D-07's existing amendment format.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Colab VM apt/pip -> Ubuntu package mirrors / PyPI (EGL libs, robosuite/mujoco/bddl/gym, numpy) | Same install sources Notebook A already uses in its own sign-off-approved Block A; no new registry or mirror introduced |
| Notebook B kernel `numpy`/`robosuite`/`mujoco` <-> openpi's isolated `uv` venv at `/content/openpi` | Two separate dependency stacks in the same Colab VM; must stay non-interacting (D-06) — this plan's numpy==1.26.4 pin is chosen specifically to satisfy openpi's own numpy>=1.22.4,<2.0.0 constraint without merging the environments |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-12 | Tampering | `pip install mujoco==3.3.2 robosuite==1.4.0 bddl==1.0.1 gym==0.25.2` in Notebook B's kernel (Block A2) | low | accept | These are not new packages requiring a fresh legitimacy review — they are the exact same packages/versions already reviewed in `01-RESEARCH.md`'s Package Legitimacy Audit (all four verdicted SUS-by-unknown-downloads-only, approved without a checkpoint as legitimate research-lab packages from ARISE-Initiative/Stanford/Google DeepMind/Farama) and already running live on Colab A100 in Notebook A since sign-off 2026-07-18. Reusing identical pins in a second kernel does not introduce new supply-chain exposure. |
| T-3-13 | Denial of Service | Block A2's numpy purge+pin+ABI-probe cell, run in the wrong order relative to the pip installs above it | low | mitigate | Cell ordering is fixed and documented (EGL apt -> robosuite/mujoco/bddl/gym pip install -> numpy gate as the LAST cell before restart), mirroring Notebook A's own proven Block A order exactly (`01-DEBUG-HISTORY.md`'s documented "mixed numpy ABI" failure mode). The numpy gate cell raises `RuntimeError` on ABI-probe failure rather than silently continuing, so a misordering surfaces immediately instead of deferring to a confusing downstream import crash. |

</threat_model>

<verification>
- Task 1's automated check confirms the notebook is valid JSON, has exactly 24 cells, contains all 4 new package pins (`mujoco==3.3.2`, `robosuite==1.4.0`, `bddl==1.0.1`, `gym==0.25.2`) plus the `NUMPY_PIN` gate and the restart-stop markdown text, and that the original VLA-04 proof cell (now index 22) is untouched.
- Task 1's automated check also confirms `03-CONTEXT.md` gained a second "Amendment (2026-07-26)" occurrence (D-06's, alongside D-07's pre-existing one) referencing "Block A2".
- No live Colab re-run is part of this plan's scope — the user re-runs Notebook B from the top (through the new Block A2 cells, the restart, then the existing Block A and VLA-04 cells) as a follow-up to confirm `robosuite` now imports successfully and the VLA-04 smoke test proceeds past the point it previously crashed.
</verification>

<success_criteria>
- `libero/notebooks/03b-pi0-inference-smoketest.ipynb` has 24 valid cells; the 5 new Block A2 cells install the LIBERO simulation stack (EGL apt packages, robosuite/mujoco/bddl/gym pip install, numpy purge+pin+ABI-gate) in the proven order, followed by a mandatory restart-stop cell, positioned before openpi's own Block A and the VLA-04 proof cell.
- All 19 pre-existing cells are unmodified in content, only shifted in position.
- `.planning/phases/03-vla-inference-loop/03-CONTEXT.md`'s D-06 documents this was a 03-03-PLAN.md planning gap (not a D-06 conflict) and records the Block A2 fix, in the file's established same-day amendment format.
</success_criteria>

<output>
Create `.planning/quick/260726-io0-add-a-block-a2-to-libero-notebooks-03b-p/260726-io0-SUMMARY.md` when done
</output>