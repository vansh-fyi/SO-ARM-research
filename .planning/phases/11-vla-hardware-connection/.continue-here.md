---
context: phase
phase: 11-vla-hardware-connection
task: 3
total_tasks: 4
status: in_progress
last_updated: 2026-09-22T05:49:51.378Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: never trust cv2 camera index identity without cross-checking — this rig has a laptop built-in webcam that OpenCV happily opens alongside the two real cameras, and resolution alone cannot distinguish the wrist camera from the built-in cam (both report ~1920x1080). The first e-stop test attempt this phase silently recorded the laptop's own webcam as "the wrist camera" because of this. `control/detect_devices.py` (Task 2, now built) fixes this structurally — excludes any camera named via `system_profiler SPCameraDataType` as "FaceTime"/built-in, and disambiguates the AR0144 by its unique 2560x720 resolution. Always run `python detect_devices.py` fresh after any USB replug, don't reuse a stale `device_map.json`.
- [ ] CONSTRAINT: worktree-merge fork-point base — `worktree.cleanup-wave`'s `expected_base` must be the ORIGINAL fork-point commit (the master HEAD at dispatch time), not the agent's current HEAD after it advances through checkpoint continuations. This bit twice during Wave 1 merging earlier in this phase.
- [ ] CONSTRAINT: main-checkout `control/.venv` deps after any worktree merge — worktree-created venvs are gitignored and never survive a merge back to the main checkout. After merging any wave whose executor installed new packages inside its own worktree venv, manually `pip install` the same packages into the MAIN checkout's `control/.venv` before running tests there.

**Do not proceed until all three boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Silent camera substitution | `_probe_camera_indices()`'s old behavior treated "any cv2 index that opens" as a valid camera, with no identity check — silently substituted the laptop's built-in webcam for a real sensor | blocking | `control/detect_devices.py` now excludes built-in cameras by name (`system_profiler`) and cross-validates by resolution; `run_vla_episode.py` errors clearly if neither `device_map.json` nor explicit `--camera` flags are available, instead of silently falling back to the old any-device-that-opens probe |
| Bridge camera1 never wired | `robot_client.py`'s `connect_bridge()` only populated `camera2`/`camera3` (AR0144 stereo split) in the bridge's observation dict — `camera1` (wrist) was silently missing, contradicting 11-04-SUMMARY.md's "three genuinely distinct real camera views" claim | blocking | Fixed in Task 2 (commit `fa99468`) — `camera1` is now genuinely wired. UNVERIFIED risk remains: whether the wrist camera's driver tolerates the resulting second concurrent `cv2.VideoCapture` open on that index (separate from `run_vla_episode.py`'s own recording capture) — watch for an exception at startup, before the bridge handshake, during Task 3's live run |
| worktree `expected_base` = current HEAD instead of fork point | Recording an executor's own returned `expected_base` (current HEAD after checkpoint continuations) into the wave manifest, instead of the master commit the worktree actually forked from | advisory (Wave 1 only, already resolved) | Always use the master HEAD captured immediately before `Agent()` dispatch as `expected_base`, never the value an executor self-reports later |

**Severity values:** `blocking` — resuming agent must pass understanding check before proceeding. `advisory` — important context, does not gate resumption.

<current_state>
Phase 11 (VLA Hardware Connection) is on its final plan, 11-05, now expanded to **4 tasks** (a device-auto-discovery task was inserted mid-session, between the original Task 1/e-stop and Task 2/live-episode, after live testing surfaced real bugs — camera identity confusion and a bridge wiring gap). Tasks 1 and 2 are done. **This session paused at Task 3's checkpoint (the full live VLA-driven episode against real hardware + Colab + ngrok) — awaiting the user's report. No response received yet.**

The worktree is still open and NOT yet merged to master:
- Path: `.claude/worktrees/agent-afe9e290a91f86d2e`
- Branch: `worktree-agent-afe9e290a91f86d2e`
- Current HEAD: `6ca93fe` (Task 2 complete: device auto-discovery + camera1 fix, 77/77 `control/` tests passing)
- Has been merged FROM master once already (commit `52396c8`) to pick up the mid-phase plan amendment — this is fine, expected, not a conflict.
</current_state>

<completed_work>

Completed Tasks (11-05):
- Task 1: E-stop hardware-in-the-loop verification — Done. Two attempts: first (`estop_check_001`) silently failed to actually trigger the interrupt due to the camera-identity bug (script ran to `max_steps_reached`, not `keyboard_interrupt`) — root-caused live to the built-in webcam substitution. Second attempt (`estop_check_002`), after both real cameras were connected and explicit `--camera` flags used, produced a genuine `keyboard_interrupt` termination with confirmed arm return-to-start. Approved.
- Task 2: Device auto-discovery (`device_map.json`) + bridge `camera1` wiring fix — Done. `control/detect_devices.py` (excludes built-in cameras by name, disambiguates AR0144 by resolution, interactive fallback for genuine ambiguity) + `control/test_detect_devices.py` (24 tests) + `camera1` wiring fix in `robot_client.py`. 77/77 `control/` tests passing. Commits `fa99468`/`6ca93fe`.

Completed Waves (prior, all merged to master):
- Wave 1: 11-01 (action contract + safety validator), 11-03 (checkpoint selection + Colab bridge docs)
- Wave 2: 11-02 (JSON Lines I/O logger + scripted-dry-run harness)
- Wave 3: 11-04 (real Colab-bridged SmolVLA wiring + split-stereo camera wrapper)
</completed_work>

<remaining_work>

- Task 3 (11-05): Full VLA-driven episode, recorded end-to-end — **awaiting the user's report**. Checkpoint text already presented (measure cube, run `detect_devices.py`, start Colab, run the live episode command, report outcome). User has not yet replied.
- Task 4 (11-05): Findings write-up + go/no-go recommendation (`control/vla_bridge/FINDINGS.md`), grounded in Task 3's actual observed outcome — must restate D-05 (no reasoning trace) and reference the device-identity/camera1 bugs found this session as part of "upstream issues hit" (even though neither is `lerobot#2210` itself)
</remaining_work>

<decisions_made>

- Checkpoint: `victorvanhalst/smolvla_so101_cube`; task = red cube pick; camera mapping = wrist + AR0144 stereo split — carried forward from the prior pause, unchanged this session.
- Built `control/detect_devices.py` because live testing found ports AND camera indices drift across sessions/replugs even for the same physical hardware — not a one-off fluke, a recurring operational problem the user explicitly asked to solve structurally rather than re-debug every session.
- Camera identification strategy: exclude by `system_profiler SPCameraDataType` name (built-in cameras reliably report "FaceTime HD Camera" on this Mac) + disambiguate by resolution (AR0144's 2560x720 is unique) + interactive cover-the-lens fallback only when both signals are insufficient. Chosen over relying on cv2 index order alone, which is exactly what caused the original silent-substitution bug.
- Fixed the `camera1` (wrist) bridge-wiring gap by giving it its own independent camera open via lerobot's camera-config route, accepting the unverified risk of a second concurrent device open, rather than attempting a more invasive shared-capture refactor without hardware to test it against.
</decisions_made>

<blockers>
- Task 3's checkpoint is awaiting the user's report of the full live episode run. No workaround — requires physical robot + Colab + ngrok tunnel all running together. This is the phase's centerpiece verification.
</blockers>

## Required Reading (in order)
1. `.planning/phases/11-vla-hardware-connection/11-05-PLAN.md` — Task 3's exact checkpoint structure (now Task 3 of 4, not Task 2 of 3 — the plan was amended mid-session)
2. `.planning/phases/11-vla-hardware-connection/11-05-SUMMARY.md` (in the worktree) — Task 1/2's full findings, including the camera-identity root cause
3. `control/vla_bridge/policy_server_launch.md` — Colab launch instructions for Task 3
4. `.planning/phases/11-vla-hardware-connection/11-CONTEXT.md` — D-01 through D-05, all still authoritative

## Infrastructure State
- Open worktree: `.claude/worktrees/agent-afe9e290a91f86d2e` (branch `worktree-agent-afe9e290a91f86d2e`, HEAD `6ca93fe`) — reattach to this on resume, do not create a new one.
- No running servers, no Colab session active, no ngrok tunnel active (Task 3 hasn't started).
- Main checkout `control/.venv` has: `pytest==8.3.4`, `grpcio==1.84.0`, `protobuf==7.36.2` added on top of the pre-existing real hardware-control venv. The Task 2 work happened only in the worktree's own venv — if merging to master later, remember to re-verify/install whatever Task 2 needed there too (same recurring gotcha as prior waves).
- `control/device_map.json` (if it exists on disk) reflects whatever `detect_devices.py` last wrote — it's gitignored, machine-local, and can go stale after any USB replug. Don't trust it blindly; the plan's own Task 3 instructions say to re-run `detect_devices.py` first.

<context>
This phase connects a fine-tuned SmolVLA checkpoint to the real SO-ARM101 over a Colab-hosted gRPC bridge. All code-writable work is done (safety validator, action contract, I/O logger, bridge wrapper, split-stereo camera, device auto-discovery, camera1 fix — 77/77 tests green). What's left is exclusively the human physically running the full live episode — I cannot watch the robot arm move, judge whether the cube was picked up, or operate Colab/ngrok myself. The session paused right after presenting Task 3's checkpoint and before receiving any reply. A real, not-yet-verified risk is flagged in the checkpoint text: the camera1 fix may or may not tolerate a second concurrent open of the wrist camera device — this needs to be watched for during the actual run, not assumed safe.
</context>

<next_action>
Start with: check whether the user has replied to Task 3's checkpoint (re-read the last few turns of conversation history, or ask them directly if resuming a fresh session). On their reply, spawn a fresh `gsd-executor` continuation agent that `cd`s into the EXISTING worktree at `.claude/worktrees/agent-afe9e290a91f86d2e` (do NOT dispatch with `isolation="worktree"` again — that would create a second, separate worktree), verifies it's on branch `worktree-agent-afe9e290a91f86d2e` at or after commit `6ca93fe`, and continues Task 3's resolution (recording the report) before proceeding to Task 4 (findings write-up) if approved.
</next_action>
