---
phase: 11-vla-hardware-connection
plan: 05
type: execute
wave: 4
depends_on: ["11-04"]
files_modified:
  - control/vla_bridge/FINDINGS.md
  - control/detect_devices.py
  - control/test_detect_devices.py
  - control/device_map.json
  - control/run_vla_episode.py
  - control/vla_bridge/robot_client.py
autonomous: false
requirements: [VLAHW-01, VLAHW-02, VLAHW-04, VLAHW-05]
tags: [vla-hardware, hardware-in-the-loop, findings, go-no-go, device-discovery]

must_haves:
  truths:
    - "The e-stop halts real motor commands on the physical SO-ARM101, verified hands-on, not just by a mocked unit test"
    - "At least one full episode — task prompt to termination — runs end-to-end on the physical robot, producing synchronized video, a matching per-step JSON Lines log, and a recorded termination reason and success/failure outcome"
    - "A findings write-up honestly synthesizes what was observed (including whether lerobot#2210 reproduced) into an explicit go/no-go recommendation for the next milestone phases, and explicitly states SmolVLA has no reasoning trace to capture (D-05)"
    - "Serial ports (follower/leader) and camera indices are auto-detected and persisted to device_map.json rather than assumed fixed — live testing this session found both shift across sessions/replugs, and camera identity was being silently confused with the laptop's built-in webcam"
    - "The bridge's observation dict actually includes camera1 (wrist), not just camera2/camera3 (stereo split) — a gap found live this session that would have silently starved the VLA checkpoint of its wrist view during Task 2's live episode"
  artifacts:
    - control/vla_bridge/FINDINGS.md
    - control/detect_devices.py
    - control/device_map.json
  key_links:
    - "This plan's live episode is the first real exercise of Plan 11-04's BridgeActionSource + Plan 11-01's safety_validator + Plan 11-02's IOLogger, all wired together as a whole"
    - "control/device_map.json (Task 2's output) is what Task 3's live episode command and any future control/ script should read port/camera defaults from, instead of hardcoded assumptions"
---

<objective>
Perform the two mandatory hardware-in-the-loop verifications this phase cannot automate — e-stop halting real servo motion, and one full VLA-driven episode recorded end-to-end — and synthesize the results into a findings write-up with an explicit go/no-go recommendation for the milestone's next phases.

Purpose: VLAHW-02's e-stop requirement, VLAHW-04's full-episode requirement, and VLAHW-05's findings write-up are all, by RESEARCH.md's own Validation Architecture, impossible to verify without the real robot — this plan is where that verification actually happens, closing out Phase 11.
Output: A verified e-stop, one recorded episode (`control/outputs/vla_episode_001/` — video, JSONL log, `termination.json`), and `control/vla_bridge/FINDINGS.md`.
</objective>

<execution_context>
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/workflows/execute-plan.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-vla-hardware-connection/11-RESEARCH.md
@.planning/phases/11-vla-hardware-connection/11-CONTEXT.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: E-stop hardware-in-the-loop verification</name>
  <files>control/outputs/estop_check_001/ (runtime output, gitignored — not a tracked source file)</files>
  <read_first>
    control/run_vla_episode.py (Plan 11-02/11-04's e-stop path: `try/except KeyboardInterrupt` around the main loop, calling `move_to_positions(robot, start_positions, ...)` before `robot.disconnect()`)
    control/test_run_vla_episode.py (the existing mocked-hardware test proving this code path is exercised correctly in unit tests — this checkpoint is the same behavior verified against REAL hardware)
  </read_first>
  <action>Blocking human verification: run the e-stop check below against the real robot and halt for an explicit report — do not proceed to Task 3's live episode until e-stop is confirmed safe on real hardware.</action>
  <what-built>
    `run_vla_episode.py`'s e-stop path: a `KeyboardInterrupt` (Ctrl+C) mid-episode halts the control loop, moves the arm back to its start position via `move_to_positions()`, disconnects, and records `{"reason": "keyboard_interrupt"}` in `termination.json` — already proven against a mocked robot in `test_run_vla_episode.py`; this checkpoint proves it against the real SO-ARM101.
  </what-built>
  <how-to-verify>
    1. Confirm the real follower is connected: `ls /dev/cu.usbmodem*` (per `control/COMMANDS.md`'s hardware table, expect `soarm_follower_02` on whatever port is currently assigned).
    2. Run: `cd control && .venv/bin/python run_vla_episode.py &lt;PORT&gt; soarm_follower_02 --out outputs/estop_check_001 --max-steps 200 --control-hz 2.0` (uses the Plan 11-02 `ScriptedActionSource` — no Colab bridge needed for this check).
    3. While the arm is mid-motion (it will be toggling the gripper open/closed every 30 steps), press Ctrl+C.
    4. Confirm all four: (a) no further gripper toggle or joint motion happens after the interrupt is registered, (b) the arm visibly moves back toward its start position within about 5 seconds, (c) the process exits cleanly (no unhandled traceback), (d) `control/outputs/estop_check_001/termination.json` contains `{"reason": "keyboard_interrupt", ...}`.
  </how-to-verify>
  <resume-signal>Type "approved" if all four confirmations held, or describe exactly what happened (e.g. "arm kept moving for 2 more seconds after Ctrl+C" / "process crashed with &lt;traceback&gt;").</resume-signal>
  <done>Human has typed "approved" (or reported a failure) confirming all four e-stop behaviors held against the real robot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Device auto-discovery (device_map.json) + bridge camera1 wiring fix</name>
  <files>control/detect_devices.py, control/test_detect_devices.py, control/device_map.json, control/run_vla_episode.py, control/vla_bridge/robot_client.py</files>
  <read_first>
    control/COMMANDS.md (the existing known-hardware table this task supersedes with live auto-detection — ports/camera indices documented there are already stale per this session's live findings)
    control/keyboard_joint_control.py, control/record_episode.py (existing `PORT ROBOT_ID` positional-arg CLI convention and `SOFollowerRobotConfig`/`SO101Follower` connect pattern to reuse, not reinvent)
    control/run_vla_episode.py (the `--camera`/`_probe_camera_indices()` logic this task extends with a `device_map.json`-backed default, and the CLI entrypoint that gains an optional device_map read)
    control/vla_bridge/robot_client.py, control/vla_bridge/stereo_camera.py (Plan 11-04's bridge wrapper and `StereoSplitCamera` — read `connect_bridge()`'s actual `robot_config.cameras` construction in full; this task's camera1 fix and device-map wiring both land here)
    control/.venv/lib/python3.12/site-packages/lerobot/robots/so_follower/config_so_follower.py (the real `SOFollowerRobotConfig` fields — confirm exactly how its `cameras` dict is structured and consumed before writing code against it, don't guess)
  </read_first>
  <behavior>
    - Running `control/detect_devices.py` with the follower/leader physically connected produces a `control/device_map.json` containing resolved `follower`/`leader` ports+ids and `cameras.wrist`/`cameras.stereo_overhead` cv2 indices, without requiring any manual index/port entry
    - Camera detection excludes any device whose `system_profiler SPCameraDataType` name contains "FaceTime" (or otherwise matches known built-in-camera naming) from ever being assigned to `wrist` or `stereo_overhead` — the built-in laptop camera must never be silently selected again (this session's root-caused bug)
    - The AR0144 stereo camera is identified by its distinctive 2560x720 capture resolution (unique among this rig's cameras); if name-based exclusion and resolution fingerprinting together cannot uniquely resolve wrist vs. an ambiguous second candidate, `detect_devices.py` falls back to an interactive cover-the-lens brightness check (matching this session's manual process) rather than guessing
    - Serial port → robot identity resolution (`follower`=`soarm_follower_02`, `leader`=`soarm_leader_01`) is done by an actual connect+calibration-load round-trip per candidate `/dev/cu.usbmodem*` port against each known robot id's calibration file — not by fixed port-string matching, which this session confirmed drifts
    - `run_vla_episode.py`, run with no `--camera`/port override, uses `device_map.json`'s values automatically if the file exists and is reasonably fresh; explicit CLI flags still override it when given
    - `connect_bridge()`'s `robot_config.cameras` includes `camera1` (wrist) wired to a real, live camera source — not left as an empty/missing key the way this session found it — without opening the wrist device's cv2 index a second time if `run_vla_episode.py`'s own local caps already have it open (reuse the existing open handle/frame source rather than a second concurrent `cv2.VideoCapture` on the same index)
  </behavior>
  <action>
    Create `control/detect_devices.py`:
    - Camera detection: run `system_profiler SPCameraDataType`, parse device names (stdlib `subprocess` + simple text parsing, no new dependency). Probe cv2 indices 0-5 (reuse `run_vla_episode.py`'s `_probe_camera_indices()` open pattern), capture one frame per open index, record resolution. Exclude any index whose best-effort name-to-index correlation matches a "FaceTime"/built-in name. Among the remaining candidates: the one reporting ~2560x720 is `stereo_overhead`; the other is `wrist`. If more than one non-excluded candidate remains ambiguous after this, prompt interactively: "Cover camera at index N and press Enter" and compare mean frame brightness before/after (same technique used live this session) to resolve it.
    - Serial port detection: list `/dev/cu.usbmodem*` (reuse the existing `ls`-based discovery convention from `control/COMMANDS.md`). For each port, attempt to construct+connect a `SOFollowerRobotConfig`/`SO101Follower` using the `soarm_follower_02` calibration id; on success, record it as `follower`. Repeat for `soarm_leader_01` as `leader` (adjust class if leader uses a distinct LeRobot type — check `compare_leader_follower.py` for the existing leader-connect pattern). A port that fails both should be left unassigned with a printed warning, not crash the whole script.
    - Write `control/device_map.json`: `{"detected_at": "<ISO8601>", "follower": {"port": "...", "id": "soarm_follower_02"}, "leader": {"port": "...", "id": "soarm_leader_01"} | null, "cameras": {"wrist": <int>, "stereo_overhead": <int>}}`. `leader` may be `null` if not connected — this script must work with just the follower + cameras present, since `run_vla_episode.py` never needs the leader.
    - Add `control/device_map.json` to `.gitignore` if not already covered by an existing `outputs/`-style ignore pattern (it's live machine-specific state, not source) — verify `control/.gitignore` or the repo root `.gitignore` and add an entry if missing.

    Update `control/run_vla_episode.py`: make the `PORT`/`ROBOT_ID` positional args and `--camera` optional; if omitted, load `control/device_map.json` (if present) and use its `follower.port`/`follower.id`/`cameras.wrist`/`cameras.stereo_overhead` as defaults, printing which source (CLI flag vs. device_map.json) was used for each value. If `device_map.json` is missing and no CLI override given, fail with a clear error telling the user to run `detect_devices.py` first or pass explicit args — do not silently fall back to `_probe_camera_indices()`'s old any-device-that-opens behavior.

    Fix `connect_bridge()` in `control/vla_bridge/robot_client.py`: wire `camera1` into `robot_config.cameras` using `device_map.json`'s `cameras.wrist` index. Read the installed `lerobot` camera-config source (per read_first) to determine the correct, real mechanism for sharing a single already-open wrist capture between `run_vla_episode.py`'s local logging path and the bridge's own observation-building path, OR — if sharing isn't cleanly possible without deeper surgery — have the bridge path open its own independent capture of the wrist camera (same index, a second `cv2.VideoCapture` call), since unlike the AR0144 stereo pair (which `StereoSplitCamera` already serializes through one shared open), a standard USB webcam driver may tolerate two independent opens if the stereo camera does not — verify actual driver behavior empirically rather than assuming either way, and document whatever is found in this task's SUMMARY.md.

    Create `control/test_detect_devices.py`: mock `subprocess.run` (for `system_profiler`), `cv2.VideoCapture`, and the robot connect calls — no real hardware or `system_profiler` call during tests. Cover: FaceTime-name exclusion, resolution-based stereo/wrist disambiguation, the interactive-fallback path (mocked non-interactively via a stubbed input), and port-to-identity resolution success/failure per candidate port.
  </action>
  <acceptance_criteria>
    - `test_detect_devices.py` proves a camera named "FaceTime HD Camera" (or matching name) is never assigned to `wrist` or `stereo_overhead`, even if it's the only "extra" camera detected
    - `test_detect_devices.py` proves the 2560x720-resolution candidate is always assigned to `stereo_overhead`, never `wrist`
    - `run_vla_episode.py --help` still documents `PORT`/`ROBOT_ID`/`--camera` as available explicit overrides (regression: existing explicit-arg invocations from Plan 11-02/11-04's own tests must keep working unchanged)
    - `robot_client.py`'s bridge camera config includes a `camera1` entry sourced from a real camera, not an empty dict, when `connect_bridge()` is called with a populated `device_map.json`
  </acceptance_criteria>
  <verify>
    <automated>cd control && .venv/bin/python -m pytest test_detect_devices.py test_run_vla_episode.py test_robot_client.py -x -q</automated>
  </verify>
  <done>`control/detect_devices.py` exists, tested, and reliably distinguishes the built-in webcam and the two real cameras by name+resolution (falling back to an interactive check only when genuinely ambiguous); `device_map.json` is the new default source of truth for ports/camera indices; the bridge's `camera1` wiring gap found live this session is fixed and covered by a test.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Full VLA-driven episode, recorded end-to-end</name>
  <files>control/outputs/vla_episode_001/ (runtime output, gitignored — not a tracked source file)</files>
  <read_first>
    control/vla_bridge/policy_server_launch.md (Plan 11-03 — the Colab notebook cells and local-side CLI-equivalent invocation to start)
    control/vla_bridge/checkpoint_candidates.md (Plan 11-03 — the checkpoint id selected in that plan's decision checkpoint; use it as `--checkpoint` here)
    .planning/STATE.md (Blockers/Concerns — the real, measured gripper collision-proxy aperture is 6-78mm, not the originally-approved 84mm cap; the red cube must fit within this real number)
    control/device_map.json (Task 2's output — the resolved port/camera indices this task's run command reads by default)
  </read_first>
  <action>Blocking human verification: run the full VLA-driven episode below against the real robot and halt for the human's reported outcome (success/failure, termination reason, lerobot#2210 status) before Task 4 drafts the findings write-up.</action>
  <what-built>
    The complete pipeline: a Colab-hosted SmolVLA `PolicyServer` (Plan 11-03's selected checkpoint) reachable over an ngrok TCP tunnel, driving `run_vla_episode.py`'s `BridgeActionSource` (Plan 11-04) → `safety_validator.validate_action()` (Plan 11-01) → the real SO-ARM101 → `IOLogger` (Plan 11-02), all wired together for the first time as a complete system, with ports/cameras resolved via Task 2's `device_map.json` instead of hardcoded assumptions.
  </what-built>
  <how-to-verify>
    1. Measure the red cube (the D-02 manipulation object): confirm its graspable width is ≤78mm (this project's actual measured collision-proxy gripper aperture per STATE.md, not the earlier 84mm design cap), and place it within the arm's ~0.45m reach, off the base's forward centerline (the arm's forearm sweeps through that corridor — per the Phase 4/8 embodiment note in STATE.md).
    2. Re-run `python detect_devices.py` first if any USB device has been unplugged/replugged since Task 2 (ports and camera indices can shift again) — confirm `device_map.json`'s `detected_at` timestamp is from this session before trusting it.
    3. Start the Colab notebook using `control/vla_bridge/policy_server_launch.md`'s cells; note the printed ngrok TCP host:port.
    4. Run: `cd control && .venv/bin/python run_vla_episode.py --out outputs/vla_episode_001 --instruction "Pick the red cube and place it in the bowl" --server-address &lt;ngrok_host:port&gt; --checkpoint &lt;Plan 11-03's selected checkpoint id&gt; --max-steps &lt;a value long enough to complete the pick, e.g. 150-300&gt;` — PORT/ROBOT_ID/`--camera` are omitted deliberately so Task 2's `device_map.json` supplies them; pass them explicitly only if `device_map.json` is stale or missing. Note the task instruction now matches the checkpoint's own exact trained wording ("Pick the red cube and place it in the bowl"), not a paraphrase.
    5. Let the episode run to completion or termination (max-steps, e-stop, or bridge failure).
    6. Confirm `control/outputs/vla_episode_001/` contains: `episode.jsonl` (one record per step, with real — not placeholder — `raw_model_output`, `latency_ms`, and `model_version` values reflecting the actual Colab bridge, not `"scripted-dry-run-v1"`), `camera_wrist/`/`camera_overhead/` PNG sequences, and `termination.json` with a real reason.
    7. Judge and record whether the cube was actually picked up (success/failure) — this is a human judgment call per RESEARCH.md's Validation Architecture (no automated vision-based success detection exists yet).
    8. Note explicitly whether lerobot#2210's zero-shot crash (`AssertionError: mean is infinity`) reproduced at any point, and note any other upstream issue encountered.
  </how-to-verify>
  <resume-signal>Type "approved" along with: the observed success/failure outcome, the termination reason, and whether lerobot#2210 reproduced — or describe what went wrong if the episode could not be completed.</resume-signal>
  <done>Human has reported the episode's success/failure outcome, termination reason, and lerobot#2210 status; `control/outputs/vla_episode_001/` contains real (non-scripted) episode data.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Findings write-up and go/no-go recommendation</name>
  <files>control/vla_bridge/FINDINGS.md</files>
  <read_first>
    control/outputs/vla_episode_001/episode.jsonl, control/outputs/vla_episode_001/termination.json (Task 3's actual recorded output — the write-up must be grounded in this real data, not invented)
    .planning/phases/11-vla-hardware-connection/11-CONTEXT.md (D-05 — the explicit "no reasoning trace" non-decision that must be restated here, not re-litigated)
    .planning/REQUIREMENTS.md (the "Future Requirements (deferred pending v2.0's early results)" section — the go/no-go recommendation should reference these as the concrete next-phase candidates)
  </read_first>
  <action>Draft `control/vla_bridge/FINDINGS.md` (content described in what-built below), grounded strictly in Task 1/Task 2/Task 3's actual reported outcomes, then halt for human review before this phase closes.</action>
  <what-built>
    Drafted `control/vla_bridge/FINDINGS.md` synthesizing: (1) whether lerobot#2210's zero-shot crash reproduced (Task 2's report — expected "no," since a fine-tuned checkpoint was used per Pitfall 2's resolution) or any other upstream issue hit; (2) the observed episode outcome from Task 2 (success/failure, termination reason, cube-pick judgment); (3) the e-stop verification outcome from Task 1; (4) an explicit statement that SmolVLA has no reasoning trace and that VLAHW-03's "complete I/O" JSONL log is the correct substitute artifact for this phase, per D-05 — stated explicitly so the next MLLM-comparison phase doesn't re-litigate this framing; (5) an explicit go/no-go recommendation for the next milestone phases (deep-reasoning MLLM comparison, raw-autonomy design — per PROJECT.md/REQUIREMENTS.md's Future Requirements), grounded in what was actually observed, not aspirational.
  </what-built>
  <how-to-verify>
    Read `control/vla_bridge/FINDINGS.md` end to end; confirm every claim in it traces back to something actually observed in Task 1 or Task 2 (not invented or assumed), and that the go/no-go recommendation is justified by that evidence rather than restating the milestone's original hopes.
  </how-to-verify>
  <resume-signal>Type "approved" if the write-up is accurate, or provide corrections to fold into `FINDINGS.md` before this phase closes.</resume-signal>
  <done>Human has typed "approved" or provided corrections that have been folded into `FINDINGS.md`; the file exists and accurately reflects Tasks 1-3.</done>
</task>

</tasks>

## Artifacts this phase produces

- `control/vla_bridge/FINDINGS.md`: go/no-go findings write-up (docs artifact)
- `control/outputs/vla_episode_001/`: real episode recording — `episode.jsonl`, `camera_wrist/`, `camera_overhead/`, `termination.json` (gitignored runtime output, not committed)
- `control/outputs/estop_check_001/`, `control/outputs/estop_check_002/`: e-stop verification recordings (gitignored runtime output)
- `control/detect_devices.py`: auto-discovery script for follower/leader serial ports and camera indices — added mid-phase (Task 2) after live hardware testing found both drift across sessions and the built-in laptop webcam was being silently substituted for a real camera
- `control/test_detect_devices.py` (new test file)
- `control/device_map.json`: resolved device mapping, machine-local state (gitignored, not committed)
- `control/vla_bridge/robot_client.py`: `camera1` (wrist) wiring gap fixed — the bridge's observation dict previously only included `camera2`/`camera3` (stereo split)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human operator ↔ live physical hardware | The only point in this phase where a real, uncontrolled physical action (arm motion near a human) actually happens |
| findings write-up ↔ actual observed behavior | The write-up is a single-observer research artifact informing a real go/no-go decision on later milestone phases |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-12 | Denial of Service (physical safety) | e-stop under real load | critical | mitigate | Task 1 is a dedicated, blocking hardware-in-the-loop checkpoint verifying e-stop against the real servo bus BEFORE Task 3's full episode is attempted — not just the mocked unit test from Plan 11-02 |
| T-11-13 | Repudiation | findings write-up accuracy | low | accept | Task 4's human-review checkpoint cross-checks the write-up against the actual `episode.jsonl`/`termination.json` output before sign-off; residual risk of single-observer interpretation error is accepted as inherent to a first-pass research write-up, to be revisited if the next milestone phase's results contradict it |
| T-11-14 | Tampering (silent wrong input) | camera device identity | high | mitigate | Task 1's first attempt silently used the laptop's built-in webcam instead of a real camera because `_probe_camera_indices()` had no identity check — Task 2's `detect_devices.py` excludes built-in cameras by name and cross-validates by resolution, closing this gap before Task 3's live episode depends on correct camera identity |

</threat_model>

<verification>
- Task 1 approved: e-stop confirmed against real hardware
- Task 2 complete: `control/detect_devices.py` reliably excludes the built-in webcam and resolves wrist/stereo camera identity; `device_map.json` exists; `robot_client.py`'s `camera1` wiring gap is fixed and tested
- Task 3 approved: `control/outputs/vla_episode_001/` contains real (non-scripted) `episode.jsonl`, camera frame sequences, and `termination.json`
- Task 4 approved: `control/vla_bridge/FINDINGS.md` exists and accurately reflects Tasks 1-3
</verification>

<success_criteria>
Phase 11 closes with a verified e-stop, one complete real-hardware VLA-driven episode with synchronized video + full I/O log + termination outcome, and an evidence-grounded findings write-up recommending go/no-go on the milestone's next phases.
</success_criteria>

<output>
Create `.planning/phases/11-vla-hardware-connection/11-05-SUMMARY.md` when done
</output>
