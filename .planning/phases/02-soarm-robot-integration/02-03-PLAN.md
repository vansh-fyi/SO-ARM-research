---
phase: 02-soarm-robot-integration
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - LIBERO/libero/libero/envs/robots/soarm.py
  - LIBERO/libero/libero/assets/robots/soarm101/robot.xml
  - LIBERO/libero/libero/assets/grippers/soarm_gripper.xml
  - LIBERO/libero/libero/envs/grippers/soarm_gripper.py
autonomous: true
requirements: [ENV-05, ENV-07]
must_haves:
  truths:
    - "soarm_sanity.py --check reset passes: OffScreenRenderEnv(robots=['Soarm101']) on the default libero_spatial BDDL resets without error and the printed peak contact force after 10 settle steps is below 10.0 N (phase success criterion 1)"
    - "soarm_sanity.py --check soak passes: 500 random-action steps x 3 seeds complete with no exception and finite qpos throughout"
    - "reset obs contains both agentview_image and robot0_eye_in_hand_image keys (default LIBERO camera_names resolve against the SOARM arm XML)"
  artifacts:
    - path: "LIBERO/libero/libero/envs/robots/soarm.py"
      provides: "Tuned base_xpos_offset / init_qpos / damping values that produce a stable reset (D-11 outcome)"
      contains: "base_xpos_offset"
    - path: "LIBERO/libero/libero/assets/robots/soarm101/robot.xml"
      provides: "Arm MJCF with any physics-value corrections discovered during reset/soak iteration (inline values only — still zero default blocks)"
      contains: "right_hand"
  key_links:
    - from: "explorations/soarm_sanity.py"
      to: "LIBERO/libero/libero/envs/robots/soarm.py"
      via: "check_reset builds the env with robots=['Soarm101'] — Libero_Tabletop_Manipulation prepends Mounted and resolves MountedSoarm101"
      pattern: "robots=\\[.Soarm101.\\]"
    - from: "LIBERO/libero/libero/envs/robots/soarm.py"
      to: "LIBERO env table geometry"
      via: "base_xpos_offset table lambda placing the base ON the tabletop at z=0.90 (SO101 reach 0.479 m)"
      pattern: "0\\.90"
---

# Plan 02-03: Physics Stabilization — Stable Reset (<10 N) and Random-Action Soak

## Phase Goal

**As a** VLA researcher, **I want to** select the SOARM SO101 robot by name in LIBERO and run libero_spatial tasks with it — with stable physics and correct rendering, **so that** the Phase 3 inference loop can drive SOARM from language prompts instead of the stock Panda.

<objective>
Make the first full SOARM LIBERO environment actually run: debug env creation end-to-end (merge/namespacing/camera resolution), then iterate base placement, init_qpos, and damping (D-11) until `soarm_sanity.py --check reset` and `--check soak` are GREEN. This is the phase's flagged risk item (STATE.md: budget 1-2 days of iterative MJCF editing) and is pure local CPU work per D-09 (`MUJOCO_GL=glfw`, `libero` conda env — no Colab needed).

This delivers ENV-05's instantiation half (a LIBERO environment actually instantiates and resets with SOARM) and ENV-07's physics-stability half (contact forces <10 N at initialization — phase success criterion 1).

Purpose: every later artifact (render tuning in 02-04, BDDL task runs, the 02-05 Colab notebook) steps this environment; an unstable reset blocks the whole phase.
Output: tuned soarm.py / robot.xml / soarm_gripper.xml values with GREEN reset+soak checks, and a SUMMARY documenting the final numbers for the Colab notebook.
</objective>

<execution_context>
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/workflows/execute-plan.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-soarm-robot-integration/02-CONTEXT.md
@.planning/phases/02-soarm-robot-integration/02-RESEARCH.md
@.planning/phases/02-soarm-robot-integration/02-PATTERNS.md
@.planning/phases/02-soarm-robot-integration/02-01-SUMMARY.md
@.planning/phases/02-soarm-robot-integration/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Debug env creation to a stable reset — flip --check reset GREEN</name>
  <files>LIBERO/libero/libero/envs/robots/soarm.py, LIBERO/libero/libero/assets/robots/soarm101/robot.xml, LIBERO/libero/libero/assets/grippers/soarm_gripper.xml</files>
  <read_first>
    - explorations/soarm_sanity.py — check_reset() and max_contact_force() (the failing test and its measurement recipe)
    - LIBERO/libero/libero/envs/robots/soarm.py (current base_xpos_offset / init_qpos values to tune)
    - .planning/phases/02-soarm-robot-integration/02-RESEARCH.md — Pitfalls 1, 4, 6 and Open Question 1 (home-pose interpenetration analysis) and Assumptions A1/A5
    - LIBERO/libero/libero/bddl_files/libero_spatial/pick_up_the_black_bowl_from_table_center_and_place_it_on_the_plate.bddl (the default --bddl task: object region coordinates the home pose must not intersect)
  </read_first>
  <action>
    Run `conda run -n libero python explorations/soarm_sanity.py --check reset` and fix failures until GREEN. Expected failure classes, in likely order (D-11 scope: tune only until criteria pass, no hardware-realism polish):

    1. Wiring/merge errors — site or body lookups with `robot0_`/`gripper0_` prefixes (robosuite namespaces merged elements), gripper attach onto `right_hand`, camera resolution for `robot0_eye_in_hand`, mesh path resolution in the merged task XML. These are fix-the-XML/class bugs, not tuning: correct names/paths in robot.xml, soarm_gripper.xml, or soarm.py per RESEARCH Pitfalls 1 and 4. Any inline-value edit to the XMLs must preserve the 02-01/02-02 structural contracts (zero default blocks, motor arm actuators, position gripper actuator).
    2. Contact-force spikes at reset (>10 N) — iterate `base_xpos_offset` table-lambda x within [-0.45, -0.30] (z stays 0.90: base ON the tabletop, Pitfall 6; anti-pattern: never copy Panda's floor-level z=0), and if the extended home pose (gripper 0.39 m forward at z+0.23) interpenetrates the table or object spawn regions, tuck `init_qpos` (e.g., shoulder_lift back, elbow up) — D-07 explicitly allows adjusting the calibration pose when it proves unstable in sim. Re-measure with the printed peak force after every change.
    3. If convex-hull STL collisions themselves are unstable (contact explosion regardless of placement — RESEARCH A4 fallback): replace jaw/arm collision geoms with primitive boxes/capsules sized to the meshes, keeping visual geoms as meshes. Do this only if placement/pose tuning fails.

    Acceptance target is check_reset's own gate: env.reset() + 10 zero-action settle steps, then max_contact_force(env) < 10.0 with both camera obs keys present. Record every value changed and why (feeds the SUMMARY and the 02-05 notebook).
  </action>
  <verify>
    <automated>conda run -n libero python explorations/soarm_sanity.py --check reset</automated>
  </verify>
  <acceptance_criteria>
    - `conda run -n libero python explorations/soarm_sanity.py --check reset` exits 0 and its PASS line prints a measured peak contact force strictly below 10.0
    - Structural contract still holds after any XML edits: `conda run -n libero python -c "import xml.etree.ElementTree as ET; r=ET.parse('LIBERO/libero/libero/assets/robots/soarm101/robot.xml').getroot(); assert not r.findall('.//default'); assert len(r.findall('.//actuator/motor'))==5; g=ET.parse('LIBERO/libero/libero/assets/grippers/soarm_gripper.xml').getroot(); assert not g.findall('.//default'); assert len(g.findall('.//actuator/position'))==1; print('CONTRACTS HOLD')"` prints CONTRACTS HOLD
    - The tuned table lambda in soarm.py still returns z == 0.90 (tabletop placement preserved)
    - `--check model` still exits 0 (no registration regression)
  </acceptance_criteria>
  <done>A SOARM libero_spatial environment resets cleanly with sub-10 N contact forces on local CPU — phase success criterion 1 satisfied locally.</done>
</task>

<task type="auto">
  <name>Task 2: Random-action soak — flip --check soak GREEN (controller/damping stability)</name>
  <files>LIBERO/libero/libero/envs/robots/soarm.py, LIBERO/libero/libero/assets/robots/soarm101/robot.xml, LIBERO/libero/libero/envs/grippers/soarm_gripper.py</files>
  <read_first>
    - explorations/soarm_sanity.py — check_soak() (the failing test: 500 uniform random actions x 3 seeds, finite-qpos asserts every 100 steps)
    - .planning/phases/02-soarm-robot-integration/02-RESEARCH.md — Pitfalls 7 and 8, Open Question 2 (OSC torque clipping at ±2.94 Nm), Assumption A2
    - .planning/phases/02-soarm-robot-integration/02-03 Task 1 findings (values already tuned in Task 1 of this plan — do not undo them)
  </read_first>
  <action>
    Run `conda run -n libero python explorations/soarm_sanity.py --check soak` and stabilize until GREEN. The generic OSC_POSE config (kp=150) was tuned for Panda-scale dynamics; the SO101 is 0.632 kg with ±2.94 Nm torque limits (Pitfall 7). Tuning order per D-11 (stop as soon as soak passes — no-crash stepping is the bar, not tracking quality):

    1. Joint damping in soarm.py's set_joint_attribute (start 0.6 per joint, the STS3215 hardware value; raise toward 1.5-3.0 if oscillation/qacc warnings, lower if the arm moves through honey — Pitfall 2's warning sign means inlined values got stomped, check robot.xml inline attributes first).
    2. Gripper dynamics if the jaw chatters: SoarmGripper.speed (0.10 start) and init_qpos jaw value (0.8 start) per RESEARCH A3.
    3. If damping alone cannot stabilize: note that a custom `controller_configs` dict (lower kp / output_max) can be passed as an env kwarg — a kwarg-level fix requiring zero robosuite file edits. If used, the exact dict MUST be recorded in the SUMMARY because the 02-05 notebook and Phase 3 inference must pass the same kwarg.

    Also note for the SUMMARY (Pitfall 8, no action needed): the 5-DOF arm under the 6-DOF OSC task sacrifices one rotational direction structurally — expected, not a bug; Phase 3's 7-D action contract still holds (control_dim 6 + gripper dof 1).
  </action>
  <verify>
    <automated>conda run -n libero python explorations/soarm_sanity.py --check soak</automated>
  </verify>
  <acceptance_criteria>
    - `conda run -n libero python explorations/soarm_sanity.py --check soak` exits 0 (500 steps x 3 seeds, no exception, finite qpos)
    - `conda run -n libero python explorations/soarm_sanity.py --check reset` still exits 0 with peak force < 10.0 (soak tuning must not regress reset stability)
    - If a custom controller_configs dict was needed, it is written into the SUMMARY verbatim; if not needed, the SUMMARY states the generic OSC_POSE config sufficed
  </acceptance_criteria>
  <done>SOARM survives sustained random-action stepping across seeds with reset stability intact — the physics foundation for ENV-06 task runs and the Colab verification.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols created/finalized by this plan (new — exclude from drift verification):

- Tuned values (no new symbols — this plan finalizes numeric values inside existing 02-02 symbols): `MountedSoarm101.base_xpos_offset` table-lambda x offset, `MountedSoarm101.init_qpos` (possibly tucked pose per D-07), set_joint_attribute damping array, `SoarmGripper.speed`/`init_qpos` jaw values
- Possible (contingency only): primitive collision geoms replacing convex-hull mesh collisions in `robot.xml`/`soarm_gripper.xml` (RESEARCH A4 fallback); a documented `controller_configs` kwarg dict recorded in 02-03-SUMMARY.md (Pitfall 7 fallback)
- Files: none new — modifies 02-01/02-02 files in place

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Local CPU iteration over git-tracked fork files; no network, no installs, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package-manager installs this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `conda run -n libero python explorations/soarm_sanity.py --check reset` exits 0, printed peak force < 10.0 N (SC-1)
- `conda run -n libero python explorations/soarm_sanity.py --check soak` exits 0
- `--check compile` and `--check model` still exit 0 (no regression of 02-01/02-02 greens)
- Expected RED at plan end: `--check render` (camera pose untuned) and `--check tasks` (TASKS unfinalized) — both go green in plan 02-04
</verification>

<success_criteria>
- env.reset() on a SOARM libero_spatial environment completes without errors with contact forces below 10 N (phase success criterion 1, verified locally; Colab re-verification in 02-05)
- 500-step x 3-seed random-action soak passes with finite state throughout
- All tuned values and any controller_configs requirement documented in the SUMMARY for the 02-05 notebook and Phase 3
</success_criteria>

<output>
Create `.planning/phases/02-soarm-robot-integration/02-03-SUMMARY.md` when done. Record: final base_xpos_offset/init_qpos/damping/gripper values, whether the A4 primitive-collision fallback was needed, whether a custom controller_configs dict is required (verbatim dict if so), and the measured reset peak force — plan 02-04's task selection and plan 02-05's notebook consume these numbers.
</output>
