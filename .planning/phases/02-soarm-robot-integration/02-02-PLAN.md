---
phase: 02-soarm-robot-integration
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - LIBERO/libero/libero/assets/grippers/soarm_gripper.xml
  - LIBERO/libero/libero/envs/grippers/__init__.py
  - LIBERO/libero/libero/envs/grippers/soarm_gripper.py
  - LIBERO/libero/libero/envs/robots/soarm.py
  - LIBERO/libero/libero/envs/robots/__init__.py
autonomous: true
requirements: [ENV-04, ENV-05]
must_haves:
  truths:
    - "soarm_sanity.py --check compile passes: both the arm MJCF and the gripper MJCF compile standalone under MuJoCo 2.3.7"
    - "soarm_sanity.py --check model passes: MountedSoarm101 is in robosuite ROBOT_CLASS_MAPPING, SoarmGripper is in GRIPPER_MAPPING, and both classes instantiate"
    - "Registration happens entirely inside the LIBERO fork at import time — zero files in robosuite site-packages are edited"
  artifacts:
    - path: "LIBERO/libero/libero/assets/grippers/soarm_gripper.xml"
      provides: "robosuite-contract gripper MJCF: eef body, grip_site/grip_site_cylinder/ee sites, ft_frame force/torque sensors, single jaw position actuator (D-05, D-06)"
      contains: "grip_site"
    - path: "LIBERO/libero/libero/envs/robots/soarm.py"
      provides: "MountedSoarm101(ManipulatorModel) implementing the 8-property contract with tabletop base placement (D-04, D-07, D-08)"
      contains: "class MountedSoarm101"
    - path: "LIBERO/libero/libero/envs/grippers/soarm_gripper.py"
      provides: "SoarmGripper(GripperModel) 1-DOF integrated jaw action (D-05, D-06, D-08)"
      contains: "class SoarmGripper"
    - path: "LIBERO/libero/libero/envs/grippers/__init__.py"
      provides: "new libero.libero.envs.grippers package"
    - path: "LIBERO/libero/libero/envs/robots/__init__.py"
      provides: "ROBOT_CLASS_MAPPING + GRIPPER_MAPPING registration extending the existing Panda block (ENV-05)"
      contains: "MountedSoarm101"
  key_links:
    - from: "LIBERO/libero/libero/envs/robots/soarm.py"
      to: "LIBERO/libero/libero/assets/robots/soarm101/robot.xml"
      via: "fork-local ASSETS constant joined with robots/soarm101/robot.xml in __init__ (never xml_path_completion)"
      pattern: "robots/soarm101/robot\\.xml"
    - from: "LIBERO/libero/libero/envs/grippers/soarm_gripper.py"
      to: "LIBERO/libero/libero/assets/grippers/soarm_gripper.xml"
      via: "fork-local ASSETS constant joined with grippers/soarm_gripper.xml in __init__"
      pattern: "grippers/soarm_gripper\\.xml"
    - from: "LIBERO/libero/libero/envs/robots/__init__.py"
      to: "LIBERO/libero/libero/envs/robots/soarm.py"
      via: "from .soarm import MountedSoarm101 — class definition auto-registers in robosuite REGISTERED_ROBOTS via metaclass"
      pattern: "from \\.soarm import MountedSoarm101"
    - from: "LIBERO/libero/libero/envs/robots/__init__.py"
      to: "LIBERO/libero/libero/envs/grippers/soarm_gripper.py"
      via: "relative import ..grippers.soarm_gripper then GRIPPER_MAPPING assignment (gripper_factory asserts membership)"
      pattern: "GRIPPER_MAPPING\\[.SoarmGripper.\\]"
---

# Plan 02-02: Gripper MJCF + Robot/Gripper Classes + LIBERO Registration

## Phase Goal

**As a** VLA researcher, **I want to** select the SOARM SO101 robot by name in LIBERO and run libero_spatial tasks with it — with stable physics and correct rendering, **so that** the Phase 3 inference loop can drive SOARM from language prompts instead of the stock Panda.

<objective>
Turn plan 02-01's RED checks green: build the gripper MJCF from the jaw parts excluded from the arm XML (D-05), wrap arm and gripper in `MountedSoarm101(ManipulatorModel)` and `SoarmGripper(GripperModel)` classes as real git-tracked fork modules (D-06, D-08), and register both in robosuite's `ROBOT_CLASS_MAPPING`/`GRIPPER_MAPPING` via `LIBERO/libero/libero/envs/robots/__init__.py` — the same pattern as the existing Panda variants.

This completes ENV-04 (ManipulatorModel subclass registered) and delivers ENV-05's mapping half (name-selectable exactly as Panda). After this plan, `soarm_sanity.py --check compile` and `--check model` are GREEN (MVP slice: a researcher can instantiate SOARM by name); `--check reset` remains RED until plan 02-03 tunes physics.

Purpose: registration is the load-bearing wiring between the vendored geometry and every LIBERO env creation downstream; nothing in plans 02-03..02-05 can run without it.
Output: `soarm_gripper.xml`, `soarm.py`, `soarm_gripper.py`, new `grippers` package, extended `robots/__init__.py`.
</objective>

<execution_context>
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/workflows/execute-plan.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-soarm-robot-integration/02-CONTEXT.md
@.planning/phases/02-soarm-robot-integration/02-RESEARCH.md
@.planning/phases/02-soarm-robot-integration/02-PATTERNS.md
@.planning/phases/02-soarm-robot-integration/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the gripper MJCF — assets/grippers/soarm_gripper.xml</name>
  <files>LIBERO/libero/libero/assets/grippers/soarm_gripper.xml</files>
  <read_first>
    - LIBERO/libero/libero/assets/robots/soarm101/so101_new_calib.source.xml (jaw bodies, joint, actuator, and sts3215 default values to inline — the geometry source)
    - LIBERO/libero/libero/assets/robots/soarm101/robot.xml (what 02-01 kept in the arm — the gripper XML gets exactly what was excluded)
    - /opt/homebrew/Caskroom/miniconda/base/envs/libero/lib/python3.9/site-packages/robosuite/models/assets/grippers/panda_gripper.xml (read-only structural skeleton: eef body, sites, sensors, actuator shape)
    - .planning/phases/02-soarm-robot-integration/02-RESEARCH.md — Pattern 1 items 3-6 and Pitfall 4
    - .planning/phases/02-soarm-robot-integration/02-PATTERNS.md — section "LIBERO/libero/libero/assets/grippers/soarm_gripper.xml" (mandatory skeleton excerpts)
  </read_first>
  <action>
    Create the gripper MJCF from the jaw parts the 02-01 arm split excluded, mirroring panda_gripper.xml's structural contract (D-05: vendored geometry, never re-modeled; D-06: separate GripperModel asset). Concrete target state:

    - Root: mujoco model "soarm_gripper" with a worldbody whose single root body is named `right_gripper` at identity pose (pos "0 0 0", quat "1 0 0 0") — jaw bodies/geoms keep their source-relative pos/quat so the assembled robot matches source kinematics (right_gripper attaches onto the arm's right_hand body, which IS the source gripper body frame).
    - `right_gripper` contains: site `ft_frame` (sphere, size 0.01, group 1); the fixed-jaw geoms from the source (mesh wrist_roll_follower_so101_v1) as a visual geom (contype "0", conaffinity "0", group "1") plus a collision geom named `fixed_jaw_collision` (group "0", contype "0", conaffinity "1", solref "0.02 1", friction "1 0.005 0.0001", condim "4" — panda jaw collision style for graspability); and the `moving_jaw_so101_v1` body from the source with its hinge joint named `gripper` (axis/pos/range copied from source; inline damping "0.6" frictionloss "0.052" armature "0.028" from the sts3215 class), a visual geom, and a collision geom named `moving_jaw_collision` (same collision style as the fixed jaw).
    - Body `eef` inside right_gripper at pos "-0.0079 -0.0002 -0.0981" (the source gripperframe site position — RESEARCH Pattern 1 item 5), containing sites `grip_site` (sphere 0.01, rgba "1 0 0 0.5", group 1), `grip_site_cylinder` (cylinder, size "0.005 10", rgba "0 1 0 0.3", group 1), and `ee`, `ee_x`, `ee_y`, `ee_z` mirroring panda_gripper.xml's axis-cylinder sites. Missing any of grip_site/grip_site_cylinder crashes SingleArm._setup_references; missing eef crashes GripperModel.__init__ (Pitfall 4).
    - Actuator: exactly one `position` actuator driving joint `gripper`, keeping the source kp (≈998) with the MuJoCo-3.x velocity-gain attribute stripped (schema blocker under 2.3.7 — Pitfall 3), ctrllimited "true", ctrlrange equal to the source joint range, and the source actuator's forcerange. robosuite's grip_action rescales the -1..1 gripper action into this ctrlrange as a position target, so it stays a position actuator (unlike the arm's motors).
    - Sensors: `force` named `force_ee` and `torque` named `torque_ee`, both on site `ft_frame` — SingleArm.control() reads them every step (Pitfall 4).
    - Assets: mesh elements for the two jaw meshes, each with an explicit name and file attribute of the form "../robots/soarm101/assets/NAME.stl" (relative to assets/grippers/ this resolves into the 02-01 vendored STL dir; robosuite resolve_asset_dependency joins XML folder + file attribute). Trust the source XML's jaw-section mesh enumeration over this list if it differs.
    - Fully inlined: zero default blocks anywhere (robosuite merge drops them — Pitfall 2).
  </action>
  <verify>
    <automated>conda run -n libero python -c "
import mujoco, xml.etree.ElementTree as ET
p = 'LIBERO/libero/libero/assets/grippers/soarm_gripper.xml'
mujoco.MjModel.from_xml_path(p)
root = ET.parse(p).getroot()
sites = {s.get('name') for s in root.iter('site')}
need = {'ft_frame','grip_site','grip_site_cylinder','ee','ee_x','ee_y','ee_z'}
assert need <= sites, f'missing sites: {need - sites}'
assert [b for b in root.iter('body') if b.get('name') == 'eef'], 'no eef body'
assert [s for s in root.iter('force') if s.get('name') == 'force_ee'], 'no force_ee sensor'
assert [s for s in root.iter('torque') if s.get('name') == 'torque_ee'], 'no torque_ee sensor'
pos = root.findall('.//actuator/position')
assert len(pos) == 1, f'expected 1 position actuator, got {len(pos)}'
bad = [e.tag for e in root.iter() if 'kv' in e.attrib]
assert not bad, f'MuJoCo-3.x velocity-gain attribute on: {bad}'
geoms = {g.get('name') for g in root.iter('geom')}
assert {'fixed_jaw_collision','moving_jaw_collision'} <= geoms, 'missing jaw collision geoms'
for m in root.iter('mesh'):
    assert m.get('name') and m.get('file','').startswith('../robots/soarm101/assets/'), ET.tostring(m)
assert not root.findall('.//default'), 'default block(s) present'
print('GRIPPER XML OK — compiles under MuJoCo 2.3.7, contract sites/sensors present')
"</automated>
  </verify>
  <acceptance_criteria>
    - `mujoco.MjModel.from_xml_path` succeeds on soarm_gripper.xml under MuJoCo 2.3.7
    - ElementTree assertions pass: body `eef`; sites ft_frame, grip_site, grip_site_cylinder, ee, ee_x, ee_y, ee_z; sensors force_ee + torque_ee; exactly 1 position actuator; geoms fixed_jaw_collision + moving_jaw_collision; every mesh named with file under ../robots/soarm101/assets/; zero default blocks; zero elements carrying the MuJoCo-3.x velocity-gain attribute
    - `conda run -n libero python explorations/soarm_sanity.py --check compile` exits 0 (GREEN — both ARM_XML and GRIPPER_XML now compile; was RED after 02-01)
  </acceptance_criteria>
  <done>Gripper MJCF compiles standalone and satisfies every robosuite hard requirement from Pitfall 4; the harness compile check flips RED→GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create MountedSoarm101 and SoarmGripper classes (+ new grippers package)</name>
  <files>LIBERO/libero/libero/envs/robots/soarm.py, LIBERO/libero/libero/envs/grippers/__init__.py, LIBERO/libero/libero/envs/grippers/soarm_gripper.py</files>
  <read_first>
    - LIBERO/libero/libero/envs/robots/on_the_ground_panda.py (the exact class-shape analog: __init__ + set_joint_attribute + 8 properties)
    - /opt/homebrew/Caskroom/miniconda/base/envs/libero/lib/python3.9/site-packages/robosuite/models/grippers/panda_gripper.py (read-only analog for format_action/speed/dof/_important_geoms)
    - .planning/phases/02-soarm-robot-integration/02-RESEARCH.md — Pattern 2 (full MountedSoarm101 starting values) and Pattern 3 (SoarmGripper 1-actuator variant)
    - .planning/phases/02-soarm-robot-integration/02-PATTERNS.md — sections "soarm.py" and "soarm_gripper.py" (deviations from the Panda analogs, ASSETS-constant pattern)
    - LIBERO/libero/libero/assets/robots/soarm101/robot.xml (joint count/names the class contract must match)
  </read_first>
  <behavior>
    - MountedSoarm101(idn=0) instantiates: robot.xml resolves via fork-local path, set_joint_attribute damping array length matches the 5 arm joints
    - default_mount returns None (D-04: no pedestal despite the Mounted name); default_gripper returns "SoarmGripper"; arm_type returns "single"
    - init_qpos returns np.zeros(5) (D-07: new-calib zero IS the documented home pose)
    - base_xpos_offset["table"](L) returns z == 0.90 for any L (base ON the tabletop — SO101 reach 0.479 m vs table surface 0.90 m, Pitfall 6)
    - SoarmGripper(idn=0) instantiates: dof == 1, format_action integrates a 1-element action with speed 0.10, init_qpos == [0.8] (jaw open)
  </behavior>
  <action>
    Create `soarm.py` mirroring on_the_ground_panda.py's shape with SOARM values per RESEARCH Pattern 2, and `soarm_gripper.py` per Pattern 3, plus an empty-docstring `grippers/__init__.py` to make the new package importable (D-08: real fork modules, not notebook code).

    soarm.py concrete targets: module-level `ASSETS = os.path.abspath(os.path.join(os.path.dirname(__file__), "..", "..", "assets"))` (do NOT use robosuite's xml_path_completion — it resolves inside site-packages, PATTERNS deviation note); class `MountedSoarm101(ManipulatorModel)` — named Mounted because Libero_Tabletop_Manipulation prepends "Mounted" to the robots kwarg (Pitfall 1); document that naming irony in the class docstring. `__init__(self, idn=0)` calls super with os.path.join(ASSETS, "robots/soarm101/robot.xml") then `set_joint_attribute(attrib="damping", values=np.array((0.6,)*5))` — 5 elements, not Panda's 7 (assert-fails otherwise). Properties: default_mount None (D-04); default_gripper "SoarmGripper"; default_controller_config "default_panda" (fallback only — LIBERO always passes OSC_POSE explicitly); init_qpos np.zeros(5) (D-07); base_xpos_offset dict from RESEARCH Pattern 2 — "table"/"kitchen_table"/"study_table" lambdas return (-0.38, 0, 0.90), "coffee_table" (-0.30, 0, 0.41), "living_room_table" (-0.30, 0, 0.42), "bins" (-0.5, -0.1, 0), "empty" (-0.6, 0, 0) — starting points, tuned in plan 02-03 per D-11; top_offset np.array((0, 0, 0.30)); _horizontal_radius 0.25; arm_type "single".

    soarm_gripper.py concrete targets: same ASSETS constant pattern; class `SoarmGripper(GripperModel)` with __init__ passing os.path.join(ASSETS, "grippers/soarm_gripper.xml"); format_action asserting len(action) == self.dof then integrating self.current_action via np.clip(self.current_action + self.speed * np.sign(action), -1.0, 1.0) — the single-actuator collapse of Panda's two-finger direction array (PATTERNS deviation); init_qpos np.array([0.8]) (jaw open, tune in 02-03 if needed — RESEARCH A3); speed 0.10 (rad-scale jaw vs Panda's 0.01 m fingers); dof 1 (total action dim = OSC 6 + 1 = 7, the Phase 3 VLA contract); _important_geoms mapping left_finger/left_fingerpad to ["fixed_jaw_collision"] and right_finger/right_fingerpad to ["moving_jaw_collision"] (names from Task 1's XML).

    Follow project conventions: snake_case files, 4-space indent, module docstrings.
  </action>
  <verify>
    <automated>conda run -n libero python -c "
import os, sys
os.environ['MUJOCO_GL'] = 'glfw'
sys.path.insert(0, os.path.abspath('LIBERO'))
import numpy as np
from libero.libero.envs.robots.soarm import MountedSoarm101
from libero.libero.envs.grippers.soarm_gripper import SoarmGripper
r = MountedSoarm101(idn=0)
assert r.default_mount is None, 'D-04 violated: default_mount must be None'
assert r.default_gripper == 'SoarmGripper'
assert r.init_qpos.shape == (5,) and not r.init_qpos.any(), 'D-07: init_qpos must be zeros(5)'
assert r.arm_type == 'single'
tb = r.base_xpos_offset['table'](1.0)
assert abs(tb[2] - 0.90) < 1e-9, f'table lambda z must be 0.90 (tabletop), got {tb[2]}'
g = SoarmGripper(idn=0)
assert g.dof == 1
a = g.format_action(np.array([1.0]))
assert a.shape == (1,), f'format_action shape {a.shape}'
print('CLASSES OK — both instantiate, property contract satisfied')
"</automated>
  </verify>
  <acceptance_criteria>
    - Both classes instantiate from a fresh interpreter with MUJOCO_GL set and LIBERO on sys.path (proves both XML paths resolve and the 5-element damping array matches the arm joint count)
    - Property assertions pass: default_mount is None, default_gripper == "SoarmGripper", init_qpos == zeros(5), arm_type == "single", base_xpos_offset["table"](L)[2] == 0.90, gripper dof == 1, format_action returns shape (1,)
    - `grep -c "xml_path_completion" LIBERO/libero/libero/envs/robots/soarm.py LIBERO/libero/libero/envs/grippers/soarm_gripper.py | grep -v ':0$' | wc -l` returns 0 (fork-local ASSETS paths only)
  </acceptance_criteria>
  <done>Both model classes exist as importable fork modules and pass the full property-contract assertion suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Register in ROBOT_CLASS_MAPPING + GRIPPER_MAPPING — flip --check model GREEN</name>
  <files>LIBERO/libero/libero/envs/robots/__init__.py</files>
  <read_first>
    - LIBERO/libero/libero/envs/robots/__init__.py (current 13-line registration block — extend, don't replace)
    - .planning/phases/02-soarm-robot-integration/02-PATTERNS.md — section "robots/__init__.py (registration — MODIFY)" (exact extension including the relative-import correction)
    - explorations/soarm_sanity.py — check_model() (the failing test this task turns green)
  </read_first>
  <behavior>
    - Importing libero.libero.envs.robots puts "MountedSoarm101" into robosuite ROBOT_CLASS_MAPPING (mapped to SingleArm) and "SoarmGripper" into GRIPPER_MAPPING
    - Existing "MountedPanda" and "OnTheGroundPanda" entries survive unchanged
    - soarm_sanity.py --check model exits 0 (RED since 02-01 → GREEN)
  </behavior>
  <action>
    Extend `robots/__init__.py` per the PATTERNS registration section (ENV-05, D-08): add `from .soarm import MountedSoarm101` (class definition auto-registers in robosuite REGISTERED_ROBOTS via RobotModelMeta — the import IS the model registration); add `"MountedSoarm101": SingleArm` to the existing ROBOT_CLASS_MAPPING.update dict alongside the two Panda entries; add `from robosuite.models.grippers import GRIPPER_MAPPING`, `from ..grippers.soarm_gripper import SoarmGripper` (relative import — NOT the absolute LIBERO.libero... path shown in RESEARCH Pattern 3, because the package imports as libero.libero.envs), and `GRIPPER_MAPPING["SoarmGripper"] = SoarmGripper` (gripper_factory asserts membership). Anti-pattern guard: touch nothing in robosuite site-packages — all dict mutation happens in the fork at import time.
  </action>
  <verify>
    <automated>conda run -n libero python explorations/soarm_sanity.py --check model && conda run -n libero python -c "
import os, sys
os.environ['MUJOCO_GL'] = 'glfw'
sys.path.insert(0, os.path.abspath('LIBERO'))
import libero.libero.envs.robots  # noqa: F401 — registration side effects
from robosuite.robots import ROBOT_CLASS_MAPPING
from robosuite.models.grippers import GRIPPER_MAPPING
for k in ('MountedPanda', 'OnTheGroundPanda', 'MountedSoarm101'):
    assert k in ROBOT_CLASS_MAPPING, f'{k} missing from ROBOT_CLASS_MAPPING'
assert 'SoarmGripper' in GRIPPER_MAPPING
print('REGISTRATION OK — Panda entries preserved, SOARM entries added')
"</automated>
  </verify>
  <acceptance_criteria>
    - `conda run -n libero python explorations/soarm_sanity.py --check model` exits 0 and prints its PASS line (was RED after 02-01)
    - ROBOT_CLASS_MAPPING contains all three of MountedPanda, OnTheGroundPanda, MountedSoarm101 after importing libero.libero.envs.robots
    - GRIPPER_MAPPING contains SoarmGripper
    - `git diff --stat` for this task touches only LIBERO/libero/libero/envs/robots/__init__.py
  </acceptance_criteria>
  <done>SOARM is selectable by name exactly as Panda is today (phase success criterion 3); --check compile and --check model are both GREEN.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols created by this plan (new — exclude from drift verification):

- Classes: `MountedSoarm101` (ManipulatorModel subclass, `LIBERO/libero/libero/envs/robots/soarm.py`); `SoarmGripper` (GripperModel subclass, `LIBERO/libero/libero/envs/grippers/soarm_gripper.py`)
- New package: `libero.libero.envs.grippers` via `LIBERO/libero/libero/envs/grippers/__init__.py`
- Module constants: `ASSETS` (in both soarm.py and soarm_gripper.py)
- Class members: `MountedSoarm101.default_mount/default_gripper/default_controller_config/init_qpos/base_xpos_offset/top_offset/_horizontal_radius/arm_type`; `SoarmGripper.format_action/init_qpos/speed/dof/_important_geoms`
- Registration keys: `"MountedSoarm101"` in robosuite `ROBOT_CLASS_MAPPING`; `"SoarmGripper"` in robosuite `GRIPPER_MAPPING` (added in `LIBERO/libero/libero/envs/robots/__init__.py`)
- Files: `LIBERO/libero/libero/assets/grippers/soarm_gripper.xml`
- MJCF symbols (gripper XML): bodies `right_gripper`, `eef`; joint `gripper`; sites `ft_frame`, `grip_site`, `grip_site_cylinder`, `ee`, `ee_x`, `ee_y`, `ee_z`; sensors `force_ee`, `torque_ee`; geoms `fixed_jaw_collision`, `moving_jaw_collision`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | All geometry consumed here was vendored and SHA-pinned in plan 02-01; this plan adds fork-local code only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | Vendored SO-ARM100 XML + STLs | medium | mitigate | Already mitigated in 02-01 (pinned SHA + VENDOR.txt sha256 list); this plan only references those git-tracked files |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package-manager installs this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- Task 1 gripper-XML structural check passes (MuJoCo 2.3.7 compile + ElementTree contract assertions)
- Task 2 class-contract assertion suite passes
- `conda run -n libero python explorations/soarm_sanity.py --check compile` exits 0 (GREEN)
- `conda run -n libero python explorations/soarm_sanity.py --check model` exits 0 (GREEN)
- Expected RED at plan end: `--check reset|render|soak|tasks` still fail — physics/base placement tuning is plan 02-03, render/task selection is plan 02-04 (MVP RED→GREEN progression)
</verification>

<success_criteria>
- Both SOARM MJCFs compile standalone under MuJoCo 2.3.7 (ENV-04 complete: adapted MJCF + registered ManipulatorModel subclass)
- MountedSoarm101 and SoarmGripper resolve by name through the same mappings Panda uses (ENV-05 mapping half; phase success criterion 3)
- No robosuite site-packages file modified; all changes live in the LIBERO fork (D-08)
</success_criteria>

<output>
Create `.planning/phases/02-soarm-robot-integration/02-02-SUMMARY.md` when done. Record: final gripper joint range/kp/forcerange values taken from the source XML, any deviation from the Pattern 2/3 starting values, and anything learned about the merge/namespacing that plan 02-03's reset debugging should know.
</output>
