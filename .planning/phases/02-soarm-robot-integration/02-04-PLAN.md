---
phase: 02-soarm-robot-integration
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - LIBERO/libero/libero/assets/robots/soarm101/robot.xml
  - explorations/soarm_sanity.py
autonomous: true
requirements: [ENV-06, ENV-07]
must_haves:
  truths:
    - "soarm_sanity.py --check render passes and the two saved PNGs show a right-side-up scene with the SOARM arm visible, intact meshes, and the gripper jaws in the eye_in_hand frame"
    - "soarm_sanity.py --check tasks passes: the 3 finalized libero_spatial BDDLs each run 50 random-action steps with robots=['Soarm101'] without crashing (ENV-06, phase success criterion 4 — local)"
    - "soarm_sanity.py --check all passes: the complete local gate is GREEN before any Colab work"
  artifacts:
    - path: "explorations/outputs/soarm_agentview.png"
      provides: "Rendered agentview frame proving right-side-up orientation and mesh integrity (ENV-07 local evidence)"
    - path: "explorations/outputs/soarm_eye_in_hand.png"
      provides: "Rendered wrist-camera frame proving the tuned eye_in_hand pose (jaws visible, workspace centered)"
    - path: "explorations/soarm_sanity.py"
      provides: "Finalized TASKS constant: the 3 selected libero_spatial BDDL filenames (D-03 resolved)"
      contains: "TASKS"
  key_links:
    - from: "LIBERO/libero/libero/assets/robots/soarm101/robot.xml"
      to: "explorations/outputs/soarm_eye_in_hand.png"
      via: "eye_in_hand camera pos/quat inside the right_hand body drives the robot0_eye_in_hand observation"
      pattern: "eye_in_hand"
    - from: "explorations/soarm_sanity.py"
      to: "LIBERO/libero/libero/bddl_files/libero_spatial/"
      via: "TASKS entries resolve as BDDL_DIR/<name>.bddl at env creation"
      pattern: "\\.bddl"
---

# Plan 02-04: Render Correctness (Camera Tuning) + Final 3 BDDL Task Selection

## Phase Goal

**As a** VLA researcher, **I want to** select the SOARM SO101 robot by name in LIBERO and run libero_spatial tasks with it — with stable physics and correct rendering, **so that** the Phase 3 inference loop can drive SOARM from language prompts instead of the stock Panda.

<objective>
Finish the local slice of the phase: tune the `eye_in_hand` camera pose by render iteration until frames are visually correct (ENV-07's visual half — right-side-up, correct camera angle, no mesh artifacts), then resolve D-03 by selecting the final 3 `libero_spatial` tasks from actual region coordinates versus the 02-03-tuned base position, and prove them crash-free (ENV-06). Ends with the full local gate (`--check all`) GREEN.

All work is local CPU per D-09. The rendered PNGs are read and judged directly during execution (the executor can view images); the authoritative human visual sign-off happens on Colab in plan 02-05 per D-10.

Purpose: ENV-06 and the visual half of ENV-07 are the last two requirements with no local evidence; this plan produces that evidence and freezes the task list the 02-05 notebook and Phase 3 will use.
Output: tuned eye_in_hand camera in robot.xml, finalized TASKS constant in soarm_sanity.py, two rendered PNG artifacts, full local gate GREEN.
</objective>

<execution_context>
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/workflows/execute-plan.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-soarm-robot-integration/02-CONTEXT.md
@.planning/phases/02-soarm-robot-integration/02-RESEARCH.md
@.planning/phases/02-soarm-robot-integration/02-PATTERNS.md
@.planning/phases/02-soarm-robot-integration/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tune the eye_in_hand camera by render loop — flip --check render GREEN</name>
  <files>LIBERO/libero/libero/assets/robots/soarm101/robot.xml</files>
  <read_first>
    - explorations/soarm_sanity.py — check_render() (saves soarm_agentview.png and soarm_eye_in_hand.png with the [::-1] flip, asserts non-black frames)
    - LIBERO/libero/libero/assets/robots/soarm101/robot.xml (current eye_in_hand camera element: Panda starting pos "0.05 0 0" quat "0 0.707108 0.707108 0" fovy "75" inside right_hand)
    - .planning/phases/02-soarm-robot-integration/02-RESEARCH.md — Pitfall 5, Open Question 3 (camera acceptance: jaws visible at frame bottom, workspace centered), and the "Rendering correctness check" code example
    - .planning/phases/02-soarm-robot-integration/02-PATTERNS.md — "Frame flip convention" (obs frames are already flipped by the harness; judge the saved PNGs as-is)
  </read_first>
  <action>
    Iterate: run `conda run -n libero python explorations/soarm_sanity.py --check render`, Read both saved PNGs (explorations/outputs/soarm_agentview.png, explorations/outputs/soarm_eye_in_hand.png), judge against ENV-07's criteria, adjust ONLY the eye_in_hand camera pos/quat (and fovy if the workspace doesn't fit) inside the right_hand body of robot.xml, repeat until:

    - soarm_agentview.png: scene right-side-up (table below the background, objects resting ON the table), SOARM arm fully visible on the tabletop, no missing/garbled STL meshes, no z-fighting artifacts. If agentview looks wrong, the problem is upstream (frame flip in the harness or mesh issues) — fix the actual cause, do not touch the arena camera.
    - soarm_eye_in_hand.png: gripper jaws visible at the bottom of the frame, table workspace centered, not all-black (all-black means the camera points inside a mesh or away from the scene — flip the quat first). SO101's right_hand frame differs from Panda's, so expect the Panda starting quat to need correction; a camera looking along the gripper approach axis is the target (Open Question 3).

    Only the camera element changes in this task — physics values from 02-03 are frozen. If a mesh artifact (not camera pose) is discovered, fix the mesh element or geom referencing it and record it in the SUMMARY.
  </action>
  <verify>
    <automated>conda run -n libero python explorations/soarm_sanity.py --check render && test -s explorations/outputs/soarm_agentview.png && test -s explorations/outputs/soarm_eye_in_hand.png && echo "RENDER OK"</automated>
  </verify>
  <acceptance_criteria>
    - `conda run -n libero python explorations/soarm_sanity.py --check render` exits 0 (both frames non-black, correct shape, files saved with "Saved →" lines)
    - Both PNGs exist and are non-empty in explorations/outputs/
    - Executor has Read both PNGs and states in the SUMMARY, per frame, which ENV-07 criteria were visually confirmed (right-side-up, arm visible, meshes intact, jaws visible in wrist cam)
    - `--check reset` still exits 0 (camera edits caused no physics regression)
  </acceptance_criteria>
  <done>Both camera views render visually correct frames locally; the eye_in_hand pose is frozen for the 02-05 Colab verification and Phase 5's wrist-camera reuse (SPAT-01).</done>
</task>

<task type="auto">
  <name>Task 2: Finalize the 3 BDDL tasks (D-03) — flip --check tasks GREEN, then --check all</name>
  <files>explorations/soarm_sanity.py</files>
  <read_first>
    - LIBERO/libero/libero/bddl_files/libero_spatial/ (all 10 .bddl files — the :regions blocks give each object region's x/y ranges)
    - explorations/soarm_sanity.py — TASKS constant (the 3 candidates from 02-01) and check_tasks()
    - .planning/phases/02-soarm-robot-integration/02-03-SUMMARY.md (the tuned base x position — reach math input)
    - .planning/phases/02-soarm-robot-integration/02-RESEARCH.md — Pattern 4 (candidate rationale) and Pitfall 6 (0.479 m max horizontal reach)
  </read_first>
  <action>
    Resolve D-03 with data instead of assumption: parse the :regions coordinates of all 10 libero_spatial BDDLs, compute each task's object-region distance from the tuned base position (from 02-03-SUMMARY), and select the 3 tasks whose regions sit inside SO101's 0.479 m reach with the most margin. The 02-01 candidates (pick_up_the_black_bowl_from_table_center..., ...next_to_the_plate..., ...between_the_plate_and_the_ramekin...) are the prior — keep any that check out, swap any that don't for a closer-region libero_spatial task (D-01: stay in this suite). Update the TASKS constant in soarm_sanity.py if the final set differs; keep exactly 3 entries.

    Then run `conda run -n libero python explorations/soarm_sanity.py --check tasks` — each of the 3 BDDLs must build with robots=["Soarm101"], reset, and survive 50 random-action steps (ENV-06's bar is no-crash, not task success). Per D-02: if a task fails for reach/placement reasons, first swap to a different libero_spatial task; only modify a BDDL's scene/object placement as a last resort if no 3 tasks pass as-is, and document exactly what changed and why in the SUMMARY.

    Finish with `conda run -n libero python explorations/soarm_sanity.py --check all` — the complete local gate (compile, model, reset, render, soak, tasks) must be GREEN in one run before the phase moves to Colab.
  </action>
  <verify>
    <automated>conda run -n libero python explorations/soarm_sanity.py --check tasks && conda run -n libero python explorations/soarm_sanity.py --check all</automated>
  </verify>
  <acceptance_criteria>
    - `conda run -n libero python explorations/soarm_sanity.py --check tasks` exits 0 with a per-task PASS line for each of the 3 BDDLs
    - The TASKS constant contains exactly 3 .bddl filenames, all present in LIBERO/libero/libero/bddl_files/libero_spatial/
    - `conda run -n libero python explorations/soarm_sanity.py --check all` exits 0 (every check mode GREEN in a single run)
    - The SUMMARY lists the per-task region-distance numbers that justified the final selection (or the swap rationale if candidates changed)
    - No BDDL file modified unless the SUMMARY documents the D-02 last-resort justification
  </acceptance_criteria>
  <done>Three named libero_spatial tasks run end-to-end with SOARM locally (ENV-06); the entire local validation gate is GREEN and the phase is ready for Colab verification.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols created/finalized by this plan (new — exclude from drift verification):

- Finalized values: `TASKS` constant in `explorations/soarm_sanity.py` (final 3 libero_spatial BDDL filenames — D-03 resolved); `eye_in_hand` camera pos/quat/fovy in `LIBERO/libero/libero/assets/robots/soarm101/robot.xml`
- Files (generated outputs): `explorations/outputs/soarm_agentview.png`, `explorations/outputs/soarm_eye_in_hand.png`
- No new code symbols — this plan finalizes values inside 02-01 artifacts

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Local CPU rendering and BDDL parsing of repo-local files; no network, no installs, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package-manager installs this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `conda run -n libero python explorations/soarm_sanity.py --check render` exits 0; both PNGs saved and visually judged correct
- `conda run -n libero python explorations/soarm_sanity.py --check tasks` exits 0 for the 3 finalized BDDLs
- `conda run -n libero python explorations/soarm_sanity.py --check all` exits 0 — full local gate GREEN
- Remaining phase work: Colab GPU re-verification of ENV-04..07 with human visual sign-off (plan 02-05, D-10)
</verification>

<success_criteria>
- Rendered frames are visually correct locally: right-side-up, correct camera angle, no mesh artifacts (phase success criterion 2, local evidence)
- At least 3 named BDDL tasks run end-to-end with SOARM without crashing (phase success criterion 4, local evidence)
- The complete sanity harness passes in one `--check all` run
</success_criteria>

<output>
Create `.planning/phases/02-soarm-robot-integration/02-04-SUMMARY.md` when done. Record: final eye_in_hand pos/quat/fovy, per-frame visual confirmation notes, the 3 selected BDDL filenames with region-distance justification, and any D-02 swaps/edits — plan 02-05's notebook cells consume the task list and camera state verbatim.
</output>
