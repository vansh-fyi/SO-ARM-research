---
phase: 02-soarm-robot-integration
plan: 05
type: execute
wave: 5
depends_on: ["02-04"]
files_modified:
  - LIBERO/notebooks/02-soarm-integration-check.ipynb
autonomous: false
requirements: [ENV-04, ENV-05, ENV-06, ENV-07]
must_haves:
  truths:
    - "LIBERO/notebooks/02-soarm-integration-check.ipynb exists with one PASS/FAIL cell per requirement (ENV-04, ENV-05, SC-1 contact force, ENV-07 render, ENV-06 tasks) plus a final summary table (D-10)"
    - "The notebook reuses Phase 1's proven install/bootstrap sequence (Block A cells, restart STOP marker, EGL bootstrap, libero config bootstrap, numba stub shim) with MUJOCO_GL=egl"
    - "A human has run the notebook on a Colab GPU runtime and confirmed all PASS cells green and frames visually correct (right-side-up, correct camera angle, no mesh artifacts)"
  artifacts:
    - path: "LIBERO/notebooks/02-soarm-integration-check.ipynb"
      provides: "Colab GPU verification of ENV-04 through ENV-07 with PASS/FAIL cells (Wave 0 validation gap #2; D-10)"
      contains: "ENV-04"
  key_links:
    - from: "LIBERO/notebooks/02-soarm-integration-check.ipynb"
      to: "LIBERO/libero/libero/envs/robots/soarm.py"
      via: "notebook imports libero.libero.envs.robots (registration side effects) and creates envs with robots=['Soarm101']"
      pattern: "robots=\\[.Soarm101.\\]"
    - from: "LIBERO/notebooks/02-soarm-integration-check.ipynb"
      to: "explorations/soarm_sanity.py"
      via: "ENV-06 cell runs the same 3 finalized TASKS bddl filenames the local harness proved in 02-04"
      pattern: "\\.bddl"
---

# Plan 02-05: Colab Verification Notebook — ENV-04..07 PASS/FAIL + Human Sign-Off

## Phase Goal

**As a** VLA researcher, **I want to** select the SOARM SO101 robot by name in LIBERO and run libero_spatial tasks with it — with stable physics and correct rendering, **so that** the Phase 3 inference loop can drive SOARM from language prompts instead of the stock Panda.

<objective>
Build the Colab verification notebook (D-10: the ONLY part of this phase needing the GPU runtime) that re-proves every phase requirement on the target environment: ENV-04 (MJCF compiles + class registered), ENV-05 (mapping + env instantiation), SC-1 (contact force <10 N at reset), ENV-07 (rendered frames displayed for human visual judgment), ENV-06 (the 3 finalized BDDL tasks run without crashing). Then a blocking human checkpoint: the user runs it on Colab and signs off.

Local greens from 02-01..02-04 are necessary but not sufficient — the phase's success criteria are defined against the Colab environment where Phase 3 will actually run (EGL rendering, GPU, Phase 1's pinned dependency stack).

Purpose: closes the phase with the same PASS/FAIL UAT pattern Phase 1 established, producing the evidence `/gsd-verify-work` will check.
Output: `LIBERO/notebooks/02-soarm-integration-check.ipynb`, human-confirmed green on Colab.
</objective>

<execution_context>
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/workflows/execute-plan.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-soarm-robot-integration/02-CONTEXT.md
@.planning/phases/02-soarm-robot-integration/02-RESEARCH.md
@.planning/phases/02-soarm-robot-integration/02-PATTERNS.md
@.planning/phases/01-colab-environment-setup/01-DEBUG-HISTORY.md
@.planning/phases/02-soarm-robot-integration/02-03-SUMMARY.md
@.planning/phases/02-soarm-robot-integration/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build 02-soarm-integration-check.ipynb from the Phase 1 notebook pattern</name>
  <files>LIBERO/notebooks/02-soarm-integration-check.ipynb</files>
  <read_first>
    - .planning/phases/01-colab-environment-setup/01-DEBUG-HISTORY.md (REQUIRED per CONTEXT.md before any Colab work: the 7-invariant environment contract, the --no-deps install contract, restart-vs-clean-slate distinction)
    - LIBERO/notebooks/01-colab-env-setup.ipynb (source of the install cells, restart STOP marker, EGL bootstrap, libero config bootstrap, numba stub shim, and the canonical PASS/FAIL cell shape — Cell 20)
    - .planning/phases/02-soarm-robot-integration/02-PATTERNS.md — section "02-soarm-integration-check.ipynb" (exact cell-reuse map)
    - .planning/phases/02-soarm-robot-integration/02-RESEARCH.md — "Verification notebook skeleton" and the max_contact_force code example
    - .planning/phases/02-soarm-robot-integration/02-03-SUMMARY.md and 02-04-SUMMARY.md (tuned values, controller_configs dict if any, final TASKS list — the notebook must use these verbatim)
    - explorations/soarm_sanity.py (check logic to port into cells: settle-step recipe, contact-force gate, frame flip)
  </read_first>
  <action>
    Create the Colab verification notebook per D-10, structured as (cell-reuse map from PATTERNS):

    - Block A: Phase 1's install-cell sequence reused (GPU check, apt, pip cells in the proven order), then the restart-runtime STOP markdown cell. Environment contract from 01-DEBUG-HISTORY.md is binding — do not reorder or "simplify" install cells.
    - Post-restart bootstrap: EGL bootstrap first (NVIDIA ICD JSON before setting MUJOCO_GL=egl — Colab uses egl, NOT the local glfw), then ~/.libero/config.yaml bootstrap before any libero import, sys.path insertion for the repo's LIBERO dir, the numba stub compatibility shim copied verbatim (robosuite imports break on Colab without it), matplotlib.use("Agg") before pyplot. No HF token cell — this notebook loads no models, so the Phase 1 Drive-token pattern is intentionally omitted (no secret surface).
    - PASS/FAIL cells, one per criterion, each with a comment header naming the requirement ID, hard asserts for the automated part, an explicit "PASS — ..."/"FAIL — ..." print, and inline frame display where relevant (Phase 1 Cell 20 shape):
      - Cell ENV-04: mujoco.MjModel.from_xml_path on both soarm101/robot.xml and grippers/soarm_gripper.xml (repo-relative paths); import libero.libero.envs.robots; assert "MountedSoarm101" in robosuite REGISTERED_ROBOTS.
      - Cell ENV-05: assert "MountedSoarm101" in ROBOT_CLASS_MAPPING and "SoarmGripper" in GRIPPER_MAPPING; build OffScreenRenderEnv(bddl_file_name=<first TASKS entry>, robots=["Soarm101"], camera_heights=256, camera_widths=256) — pass the 02-03 controller_configs kwarg if the SUMMARY recorded one.
      - Cell SC-1: env.reset(), 10 zero-action settle steps, max_contact_force(env) < 10.0 (port the exact recipe), print the measured peak.
      - Cell ENV-07: display obs["agentview_image"][::-1] and obs["robot0_eye_in_hand_image"][::-1] inline (frame flip per convention) with a markdown note telling the human what to verify: right-side-up, arm visible, no mesh artifacts, jaws visible in the wrist view.
      - Cell ENV-06: loop over the 3 finalized TASKS bddl filenames (from 02-04, hardcoded verbatim), fresh env per task with robots=["Soarm101"], reset + 50 random-action steps, per-task PASS/FAIL print, overall assert.
    - Final markdown summary table mapping ENV-04/ENV-05/ENV-06/ENV-07 → status (Phase 1 Cell 24 pattern), extendable by Phase 3 the same way Phase 1's was designed for extension.
  </action>
  <verify>
    <automated>conda run -n libero python -c "
import json
nb = json.load(open('LIBERO/notebooks/02-soarm-integration-check.ipynb'))
src = '\n'.join(''.join(c.get('source', [])) for c in nb['cells'])
for marker in ('ENV-04', 'ENV-05', 'ENV-06', 'ENV-07', 'MUJOCO_GL', 'egl', 'max_contact_force', '[::-1]'):
    assert marker in src, f'missing marker: {marker}'
assert 'robots=[\"Soarm101\"]' in src.replace(\"'\", '\"'), 'no Soarm101 env creation'
assert src.count('.bddl') >= 3, 'fewer than 3 bddl references'
kinds = {c['cell_type'] for c in nb['cells']}
assert kinds == {'markdown', 'code'} or kinds == {'code', 'markdown'}
print(f'NOTEBOOK OK — {len(nb[\"cells\"])} cells, all requirement markers present')
"</automated>
  </verify>
  <acceptance_criteria>
    - The notebook file parses as valid JSON with markdown+code cells and contains cell markers for all four requirement IDs plus MUJOCO_GL=egl, max_contact_force, the [::-1] frame flip, robots=["Soarm101"], and at least 3 .bddl references
    - The MUJOCO_GL assignment appears in the post-restart bootstrap before any mujoco/robosuite/libero import cell (EGL bootstrap first — 01-DEBUG-HISTORY invariant)
    - The ENV-06 cell's 3 bddl filenames exactly match the TASKS constant in explorations/soarm_sanity.py
    - No token/secret literal appears anywhere in the notebook source
  </acceptance_criteria>
  <done>The notebook is structurally complete, contract-compliant with Phase 1's environment invariants, and ready for a Colab run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human runs the notebook on Colab — phase verification sign-off</name>
  <read_first>
    - LIBERO/notebooks/02-soarm-integration-check.ipynb (what you are about to run)
  </read_first>
  <action>Blocking human verification per D-10 (Claude cannot execute Colab runtimes): present the how-to-verify steps below to the user, wait for the resume signal, and do not proceed to phase closure until every PASS cell is confirmed green and the ENV-07 frames are visually approved. If the user reports a failure, capture the failing cell output, fix locally (re-running the relevant 02-03/02-04 checks), push, and re-request the Colab run.</action>
  <what-built>Plans 02-01..02-04 built and locally validated the complete SOARM integration: vendored SO101 assets, adapted arm+gripper MJCFs, MountedSoarm101/SoarmGripper classes, LIBERO registration, tuned physics (<10 N reset), tuned cameras, and 3 crash-free libero_spatial tasks. Task 1 of this plan packaged all of it into a Colab PASS/FAIL notebook mirroring Phase 1's UAT pattern.</what-built>
  <how-to-verify>
    1. Push the current branch (the notebook reads repo files — Colab must see the same commit).
    2. Open LIBERO/notebooks/02-soarm-integration-check.ipynb in Google Colab on a GPU runtime (T4 is sufficient — no VLA model loads in this notebook).
    3. Run the Block A install cells top-to-bottom, then STOP at the restart marker and do Runtime → Restart runtime (NOT disconnect/delete).
    4. Run all post-restart cells in order.
    5. Confirm each automated cell prints PASS: ENV-04 (compile + registration), ENV-05 (mapping + env instantiation), SC-1 (peak contact force printed, below 10 N), ENV-06 (3/3 tasks no-crash).
    6. Visually judge the ENV-07 frames: agentview right-side-up with the SOARM arm on the table and intact meshes; eye_in_hand showing the gripper jaws with the workspace centered.
    7. Confirm the final summary table shows all four requirements green.
  </how-to-verify>
  <acceptance_criteria>
    - All automated PASS/FAIL cells print PASS on the Colab GPU runtime
    - Human confirms the two displayed frames meet ENV-07's visual criteria (right-side-up, correct camera angle, no mesh artifacts)
    - Final summary table shows ENV-04, ENV-05, ENV-06, ENV-07 all green
  </acceptance_criteria>
  <resume-signal>Type "approved" if every cell is green and frames look correct; otherwise paste the first failing cell's output (and screenshot the frames if the failure is visual)</resume-signal>
  <done>Human sign-off received: all automated PASS cells green on Colab GPU and ENV-07 frames visually approved — phase requirements verified on the target environment.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols created by this plan (new — exclude from drift verification):

- Files: `LIBERO/notebooks/02-soarm-integration-check.ipynb`
- Notebook cell markers/identifiers: PASS/FAIL cells headed `ENV-04`, `ENV-05`, `SC-1`, `ENV-07`, `ENV-06`; final requirement-status summary table; ported `max_contact_force` helper inside the SC-1 cell
- No new library code symbols — the notebook consumes symbols created in plans 02-01..02-04

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Colab runtime → repo | Notebook executes on Google's runtime against the pushed repo commit; install cells fetch the Phase 1 pinned dependency set |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | Information disclosure | Notebook secrets on Colab | low | mitigate | This notebook needs no HF token (no model download) — omit all token handling; if a later edit ever needs one, reuse Phase 1's Drive-file pattern (MyDrive/SoARM-Research/.hf_token), never inline secrets in cells |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | Install cells reuse Phase 1's already-verified pinned sequence verbatim (RESEARCH Package Legitimacy Audit: no new packages this phase) |
</threat_model>

<verification>
- Task 1 structural check passes locally (JSON-valid notebook, all requirement markers, egl bootstrap ordering, TASKS parity with soarm_sanity.py)
- Human checkpoint approved: all PASS cells green on Colab GPU, ENV-07 frames visually confirmed
- Phase-complete state: local `--check all` GREEN (02-04) + Colab notebook GREEN (this plan) — ready for `/gsd-verify-work`
</verification>

<success_criteria>
- All four phase requirements re-verified on the Colab GPU environment where Phase 3 will run (D-10)
- Human visual sign-off on rendered frames (the manual-only half of ENV-07)
- Notebook committed and extendable by Phase 3, mirroring the Phase 1 → Phase 2 extension pattern
</success_criteria>

<output>
Create `.planning/phases/02-soarm-robot-integration/02-05-SUMMARY.md` when done. Record: Colab run outcome per requirement, the measured Colab peak contact force, any Colab-vs-local divergence discovered (and how it was fixed), and the Pitfall 8 note for Phase 3 (5-DOF arm under 6-DOF OSC — partial orientation tracking is structural; action dim stays 7).
</output>
