---
phase: 03-vla-inference-loop
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - LIBERO/libero/libero/vla/pi0_backend.py
  - LIBERO/libero/libero/vla/__init__.py
  - LIBERO/notebooks/03b-pi0-inference-smoketest.ipynb
autonomous: false
requirements: [VLA-04]
user_setup:
  - service: openpi (Physical Intelligence)
    why: "Second VLA backend (π0/π0-FAST) proving the shared predict() interface is swappable — must run real inference on Colab, not a stub (D-05)"
    env_vars: []
    dashboard_config: []

must_haves:
  truths:
    - "Pi0Backend implements the exact same VLABackend.predict(images, language) -> np.ndarray interface as OFTBackend, with zero changes to eval_loop.py's run_episode/run_suite code (ROADMAP success criterion #3, VLA-04)"
    - "Notebook B runs real π0/π0-FAST inference on Colab (via a separate openpi kernel serving a websocket policy) against at least 1 frozen libero_spatial SOARM task for 1-2 episodes (D-05, D-10) — not an interface-only stub"
    - "openpi's torch/transformers/jax dependency stack never gets installed into the same kernel as OFT's — confirmed via two separate notebooks/kernels (D-06), avoiding the version conflict documented in 03-RESEARCH.md"
  artifacts:
    - LIBERO/libero/libero/vla/pi0_backend.py
    - LIBERO/notebooks/03b-pi0-inference-smoketest.ipynb
  key_links:
    - "Pi0Backend.predict() connects to serve_policy.py via openpi_client.WebsocketClientPolicy — the same eval_loop.run_episode() from Plan 01 drives it, unmodified"
    - "Notebook B's env/eval-loop-driving cell imports LIBERO/libero/libero/vla (Pi0Backend, run_episode/run_suite) exactly as Notebook A imports OFTBackend — proving the interface swap literally, not just structurally"
    - "Notebook B binds the websocket server to localhost/127.0.0.1 only (not 0.0.0.0) per 03-RESEARCH.md Security Domain"
</must_haves>
---

<objective>
Build the π0 (openpi) VLA backend and a lightweight Colab smoke-test notebook proving the shared `predict(images, language) -> action` interface is genuinely swappable — the same `eval_loop.run_episode()`/`run_suite()` code from Plan 01 must drive π0 exactly as it drives OFT, with zero downstream code changes (ROADMAP success criterion #3, VLA-04).

Purpose: This is the phase's second vertical slice — it proves the interface abstraction is real, not just a paper design. Per D-05 this must be real, working inference (not a stub): openpi's own documented remote-inference pattern (separate process/kernel + websocket client) sidesteps the confirmed torch/transformers/jax dependency conflict with OFT's stack without touching Notebook A's environment.
Output: `LIBERO/libero/libero/vla/pi0_backend.py` (websocket client wrapper) and `LIBERO/notebooks/03b-pi0-inference-smoketest.ipynb` (separate-kernel Colab notebook: openpi install + serve_policy.py server + client-driven 1-task x 1-2-episode smoke test via the same eval_loop code Notebook A uses).
</objective>

<execution_context>
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/workflows/execute-plan.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-vla-inference-loop/03-CONTEXT.md
@.planning/phases/03-vla-inference-loop/03-RESEARCH.md
@.planning/phases/03-vla-inference-loop/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pi0Backend — websocket client implementing VLABackend</name>
  <files>LIBERO/libero/libero/vla/pi0_backend.py, LIBERO/libero/libero/vla/__init__.py</files>
  <read_first>
    LIBERO/libero/libero/vla/interface.py (the VLABackend Protocol this class must satisfy — Plan 01 output; read to confirm exact predict() signature)
    LIBERO/libero/libero/vla/oft_backend.py (sibling backend from Plan 01 — mirror its method-signature shape: __init__ connects/loads, predict(images, language) returns ndarray — for structural consistency only, not the RPC mechanics)
    .planning/phases/03-vla-inference-loop/03-RESEARCH.md (Pattern 1 pi0_backend.py example + Code Examples section — openpi_client.websocket_client_policy.WebsocketClientPolicy usage, image_tools.resize_with_pad/convert_to_uint8, observation dict keys "observation/image", "observation/wrist_image", "observation/state", "prompt" — this is openpi's own documented remote-inference contract, treat as authoritative since no in-repo analog exists)
  </read_first>
  <behavior>
    - Test 1: `Pi0Backend.__init__(host="localhost", port=8000)` constructs `openpi_client.websocket_client_policy.WebsocketClientPolicy(host, port)` and stores it as `self.client` — verified via a mock/monkeypatched `WebsocketClientPolicy` class asserting it was called with `("localhost", 8000)`.
    - Test 2: `predict(images, language)` builds an observation dict with keys `"observation/image"`, `"observation/wrist_image"`, `"observation/state"`, `"prompt"` — both image keys built from `images["eye_in_hand"]` via `image_tools.convert_to_uint8(image_tools.resize_with_pad(images["eye_in_hand"], 224, 224))` (per D-01, this phase only populates the `"eye_in_hand"` key of the input dict; both openpi obs keys reuse it since no separate wrist camera exists in this phase's scope) and `"prompt": language` — verified via a mock client capturing the `infer()` call's argument dict and asserting all 4 keys are present with the right shapes/types.
    - Test 3: `predict()` calls `self.client.infer(obs)["actions"]` and returns the result as `np.ndarray` — verified via a mock client returning `{"actions": np.zeros((10, 7))}` and asserting `predict()` returns that array unchanged.
  </behavior>
  <action>
    Implement `Pi0Backend` in `pi0_backend.py`. `__init__(self, host="localhost", port=8000)`: import `from openpi_client import websocket_client_policy as wcp` and `from openpi_client import image_tools`; set `self.client = wcp.WebsocketClientPolicy(host, port)`. Per D-02, no shared normalization layer — all normalization is openpi's internal responsibility once observations are handed to `infer()`. `predict(self, images: dict, language: str) -> np.ndarray`: build `img = image_tools.convert_to_uint8(image_tools.resize_with_pad(np.asarray(images["eye_in_hand"]), 224, 224))`; construct `obs = {"observation/image": img, "observation/wrist_image": img, "observation/state": self._robot_state(), "prompt": language}` where `_robot_state()` is a small helper method returning a zero-vector placeholder of the appropriate dimensionality if no proprioceptive state is wired yet in this phase (document this as a documented limitation in a comment, not a silent gap — Phase 3's scope per CONTEXT.md is proving the interface swap works, and openpi's LIBERO configs are documented to tolerate a state vector; do not skip the key entirely, as `serve_policy.py --env=pi0_fast_libero` expects it present). Call `result = self.client.infer(obs)` and return `np.asarray(result["actions"])`. Update `vla/__init__.py` to add `from .pi0_backend import Pi0Backend` (wrap this import in a try/except ImportError with a clear message if `openpi_client` isn't installed in the current kernel — this module is only usable from Notebook B's kernel, never Notebook A's, per D-06's confirmed dependency conflict, so `vla/__init__.py` must not hard-fail Notebook A's `from libero.libero.vla import OFTBackend` if `openpi_client` is absent there).
  </action>
  <acceptance_criteria>
    - `grep -n "observation/image\|observation/wrist_image\|observation/state\|\"prompt\"" LIBERO/libero/libero/vla/pi0_backend.py` finds all four obs dict keys
    - `grep -n "resize_with_pad(.*224, 224)" LIBERO/libero/libero/vla/pi0_backend.py` confirms the 224x224 resize per openpi's documented contract
    - `grep -n "class Pi0Backend" LIBERO/libero/libero/vla/pi0_backend.py` == 1 occurrence
    - `grep -n "try:" LIBERO/libero/libero/vla/__init__.py` shows the Pi0Backend import is exception-guarded so Notebook A's kernel (no openpi_client installed) doesn't break
    - `python3 -m pytest LIBERO/libero/libero/vla/test_pi0_backend.py -q` (new test file, mock WebsocketClientPolicy, no real network/openpi install needed) passes 3/3
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest LIBERO/libero/libero/vla/test_pi0_backend.py -q</automated>
  </verify>
  <done>pi0_backend.py implements Pi0Backend satisfying VLABackend via openpi's documented WebsocketClientPolicy client pattern; vla/__init__.py imports it defensively so it never breaks OFT-only kernels; a local mock-based pytest file proves the obs-dict construction and predict() plumbing without needing openpi installed.</done>
</task>

<task type="auto">
  <name>Task 2: Notebook B — openpi install + serve_policy.py server + client smoke test (VLA-04)</name>
  <files>LIBERO/notebooks/03b-pi0-inference-smoketest.ipynb</files>
  <read_first>
    LIBERO/notebooks/02-soarm-integration-check.ipynb (cells 1-3, 13-18 — Drive mount/REPO_ROOT/GPU-assertion/BLOCK A-B markdown-divider/restart-stop cell conventions to copy identically; this notebook needs the LIBERO env + client in one kernel, per 03-RESEARCH.md's architecture diagram, while serve_policy.py runs as a background process in the SAME Colab VM but ideally an isolated venv)
    .planning/phases/03-vla-inference-loop/03-RESEARCH.md (Installation section: `git clone --recurse-submodules ... && GIT_LFS_SKIP_SMUDGE=1 uv sync && GIT_LFS_SKIP_SMUDGE=1 uv pip install -e .`; Package Legitimacy Audit table — openpi-client flagged SUS, requires a human-verify checkpoint before install per the audit's disposition; pi0_fast_libero config name, gs://openpi-assets checkpoint path; Security Domain — bind server to localhost/127.0.0.1 only)
    LIBERO/libero/libero/vla/pi0_backend.py (Task 1 output — this notebook's client cell instantiates Pi0Backend(host="localhost", port=8000) exactly as written)
  </read_first>
  <action>
    Create `LIBERO/notebooks/03b-pi0-inference-smoketest.ipynb` as a fresh notebook. Markdown header: "# Phase 3b: π0 (openpi) Inference Smoke Test", stating this notebook proves VLA-04 (interface swap) via a real 1-task x 1-2-episode run, deliberately lighter than Notebook A's full OFT eval per D-10, and that it uses a SEPARATE kernel/environment for openpi to avoid the confirmed torch/transformers/jax version conflict with Notebook A (D-06). Cell: Drive mount + repo unzip (identical pattern). Cell: `REPO_ROOT`/derived path constants, reusing the same `TASKS[0]` (single task) and its `LANGUAGE_MAP` entry from Notebook A's convention (hardcode the same first BDDL filename + instruction string). Cell: GPU assertion (openpi's ~3.3B param model needs meaningful VRAM per 03-RESEARCH.md Open Question #2 — target the same A100 tier as Notebook A for the first run). Markdown "## BLOCK A: openpi install (separate kernel/venv)" explaining this is a fully separate dependency stack from Notebook A. The `pi0_fast_libero` config (autoregressive FAST tokenizer variant, not the flow-based `pi0_libero`) is the specific π0 checkpoint/variant used throughout this notebook (D-07: resolved by research as the smallest/fastest Colab-GPU-compatible option, mirroring how Phase 1 chose OFT's checkpoint for T4/A100 compatibility). Cell: install `uv` if absent (`pip install uv -q` guarded by an `importlib.util.find_spec` check). Cell: `git clone --recurse-submodules https://github.com/Physical-Intelligence/openpi.git /content/openpi` (guarded by existence check), `cd`+`GIT_LFS_SKIP_SMUDGE=1 uv sync`, `GIT_LFS_SKIP_SMUDGE=1 uv pip install -e .` via `subprocess.run` with `cwd="/content/openpi"`. Add a `checkpoint:human-verify` task (see below) BEFORE the `openpi-client` install cell per the Package Legitimacy Audit's SUS disposition. Cell: `pip install openpi-client -q` (after the human-verify checkpoint passes). Cell: start `serve_policy.py --env=pi0_fast_libero` as a background subprocess (`subprocess.Popen`, binding to `127.0.0.1` only per the Security Domain mitigation — pass `--host 127.0.0.1` or equivalent if the script supports it, and note in a comment if it does not, that the Colab VM has no external network exposure by default so this is defense-in-depth); set `OPENPI_DATA_HOME` to a Drive-backed path before starting the server, mirroring Phase 1's Drive-based persistence pattern, so the `gs://openpi-assets` checkpoint download survives session restarts; poll/sleep with a timeout until the websocket port is accepting connections, printing progress. Markdown "## VLA-04: Interface Swap Smoke Test". Cell: `from libero.libero.vla import Pi0Backend, run_suite` (same import path as Notebook A's `OFTBackend` import — this IS the literal proof of interface swap); `backend = Pi0Backend(host="localhost", port=8000)`; `results = run_suite(env_factory, backend, TASKS[:1], LANGUAGE_MAP, episodes_per_task=2, video_dir=VIDEO_DIR_PI0, max_steps=600)` reusing the exact same `run_suite` function Notebook A calls, unmodified — proving VLA-04 literally. Final markdown "## Phase 3b Summary" table with a VLA-04 row.
  </action>
  <acceptance_criteria>
    - `grep -n "recurse-submodules.*Physical-Intelligence/openpi" LIBERO/notebooks/03b-pi0-inference-smoketest.ipynb` (via extracted source) confirms the correct clone URL
    - `grep -n "uv sync\|uv pip install -e" LIBERO/notebooks/03b-pi0-inference-smoketest.ipynb` confirms openpi's documented uv-based install, not a raw pip install of its requirements
    - `grep -n "127.0.0.1\|localhost" LIBERO/notebooks/03b-pi0-inference-smoketest.ipynb` confirms the server bind target is local-only, not `0.0.0.0`
    - `grep -n "from libero.libero.vla import Pi0Backend, run_suite\|from libero.libero.vla import Pi0Backend" LIBERO/notebooks/03b-pi0-inference-smoketest.ipynb` confirms the same import path used by Notebook A for OFTBackend
    - `grep -n "run_suite(" LIBERO/notebooks/03b-pi0-inference-smoketest.ipynb` shows `episodes_per_task=2` and a single-element task list slice (D-10's lighter smoke test)
    - A `checkpoint:human-verify` task exists in this plan's task list positioned before any `pip install openpi-client` cell is described as runnable
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "
import json
nb = json.load(open('LIBERO/notebooks/03b-pi0-inference-smoketest.ipynb'))
full = '\n'.join(''.join(c['source']) for c in nb['cells'])
assert 'Physical-Intelligence/openpi' in full and 'recurse-submodules' in full
assert 'uv sync' in full or 'uv pip install' in full
assert '0.0.0.0' not in full, 'server must not bind to all interfaces'
assert '127.0.0.1' in full or 'localhost' in full
assert 'from libero.libero.vla import' in full and 'Pi0Backend' in full
assert 'run_suite(' in full
print('Notebook B structural checks OK')
"</automated>
  </verify>
  <done>Notebook B installs openpi via its own documented uv-based flow in a self-contained cell sequence, starts serve_policy.py bound to localhost only, and runs the exact same run_suite/eval_loop code from Plan 01 against Pi0Backend for 1 task x 2 episodes — proving the interface swap literally per VLA-04 and ROADMAP success criterion #3.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Human verifies openpi-client package legitimacy before install</name>
  <read_first>
    - .planning/phases/03-vla-inference-loop/03-RESEARCH.md (Package Legitimacy Audit table — openpi-client SUS disposition and rationale)
  </read_first>
  <action>Blocking human verification per the Package Legitimacy Gate protocol (openpi-client flagged SUS — unknown downloads, no repository URL in registry metadata): present the how-to-verify steps below to the user, wait for the resume signal, and do not run the `pip install openpi-client` cell until the user confirms the package's origin matches Physical Intelligence. This checkpoint is never auto-approvable regardless of workflow.auto_advance.</action>
  <what-built>Notebook B's cell that installs `openpi-client` from PyPI — flagged SUS by the Package Legitimacy Audit (03-RESEARCH.md: unknown download count, no repository URL in registry metadata) but assessed as Physical Intelligence's own official companion package.</what-built>
  <how-to-verify>
    1. After Notebook B's openpi repo clone + `uv sync` cells run successfully, before running the `pip install openpi-client` cell, run `pip index versions openpi-client` or check `https://pypi.org/project/openpi-client/` directly.
    2. Confirm the package's homepage/project-url metadata (or PyPI page description) points to `github.com/Physical-Intelligence/openpi` (the same org whose `serve_policy.py` this notebook already cloned and is running).
    3. After installing, run `pip show openpi-client` and confirm the `Author`/`Home-page` fields are consistent with Physical Intelligence, not an unrelated/impostor publisher.
  </how-to-verify>
  <acceptance_criteria>
    - `pip show openpi-client` output's Author/Home-page fields reference Physical Intelligence / `Physical-Intelligence/openpi`
  </acceptance_criteria>
  <resume-signal>Type "approved" once `pip show openpi-client` confirms the Physical Intelligence origin, or "blocked" with the observed metadata if it looks like an impostor package.</resume-signal>
  <done>Human confirmed openpi-client's PyPI metadata matches Physical Intelligence's official package before install proceeded — Package Legitimacy Gate satisfied for this SUS-flagged dependency.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human runs Notebook B on Colab — π0 smoke-test sign-off</name>
  <read_first>
    - LIBERO/notebooks/03b-pi0-inference-smoketest.ipynb (what you are about to run)
  </read_first>
  <action>Blocking human verification per D-05 (π0 must run real inference, not a stub — Claude cannot execute Colab runtimes directly): present the how-to-verify steps below to the user, wait for the resume signal, and do not proceed to phase closure until the VLA-04 smoke test completes and at least one video is visually approved. If the user reports a failure, capture the failing cell output, fix the relevant Plan 01/03 code locally, push, and re-request the Colab run.</action>
  <what-built>A separate-kernel Colab notebook (03b-pi0-inference-smoketest.ipynb) that installs openpi, serves a π0-FAST policy over websocket, and runs a real 1-task x 2-episode smoke test using the identical eval_loop/run_suite code Notebook A uses for OFT.</what-built>
  <how-to-verify>
    1. Open `LIBERO/notebooks/03b-pi0-inference-smoketest.ipynb` in a SEPARATE Colab runtime/kernel from Notebook A (do not reuse Notebook A's session — this proves the dependency isolation per D-06).
    2. Run Block A cells (openpi install) top to bottom; approve the openpi-client legitimacy checkpoint when it appears.
    3. Confirm `serve_policy.py --env=pi0_fast_libero` starts without error and the checkpoint downloads from `gs://openpi-assets` (or a Drive-cached copy) without hanging indefinitely.
    4. Run the VLA-04 smoke-test cell; confirm `run_suite` completes for 1 task x 2 episodes, printing PASS/FAIL per episode and a summary table, using the exact same `run_suite` import as Notebook A (visually confirm the import line and function call are unchanged from Plan 01's shared module).
    5. Spot-check the saved video(s) under the π0 video directory — confirm SOARM visibly attempts the task.
    6. Fill in the Phase 3b Summary markdown table's Status column.
  </how-to-verify>
  <acceptance_criteria>
    - serve_policy.py starts successfully and the websocket client connects
    - run_suite completes for 1 task x 2 episodes using the unmodified Plan 01 eval_loop code, printing PASS/FAIL per episode plus a summary table
    - At least one saved video is human-spot-checked and shows SOARM visibly attempting the task
  </acceptance_criteria>
  <resume-signal>Type "approved" once the smoke test completes and at least one video shows a visually correct SOARM attempt driven by π0, or describe issues (e.g. install failure, OOM, hung checkpoint download, crash).</resume-signal>
  <done>Human sign-off received: π0 smoke test ran real inference on Colab via a separate kernel, driven by the unmodified shared eval_loop/run_suite code, with at least one visually approved video — VLA-04 and ROADMAP Phase 3 success criterion #3 verified on the target environment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Colab VM -> PyPI (`openpi-client` install) | Flagged SUS by the Package Legitimacy Audit (03-RESEARCH.md) — unknown download count, no repo URL in registry metadata, but confirmed to be Physical Intelligence's own companion package |
| Colab VM -> GitHub (`Physical-Intelligence/openpi` clone) | Official upstream source, `--recurse-submodules`, tracked at `main` (no pinned release tag per Assumption A1) |
| Colab VM -> `gs://openpi-assets` (GCS bucket) | Public checkpoint download, no credentials required |
| `serve_policy.py` websocket server <-> `Pi0Backend` client | Both run on the same Colab VM; server must bind to `localhost`/`127.0.0.1` only, never `0.0.0.0` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-SC | Tampering | `pip install openpi-client` (npm/pip package legitimacy) | high | mitigate | Blocking human-verify checkpoint before install (task above) — confirm PyPI metadata / `pip show` origin matches Physical Intelligence, per Package Legitimacy Audit disposition. Never auto-approved. |
| T-3-06 | Information Disclosure / Tampering | `serve_policy.py` bound to all interfaces (`0.0.0.0`) on a shared Colab VM | medium | mitigate | Notebook B explicitly binds to `127.0.0.1`/`localhost` only (03-RESEARCH.md Security Domain); acceptance criteria greps for absence of `0.0.0.0` in the notebook source. |
| T-3-07 | Tampering | `openpi` cloned from `main` branch with no pinned commit SHA (Assumption A1) | medium | mitigate | Notebook B's install cell prints the resolved commit SHA after clone (`git rev-parse HEAD`) so the actual version used is recorded in cell output for reproducibility, mirroring Phase 1's Step 5b documentation discipline. |
| T-3-08 | Denial of Service | `gs://openpi-assets` checkpoint download hanging/failing mid-session (03-RESEARCH.md Pitfall 5) | low | mitigate | `OPENPI_DATA_HOME` set to a Drive-backed path before first download so a successful download persists across session restarts; notebook prints visible progress rather than silently hanging. |

</threat_model>

<verification>
- `test_pi0_backend.py` (new, mock-based) passes 3/3 locally — no real openpi install or network needed for Plan 03's structural verification of `Pi0Backend`.
- Notebook B's JSON structure passes all automated `python3 -c` checks (correct clone URL, uv-based install, localhost-only bind, shared `run_suite`/`Pi0Backend` import matching Notebook A's pattern).
- Both human-verify checkpoints (package legitimacy + full smoke-test run) confirm real Colab execution — per D-05, this cannot be a stub.
</verification>

<success_criteria>
- `Pi0Backend` satisfies `VLABackend` and is driven by the exact same `eval_loop.run_episode`/`run_suite` code as `OFTBackend`, with zero changes to that shared module (ROADMAP success criterion #3).
- `LIBERO/notebooks/03b-pi0-inference-smoketest.ipynb` runs real π0/π0-FAST inference on Colab (D-05) in a separate kernel from Notebook A (D-06), against 1 frozen `libero_spatial` task for 1-2 episodes (D-10).
- VLA-04 verified via the human-verify checkpoint; openpi-client's SUS legitimacy flag resolved via its own dedicated blocking checkpoint.
</success_criteria>

<output>
Create `.planning/phases/03-vla-inference-loop/03-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `LIBERO/libero/libero/vla/pi0_backend.py` — new module; `Pi0Backend` class (websocket client wrapper satisfying `VLABackend`)
- `LIBERO/libero/libero/vla/test_pi0_backend.py` — new local pytest file (mock `WebsocketClientPolicy`, no real openpi/network needed)
- `LIBERO/notebooks/03b-pi0-inference-smoketest.ipynb` — new Colab notebook: openpi install (separate kernel/venv via `uv`), `serve_policy.py --env=pi0_fast_libero` background server (localhost-only), `Pi0Backend`-driven smoke test reusing Plan 01's `run_suite`, Phase 3b Summary table
- `vla/__init__.py` updated (Plan 01's file) — adds a defensively try/except-guarded `from .pi0_backend import Pi0Backend` export
