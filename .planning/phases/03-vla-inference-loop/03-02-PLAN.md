---
phase: 03-vla-inference-loop
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - LIBERO/notebooks/03a-oft-inference-eval.ipynb
autonomous: false
requirements: [VLA-01, VLA-02, VLA-03]

must_haves:
  truths:
    - "Running Notebook A's demo cell with a text prompt produces a saved per-episode video file of SOARM attempting the task in simulation (ROADMAP success criterion #1)"
    - "Task success or failure is printed after each episode using LIBERO's BDDL evaluation protocol (check_success/done), plus an aggregated success-rate summary table across all 3 tasks x 5-10 episodes (ROADMAP success criterion #2, D-09, D-14)"
    - "OpenVLA-OFT loads on Colab GPU (A100 bf16), produces a valid (8,7) action chunk from (image, language), and the eval loop drives all 3 frozen libero_spatial SOARM tasks end-to-end without crashing (VLA-01, D-08)"
  artifacts:
    - LIBERO/notebooks/03a-oft-inference-eval.ipynb
  key_links:
    - "Notebook A imports LIBERO/libero/libero/vla (OFTBackend, run_suite) from Plan 01 — no notebook-embedded reimplementation of the backend or eval loop"
    - "Notebook A's env construction reuses robots=['Soarm101'] and the exact 3 TASKS BDDL filenames frozen in explorations/soarm_sanity.py / 02-soarm-integration-check.ipynb"
    - "Notebook A's Block A install cells extend Phase 1's baked environment (no new pip installs needed — 03-RESEARCH.md confirms this phase only adds eval-loop code)"
</must_haves>
---

<objective>
Build the Colab notebook that closes the loop from a language prompt to a rendered SOARM episode video with printed success/failure, running OpenVLA-OFT as the primary backend across all 3 frozen `libero_spatial` tasks x 5-10 episodes each (D-09), using the shared `vla` package from Plan 01.

Purpose: This is the phase's primary vertical slice — a researcher runs one notebook and watches SOARM attempt real LIBERO tasks driven by real VLA inference, with a real (not smoke-test) success-rate number. Satisfies ROADMAP Phase 3 success criteria #1 and #2, and VLA-01/02/03 in full.
Output: `LIBERO/notebooks/03a-oft-inference-eval.ipynb` — a Colab-runnable notebook following this project's established Block A (install) / restart / Block B (verification) PASS/FAIL convention, extended with an OFT eval-loop demo section.
</objective>

<execution_context>
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/workflows/execute-plan.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-vla-inference-loop/03-CONTEXT.md
@.planning/phases/03-vla-inference-loop/03-RESEARCH.md
@.planning/phases/03-vla-inference-loop/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Notebook A scaffold — Drive mount, REPO_ROOT, GPU assertion, Block A (no new installs)</name>
  <files>LIBERO/notebooks/03a-oft-inference-eval.ipynb</files>
  <read_first>
    LIBERO/notebooks/01-colab-env-setup.ipynb (cells 0-12 — full Block A structure: markdown header, Drive mount, REPO_ROOT config, GPU assertion with A100 warning, numbered Step-N-of-M install cells, numpy ABI final gate cell — this notebook extends Phase 1's baked environment, so Block A here should be a THIN re-verification, not a re-install)
    LIBERO/notebooks/02-soarm-integration-check.ipynb (cells 1-3 — REPO_ROOT/path-constants cell pattern and TASKS constant hardcoded from explorations/soarm_sanity.py, to replicate verbatim in this notebook's own path-constants cell)
    explorations/soarm_sanity.py (lines 65-69 — the exact TASKS list of 3 BDDL filenames to hardcode verbatim; lines 81-92 _build_env — robots=["Soarm101"] instantiation pattern with no custom controller_configs kwarg)
  </read_first>
  <action>
    Create `LIBERO/notebooks/03a-oft-inference-eval.ipynb` as a fresh notebook (new file, do not extend 02-soarm-integration-check.ipynb — 03-RESEARCH.md's Architectural Responsibility Map and Recommended Project Structure both specify a new notebook for Phase 3). Markdown header cell: "# Phase 3a: OpenVLA-OFT Inference Eval Loop", stating this notebook drives all 3 frozen `libero_spatial` SOARM tasks (D-08) through OFTBackend for a full 3-tasks x 5-10-episodes eval (D-09), referencing Phase 1's confirmed OFT load and Phase 2's confirmed SOARM registration as prerequisites. Cell: Drive mount + repo unzip (`from google.colab import drive; drive.mount('/content/drive'); !unzip -q /content/drive/MyDrive/SoARM-Research-colab.zip -d /content` — identical pattern to notebook 01/02 cell 1). Cell: `REPO_ROOT = "/content/SoARM-Research"` plus derived `LIBERO_PKG`, `LIBERO_ROOT`, `BDDL_DIR = f"{LIBERO_ROOT}/bddl_files/libero_spatial"`, `VIDEO_DIR = f"{REPO_ROOT}/LIBERO/notebooks/outputs/videos_oft"`, and the hardcoded `TASKS` list (verbatim 3 filenames from `soarm_sanity.py` lines 65-69), plus a `LANGUAGE_MAP` dict mapping each of the 3 BDDL filenames to a natural-language instruction string derived from the BDDL task name (e.g. `"pick up the black bowl from table center and place it on the plate"` — strip `.bddl`, replace underscores with spaces). Cell: GPU assertion identical to Notebook 01 cell 2 (assert `torch.cuda.is_available()`, print GPU name/VRAM, warn loudly and specifically that OFT bf16 needs A100/~16GB if not A100 — matching D-05's no-quantization decision). Markdown cell: "## BLOCK A: Install" stating explicitly that this notebook requires NO new pip installs beyond Phase 1's baked environment (03-RESEARCH.md Standard Stack Installation section: "No new installs needed beyond Phase 1's baked environment") — Block A here is a single verification cell asserting `torch`, `transformers`, `libero` all importable and matching Phase 1's pinned versions, not a fresh install chain. Markdown cell: "*** STOP — Restart runtime now ***" identical wording/warning to Notebook 01/02's restart-not-delete distinction.
  </action>
  <acceptance_criteria>
    - `python3 -c "import json; nb=json.load(open('LIBERO/notebooks/03a-oft-inference-eval.ipynb')); print(len(nb['cells']))"` succeeds and returns > 0
    - Notebook JSON contains a cell whose source includes exactly the 3 TASKS filenames from `explorations/soarm_sanity.py` (grep both files for `pick_up_the_black_bowl_from_table_center_and_place_it_on_the_plate.bddl` and confirm match)
    - Notebook JSON contains a cell asserting `torch.cuda.is_available()`
    - Notebook JSON contains a markdown cell with the literal text `STOP` and `Restart runtime`
    - No cell contains a `pip install` of `torch`, `transformers`, or `openvla-oft` (Block A here does not reinstall — only verifies)
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "
import json
nb = json.load(open('LIBERO/notebooks/03a-oft-inference-eval.ipynb'))
src = '\n'.join(''.join(c['source']) for c in nb['cells'])
assert 'pick_up_the_black_bowl_from_table_center_and_place_it_on_the_plate.bddl' in src
assert 'torch.cuda.is_available()' in src
assert 'STOP' in src and 'Restart runtime' in src
assert 'pip install torch' not in src and 'pip install transformers' not in src
print('scaffold checks OK, cells=', len(nb['cells']))
"</automated>
  </verify>
  <done>Notebook A exists with markdown header, Drive mount, REPO_ROOT/TASKS/LANGUAGE_MAP constants cell, GPU assertion, and a Block A that verifies (does not reinstall) Phase 1's baked environment, ending in the mandatory restart-stop cell.</done>
</task>

<task type="auto">
  <name>Task 2: Block B bootstrap + VLA-01 model load cell</name>
  <files>LIBERO/notebooks/03a-oft-inference-eval.ipynb</files>
  <read_first>
    LIBERO/notebooks/01-colab-env-setup.ipynb (cells 13-23 — Block B ordering: EGL bootstrap -> LIBERO config.yaml bootstrap -> sys.path setup -> matplotlib/numba shim -> ENV-03 model-load cell with prismatic guard; this notebook must replicate this EXACT ordering before doing anything OFT- or LIBERO-related)
    LIBERO/notebooks/02-soarm-integration-check.ipynb (cells 15-18 — same Block B bootstrap sequence, confirms this is the stable cross-notebook convention to copy verbatim)
    LIBERO/libero/libero/vla/oft_backend.py (Plan 01 output — this cell instantiates OFTBackend rather than reimplementing the load, but the model-load happens on GPU here for the first time; if OFTBackend's __init__ needs adjustment based on real Colab behavior, this task may patch it)
  </read_first>
  <action>
    Add Block B cells in this exact order (matching the established cross-notebook invariant from 01-DEBUG-HISTORY.md, referenced in 03-CONTEXT.md canonical_refs): (1) EGL bootstrap cell — identical to Notebook 01/02's EGL cell (NVIDIA ICD JSON creation, `MUJOCO_GL=egl` env var set before any MuJoCo import); (2) LIBERO `~/.libero/config.yaml` bootstrap cell — identical pattern; (3) `sys.path` setup cell — insert `LIBERO_PKG` and `REPO_ROOT` on `sys.path` (per CLAUDE.md's documented `LIBERO.`-prefix import quirk); (4) matplotlib `Agg` backend + numba shim cell — identical pattern; (5) markdown cell "## VLA-01: OpenVLA-OFT Model Load + Action Shape Check"; (6) code cell that imports `from libero.libero.vla import OFTBackend`, instantiates `backend = OFTBackend()` (this triggers the full Notebook-01-confirmed load: prismatic guard, bf16 load, norm_stats overlay), builds one dummy LIBERO env via `OffScreenRenderEnv(bddl_file_name=os.path.join(BDDL_DIR, TASKS[0]), robots=["Soarm101"], camera_heights=256, camera_widths=256, has_renderer=False, has_offscreen_renderer=True)`, resets it, calls `backend.predict({"eye_in_hand": obs["robot0_eye_in_hand_image"]}, LANGUAGE_MAP[TASKS[0]])`, and asserts `actions.shape == (8, 7)` printing `"VLA-01: PASS — action shape {actions.shape}, dtype {actions.dtype}"` on success or a clear `FAIL` message with the exception on failure (mirrors Notebook 01 cell 23's exact PASS/FAIL print convention). Close the env after the check.
  </action>
  <acceptance_criteria>
    - Notebook JSON cell order (by index) has EGL bootstrap strictly before any cell containing `import libero` or `OffScreenRenderEnv`
    - `grep` of notebook source shows `MUJOCO_GL` set via an `os.environ` assignment before the first `OffScreenRenderEnv(` occurrence
    - A cell asserts `actions.shape == (8, 7)` (or equivalent `assert .shape == (8, 7)`) referencing a variable produced by `backend.predict(...)`
    - A cell prints a string containing both `VLA-01` and `PASS` (or `FAIL`) matching this project's PASS/FAIL convention
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "
import json
nb = json.load(open('LIBERO/notebooks/03a-oft-inference-eval.ipynb'))
cells = [''.join(c['source']) for c in nb['cells']]
egl_idx = next(i for i,s in enumerate(cells) if 'MUJOCO_GL' in s and 'egl' in s)
env_idx = next(i for i,s in enumerate(cells) if 'OffScreenRenderEnv(' in s)
assert egl_idx < env_idx, f'EGL bootstrap (cell {egl_idx}) must precede first OffScreenRenderEnv usage (cell {env_idx})'
full = '\n'.join(cells)
assert 'actions.shape == (8, 7)' in full or 'shape == (8, 7)' in full
assert 'VLA-01' in full
print('Block B ordering + VLA-01 cell checks OK')
"</automated>
  </verify>
  <done>Block B cells (EGL -> config.yaml -> sys.path -> matplotlib/numba, in that exact order) precede the VLA-01 model-load cell, which instantiates OFTBackend, runs one real prediction against a live SOARM env, and prints PASS/FAIL with the asserted (8,7) action shape.</done>
</task>

<task type="auto">
  <name>Task 3: Full eval-loop demo cell (VLA-02, VLA-03) + Colab human verification checkpoint</name>
  <files>LIBERO/notebooks/03a-oft-inference-eval.ipynb</files>
  <read_first>
    LIBERO/libero/libero/vla/eval_loop.py (Plan 01 output — run_suite/run_episode/print_episode_result signatures this cell calls; do not reimplement any of this logic inline in the notebook)
    LIBERO/notebooks/02-soarm-integration-check.ipynb (cell 25 — final "Phase N Summary" markdown table format with a Requirement | Check | Status | Notes header, to adapt for this phase's VLA-01/02/03 + aggregated success-rate rows)
    .planning/phases/03-vla-inference-loop/03-CONTEXT.md (D-09: 3 tasks x 5-10 episodes; D-14: printed PASS/FAIL per episode + aggregated summary table)
  </read_first>
  <action>
    Add markdown cell "## VLA-02 / VLA-03: Full OFT Eval Loop — 3 tasks x N episodes" stating the episode count per task is configurable via an `EPISODES_PER_TASK = 8` constant (within D-09's 5-10 range) and that videos save to `VIDEO_DIR`. Add a code cell defining `env_factory = lambda bddl: OffScreenRenderEnv(bddl_file_name=os.path.join(BDDL_DIR, bddl), robots=["Soarm101"], camera_heights=256, camera_widths=256, has_renderer=False, has_offscreen_renderer=True)` and calling `results = run_suite(env_factory, backend, TASKS, LANGUAGE_MAP, episodes_per_task=EPISODES_PER_TASK, video_dir=VIDEO_DIR, max_steps=600)` (max_steps=600 per this project's confirmed LIBERO eval config default). This cell's execution IS the VLA-02 (video saved per episode) and VLA-03 (success rate measured) proof — `run_suite` already prints PASS/FAIL per episode and the aggregated summary table (D-14) internally; add an explicit `assert os.path.exists(...)` check after the loop confirming at least one video file exists under `VIDEO_DIR` for VLA-02's file-existence proof (per 03-VALIDATION.md's Per-Task Verification Map). Add a final markdown "## Phase 3a Summary" cell with a table: rows for VLA-01 (action shape check), VLA-02 (video file existence), VLA-03 (success rate table), each with a Status column (`☐`, to be filled in after running) and a Notes column referencing where the aggregated success-rate numbers will appear once run. Add a code cell displaying one sample saved video's frame count or file size (`os.path.getsize`) as a lightweight sanity print, matching this project's `Saved -> {path}` arrow-notation convention.
  </action>
  <acceptance_criteria>
    - `grep -n "run_suite(" LIBERO/notebooks/03a-oft-inference-eval.ipynb` (via extracted cell source) shows the call with `episodes_per_task=EPISODES_PER_TASK` and `max_steps=600`
    - `EPISODES_PER_TASK` is defined as an integer literal between 5 and 10 inclusive (D-09)
    - A cell asserts video file existence via `os.path.exists` referencing `VIDEO_DIR`
    - Final markdown cell contains a table with `VLA-01`, `VLA-02`, `VLA-03` rows
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "
import json, re
nb = json.load(open('LIBERO/notebooks/03a-oft-inference-eval.ipynb'))
full = '\n'.join(''.join(c['source']) for c in nb['cells'])
assert 'run_suite(' in full and 'episodes_per_task=EPISODES_PER_TASK' in full
assert 'max_steps=600' in full
m = re.search(r'EPISODES_PER_TASK\s*=\s*(\d+)', full)
assert m and 5 <= int(m.group(1)) <= 10, f'EPISODES_PER_TASK out of D-09 range: {m}'
assert 'os.path.exists' in full and 'VIDEO_DIR' in full
assert 'VLA-01' in full and 'VLA-02' in full and 'VLA-03' in full
print('Full eval-loop demo cell checks OK')
"</automated>
  </verify>
  <done>Notebook A's demo cell runs run_suite across all 3 tasks x EPISODES_PER_TASK (5-10) episodes, produces per-episode videos + printed PASS/FAIL + aggregated summary table (D-14), asserts at least one video file exists (VLA-02 proof), and ends with a Phase 3a Summary table covering VLA-01/02/03.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human runs Notebook A on Colab — OFT eval loop sign-off</name>
  <read_first>
    - LIBERO/notebooks/03a-oft-inference-eval.ipynb (what you are about to run)
  </read_first>
  <action>Blocking human verification per this project's established Colab-execution constraint (Claude cannot run Colab runtimes directly): present the how-to-verify steps below to the user, wait for the resume signal, and do not proceed to phase closure until VLA-01/02/03 cells are confirmed PASS and at least one spot-checked video is visually approved. If the user reports a failure, capture the failing cell output, fix the relevant Plan 01/02 code locally, push, and re-request the Colab run.</action>
  <what-built>A Colab notebook (03a-oft-inference-eval.ipynb) that loads OpenVLA-OFT in bf16 on GPU, runs it against all 3 frozen libero_spatial SOARM tasks for EPISODES_PER_TASK episodes each, saves one video per episode, prints PASS/FAIL per episode, and prints an aggregated success-rate summary table.</what-built>
  <how-to-verify>
    1. Upload/sync the repo zip to Google Drive at the path this notebook's Cell 1 expects (`/content/drive/MyDrive/SoARM-Research-colab.zip`), matching Phase 1/2's established Drive-based workflow.
    2. Open `LIBERO/notebooks/03a-oft-inference-eval.ipynb` in Colab with an A100 runtime (Runtime > Change runtime type > A100 GPU).
    3. Run Block A cells top to bottom, then Runtime > Restart session (NOT "Disconnect and delete runtime" — Colab disk persists installs).
    4. Re-run Cell 1 (path constants), then run Block B cells top to bottom in order.
    5. Confirm the VLA-01 cell prints `VLA-01: PASS` with action shape `(8, 7)`.
    6. Run the full eval-loop demo cell; confirm it runs to completion (~15-30 min) without crashing, prints PASS/FAIL for each of the 3 tasks x EPISODES_PER_TASK episodes, and prints a final aggregated success-rate table.
    7. Spot-check 2-3 saved video files under `VIDEO_DIR` — confirm SOARM is visibly attempting the task (arm moves, gripper interacts with objects) and the video is not black/corrupted.
    8. Fill in the Phase 3a Summary markdown table's Status column with the actual run results.
  </how-to-verify>
  <acceptance_criteria>
    - VLA-01 cell prints PASS with action shape (8, 7)
    - VLA-02 cell confirms at least one video file exists and a human-spot-checked sample is visually correct (not black/corrupted)
    - VLA-03 cell prints per-episode PASS/FAIL plus an aggregated success-rate summary table across all 3 tasks x EPISODES_PER_TASK episodes
  </acceptance_criteria>
  <resume-signal>Type "approved" once all cells are green and at least one spot-checked video shows a visually correct SOARM attempt, or describe issues (e.g. OOM, black frames, crash cell/step).</resume-signal>
  <done>Human sign-off received: VLA-01/02/03 cells all PASS on Colab A100 GPU, videos visually approved, and the aggregated success-rate summary table is populated — ROADMAP Phase 3 success criteria #1 and #2 verified on the target environment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Language prompt (LANGUAGE_MAP) -> OFTBackend.predict | Fixed set of 3 known task strings derived from frozen BDDL filenames — no free-text user input in this phase |
| Colab notebook -> HuggingFace Hub | Model weights + norm_stats fetched from public `moojink/openvla-7b-oft-finetuned-libero-spatial` repo (unchanged from Phase 1's already-accepted pattern) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-04 | Denial of Service | Full 3-task x 5-10-episode eval loop consuming the entire Colab session time budget | medium | mitigate | `max_steps=600` per-episode cap (LIBERO's own confirmed default) bounds worst-case episode length; D-12's per-step success polling breaks early on success rather than always running to `max_steps`, reducing average wall-clock cost. |
| T-3-05 | Repudiation | No persistent record of which run produced which video/success numbers if the notebook is re-run and overwrites `VIDEO_DIR` | low | accept | Research-notebook context, not a production audit trail; per-episode video filenames already encode task+episode index, and the printed summary table is the human-reviewed record for this MVP phase. |

</threat_model>

<verification>
- Notebook A's JSON structure passes all three tasks' automated `python3 -c` checks (scaffold, Block B ordering, eval-loop demo cell wiring).
- Human checkpoint confirms actual Colab execution: VLA-01 PASS with `(8,7)` action shape, VLA-02 video files exist and are visually correct, VLA-03 aggregated success-rate table printed.
- No automated CI exists for this project (03-VALIDATION.md) — the human-verify checkpoint is the authoritative gate per `human_verify_mode: end-of-phase`.
</verification>

<success_criteria>
- `LIBERO/notebooks/03a-oft-inference-eval.ipynb` runs end-to-end on Colab A100: Block A (verify-only) -> restart -> Block B (EGL/config/sys.path/matplotlib bootstrap) -> VLA-01 model load+shape check -> full 3-task x 5-10-episode eval loop -> per-episode videos + PASS/FAIL + aggregated summary table.
- ROADMAP Phase 3 success criteria #1 (prompt -> saved video) and #2 (success/failure detection via BDDL protocol) are satisfied for the OFT backend.
- VLA-01, VLA-02, VLA-03 all verified via the human-verify checkpoint.
</success_criteria>

<output>
Create `.planning/phases/03-vla-inference-loop/03-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `LIBERO/notebooks/03a-oft-inference-eval.ipynb` — new Colab notebook: Drive mount, REPO_ROOT/TASKS/LANGUAGE_MAP/VIDEO_DIR/EPISODES_PER_TASK constants, GPU assertion, Block A (verify-only), restart-stop cell, Block B bootstrap (EGL/config/sys.path/matplotlib), VLA-01 model-load+shape-check cell, full eval-loop demo cell (VLA-02/VLA-03), Phase 3a Summary table
- `LIBERO/notebooks/outputs/videos_oft/` — new output directory for per-episode video files (created at runtime by the notebook, not committed)