---
phase: 03-vla-inference-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - LIBERO/libero/libero/vla/__init__.py
  - LIBERO/libero/libero/vla/interface.py
  - LIBERO/libero/libero/vla/oft_backend.py
  - LIBERO/libero/libero/vla/eval_loop.py
autonomous: true
requirements: [VLA-01, VLA-02, VLA-03]

must_haves:
  truths:
    - "A single Python import (`from libero.libero.vla import VLABackend, OFTBackend, run_episode`) exposes the full interface + OFT backend + eval-loop helper without any notebook-embedded code"
    - "OFTBackend.predict(images, language) returns a numpy array of shape (8, 7) — the confirmed OFT action chunk — by unpacking predict_action's (actions, hidden_states) tuple and overlaying libero_spatial_no_noops norm_stats exactly as Phase 1 confirmed"
    - "run_episode() polls check_success() every simulation step, breaks the instant it returns true (D-12), replays the full 8-step action chunk open-loop before re-inferring (D-03), and writes one video file per episode via VideoWriter with single_video=True (D-11)"
  artifacts:
    - LIBERO/libero/libero/vla/__init__.py
    - LIBERO/libero/libero/vla/interface.py
    - LIBERO/libero/libero/vla/oft_backend.py
    - LIBERO/libero/libero/vla/eval_loop.py
  key_links:
    - "oft_backend.OFTBackend implements the VLABackend Protocol from interface.py — eval_loop.run_episode() must work with either backend interchangeably (VLA-04's swap requirement)"
    - "eval_loop.run_episode() calls LIBERO/libero/libero/utils/video_utils.py::VideoWriter and env.check_success() / env.step()'s done flag — no reimplementation of either"
    - "vla/__init__.py re-exports VLABackend, OFTBackend, run_episode so Phase 4/6 can do `from libero.libero.vla import OFTBackend` per CONTEXT.md code_context Integration Points"
</must_haves>
---

<objective>
Create the shared `predict(images, language) -> action` interface (D-01, D-02, D-04), the OpenVLA-OFT backend implementation that satisfies it (VLA-01), and the reusable eval-loop helper (per-step success polling + open-loop chunk replay + per-episode video saving) that both Notebook A (OFT) and Notebook B (π0) will call in Wave 2 (VLA-02, VLA-03).

Purpose: This is the foundational vertical slice — without this module, neither notebook has anything to import. It must be genuinely reusable (not notebook-embedded code) because Phase 4 (dataset collection) and Phase 6 (fine-tuning) import `OFTBackend`/`VLABackend` directly per CONTEXT.md's Integration Points.
Output: `LIBERO/libero/libero/vla/` package with `interface.py`, `oft_backend.py`, `eval_loop.py`, `__init__.py`. No new pip installs — this plan only writes Python glue code around Phase 1's already-installed OFT stack and Phase 2's already-registered SOARM robot.
</objective>

<execution_context>
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/workflows/execute-plan.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-vla-inference-loop/03-CONTEXT.md
@.planning/phases/03-vla-inference-loop/03-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared VLABackend interface + package scaffold</name>
  <files>LIBERO/libero/libero/vla/__init__.py, LIBERO/libero/libero/vla/interface.py</files>
  <read_first>
    LIBERO/libero/libero/envs/robots/__init__.py (module-registration-via-import convention to mirror in vla/__init__.py — see full pattern in 03-PATTERNS.md)
    LIBERO/libero/libero/envs/robots/on_the_ground_panda.py (this codebase's contract-via-small-methods style — no ABCMeta/framework machinery)
    .planning/phases/03-vla-inference-loop/03-CONTEXT.md (D-01, D-02, D-04 — exact interface signature and rationale)
  </read_first>
  <behavior>
    - Test 1: `interface.VLABackend` is a `typing.Protocol` with exactly one method `predict(self, images: dict[str, Image.Image], language: str) -> np.ndarray`; a plain duck-typed class implementing only that method passes `isinstance(x, VLABackend)` under `@runtime_checkable`.
    - Test 2: `from libero.libero.vla import VLABackend` succeeds (package import, no side-effect errors) — proves `__init__.py` re-export wiring works before `OFTBackend`/`run_episode` exist (Task 2/3 add those exports next).
  </behavior>
  <action>
    Create the `LIBERO/libero/libero/vla/` package. In `interface.py`, define `VLABackend` as a `@runtime_checkable typing.Protocol` with a single method `predict(self, images: dict[str, Image.Image], language: str) -> np.ndarray`, docstring stating per D-01 that `images` is a dict of named camera views (this phase populates only the `"eye_in_hand"` key, Phase 5 will add more keys without a signature change) and per D-02 that each backend normalizes internally — no shared normalization layer, no base-class `__init__`. Do not use `abc.ABC`/`ABCMeta` — this codebase's existing contract style (`on_the_ground_panda.py`) uses plain small methods/properties, not formal ABCs; a `Protocol` is the lightest-weight equivalent. In `__init__.py`, start with `from .interface import VLABackend` (Task 2/3 will extend this file's imports — do not finalize the file yet, leave it importable and correct as-is for this task).
  </action>
  <acceptance_criteria>
    - `python3 -c "from typing import runtime_checkable; import sys; sys.path.insert(0,'LIBERO'); from libero.libero.vla import VLABackend; print(VLABackend)"` runs without error from repo root (with `libero` package on path per LIBERO's editable-install convention, or via direct module exec if package not installed locally)
    - `grep -n "class VLABackend" LIBERO/libero/libero/vla/interface.py` shows a `Protocol` subclass, not `ABC`
    - `grep -c "runtime_checkable" LIBERO/libero/libero/vla/interface.py` >= 1
  </acceptance_criteria>
  <verify>
    <automated>cd LIBERO/libero/libero/vla && python3 -c "import ast; ast.parse(open('interface.py').read()); ast.parse(open('__init__.py').read()); print('syntax OK')"</automated>
  </verify>
  <done>interface.py defines VLABackend as a runtime_checkable Protocol with the exact D-01 signature; __init__.py imports it without error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OFTBackend implementing VLABackend (VLA-01)</name>
  <files>LIBERO/libero/libero/vla/oft_backend.py, LIBERO/libero/libero/vla/__init__.py</files>
  <read_first>
    LIBERO/notebooks/01-colab-env-setup.ipynb (cell 23 — the EXACT confirmed Phase 1 loading pattern: prismatic guard, checkpoint name, bf16 load, norm_stats overlay, unnorm_key resolution, predict_action tuple unpack — replicate this verbatim as a class, do not re-derive)
    .planning/phases/03-vla-inference-loop/03-CONTEXT.md (canonical_refs — OFT Integration Facts: predict_action returns (actions, hidden_states), actions shape (8,7) float64; norm_stats key "libero_spatial_no_noops")
    LIBERO/libero/libero/vla/interface.py (the Protocol this class must satisfy — written in Task 1)
  </read_first>
  <behavior>
    - Test 1: `OFTBackend.__init__` loads `AutoModelForVision2Seq.from_pretrained("moojink/openvla-7b-oft-finetuned-libero-spatial", trust_remote_code=True, torch_dtype=torch.bfloat16, low_cpu_mem_usage=True)`, overlays `model.norm_stats` from `hf_hub_download(checkpoint, "dataset_statistics.json")`, and resolves `self.unnorm_key` to `"libero_spatial_no_noops"` when `"libero_spatial"` is absent from norm_stats (exact fallback logic from Notebook 01 cell 23) — this cannot run without GPU/network, so this test is GPU-gated (see verify).
    - Test 2: `predict(images, language)` builds the prompt string `"In: What action should the robot take to {language}?\nOut:"` (or equivalent using the exact `language` argument), calls `self.model.predict_action(**inputs, unnorm_key=self.unnorm_key, do_sample=False)`, unpacks the `(actions, hidden_states)` tuple (`actions = result[0] if isinstance(result, tuple) else result`), and returns `np.asarray(actions)` with shape `(8, 7)` — asserted inline with `assert actions.shape == (8, 7), f"expected (8,7) chunk, got {actions.shape}"`.
    - Test 3 (local, no GPU needed): a module-level constant `CHECKPOINT = "moojink/openvla-7b-oft-finetuned-libero-spatial"` and `PRISMATIC_REPO = "https://github.com/moojink/openvla-oft.git"` exist and are importable without triggering any network/GPU call at import time (the class body itself must not execute loading logic at import — only inside `__init__`).
  </behavior>
  <action>
    Implement `OFTBackend` in `oft_backend.py` satisfying `VLABackend`. `__init__(self, checkpoint="moojink/openvla-7b-oft-finetuned-libero-spatial", device="cuda")` must: (1) run the prismatic-import guard exactly as Notebook 01 cell 23 does (check `prismatic.training.train_utils` importable, else clone `moojink/openvla-oft` to `/content/openvla-oft` and insert on `sys.path` — guard this clone-fallback behind a try/except so local no-GPU dev doesn't hard-fail if prismatic legitimately isn't installed, but DO fail loudly with the traceback if the clone-and-retry also fails, matching Notebook 01's exact error-surfacing behavior); (2) load `processor = AutoProcessor.from_pretrained(checkpoint, trust_remote_code=True)` and `self.model = AutoModelForVision2Seq.from_pretrained(checkpoint, trust_remote_code=True, torch_dtype=torch.bfloat16, low_cpu_mem_usage=True).to(device)`; (3) overlay `self.model.norm_stats = json.load(open(hf_hub_download(checkpoint, "dataset_statistics.json")))`; (4) resolve `self.unnorm_key`: start with `"libero_spatial"`, fall back to `"libero_spatial_no_noops"` if the former is absent from `norm_stats`, raise `KeyError` with the available keys listed if neither exists. `predict(self, images: dict, language: str) -> np.ndarray` must: use `images["eye_in_hand"]` as the input image (per D-01's dict-of-camera-views contract — this phase only populates that one key), build the OFT instruction-format prompt string embedding `language` verbatim, call `self.processor(prompt, images["eye_in_hand"]).to(device, dtype=torch.bfloat16)`, call `self.model.predict_action(**inputs, unnorm_key=self.unnorm_key, do_sample=False)` under `torch.no_grad()`, unpack the tuple, assert shape `(8, 7)`, and return the `np.ndarray`. Do NOT consume only `actions[0]` — return the full chunk; the eval loop (Task 3) owns open-loop replay of all 8 steps (D-03). Update `vla/__init__.py` to add `from .oft_backend import OFTBackend`.
  </action>
  <acceptance_criteria>
    - `grep -n "libero_spatial_no_noops" LIBERO/libero/libero/vla/oft_backend.py` finds the fallback key exactly as Phase 1 confirmed
    - `grep -n "predict_action" LIBERO/libero/libero/vla/oft_backend.py` shows the tuple-unpack pattern (`actions, .* = .*predict_action` or `result\[0\] if isinstance`)
    - `grep -n "torch_dtype=torch.bfloat16" LIBERO/libero/libero/vla/oft_backend.py` — bf16 load per D-05 (no 4-bit quantization)
    - `grep -c "class OFTBackend" LIBERO/libero/libero/vla/oft_backend.py` == 1
    - `python3 -c "import ast; ast.parse(open('LIBERO/libero/libero/vla/oft_backend.py').read()); print('syntax OK')"` passes locally (no GPU needed for this static check)
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('LIBERO/libero/libero/vla/oft_backend.py').read()); print('syntax OK')"</automated>
  </verify>
  <done>oft_backend.py defines OFTBackend with __init__ replicating Notebook 01's confirmed loading+norm_stats-overlay pattern and predict() returning a (8,7) action chunk; vla/__init__.py exports OFTBackend. Full GPU-backed behavioral verification happens in Plan 02's Notebook A (VLA-01 cell).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: eval_loop helper — success polling, chunk replay, video, PASS/FAIL (VLA-02, VLA-03)</name>
  <files>LIBERO/libero/libero/vla/eval_loop.py, LIBERO/libero/libero/vla/__init__.py</files>
  <read_first>
    LIBERO/libero/lifelong/metric.py (evaluate_one_task_success, lines ~130-157 — the per-step done-accumulation-then-break pattern to mirror, simplified to single-process per RESEARCH.md Pitfall 3; do not import the vectorized SubprocVectorEnv machinery)
    LIBERO/libero/libero/envs/env_wrapper.py (lines 87-104 — env.step() delegates to env.step(action), check_success() delegates to self.env._check_success(); env.step()'s done return already reflects success — do not reimplement)
    LIBERO/libero/libero/utils/video_utils.py (VideoWriter — full class, already read this session: __init__(video_path, save_video, fps, single_video), append_obs(obs, done, idx, camera_name) flips frame [::-1] and overlays a green success tint on the done frame, save() on __exit__)
    LIBERO/libero/configs/eval/default.yaml (max_steps: 600 — this project's own LIBERO eval config default, confirmed this session)
  </read_first>
  <behavior>
    - Test 1: `run_episode(env, backend, language, video_path, max_steps=600, camera_name="robot0_eye_in_hand_image")` calls `backend.predict({"eye_in_hand": obs[camera_name]}, language)` to get an action chunk, then replays every row of that chunk via `env.step(action_row)` in sequence (open-loop, D-03) before calling `predict` again, checking `done` after every single `env.step()` call (not just after a full chunk) so a mid-chunk success stops immediately (D-12) — verified via a mock env/backend pair with a scripted `done=True` on the 3rd of 8 chunk steps, asserting exactly 3 env.step() calls occurred and predict() was called exactly once.
    - Test 2: `run_episode()` wraps the step loop in `with VideoWriter(video_path, save_video=True, fps=30, single_video=True) as vw:` and calls `vw.append_obs(obs, done, camera_name=camera_name)` every step (D-11) — verified via mock VideoWriter counting `append_obs` calls == number of env.step() calls in the episode.
    - Test 3: `run_episode()` returns a dict `{"success": bool(done), "steps": int, "video_path": str}` and the module-level `print_episode_result(task_name, ep_idx, result)` prints `f"Episode {ep_idx} ({task_name}): {'PASS' if result['success'] else 'FAIL'} (steps={result['steps']}) -> {result['video_path']}"` (arrow-notation logging convention per CLAUDE.md/03-PATTERNS.md) — verified via capturing stdout on a scripted PASS and FAIL result.
    - Test 4: `run_suite(env_factory, backend, tasks, language_map, episodes_per_task, video_dir, max_steps=600)` loops `tasks x episodes_per_task`, calls `run_episode` for each, accumulates results, and returns/prints a summary table (rows = task, columns = episodes PASS/FAIL count + success rate; D-14) — verified with a 2-task x 2-episode mock producing a 4-row results list and a printed table containing all 2 task names and a numeric success-rate percentage per task plus an aggregate row.
  </behavior>
  <action>
    Implement `run_episode(env, backend, language, video_path, max_steps=600, camera_name="robot0_eye_in_hand_image")` in `eval_loop.py` — `max_steps` defaults to LIBERO's own confirmed eval-config value of 600 (D-13: exact max-step cap resolved by research to this project's own `LIBERO/libero/configs/eval/default.yaml` value, not an arbitrary cutoff, so VLA-03's success rate stays comparable to published LIBERO numbers). Per D-15, this phase does NOT add a near-miss/partial-credit diagnostic (e.g. distance-to-goal at timeout) — a timeout is a hard binary FAIL; this keeps the MVP eval loop simple and avoids overbuilding instrumentation not required by any of VLA-01..04. Reset is the CALLER's responsibility (caller passes an already-reset `env` and its initial `obs`, OR `run_episode` calls `env.reset()` itself — choose the latter for a self-contained per-episode function, returning `obs = env.reset()` internally first) — call `env.reset()`, then loop up to `max_steps`: build `images = {"eye_in_hand": obs[camera_name]}`, call `action_chunk = backend.predict(images, language)`, iterate each row `a` in `action_chunk` calling `obs, reward, done, info = env.step(a)`, append to `VideoWriter` via `append_obs(obs, done, camera_name=camera_name)` every step, break out of BOTH the chunk-replay loop and the outer step-budget loop the instant `done` is true (D-12) — do not finish replaying the remaining chunk rows after success. Track `steps` as the total `env.step()` calls made. Return `{"success": bool(done), "steps": steps, "video_path": video_path}`. Implement `print_episode_result(task_name, ep_idx, result)` using the arrow-notation convention. Implement `run_suite(env_factory, backend, tasks: list[str], language_map: dict[str, str], episodes_per_task: int, video_dir: str, max_steps=600)`: for each task in `tasks`, build `env = env_factory(task)` (a callable the notebook supplies, e.g. `lambda bddl: OffScreenRenderEnv(bddl_file_name=bddl, robots=["Soarm101"], camera_heights=256, camera_widths=256, has_renderer=False, has_offscreen_renderer=True)` per Phase 2's confirmed no-custom-controller-kwarg pattern), loop `episodes_per_task` episodes each calling `run_episode` with `video_path=os.path.join(video_dir, f"{task_slug}_ep{ep_idx}")`, print each result via `print_episode_result`, then print a final markdown-style summary table (task name | episodes | successes | success rate %) plus an aggregate row across all tasks/episodes — matching D-14's "printed PASS/FAIL per episode plus aggregated summary table" requirement exactly. Update `vla/__init__.py` to add `from .eval_loop import run_episode, run_suite, print_episode_result`.
  </action>
  <acceptance_criteria>
    - `grep -n "env.reset()" LIBERO/libero/libero/vla/eval_loop.py` present inside `run_episode`
    - `grep -n "if done" LIBERO/libero/libero/vla/eval_loop.py` shows an early-break check inside the per-step replay loop, not only after the full chunk (region: inside the inner `for a in action_chunk` loop)
    - `grep -n "VideoWriter" LIBERO/libero/libero/vla/eval_loop.py` shows import and usage with `single_video=True`
    - `grep -n "max_steps=600\|max_steps: *int *= *600\|max_steps=max_steps" LIBERO/libero/libero/vla/eval_loop.py` confirms the 600 default is wired through
    - `python3 -m pytest LIBERO/libero/libero/vla/test_eval_loop.py -q` (new test file, mock env/backend per Behavior above, no GPU/network needed) passes 4/4
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest LIBERO/libero/libero/vla/test_eval_loop.py -q</automated>
  </verify>
  <done>eval_loop.py implements run_episode (open-loop chunk replay + per-step success polling + early stop + per-episode VideoWriter), print_episode_result, and run_suite (multi-task/episode orchestration + aggregated summary table); a local mock-based pytest file proves the control flow without needing GPU/Colab. vla/__init__.py exports all three.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Language prompt -> VLA backend | Prompt strings are drawn from a fixed, known set of 3 frozen BDDL task descriptions (per D-08/CONTEXT.md) — not free user input in this phase |
| Notebook code -> HuggingFace Hub | `hf_hub_download`/`from_pretrained` fetch model weights and norm_stats from a public HF repo (`moojink/openvla-7b-oft-finetuned-libero-spatial`) over the network |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-01 | Tampering | `oft_backend.py`'s prismatic clone-fallback (`git clone moojink/openvla-oft`) | medium | accept | Same clone source Phase 1 already confirmed and vetted (canonical_refs); no new untrusted source introduced. Clone URL is hardcoded, not derived from any user input. |
| T-3-02 | Information Disclosure | `hf_hub_download` fetching `dataset_statistics.json` / model weights | low | accept | Public HF repo, no credentials transmitted; matches Phase 1's already-accepted pattern verbatim. |
| T-3-03 | Denial of Service | Unbounded `max_steps` loop if a caller passes a bad `max_steps` value | low | mitigate | `run_episode`'s `max_steps` parameter defaults to LIBERO's own confirmed `600` eval-config value (not user-supplied free text); no free-text prompt injection surface exists in this phase (fixed task set per D-08). |

</threat_model>

<verification>
- All three files (`interface.py`, `oft_backend.py`, `eval_loop.py`) parse as valid Python (`ast.parse`).
- `test_eval_loop.py` (new, mock-based) passes 4/4 locally — no GPU/Colab dependency for this plan's structural verification.
- `oft_backend.py`'s GPU-dependent behavior (actual model load + `(8,7)` action shape) is verified in Plan 02's Notebook A VLA-01 cell, not locally — this project has no local GPU (Environment Availability table, 03-RESEARCH.md).
</verification>

<success_criteria>
- `LIBERO/libero/libero/vla/` package exists with `__init__.py`, `interface.py`, `oft_backend.py`, `eval_loop.py`.
- `from libero.libero.vla import VLABackend, OFTBackend, run_episode, run_suite, print_episode_result` succeeds.
- `OFTBackend` satisfies `VLABackend`'s `predict(images, language) -> np.ndarray` signature.
- `run_episode`/`run_suite` implement D-03 (open-loop chunk replay), D-12 (per-step polling, early stop), D-11 (per-episode video), D-14 (PASS/FAIL + summary table) without reimplementing `check_success()` or `VideoWriter`.
</success_criteria>

<output>
Create `.planning/phases/03-vla-inference-loop/03-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `LIBERO/libero/libero/vla/__init__.py` — new package init, re-exports `VLABackend`, `OFTBackend`, `run_episode`, `run_suite`, `print_episode_result` (and, after Plan 03, `Pi0Backend`)
- `LIBERO/libero/libero/vla/interface.py` — new module; `VLABackend` (`typing.Protocol`, `@runtime_checkable`)
- `LIBERO/libero/libero/vla/oft_backend.py` — new module; `OFTBackend` class, `CHECKPOINT`/`PRISMATIC_REPO` constants
- `LIBERO/libero/libero/vla/eval_loop.py` — new module; `run_episode()`, `run_suite()`, `print_episode_result()` functions
- `LIBERO/libero/libero/vla/test_eval_loop.py` — new local pytest file (mock-based, no GPU/network)
