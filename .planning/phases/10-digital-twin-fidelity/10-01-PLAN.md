---
phase: 10-digital-twin-fidelity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/calibration_utils.py
  - scripts/test_calibration_utils.py
  - LIBERO/libero/libero/assets/robots/soarm101/robot.xml
autonomous: true
requirements: [TWIN-05, TWIN-06]

must_haves:
  truths:
    - "robot.xml's shoulder_pan/shoulder_lift/elbow_flex/wrist_flex joint ranges are derived from soarm_follower_02's live calibration file, not the generic upstream vendor XML"
    - "robot.xml's wrist_roll joint range remains its pre-existing mechanical-limit value, not a mis-derived full-turn placeholder"
    - "env.reset() on the Soarm101 LIBERO environment still completes without error after the joint-limit edits"
  artifacts:
    - scripts/calibration_utils.py
    - scripts/test_calibration_utils.py
  key_links:
    - "calibration_utils.calibration_ticks_to_radians() is the single formula reused both to hand-derive robot.xml's new values here and by Plan 10-04's test_joint_limits_match_calibration regression test"
---

<objective>
Derive the real SO-ARM101's per-joint calibration limits from the live LeRobot calibration file
(`~/.cache/huggingface/lerobot/calibration/robots/so_follower/soarm_follower_02.json`) and correct
`robot.xml`'s 4 arm-joint ranges (`shoulder_pan`, `shoulder_lift`, `elbow_flex`, `wrist_flex`) to
match, per CONTEXT.md D-06. `wrist_roll`'s existing mechanical-limit-derived range is explicitly
preserved, not recomputed (per D-06/Pitfall 1 — the calibration file's `wrist_roll` entry is a
full-turn placeholder, not a real measurement).

Purpose: `robot.xml`'s current 4 arm-joint limits were copied verbatim from TheRobotStudio's generic
upstream vendor MJCF during the Phase 2 adaptation and never independently verified against this
specific arm's own calibration — 3 of 4 diverge meaningfully (7-40° per side). TWIN-05 requires the
URDF's (and by extension the MJCF's) joint limits to match this arm's real calibrated servo ranges.
Output: `scripts/calibration_utils.py` (reusable calibration formula + CLI), its test, and a
corrected `robot.xml` with rationale comments. `env.reset()` confirmed still green (TWIN-06).
</objective>

<execution_context>
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/workflows/execute-plan.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-digital-twin-fidelity/10-CONTEXT.md
@.planning/phases/10-digital-twin-fidelity/10-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create scripts/calibration_utils.py — LeRobot calibration tick→radian formula</name>
  <files>scripts/calibration_utils.py, scripts/test_calibration_utils.py</files>
  <read_first>
    - control/.venv/lib/python3.12/site-packages/lerobot/motors/motors_bus.py (lines ~854-911, `_normalize` DEGREES-mode branch — the exact formula being reproduced)
    - ~/.cache/huggingface/lerobot/calibration/robots/so_follower/soarm_follower_02.json (live calibration data for this arm)
    - LIBERO/libero/libero/assets/robots/soarm101/robot.xml (current joint `range` attributes being replaced in Task 2)
  </read_first>
  <behavior>
    - `calibration_ticks_to_radians(range_min, range_max, max_res=4095)` returns `(-half_rad, +half_rad)` where `half_ticks=(range_max-range_min)/2`, `half_deg=half_ticks*360/max_res`, `half_rad=math.radians(half_deg)`
    - Test 1 (`test_shoulder_pan_matches_known_calibration`): `calibration_ticks_to_radians(1269, 2869)` == `(-1.227484, 1.227484)` within `abs=1e-5` (the live `shoulder_pan` calibration entry)
    - Test 2 (`test_full_range_is_full_turn_placeholder`): `calibration_ticks_to_radians(0, 4095)` == `(-math.pi, math.pi)` within `abs=1e-5` — this is a regression/documentation test for Pitfall 1's exact warning sign ("a derived range suspiciously close to ±π is the full-turn placeholder leaking through"), demonstrating why `wrist_roll`'s calibration entry (`range_min=0, range_max=4095`) must NOT be fed through this formula and applied to `robot.xml`
  </behavior>
  <action>
    Create `scripts/calibration_utils.py` with:
    - `CALIBRATION_PATH = Path.home() / ".cache" / "huggingface" / "lerobot" / "calibration" / "robots" / "so_follower" / "soarm_follower_02.json"` — per D-05, no calibration JSON lives in this repo; this is the current follower's live LeRobot calibration cache file (the actual file `control/` presently uses to drive the real robot), keyed to the `soarm_follower_02` device name per `control/COMMANDS.md`'s hardware table. If this file is missing at runtime, `main()` must fail loudly with `SystemExit` naming the expected path and pointing at `control/COMMANDS.md`'s device table (the port suffix can change on replug, but the device ID/calibration filename should not).
    - `calibration_ticks_to_radians(range_min: int, range_max: int, max_res: int = 4095) -> tuple[float, float]` implementing the formula above, with a docstring citing the exact source file/line range read above.
    - `JOINTS_FROM_CALIBRATION = ("shoulder_pan", "shoulder_lift", "elbow_flex", "wrist_flex")` — the 4 joints TWIN-05 derives from calibration. `wrist_roll` and `gripper` are explicitly excluded (document why inline, citing D-06/Pitfall 1 for `wrist_roll` and the anti-pattern note for `gripper`'s percent-mode calibration).
    - `main()`: loads `CALIBRATION_PATH`, for each joint in `JOINTS_FROM_CALIBRATION` prints `"{joint}: range_min={rm} range_max={rM} -> ({lo:.6f}, {hi:.6f}) rad"`, then prints an explicit `"wrist_roll: EXCLUDED (calibration range 0-4095 is a full-turn placeholder per Pitfall 1, not a real limit -- robot.xml's existing mechanical value is kept)"` line and a `"gripper: EXCLUDED (percent-mode calibration, not a URDF prismatic range)"` line.
    Write `scripts/test_calibration_utils.py` with the two behavior tests above, each asserting via `pytest.approx(..., abs=1e-5)`. Module docstring on both files states purpose and cites the LeRobot source file this mirrors. Follow CLAUDE.md conventions: `snake_case` names, module-level docstring, no ROS/heavy dependencies (stdlib `math`/`json`/`pathlib` only).
  </action>
  <verify>
    <automated>cd /Users/hp/Desktop/Work/Repositories/SoARM-Research && python3 -m pytest scripts/test_calibration_utils.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/calibration_utils.py` exists and exports `calibration_ticks_to_radians` and `CALIBRATION_PATH`
    - `pytest scripts/test_calibration_utils.py -x` exits 0 with 2 passed
    - Running `python3 scripts/calibration_utils.py` prints all 4 derived joint ranges plus the `wrist_roll`/`gripper` EXCLUDED lines, with `shoulder_pan`'s printed range matching `-1.227484` / `1.227484` (±1e-5)
  </acceptance_criteria>
  <done>calibration_utils.py + its test both exist, tests pass, and the CLI's printed values match the pre-computed table in 10-RESEARCH.md Pattern 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update robot.xml joint limits from calibration + re-validate env.reset()</name>
  <files>LIBERO/libero/libero/assets/robots/soarm101/robot.xml</files>
  <read_first>
    - LIBERO/libero/libero/assets/robots/soarm101/robot.xml (full file — header comment block is the established numbered-rationale convention to extend)
    - scripts/calibration_utils.py (Task 1 output — the source of the values being applied)
    - LIBERO/libero/libero/envs/test_camera_config.py (existing TWIN-06 regression test — constructs a real Soarm101 env and calls `env.reset()`)
  </read_first>
  <action>
    Update the `range` attribute on 4 `&lt;joint&gt;` elements in `robot.xml` to these exact calibration-derived values (computed via Task 1's `calibration_ticks_to_radians`, cross-checked in 10-RESEARCH.md Pattern 2 table):
    - `shoulder_pan` joint (currently `range="-1.9198621771937616 1.9198621771937634"`): set `range="-1.227484 1.227484"`
    - `shoulder_lift` joint (currently `range="-1.7453292519943224 1.7453292519943366"`): set `range="-1.875749 1.875749"`
    - `elbow_flex` joint (currently `range="-1.69 1.69"`, with an adjacent "5-degree calibration offset applied to joint range (upstream)" comment): set `range="-1.696997 1.696997"` and replace that stale comment (the new value is freshly derived from live calibration, no upstream offset hack needed)
    - `wrist_flex` joint (currently `range="-1.6580628494556928 1.6580627293335335"`): set `range="-1.780619 1.780619"`
    - `wrist_roll` joint: DO NOT CHANGE — leave `range="-2.7438472969992493 2.841206309382605"` byte-identical to its current value. This is TWIN-02's source of truth per D-06/Pitfall 1: the calibration file's `wrist_roll` entry (`range_min=0, range_max=4095`) is a full-turn placeholder, not a real measurement — feeding it through the formula would silently widen this joint to an unrestricted full turn.
    Add a rationale comment immediately above each of the 4 changed `&lt;joint&gt;` lines stating: "Re-derived from soarm_follower_02.json calibration via calibration_ticks_to_radians(), see Phase 10 D-06/TWIN-05 (previous value was copied from the generic upstream so101_new_calib.source.xml vendor file, never independently verified against this arm's live calibration)."
    After editing, run the existing TWIN-06 regression test to confirm `env.reset()` still completes without error on the new limits. The arm's `qpos=0` home pose (per `robot.xml`'s own header note, "no keyframe: qpos=0 IS the documented new-calib home pose") lies within all 4 updated ranges (0 is inside every interval above), so no other change should be needed for this to pass.
  </action>
  <verify>
    <automated>cd /Users/hp/Desktop/Work/Repositories/SoARM-Research && conda run -n libero python3 -m pytest LIBERO/libero/libero/envs/test_camera_config.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `robot.xml`'s `shoulder_pan`/`shoulder_lift`/`elbow_flex`/`wrist_flex` `range` attributes equal the 4 values above (grep confirms the exact strings are present)
    - `robot.xml`'s `wrist_roll` `range` attribute is byte-identical to its pre-edit value `"-2.7438472969992493 2.841206309382605"`
    - Each of the 4 changed joints has an adjacent rationale comment citing D-06/TWIN-05
    - `pytest LIBERO/libero/libero/envs/test_camera_config.py -x` exits 0 (both existing tests pass)
  </acceptance_criteria>
  <done>robot.xml's 4 arm-joint limits match live calibration; wrist_roll untouched; env.reset() regression test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local calibration file -> robot.xml | A locally-cached JSON file (not user/network input) feeds a hand-applied XML edit; no external trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | calibration_ticks_to_radians formula | medium | mitigate | TDD tests reproduce LeRobot's exact DEGREES formula and pin known-good input/output pairs (shoulder_pan calibration + the ±π full-turn-placeholder regression test) before any value is hand-applied to robot.xml |
| T-10-02 | Integrity | robot.xml joint range edit | medium | mitigate | Existing `test_camera_config.py::test_rgb_cameras_non_degenerate_spatial`/`test_depth_cameras_non_degenerate_spatial` (which both construct a real env and call `env.reset()`) re-run after the edit as the regression gate; wrist_roll's value is explicitly asserted byte-identical, not silently overwritten |
</threat_model>

<verification>
Run `pytest scripts/test_calibration_utils.py -x` (Task 1) then `pytest LIBERO/libero/libero/envs/test_camera_config.py -x` (Task 2). Both must exit 0.
</verification>

<success_criteria>
scripts/calibration_utils.py + test exist and pass; robot.xml's 4 arm-joint limits are calibration-derived (not vendor-XML values); wrist_roll is unchanged; env.reset() regression is green.
</success_criteria>

<output>
Create `.planning/phases/10-digital-twin-fidelity/10-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- `scripts/calibration_utils.py` — new module: `CALIBRATION_PATH`, `calibration_ticks_to_radians()`, `JOINTS_FROM_CALIBRATION`, `main()`
- `scripts/test_calibration_utils.py` — new test file: `test_shoulder_pan_matches_known_calibration`, `test_full_range_is_full_turn_placeholder`
- `LIBERO/libero/libero/assets/robots/soarm101/robot.xml` — modified in place (4 joint `range` attributes + rationale comments; `wrist_roll` unchanged)
