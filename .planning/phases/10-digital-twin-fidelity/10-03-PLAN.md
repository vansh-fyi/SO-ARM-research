---
phase: 10-digital-twin-fidelity
plan: 03
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - scripts/mjcf_to_urdf.py
  - So-101/So-101.urdf
autonomous: true
requirements: [TWIN-01, TWIN-02, TWIN-03, TWIN-04, TWIN-06]

must_haves:
  truths:
    - "The generated URDF's kinematic tree places right_gripper as a descendant of right_hand, not a root-level sibling of base"
    - "The generated URDF's wrist_roll and gripper_left/gripper_right joints carry the same limits/axes as the corrected MJCF source"
    - "Every mesh filename in the generated URDF is a relative path, not an absolute ~/Downloads/ path"
  artifacts:
    - scripts/mjcf_to_urdf.py
    - So-101/So-101.urdf
  key_links:
    - "mjcf_to_urdf.py reads robot.xml's post-Plan-10-01 corrected joint ranges and soarm_gripper.xml's post-Plan-10-02 corrected clamp offsets as its single source of truth"
---

<objective>
Generate a correct `So-101/So-101.urdf` from the two already-correct MJCF files (`robot.xml` +
`soarm_gripper.xml`), replacing the broken CoppeliaSim-exported URDF in place, per CONTEXT.md D-02.
Write `scripts/mjcf_to_urdf.py` — a small hand-rolled ElementTree-based converter, per D-03's
discretion (the phase researcher surveyed available MJCF->URDF options; no library — the one
candidate PyPI converter doesn't support prismatic joints, which this gripper needs) — that walks
both MJCF body/joint trees directly and emits a URDF with the gripper correctly parented under the
wrist chain, `wrist_roll` + `gripper_left`/`gripper_right` joints present with correct limits/axes,
and all mesh references as in-repo relative paths.

Purpose: `So-101/So-101.urdf` is currently broken (gripper as a disconnected root-level object, 30
absolute `file:///Users/hp/Downloads/...` mesh paths, no `wrist_roll`/gripper joints at all,
terminates at `wrist_link_respondable`). Generating from the MJCF (already fixed by Plans 10-01/10-02)
guarantees URDF/MJCF agreement by construction (TWIN-06) instead of two independently-drifting
kinematic definitions.
Output: `scripts/mjcf_to_urdf.py` and a regenerated `So-101/So-101.urdf` satisfying TWIN-01..04.
</objective>

<execution_context>
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/workflows/execute-plan.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-digital-twin-fidelity/10-CONTEXT.md
@.planning/phases/10-digital-twin-fidelity/10-RESEARCH.md
@.planning/phases/10-digital-twin-fidelity/10-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/mjcf_to_urdf.py and generate So-101/So-101.urdf</name>
  <files>scripts/mjcf_to_urdf.py, So-101/So-101.urdf</files>
  <read_first>
    - LIBERO/libero/libero/assets/robots/soarm101/robot.xml (post-10-01 corrected joint limits — source of truth for the arm chain)
    - LIBERO/libero/libero/assets/grippers/soarm_gripper.xml (post-10-02 corrected clamp visual offsets — source of truth for the gripper chain)
    - So-101/So-101.urdf (the broken artifact being replaced — read only to reuse its per-link CoppeliaSim mesh filename lists, NOT its kinematic structure/joint limits/parent-child chain)
    - coppelia/soarm_parallel_gripper.urdf (structural URDF-dialect template: prismatic `&lt;joint&gt;` syntax, `&lt;limit&gt;`/`&lt;dynamics&gt;` attribute conventions)
    - coppelia/export_model_library.py (module docstring + path-constant + fail-loud precondition-check pattern to mirror)
  </read_first>
  <action>
    Create `scripts/mjcf_to_urdf.py` (ElementTree-based, stdlib only — `xml.etree.ElementTree`, `math`, `pathlib` — no MuJoCo runtime needed) that parses `robot.xml`'s and `soarm_gripper.xml`'s `&lt;worldbody&gt;` trees and emits a fresh `So-101/So-101.urdf` (`&lt;robot name="so101"&gt;` root, matching the existing broken URDF's root name for continuity), per this exact body/joint/mesh mapping — 9 links, 8 joints total, do not add or drop any of these:

    Link `base` (from `robot.xml`'s `&lt;body name="base"&gt;`, root link, no joint): visual meshes `So-101_base_link_visual_vis_1.dae`..`_vis_4.dae`; collision meshes `So-101_robot_base_coll_1.dae`..`_coll_4.dae` (bare filenames, sibling of the URDF file — do NOT prefix with `So-101/`); inertial copied directly from `robot.xml`'s `base` body's `&lt;inertial pos mass fullinertia&gt;` (MJCF `fullinertia` order `ixx iyy izz ixy ixz iyz` maps 1:1 onto URDF's `&lt;inertia ixx iyy izz ixy ixz iyz&gt;`).

    Joint `shoulder_pan` (hinge->revolute, axis `"0 0 1"`, parent `base`, child `shoulder`, origin `xyz`=shoulder body's MJCF `pos`, `rpy`=quat->rpy of shoulder body's MJCF `quat` via the closed-form conversion in 10-RESEARCH.md Pattern 1, `&lt;limit&gt;` lower/upper = `robot.xml`'s current (post-10-01) `range` attribute verbatim, `effort="2.94"` (from the `torq_shoulder_pan` actuator's `ctrlrange`), `velocity="10.0"` (no calibrated velocity data exists — comment this as a placeholder), `&lt;dynamics damping="0.60" friction="0.052"/&gt;` from the joint's own `damping`/`frictionloss`). Link `shoulder`: visual `So-101_shoulder_link_visual_vis_1.dae`..`_vis_3.dae`; collision `So-101_shoulder_link_respondable_coll_1.dae`..`_coll_3.dae`; inertial from `robot.xml`'s `shoulder` body.

    Joint `shoulder_lift` (revolute, axis `"0 0 1"`, parent `shoulder`, child `upper_arm`, origin from `upper_arm` body's pos/quat, limit from `robot.xml`'s post-10-01 range, same effort/velocity/dynamics pattern as above). Link `upper_arm`: visual `So-101_upper_arm_link_visual_vis_1.dae`..`_vis_2.dae`; collision `So-101_upper_arm_link_respondable_coll_1.dae`..`_coll_2.dae`.

    Joint `elbow_flex` (revolute, axis `"0 0 1"`, parent `upper_arm`, child `lower_arm`, origin from `lower_arm` body's pos/quat, limit from `robot.xml` post-10-01). Link `lower_arm`: visual `So-101_lower_arm_link_visual_vis_1.dae`..`_vis_3.dae`; collision `So-101_lower_arm_link_respondable_coll_1.dae`..`_coll_3.dae`.

    Joint `wrist_flex` (revolute, axis `"0 0 1"`, parent `lower_arm`, child `wrist`, origin from `wrist` body's pos/quat, limit from `robot.xml` post-10-01). Link `wrist`: visual `So-101_wrist_link_visual_vis_1.dae`..`_vis_2.dae`; collision `So-101_wrist_link_respondable_coll_1.dae`..`_coll_2.dae`.

    Joint `wrist_roll` (revolute, axis `"0 0 1"`, parent `wrist`, child `right_hand`, origin from `right_hand` body's pos/quat, `&lt;limit lower="-2.7438472969992493" upper="2.841206309382605"/&gt;` — copy `robot.xml`'s value verbatim; this is TWIN-02's requirement). Link `right_hand`: NO visual/collision mesh (none exists at this split point in the CoppeliaSim export — add a comment in the generated URDF's `right_hand` link stating this explicitly); inertial copied from `robot.xml`'s `right_hand` body.

    Joint `right_hand_to_gripper` (`type="fixed"`, parent `right_hand`, child `right_gripper`, origin `xyz="0 0 0" rpy="0 0 0"` — `soarm_gripper.xml`'s `right_gripper` body is at identity offset). Link `right_gripper`: visual mesh filename `../coppelia/meshes/main_frame_visual.stl` `scale="0.001 0.001 0.001"`, origin = `soarm_gripper.xml`'s `main_frame_visual` geom's pos/quat->rpy (currently `pos="-0.015 -0.064 -0.02975"`, unchanged by Plan 10-02); inertial copied from `soarm_gripper.xml`'s `right_gripper` body's `&lt;inertial pos mass diaginertia&gt;` (diaginertia: `ixx=iyy=izz`, `ixy=ixz=iyz=0`).

    Joint `gripper_left` (slide->prismatic, axis `"0 -1 0"`, parent `right_gripper`, child `gripper_left_jaw`, origin `xyz="0 0 0" rpy="0 0 0"`, `&lt;limit lower="0" upper="0.042" effort="60" velocity="0.5"/&gt;`, `&lt;dynamics damping="50" friction="0.5"/&gt;` — all copied verbatim from `soarm_gripper.xml`'s `gripper_left` joint, matching `coppelia/soarm_parallel_gripper.urdf`'s existing `parallel_gripper_left` joint exactly). Link `gripper_left_jaw`: visual mesh filename `../coppelia/meshes/clamp_1_visual.stl` `scale="0.001 0.001 0.001"`, origin = `soarm_gripper.xml`'s CURRENT (post-10-02-corrected) `left_jaw_visual` geom's pos/quat->rpy — read this value live from the file, do not assume the pre-fix value; collision box `size` = 2x the MJCF `left_jaw_collision` box geom's half-extents (`"0.044 0.018 0.040"`), at `pos="-0.052 -0.0092 0"`; inertial from `soarm_gripper.xml`'s `gripper_left_jaw` body.

    Joint `gripper_right` (slide->prismatic, axis `"0 1 0"`, parent `right_gripper`, child `gripper_right_jaw`, same limit/effort/velocity/dynamics as `gripper_left`). Link `gripper_right_jaw`: visual mesh filename `../coppelia/meshes/clamp_2_visual.stl`, origin = `soarm_gripper.xml`'s CURRENT (post-10-02-corrected) `right_jaw_visual` geom's pos/quat->rpy; collision box `size="0.044 0.018 0.040"` at `pos="-0.052 0.0088 0"`; inertial from `soarm_gripper.xml`'s `gripper_right_jaw` body.

    Implementation requirements: use the closed-form `mjcf_quat_to_rpy(w, x, y, z)` conversion from 10-RESEARCH.md Pattern 1 (stdlib `math` only) for every `&lt;joint&gt;&lt;origin rpy="..."&gt;`. Before emitting, assert both source files exist and every body/joint name listed above is present in the parsed tree; `sys.exit(f"ERROR: missing {name} in {source_file}")` on any miss (fail-loud precondition pattern, mirrors `coppelia/export_model_library.py`). Module docstring states what this script does and does NOT do ("generates So-101/So-101.urdf fresh from robot.xml + soarm_gripper.xml's MJCF trees; does NOT patch the existing broken URDF in place — overwrites it wholesale, per Phase 10 D-02"). Path constants at module top (`ROOT = Path(__file__).resolve().parent.parent`, `ROBOT_XML`, `GRIPPER_XML`, `OUTPUT_URDF = ROOT / "So-101" / "So-101.urdf"`), mirroring `coppelia/export_model_library.py`'s constant-block convention. End with `print(f"Saved -> {OUTPUT_URDF}")`.
  </action>
  <verify>
    <automated>cd /Users/hp/Desktop/Work/Repositories/SoARM-Research && python3 scripts/mjcf_to_urdf.py && python3 -c "import xml.etree.ElementTree as ET; t=ET.parse('So-101/So-101.urdf'); links=t.findall('.//link'); joints=t.findall('.//joint'); assert len(links)==9, len(links); assert len(joints)==8, len(joints); print('OK', len(links), 'links', len(joints), 'joints')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/mjcf_to_urdf.py` exists and running it exits 0
    - Generated `So-101/So-101.urdf` contains exactly 9 `&lt;link&gt;` elements and 8 `&lt;joint&gt;` elements
    - Generated URDF contains a `&lt;joint name="wrist_roll" type="revolute"&gt;` with `&lt;limit lower="-2.7438472969992493" upper="2.841206309382605"...&gt;` (exact string match)
    - Generated URDF contains `&lt;joint name="gripper_left" type="prismatic"&gt;` with `axis xyz="0 -1 0"` and `&lt;joint name="gripper_right" type="prismatic"&gt;` with `axis xyz="0 1 0"`
    - No `filename` attribute anywhere in the generated URDF starts with `/` or `file://` (grep confirms zero matches)
    - The `right_hand_to_gripper` joint's `&lt;parent link="right_hand"/&gt;` and `&lt;child link="right_gripper"/&gt;` are both present (grep confirms)
  </acceptance_criteria>
  <done>scripts/mjcf_to_urdf.py exists; running it regenerates So-101/So-101.urdf with the exact 9-link/8-joint structure above, gripper correctly chained under right_hand, wrist_roll/gripper limits/axes correct, all mesh paths relative.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite regression confirmation (wave-merge gate)</name>
  <files>LIBERO/libero/libero/envs/, LIBERO/libero/libero/datasets/ (read-only, verification only)</files>
  <read_first>
    - .planning/phases/10-digital-twin-fidelity/10-VALIDATION.md (Sampling Rate section — "After every plan wave" rule)
  </read_first>
  <action>
    Run the full-suite regression command specified in 10-VALIDATION.md's Sampling Rate ("After every
    plan wave: pytest LIBERO/libero/libero/envs/ LIBERO/libero/libero/datasets/ -x") to confirm neither
    the robot.xml calibration-limit edit (Plan 10-01), the soarm_gripper.xml clamp-visual edit
    (Plan 10-02), nor the new So-101/So-101.urdf generation (Task 1) broke any existing test. This URDF
    is not loaded by any runtime code path (per D-09), so this is a pure regression check on the
    MJCF-side edits plus a final confirmation that scripts/mjcf_to_urdf.py's own file writes didn't
    disturb anything else in the repo. Also run `git status --short` and confirm only the files listed
    in this plan's frontmatter `files_modified` (plus Plan 10-01/10-02's already-committed files) show
    as changed — no stray/unexpected file modifications.
  </action>
  <verify>
    <automated>cd /Users/hp/Desktop/Work/Repositories/SoARM-Research && conda run -n libero python3 -m pytest LIBERO/libero/libero/envs/ LIBERO/libero/libero/datasets/ -x</automated>
  </verify>
  <acceptance_criteria>
    - Full pytest run across `LIBERO/libero/libero/envs/` and `LIBERO/libero/libero/datasets/` exits 0 with no failures
    - `git status --short` shows only the expected files changed this plan: `scripts/mjcf_to_urdf.py` (new), `So-101/So-101.urdf` (modified)
  </acceptance_criteria>
  <done>Full existing test suite green after all Phase-10-so-far MJCF/URDF changes; no unintended files touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MJCF source files -> generated URDF | The converter treats robot.xml/soarm_gripper.xml as fixed, trusted, small inputs (not arbitrary untrusted input), but must still fail loudly rather than silently emit a malformed URDF if an expected body/joint is missing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05 | Tampering / Integrity | scripts/mjcf_to_urdf.py | medium | mitigate | Fail-loud precondition assertions (`sys.exit`) before emission if any of the 6 arm bodies, 3 gripper bodies, or 8 joints listed in this plan's mapping table is missing from the parsed MJCF tree — never silently emits a partial/malformed URDF |
| T-10-06 | Tampering (of intent) | Generated URDF mesh paths | low | mitigate | All mesh `filename` attributes are written relative to the URDF file's own directory (standard URDF convention); Plan 10-04's `yourdfpy` load (which resolves mesh paths at load time) plus this plan's own no-absolute-path grep both catch path-resolution errors |
</threat_model>

<verification>
Run `python3 scripts/mjcf_to_urdf.py` (Task 1) then the structural ElementTree check, then the full-suite regression (Task 2). All must exit 0.
</verification>

<success_criteria>
scripts/mjcf_to_urdf.py exists; So-101/So-101.urdf is regenerated with the correct 9-link/8-joint kinematic chain (gripper under right_hand, wrist_roll + gripper joints present with correct limits/axes, all mesh paths relative); full existing test suite remains green.
</success_criteria>

<output>
Create `.planning/phases/10-digital-twin-fidelity/10-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- `scripts/mjcf_to_urdf.py` — new module: `ROOT`, `ROBOT_XML`, `GRIPPER_XML`, `OUTPUT_URDF`, `mjcf_quat_to_rpy()`, `main()`
- `So-101/So-101.urdf` — regenerated in place (9 links: `base`, `shoulder`, `upper_arm`, `lower_arm`, `wrist`, `right_hand`, `right_gripper`, `gripper_left_jaw`, `gripper_right_jaw`; 8 joints: `shoulder_pan`, `shoulder_lift`, `elbow_flex`, `wrist_flex`, `wrist_roll`, `right_hand_to_gripper`, `gripper_left`, `gripper_right`)