---
phase: 10-digital-twin-fidelity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - LIBERO/libero/libero/assets/grippers/soarm_gripper.xml
  - diagnostics/render_gripper_clamp.py
autonomous: false
requirements: [TWIN-03, TWIN-07]

must_haves:
  truths:
    - "The rendered gripper clamp visual mesh sits flush against the main frame housing in an L-shape (human-confirmed), not poking outside it, in both open and closed jaw states"
    - "The gripper's collision physics (box jaw geoms) are unaffected by the visual-only fix"
  artifacts:
    - diagnostics/render_gripper_clamp.py
    - diagnostics/outputs/gripper_clamp_open.png
    - diagnostics/outputs/gripper_clamp_closed.png
  key_links:
    - "soarm_gripper.xml's corrected left_jaw_visual/right_jaw_visual geom pos values become the exact input Plan 10-03's mjcf_to_urdf.py reads when emitting the generated URDF's gripper visual mesh origins"
---

<objective>
Fix the known clamp visual mesh placement bug in `soarm_gripper.xml` (CONTEXT.md D-04): the clamp
STL's gear-tooth detail currently renders poking outside the `main_frame_visual` housing instead of
sitting flush in an L-shape, in both open and closed jaw states. Physics (the box-jaw collision
geoms) is unaffected — this is purely a visual mesh recentering fix, done via the same iterative
offscreen-render-inspect-adjust method already used (and documented) for `main_frame_visual` itself.

Purpose: D-04 explicitly folds this bug into Phase 10 scope as part of gripper fidelity
(TWIN-03/TWIN-07) since it touches the same file. Plan 10-03's URDF generation reads this file's
corrected geom offsets directly, so this fix must land before URDF generation.
Output: Corrected `soarm_gripper.xml`, a reusable render diagnostic script, and a human sign-off
confirming the visual fix looks correct.
</objective>

<execution_context>
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/workflows/execute-plan.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-digital-twin-fidelity/10-CONTEXT.md
@.planning/phases/10-digital-twin-fidelity/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Iteratively fix clamp visual mesh offsets (D-04)</name>
  <files>LIBERO/libero/libero/assets/grippers/soarm_gripper.xml, diagnostics/render_gripper_clamp.py</files>
  <read_first>
    - LIBERO/libero/libero/assets/grippers/soarm_gripper.xml (full file — `main_frame_visual`'s own asset-note comment, lines ~38-46, documents the exact prior fix method to replicate)
    - diagnostics/measure_object_depth.py (diagnostic CLI pattern: module docstring, precondition checks, `Path(__file__).resolve().parent / "outputs"` output convention, `print(f"Saved -> {path}")`)
  </read_first>
  <action>
    Write `diagnostics/render_gripper_clamp.py`: loads `LIBERO/libero/libero/assets/grippers/soarm_gripper.xml` standalone via `mujoco.MjModel.from_xml_path` (no arm attachment needed — every relevant geom, `main_frame_visual`/`left_jaw_visual`/`right_jaw_visual`, is already defined in this file's own `right_gripper`-relative body-local frame). Create a `mujoco.Renderer(model, height=480, width=640)`. For `jaw_value` in `(0.042, 0.0)` (open, closed): set `data.qpos[:] = [jaw_value, jaw_value]`, call `mujoco.mj_forward(model, data)`, configure a `mujoco.MjvCamera` with `lookat=[-0.06, 0, 0]`, `distance=0.25`, `azimuth=110`, `elevation=-15` (a 3/4 side angle framing the main-frame-to-jaw junction — adjust the exact angle if this framing doesn't clearly show the junction), render, and save via PIL to `diagnostics/outputs/gripper_clamp_open.png` / `_closed.png`, printing `"Saved -> {path}"` per file (CLAUDE.md arrow-notation Logging convention).

    Run the script and inspect both PNGs. The current (buggy) offsets are: `left_jaw_visual` geom `pos="0.15241 -0.02808 0.15206"` `quat="0 0.707107 0.707107 0"`; `right_jaw_visual` geom `pos="0.15242 0.01270 0.15171"` `quat="0 0.707107 0.707107 0"`; `main_frame_visual` geom `pos="-0.015 -0.064 -0.02975"` `quat="0.5 0.5 0.5 0.5"` (already correctly dialed in per its own asset comment — do NOT touch `main_frame_visual`). Per D-04, the gear-tooth detail of both clamp meshes currently pokes outside `main_frame_visual`'s housing instead of sitting flush in an L-shape.

    Adjust ONLY the `pos` attribute on the `left_jaw_visual` and `right_jaw_visual` geoms (keep the existing `quat="0 0.707107 0.707107 0"` rotation — the file's own Fidelity note already establishes this as the correct axis mapping: mesh +y -> local +X finger axis, mesh x -> local Y opening) in small increments, re-running `diagnostics/render_gripper_clamp.py` after each adjustment, until the clamp mesh's gear-tooth end sits flush against/inside `main_frame_visual`'s housing silhouette in both open and closed renders, forming a continuous L-shape with no visible gap or protrusion. Use `main_frame_visual`'s own `pos` (`-0.015 -0.064 -0.02975`) as the geometric reference point the clamp's housing-end should converge toward.

    Once dialed in, add/replace the comment above each of the two geoms documenting: "D-04 fix: clamp visual recentered via iterative offscreen-render dialing (diagnostics/render_gripper_clamp.py), see Phase 10 CONTEXT.md D-04. Previous pos left the gear-tooth mesh detail poking outside main_frame_visual's housing." Keep the physics (`left_jaw_collision`/`right_jaw_collision` box geoms and both `&lt;joint&gt;` elements) completely untouched — this is a visual-only fix.
  </action>
  <verify>
    <automated>cd /Users/hp/Desktop/Work/Repositories/SoARM-Research && conda run -n libero python3 diagnostics/render_gripper_clamp.py && test -s diagnostics/outputs/gripper_clamp_open.png && test -s diagnostics/outputs/gripper_clamp_closed.png</automated>
  </verify>
  <acceptance_criteria>
    - `diagnostics/render_gripper_clamp.py` exists, runs to completion (exit 0) under the `libero` conda env, and writes both non-empty PNGs
    - `soarm_gripper.xml`'s `left_jaw_visual`/`right_jaw_visual` geom `pos` attributes differ from their pre-fix values (`"0.15241 -0.02808 0.15206"` / `"0.15242 0.01270 0.15171"`)
    - `soarm_gripper.xml`'s `left_jaw_collision`/`right_jaw_collision` box geoms and both jaw `&lt;joint&gt;` elements are byte-identical to their pre-fix state (physics untouched)
    - A D-04-referencing rationale comment is present above both jaw visual geoms
  </acceptance_criteria>
  <done>Render script exists and produces open/closed PNGs; jaw visual geom offsets have been changed from their buggy starting values with a documented rationale; jaw collision geoms and joints unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual confirmation of D-04 clamp mesh fix</name>
  <action>Blocking human verification per D-04 (the "flush L-shape" acceptance criterion is a visual judgment call with no closed-form automated check available, per 10-RESEARCH.md): present the how-to-verify steps below to the user, wait for the resume signal, and do not proceed to Plan 10-03 (which reads these geom offsets as input) until approved. If the user reports a problem, re-adjust the `left_jaw_visual`/`right_jaw_visual` `pos` offsets from Task 1 and re-render.</action>
  <what-built>The gripper clamp visual mesh offsets in soarm_gripper.xml were iteratively adjusted and re-rendered via diagnostics/render_gripper_clamp.py until the clamp mesh appeared to sit flush against the main frame housing in an L-shape, in both open and closed jaw states. Physics (collision jaws) was left untouched.</what-built>
  <how-to-verify>
    1. Open `diagnostics/outputs/gripper_clamp_open.png` and `diagnostics/outputs/gripper_clamp_closed.png` (or re-run `conda run -n libero python3 diagnostics/render_gripper_clamp.py` to regenerate them).
    2. Confirm: (a) the gear-tooth detail of both clamp meshes sits inside/flush against the `main_frame_visual` housing, not poking out past its silhouette; (b) the frame-to-jaw connection reads as a continuous L-shape, not two disconnected floating pieces, in both images.
  </how-to-verify>
  <acceptance_criteria>
    - Human explicitly confirms (via resume-signal) that both rendered images show a flush, connected L-shape with no visible gear-tooth protrusion
  </acceptance_criteria>
  <resume-signal>Type "approved" if the clamp meshes look flush and correctly connected in both renders, or describe what still looks wrong (e.g. "left jaw still pokes out on the +Y side") so the offset can be re-adjusted.</resume-signal>
  <done>Human sign-off received: both rendered images show the clamp mesh flush against the main frame housing in an L-shape, in open and closed states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None (local asset edit + local offscreen render) | No network/auth surface; standalone MuJoCo model load of a trusted local XML file |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-03 | Tampering | soarm_gripper.xml jaw geoms | low | mitigate | Acceptance criteria explicitly assert `left_jaw_collision`/`right_jaw_collision` box geoms and both jaw `&lt;joint&gt;` elements are byte-identical pre/post edit — only the two `*_visual` geoms' `pos` attribute may change |
| T-10-04 | Repudiation | Visual-correctness judgment | low | mitigate | The "flush L-shape" acceptance criterion is inherently a human visual judgment (no closed-form check exists per 10-RESEARCH.md), so a blocking `checkpoint:human-verify` task, not a silent automated pass, gates completion |
</threat_model>

<verification>
Run `conda run -n libero python3 diagnostics/render_gripper_clamp.py` — must exit 0 and produce both PNGs. Then complete the human-verify checkpoint before considering this plan done.
</verification>

<success_criteria>
Clamp visual mesh offsets corrected and rendered; human has confirmed the fix looks flush/L-shaped in both open and closed states; gripper physics geoms unchanged.
</success_criteria>

<output>
Create `.planning/phases/10-digital-twin-fidelity/10-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- `diagnostics/render_gripper_clamp.py` — new standalone diagnostic render script
- `diagnostics/outputs/gripper_clamp_open.png`, `diagnostics/outputs/gripper_clamp_closed.png` — new rendered diagnostic images
- `LIBERO/libero/libero/assets/grippers/soarm_gripper.xml` — modified in place (`left_jaw_visual`/`right_jaw_visual` geom `pos` + rationale comments only)
