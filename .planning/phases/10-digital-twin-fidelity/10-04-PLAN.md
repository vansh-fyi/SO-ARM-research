---
phase: 10-digital-twin-fidelity
plan: 04
type: execute
wave: 3
depends_on: ["10-03"]
files_modified:
  - scripts/verify_urdf.py
  - scripts/test_verify_urdf.py
autonomous: false
requirements: [TWIN-01, TWIN-02, TWIN-03, TWIN-04, TWIN-05, TWIN-06]

must_haves:
  truths:
    - "yourdfpy successfully loads So-101/So-101.urdf without error"
    - "All 5 automated TWIN-01..05 assertions pass against the generated URDF"
    - "The pre-existing env.reset() regression test remains green after the full Phase 10 MJCF+URDF change set"
  artifacts:
    - scripts/verify_urdf.py
    - scripts/test_verify_urdf.py
  key_links:
    - "test_verify_urdf.py imports verify_urdf.py's tree-walk helpers rather than duplicating logic, and imports calibration_utils.py's formula for TWIN-05's independent recomputation"
---

> **2026-09-20 established-model update:** [10-ESTABLISHED-MODEL.md](10-ESTABLISHED-MODEL.md)
> is authoritative for the accepted Coppelia-aligned assembly, black housing,
> yellow jaws, 0.036 m per-jaw cap, corrected starting pose and LIBERO wiring.
> Earlier settings and verification results below are historical and do not
> establish policy success for the accepted model.


<objective>
Install `yourdfpy` (after a human legitimacy sign-off — it's SUS-flagged by the automated package
check due to unavailable download-count data, not an actual red flag) and write `scripts/verify_urdf.py`
(a standalone diagnostic CLI) + `scripts/test_verify_urdf.py` (pytest) that load the URDF generated by
Plan 10-03 and independently confirm TWIN-01 through TWIN-05: gripper correctly chained under the
wrist, `wrist_roll`/gripper joints present with correct axes/limits, and no absolute mesh paths.

Purpose: This is the phase's automated acceptance gate — the concrete proof that Plan 10-03's
generated URDF actually satisfies TWIN-01..05, using a standard independent URDF parser rather than
trusting the generator's own output.
Output: `scripts/verify_urdf.py`, `scripts/test_verify_urdf.py`, all 5 named tests green.
</objective>

<execution_context>
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/workflows/execute-plan.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-digital-twin-fidelity/10-CONTEXT.md
@.planning/phases/10-digital-twin-fidelity/10-RESEARCH.md
@.planning/phases/10-digital-twin-fidelity/10-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: yourdfpy package legitimacy sign-off (Package Legitimacy Gate)</name>
  <action>Blocking human verification per the Package Legitimacy Gate protocol (yourdfpy is SUS-flagged in 10-RESEARCH.md's Package Legitimacy Audit): present the how-to-verify steps below to the user, wait for the resume signal, and do not run `pip install yourdfpy` (or proceed to Task 2) until explicitly approved. This checkpoint is never auto-approvable regardless of `workflow.auto_advance`.</action>
  <what-built>Phase 10 needs to install the `yourdfpy` PyPI package (v0.0.60) to parse and walk the generated URDF for TWIN-01..05 verification. 10-RESEARCH.md's Package Legitimacy Audit flagged it SUS only because download-count stats were unavailable in this environment's legitimacy-check tool run — not because of any actual red flag found in the package itself.</what-built>
  <how-to-verify>
    Visit https://pypi.org/project/yourdfpy/ and confirm: (1) the project page exists and lists version
    0.0.60 or later, (2) the linked source repo is https://github.com/clemense/yourdfpy, (3) the
    package description matches "a simple URDF (Unified Robot Description Format) parser". Optionally
    cross-check github.com/clemense/yourdfpy directly for recent commit/release activity.
  </how-to-verify>
  <acceptance_criteria>
    - Human explicitly confirms (via resume-signal) that the PyPI project page and linked GitHub source check out, or names an approved alternative package
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with `pip install yourdfpy`, or "reject" with an alternative (e.g. `urchin` or `urdf_parser_py` — see 10-RESEARCH.md Alternatives Considered) if the package does not check out.</resume-signal>
  <done>Human sign-off received: yourdfpy's PyPI/GitHub identity confirmed (or an approved alternative named) — install may proceed.</done>
</task>

<task type="auto">
  <name>Task 2: Write verify_urdf.py + test_verify_urdf.py, run TWIN-01..05 tests</name>
  <files>scripts/verify_urdf.py, scripts/test_verify_urdf.py</files>
  <read_first>
    - So-101/So-101.urdf (the generated artifact under test, from Plan 10-03)
    - scripts/calibration_utils.py (Plan 10-01's formula, reused for TWIN-05's independent recomputation)
    - LIBERO/libero/libero/assets/robots/soarm101/robot.xml (wrist_roll's expected unchanged value)
    - LIBERO/libero/libero/assets/grippers/soarm_gripper.xml (gripper_left/right's expected axes)
    - LIBERO/libero/libero/envs/test_camera_config.py (sys.path repo-root-anchoring pattern — mirror the general approach, though this test only needs sibling-module imports, not `libero.*` imports)
    - diagnostics/measure_object_depth.py (print ✓/⚠ diagnostic CLI pattern for verify_urdf.py itself)
  </read_first>
  <action>
    After Task 1's approval, `pip install yourdfpy` into the `libero` conda env.

    Write `scripts/verify_urdf.py` as a standalone diagnostic CLI (mirrors `diagnostics/measure_object_depth.py`'s docstring + precondition-check + print-✓/⚠ shape, NOT raised exceptions on the standalone-run path):
    - `load_generated_urdf()` -> loads `So-101/So-101.urdf` via `yourdfpy.URDF.load(path)`; if the file is missing, `raise SystemExit` pointing at `scripts/mjcf_to_urdf.py` as the fix ("run scripts/mjcf_to_urdf.py first").
    - `build_parent_map(robot)` -> `dict[child_link_name, tuple[joint_name, parent_link_name]]` built from `robot.joint_map.values()`'s `.parent`/`.child`/`.name` attributes.
    - `is_descendant(robot, link_name, ancestor_name)` -> bool, walks `build_parent_map` upward from `link_name` until it finds `ancestor_name` or runs out of parents.
    - `no_absolute_mesh_paths(robot)` -> list of any mesh filename strings starting with `/` or `file://` (empty list = pass).
    When run as `__main__`, print `✓`/`⚠` lines for each of: gripper-is-descendant-of-wrist, `wrist_roll` present with expected range, `gripper_left`/`gripper_right` prismatic with expected axes, no absolute mesh paths — mirroring `measure_object_depth.py`'s exact print-and-flag idiom (no `raise` on this path; the script always exits 0 when run standalone, failures are `⚠`-flagged text only).

    Write `scripts/test_verify_urdf.py` (pytest, real load, no mocking). Since `scripts/` has no `conftest.py`, insert `sys.path.insert(0, str(Path(__file__).resolve().parent))` at the top so `import verify_urdf` and `import calibration_utils` resolve as sibling modules (simpler than `test_camera_config.py`'s four-level-up repo-root anchoring, since this test needs no `libero.*` imports). Five tests, exact names (locked per 10-RESEARCH.md's Phase Requirements -> Test Map table):
    - `test_gripper_is_child_of_wrist` (TWIN-01): `verify_urdf.is_descendant(robot, "right_gripper", "right_hand")` is `True`, AND `robot.link_map["right_gripper"]` is not a URDF root link (it has an incoming joint).
    - `test_wrist_roll_range` (TWIN-02): `robot.joint_map["wrist_roll"].type == "revolute"`; `.limit.lower`/`.limit.upper` equal `-2.7438472969992493`/`2.841206309382605` within `abs=1e-6`.
    - `test_gripper_joint_axes` (TWIN-03): `robot.joint_map["gripper_left"].type == "prismatic"` with axis `[0, -1, 0]`; `robot.joint_map["gripper_right"].type == "prismatic"` with axis `[0, 1, 0]`.
    - `test_no_absolute_mesh_paths` (TWIN-04): `verify_urdf.no_absolute_mesh_paths(robot) == []`.
    - `test_joint_limits_match_calibration` (TWIN-05): for each of `shoulder_pan`/`shoulder_lift`/`elbow_flex`/`wrist_flex`, recompute expected `(lo, hi)` via `calibration_utils.calibration_ticks_to_radians()` against the live calibration JSON's `range_min`/`range_max`, assert `robot.joint_map[name].limit.lower`/`.upper` match within `abs=1e-5`; additionally assert `wrist_roll`'s URDF limit does NOT equal the naive full-turn calibration-derived value `(-math.pi, math.pi)` (regression guard against Pitfall 1) and DOES equal `robot.xml`'s original unchanged value.

    Run `pytest scripts/test_verify_urdf.py -x` and the existing `pytest LIBERO/libero/libero/envs/test_camera_config.py -x` regression test.
  </action>
  <verify>
    <automated>cd /Users/hp/Desktop/Work/Repositories/SoARM-Research && conda run -n libero python3 -m pytest scripts/test_verify_urdf.py LIBERO/libero/libero/envs/test_camera_config.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `yourdfpy` is importable in the `libero` conda env (`conda run -n libero python3 -c "import yourdfpy"` exits 0)
    - All 5 named tests in `scripts/test_verify_urdf.py` pass
    - `conda run -n libero python3 scripts/verify_urdf.py` (standalone) exits 0 and prints 4 `✓` lines (no `⚠` lines)
    - `test_camera_config.py` regression still green
  </acceptance_criteria>
  <done>yourdfpy installed and legitimacy-confirmed; verify_urdf.py + test_verify_urdf.py exist; all TWIN-01..05 assertions pass against the generated URDF; TWIN-06 regression confirmed green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI package install (`pip install yourdfpy`) | Third-party supply-chain surface — the only external-network trust boundary in Phase 10 |
| Generated URDF -> yourdfpy parser | Loading a locally-generated, previously-verified-structure file with a standard parser; no untrusted external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04-SC | Tampering | `pip install yourdfpy` | high | mitigate | Blocking `checkpoint:human-verify` (gate="blocking-human") requiring manual confirmation via pypi.org/project/yourdfpy and the linked GitHub source before install, per the Package Legitimacy Gate protocol — never auto-approvable regardless of `workflow.auto_advance` |
| T-10-07 | Repudiation | TWIN-05 calibration cross-check | low | mitigate | `test_joint_limits_match_calibration` recomputes expected values independently via `calibration_utils` rather than trusting the URDF's own numbers, and explicitly regression-guards against the Pitfall 1 full-turn value leaking into `wrist_roll` |
</threat_model>

<verification>
Complete Task 1's checkpoint, then run `pytest scripts/test_verify_urdf.py LIBERO/libero/libero/envs/test_camera_config.py -x`. Must exit 0.
</verification>

<success_criteria>
yourdfpy installed with human legitimacy sign-off; verify_urdf.py + test_verify_urdf.py exist; all 5 TWIN-01..05 tests pass; TWIN-06 regression green.
</success_criteria>

<output>
Create `.planning/phases/10-digital-twin-fidelity/10-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- `scripts/verify_urdf.py` — new module: `load_generated_urdf()`, `build_parent_map()`, `is_descendant()`, `no_absolute_mesh_paths()`, standalone `__main__` diagnostic CLI
- `scripts/test_verify_urdf.py` — new test file: `test_gripper_is_child_of_wrist`, `test_wrist_roll_range`, `test_gripper_joint_axes`, `test_no_absolute_mesh_paths`, `test_joint_limits_match_calibration`
- `yourdfpy` — new pip dependency installed into the `libero` conda env
