---
phase: 10-digital-twin-fidelity
status: accepted
accepted: 2026-09-20
scope: geometry, kinematics, appearance, jaw travel, LIBERO integration
full_policy_revalidation: pending
---

# Phase 10 — Established model

The user explicitly accepted the Coppelia-aligned MuJoCo model as the established
model (“absolute perfection”), then approved black housing, yellow jaws, and a
final reduction to **0.036 m travel per jaw**. This record supersedes earlier
Phase 10 assumptions and visual-placement conclusions. Preserve the accepted
assembly; future changes require a concrete reason and regression verification.

## Accepted configuration

| Property | Established value |
|---|---|
| Arm / gripper | SO-101 / Robonine parallel gripper |
| Housing RGBA | `0.1 0.1 0.1 1` |
| Jaw RGBA | `1 0.82 0.12 1`, matching arm yellow |
| Both slide joint ranges | `[0, 0.036]` metres |
| Both position-actuator ctrlranges | `[0, 0.036]` metres |
| Open / closed jaw q | `0.036` / `0` |
| Total change in aperture | 72 mm; distinct from proxy aperture 6..78 mm |
| External action | `+1` opens, `-1` closes |
| Mechanical coupling | `gripper_right = gripper_left` joint equality |
| Arm initial q (radians) | `[0, 0, 0, 1.5355350971152681, 0]` |
| Gripper initial q | `[0.036, 0.036]` |
| Arm calibration / limits | Unchanged by assembly correction |

The final cap intentionally uses less travel than the captured Coppelia interval
`[0.008, 0.050]`. Reference mapping is `Coppelia q = 0.008 + MuJoCo q`.
Do not restore the earlier 42 mm or intermediate 40 mm caps.

### Positive-opening coordinate revision — 2026-09-20

The user requested increasing position to open, matching Coppelia and the
physical arm's convention. Both axes were negated and joint/actuator ranges
changed from `[-0.036, 0]` to `[0, 0.036]`; all mesh, mounting, contact and
inertial placements remain unchanged. The LIBERO integrated-action sign was
reversed too, preserving external `+1 = open`, `-1 = close`. Earlier Phase 10
records of negative opening coordinates are superseded by this section.
Old recorded jaw positions/velocities require sign conversion before use with
the new model; this does not change hardware calibration or motor limits.

## Decisions superseding the original context

- **D-09:** The corrected saved Coppelia assembly is the geometry reference.
  Read actual shape vertices and full parent transforms; do not infer STL
  placement from screenshot Euler values or MuJoCo compiled-mesh centroids.
- **D-10:** Keep the complete `right_hand → right_gripper →
  parallel_gripper_mechanism → jaw` chain. Correct the housing, servo, jaw
  visuals, contact proxies and grip_site together. D-04's “visual-only” diagnosis
  and the early recentering attempts were incomplete.
- **D-11:** Both jaw meshes use their fitted rotations; preserve the mechanical
  equality and two-coordinate robosuite observation/action adapter contract.
- **D-12:** Black housing, yellow jaws, 36 mm per-jaw cap are the final accepted
  presentation and travel settings. The source measurement fixture is immutable
  evidence; do not rewrite it to reflect these deliberate travel/colour changes.
- **D-13:** Use the saved ~88° wrist-flex starting pose for LIBERO. Calibration
  zero is a different concept. Earlier all-zero/downward-grasp assumptions do
  not apply to the corrected mounting.

## Authoritative files and distribution

| Consumer | Source / artifact |
|---|---|
| LIBERO robot geometry | `LIBERO/libero/libero/assets/robots/soarm101/robot.xml`, `assets/*.stl` |
| LIBERO gripper geometry | `LIBERO/libero/libero/assets/grippers/soarm_gripper.xml`, `soarm_parallel/*.stl` |
| Runtime model / initial poses | `LIBERO/libero/libero/envs/robots/soarm.py`, `envs/grippers/soarm_gripper.py` |
| Registration | `LIBERO/libero/libero/envs/robots/__init__.py` |
| Portable URDF + meshes | `So-101/`, regenerated by `scripts/mjcf_to_urdf.py` |
| Packaging resources | `LIBERO/MANIFEST.in` |
| LIBERO usage instructions | `LIBERO/SOARM_MODEL.md` |
| Measured reference | `diagnostics/reference/soarm_coppelia.json` |
| Numerical checks / viewer | `diagnostics/verify_coppelia_alignment.py`, `diagnostics/test_coppelia_alignment.py` |
| Capture / fitting tools | `diagnostics/capture_coppelia_reference.lua`, `.py`, `inspect_coppelia_reference.py`, `fit_coppelia_meshes.py` |
| Detailed investigation | `diagnostics/COPPELIA_MUJOCO_ALIGNMENT.md` |

LIBERO reads the fork's assets directly. No copies into installed robosuite are
required. The converter now preserves the mounting transform, colours, servo,
pinion, grasp frame and jaw mimic relation; it copies meshes from canonical
assets when regenerating. Old Coppelia exports, vendor source XML, old datasets
and their embedded model XML remain historical inputs, not runtime alternatives.

## Verification and acceptance

The reference verifier checks 132 combinations of arm/jaw geometry across three
arm/wrist poses and four jaw openings. Maximum measured discrepancy is below
0.002 mm (5 μm tolerance). Automated coverage also checks single-actuator jaw
coupling, URDF structure/calibration/assets/appearance, and real LIBERO reset
with RGB/depth rendering. Live viewer O/C opening and closing were inspected;
the user accepted the final 36 mm configuration.

Current focused result: **11 passed** (3 geometry/coupling/action, 6 URDF/configuration,
2 LIBERO camera integration). Package manifest generation includes the canonical
robot/gripper XML, meshes, registration code and task resources.

Distribution verification: all 10 explicitly audited model/code resources are
present in the generated source-distribution manifest. Repository-based LIBERO
execution is verified; standalone wheel installation is not claimed because
the fork retains legacy repository-qualified imports. No installed-site-packages
modifications were performed for this acceptance.

```sh
conda activate libero
python diagnostics/verify_coppelia_alignment.py
python -m pytest diagnostics/test_coppelia_alignment.py scripts/test_verify_urdf.py LIBERO/libero/libero/envs/test_camera_config.py -q
python scripts/mjcf_to_urdf.py
mjpython diagnostics/verify_coppelia_alignment.py --view
```

Full collection success, camera-framing fidelity, replay of old datasets, and
identified hardware dynamics are **not** established by these checks. Revalidate
them before further data collection/training. The 2026-09-19 full-suite results
in historical Phase 10 reports apply to the earlier model, not this geometry.
Phase 11 hardware work remains independent; no hardware-control code was changed.

## Phase 11 handoff — 2026-09-20

The user approved this model as the established baseline and requested publication
to `origin/main` before moving to Phase 11. The publication includes the existing
Phase 10 commit history, canonical LIBERO assets/runtime, portable URDF, reference
fixture, regression checks, and these acceptance records. Unrelated uncommitted
workspace files are excluded. Pull `main` on Colab or other machines and restart
existing simulation processes before using the updated model.

Phase 11 has not started. Preserve the accepted assembly, black housing/yellow
jaws, and 0.036 m per-jaw travel; do not resume the superseded orientation fixes.
The simulation cap is not a hardware motor limit or a replacement for hardware
calibration and safety validation. Phase 11's hardware bridge remains a separate
integration: validate its action units, gripper mapping, and safety checks before
physical motion. Existing policies, datasets, and camera calibration are not
automatically revalidated by accepting this geometry; rerun task-level checks
before renewed simulation collection or training.
