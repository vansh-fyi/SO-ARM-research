---
phase: 01-colab-environment-setup
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - LIBERO/notebooks/01-colab-env-setup.ipynb
autonomous: false
requirements:
  - ENV-01
  - ENV-02
must_haves:
  truths:
    - First post-restart cell creates /usr/share/glvnd/egl_vendor.d/10_nvidia.json BEFORE setting os.environ["MUJOCO_GL"] = "egl"
    - MUJOCO_GL=egl is set BEFORE any import of mujoco, robosuite, or libero in any cell
    - ~/.libero/config.yaml is written with correct keys BEFORE any import libero call
    - sys.path.insert(0, LIBERO_PKG) is present BEFORE from libero.libero.envs import OffScreenRenderEnv
    - ENV-01 verification cell prints package versions and PASS/FAIL for each (per D-07)
    - ENV-02 verification cell renders a non-black LIBERO Panda frame and prints "ENV-02: PASS" (per D-04, D-07)
    - libero_render_check.png is saved to LIBERO/notebooks/outputs/ with print "Saved → {path}"
    - matplotlib.use("Agg") is called immediately after import matplotlib and before import matplotlib.pyplot
  artifacts:
    - LIBERO/notebooks/01-colab-env-setup.ipynb (cells 11-17 appended)
    - LIBERO/notebooks/outputs/libero_render_check.png (saved by ENV-02 verification cell at runtime)
  key_links:
    - NVIDIA ICD JSON (/usr/share/glvnd/egl_vendor.d/10_nvidia.json) created in cell 11 before MUJOCO_GL env var
    - MUJOCO_GL=egl set in cell 11 before any MuJoCo import in cells 12+
    - ~/.libero/config.yaml written in cell 12 before import libero in cell 14+
    - frame.mean() > 5.0 assertion in ENV-02 cell confirms non-black render (EGL working)
---

<objective>
Append Block B cells (11-17) to the existing notebook: EGL bootstrap cell, LIBERO config.yaml bootstrap cell, sys.path setup, ENV-01 version check, ENV-02 render verification, and a Block B intro markdown cell. After completing this plan, a researcher who has restarted their runtime can run cells 11-17 and see ENV-01 and ENV-02 print PASS.

Purpose: ENV-01 requires conflict verification (pip check + versions); ENV-02 requires a non-black LIBERO frame. Both depend on the critical ordering sequence: NVIDIA ICD → MUJOCO_GL → config.yaml → sys.path → import libero.

Output: Notebook cells 11-17 appended to LIBERO/notebooks/01-colab-env-setup.ipynb; libero_render_check.png produced at runtime.
</objective>

<execution_context>
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/workflows/execute-plan.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.planning/ROADMAP.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.planning/phases/01-colab-environment-setup/01-CONTEXT.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.planning/phases/01-colab-environment-setup/01-RESEARCH.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.planning/phases/01-colab-environment-setup/01-PATTERNS.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.planning/phases/01-colab-environment-setup/01-01-SUMMARY.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/explorations/create_scene.py
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/LIBERO/libero/libero/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append EGL bootstrap, LIBERO config bootstrap, and sys.path setup cells (cells 11-13)</name>
  <files>LIBERO/notebooks/01-colab-env-setup.ipynb</files>

  <read_first>
    - LIBERO/notebooks/01-colab-env-setup.ipynb — read the current notebook JSON to confirm cells 0-10 exist and to find the correct append point; extract the REPO_ROOT, LIBERO_ROOT, LIBERO_PKG variable names from Cell 1 so cells 11-13 reference the same names
    - .planning/phases/01-colab-environment-setup/01-PATTERNS.md §Cell: MUJOCO_GL + EGL Bootstrap — exact EGL setup code with NVIDIA ICD JSON creation; copy exactly, adapting only to reference LIBERO_PKG from Cell 1
    - .planning/phases/01-colab-environment-setup/01-PATTERNS.md §Cell: LIBERO config.yaml Bootstrap — the get_default_path_dict() keys from LIBERO/__init__.py lines 62-96 that config.yaml must pre-populate
    - LIBERO/libero/libero/__init__.py lines 62-96 — the actual input() call and config dict structure that the bootstrap must match exactly
    - .planning/phases/01-colab-environment-setup/01-RESEARCH.md §Common Pitfalls Pitfall 1, 2, 3, 4 — the four ordering violations that cells 11-13 must prevent
  </read_first>

  <action>
  Read the current notebook JSON, then append three new cells (cells 11, 12, 13) by editing the notebook's "cells" array.

  Cell 11 [markdown]: "## BLOCK B: Verification (run after restart)"

  Cell 12 [code] — EGL + MUJOCO_GL bootstrap (per RESEARCH.md Common Pitfalls, per PATTERNS.md EGL section). This is the FIRST EXECUTABLE CELL after restart. It must do three things in this exact order:
  (a) Create the NVIDIA ICD JSON: os.makedirs("/usr/share/glvnd/egl_vendor.d", exist_ok=True), then write {"file_format_version": "1.0.0", "ICD": {"library_path": "libEGL_nvidia.so.0"}} as JSON to /usr/share/glvnd/egl_vendor.d/10_nvidia.json.
  (b) Set os.environ["MUJOCO_GL"] = "egl", os.environ["PYOPENGL_PLATFORM"] = "egl", os.environ["TOKENIZERS_PARALLELISM"] = "false".
  (c) Print "EGL ICD created. MUJOCO_GL=egl set. Do not import mujoco/robosuite/libero before this cell." This cell must NOT import mujoco, robosuite, or libero — imports belong in later cells.

  Cell 13 [code] — LIBERO config.yaml bootstrap. Must run BEFORE any import libero. Steps:
  (a) Import yaml and pathlib.Path.
  (b) Build the config dict with keys matching get_default_path_dict() from LIBERO/__init__.py: benchmark_root (= LIBERO_ROOT from Cell 1), bddl_files, init_states, datasets, assets — all derived from LIBERO_ROOT using f-string concatenation (not hardcoded strings, so REPO_ROOT change in Cell 1 propagates).
  (c) Write the config: (Path.home() / ".libero").mkdir(parents=True, exist_ok=True), then write yaml.dump(config) to ~/.libero/config.yaml.
  (d) Print "Saved → {config_dir / 'config.yaml'}" per project print convention.

  Cell 14 [code] — sys.path setup. Insert LIBERO_PKG (defined in Cell 1) into sys.path if not already present, using sys.path.insert(0, LIBERO_PKG). This replicates the create_scene.py pattern (PATTERNS.md §sys.path insert pattern). Print the inserted path. This must run AFTER Cell 13 and BEFORE any from libero.libero.envs import statement.
  </action>

  <verify>
    <automated>python3 -c "
import json
nb = json.load(open('LIBERO/notebooks/01-colab-env-setup.ipynb'))
cells = nb['cells']
assert len(cells) >= 15, f'Expected at least 15 cells after append, got {len(cells)}'
cell12 = ''.join(cells[12]['source'])
assert '10_nvidia.json' in cell12, 'NVIDIA ICD JSON path missing from cell 12'
assert 'MUJOCO_GL' in cell12 and 'egl' in cell12, 'MUJOCO_GL=egl missing from cell 12'
assert 'import mujoco' not in cell12 and 'import robosuite' not in cell12, 'Import violation in cell 12'
cell13 = ''.join(cells[13]['source'])
assert '.libero' in cell13 and 'config.yaml' in cell13, 'LIBERO config bootstrap missing from cell 13'
assert 'benchmark_root' in cell13, 'benchmark_root key missing from config dict'
cell14 = ''.join(cells[14]['source'])
assert 'sys.path' in cell14 and 'LIBERO_PKG' in cell14, 'sys.path insert missing from cell 14'
print('EGL + config + sys.path cells: OK')
"</automated>
  </verify>

  <acceptance_criteria>
  - Cell 12 source contains "10_nvidia.json", "MUJOCO_GL", "egl", and "ICD" but does NOT contain "import mujoco", "import robosuite", or "import libero"
  - Cell 13 source contains ".libero", "config.yaml", "benchmark_root", "bddl_files", "init_states", and "Saved →"
  - Cell 14 source contains "sys.path" and "LIBERO_PKG" (the variable name from Cell 1, not a hardcoded string)
  - Cell 12 appears before cell 13, which appears before cell 14 in the cells array
  - No hardcoded absolute path string appears in cells 12-14 where a Cell 1 variable (LIBERO_ROOT, LIBERO_PKG) should be used
  </acceptance_criteria>

  <done>
  Cells 11-14 exist in the notebook in the correct order. The automated check passes with no assertion errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append ENV-01 and ENV-02 verification cells (cells 15-17)</name>
  <files>LIBERO/notebooks/01-colab-env-setup.ipynb</files>

  <read_first>
    - LIBERO/notebooks/01-colab-env-setup.ipynb — read current notebook after Task 1 to confirm cells 11-14 are present and to find correct append index for cells 15-17
    - .planning/phases/01-colab-environment-setup/01-CONTEXT.md §Decisions D-04, D-07 — D-04: render default Panda LIBERO environment to verify 4.40.1 doesn't break rendering; D-07: each ENV requirement gets explicit PASS/FAIL output with print("ENV-0X: PASS/FAIL — detail")
    - .planning/phases/01-colab-environment-setup/01-PATTERNS.md §Cell: ENV-02 Render Verification — exact OffScreenRenderEnv instantiation, obs["agentview_image"][::-1] flip, frame.mean() > 5.0 check, matplotlib.use("Agg") ordering, plt.imsave save pattern, print("Saved → {out}") convention
    - .planning/phases/01-colab-environment-setup/01-RESEARCH.md §Code Examples §ENV-02 Render Check — full reference implementation including camera config, 5-step loop, mean_pixel assertion
    - explorations/create_scene.py lines 18-20 and 80-82 — matplotlib.use("Agg") pattern and print("Saved → {out}") convention to replicate exactly
  </read_first>

  <action>
  Read the current notebook JSON (after Task 1 appended cells 11-14), then append cells 15, 16, 17.

  Cell 15 [code] — ENV-01 verification (per D-07): Use importlib.metadata.version() for each required package: mujoco, robosuite, gym, torch, timm, peft, tokenizers. Print a formatted table where each row shows package name, installed version, and expected version side by side. After the table, run "pip check" via subprocess.run(["pip", "check"], capture_output=True, text=True) and print the result. Then print the overall result: if pip check returncode is 0, print "ENV-01: PASS — all packages installed, no conflicts detected"; else print "ENV-01: FAIL — conflict: {stderr}". Use importlib.metadata import at top of cell.

  Cell 16 [code] — ENV-02 render verification (per D-04, D-07, PATTERNS.md render cell). This is the most complex cell. Steps in exact order:
  (a) Import numpy as np.
  (b) import matplotlib — then immediately call matplotlib.use("Agg") — then import matplotlib.pyplot as plt. (This is the project convention from create_scene.py lines 18-20 and CLAUDE.md.)
  (c) from libero.libero.envs import OffScreenRenderEnv (LIBERO_PKG must already be in sys.path from cell 14).
  (d) Define BDDL_FILE using BDDL_FILE from Cell 1 (which was already set there as the libero_spatial bowl task BDDL path). Do NOT redefine it as a new hardcoded string — reference the variable.
  (e) Construct env = OffScreenRenderEnv(bddl_file_name=BDDL_FILE, camera_names=["agentview"], camera_heights=256, camera_widths=256, has_renderer=False, has_offscreen_renderer=True).
  (f) obs = env.reset(). Run 5 steps: for _ in range(5): obs, _, _, _ = env.step(np.zeros(7)).
  (g) frame = obs["agentview_image"][::-1] (flip: MuJoCo renders upside-down — this is in PATTERNS.md).
  (h) mean_pixel = frame.mean(). status = "PASS" if mean_pixel > 5.0 else "FAIL". Print "ENV-02: {status} — mean pixel value: {mean_pixel:.2f} (threshold: > 5.0)".
  (i) Save: OUT_DIR is defined in Cell 1; out_path = OUT_DIR + "/libero_render_check.png". Call plt.imsave(out_path, frame). Print f"Saved → {out_path}".
  (j) Display inline: plt.figure(figsize=(4, 4)), plt.imshow(frame), plt.title("ENV-02: LIBERO Panda Render Check"), plt.axis("off"), plt.tight_layout(), plt.show().
  (k) env.close().

  Cell 17 [markdown]: "---\n### Block B Progress\n- [x] EGL configured (Cell 12)\n- [x] LIBERO config bootstrapped (Cell 13)\n- [ ] ENV-01 — run Cell 15\n- [ ] ENV-02 — run Cell 16\n- [ ] ENV-03 — run Plan 03 cells\n\nProceed to Plan 03 for ENV-03 (OpenVLA-OFT model loading)."
  </action>

  <verify>
    <automated>python3 -c "
import json
nb = json.load(open('LIBERO/notebooks/01-colab-env-setup.ipynb'))
cells = nb['cells']
assert len(cells) >= 18, f'Expected at least 18 cells, got {len(cells)}'
cell15 = ''.join(cells[15]['source'])
assert 'importlib.metadata' in cell15 or 'importlib' in cell15, 'importlib missing from ENV-01 cell'
assert 'pip check' in cell15 or 'pip_check' in cell15 or 'subprocess' in cell15, 'pip check missing from ENV-01 cell'
assert 'ENV-01' in cell15 and 'PASS' in cell15 and 'FAIL' in cell15, 'PASS/FAIL output missing from ENV-01 cell'
cell16 = ''.join(cells[16]['source'])
assert 'matplotlib.use' in cell16 and 'Agg' in cell16, 'matplotlib.use(Agg) missing from ENV-02 cell'
assert 'OffScreenRenderEnv' in cell16, 'OffScreenRenderEnv import missing from ENV-02 cell'
assert 'agentview_image' in cell16 and '[::-1]' in cell16, 'image flip missing from ENV-02 cell'
assert 'mean()' in cell16 and '5.0' in cell16, 'mean pixel check missing from ENV-02 cell'
assert 'Saved' in cell16 and 'libero_render_check' in cell16, 'save + print convention missing from ENV-02 cell'
assert 'ENV-02' in cell16 and 'PASS' in cell16 and 'FAIL' in cell16, 'PASS/FAIL output missing from ENV-02 cell'
print('ENV-01 and ENV-02 cells: OK')
"</automated>
  </verify>

  <acceptance_criteria>
  - Cell 15 uses importlib.metadata.version() to check at minimum: mujoco, robosuite, gym, torch; runs pip check via subprocess; prints "ENV-01: PASS" or "ENV-01: FAIL"
  - Cell 16 calls matplotlib.use("Agg") before import matplotlib.pyplot (per CLAUDE.md project convention)
  - Cell 16 calls OffScreenRenderEnv with camera_names=["agentview"], has_renderer=False, has_offscreen_renderer=True
  - Cell 16 flips the frame with [::-1] (MuJoCo upside-down images)
  - Cell 16 checks frame.mean() > 5.0 for the non-black assertion
  - Cell 16 prints "Saved → {out_path}" (exact arrow convention from CLAUDE.md and create_scene.py)
  - Cell 16 calls env.close() at the end
  - Cell 17 is a markdown progress tracker referencing ENV-03 and Plan 03
  - BDDL_FILE in cell 16 references the variable from Cell 1, not a new hardcoded path string
  </acceptance_criteria>

  <done>
  Cells 15-17 exist in the notebook. The automated check passes. A researcher running cells 15-16 on a correctly installed post-restart runtime will see "ENV-01: PASS" and "ENV-02: PASS" printed, and libero_render_check.png saved to LIBERO/notebooks/outputs/.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Notebook cell → LIBERO package | libero package is imported from LIBERO_PKG path; if path is wrong, EOFError or ModuleNotFoundError |
| EGL bootstrap → MuJoCo C extension | C extension reads MUJOCO_GL at import time; ordering violation causes black frames silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | /usr/share/glvnd/egl_vendor.d/10_nvidia.json write | low | accept | Required for GPU rendering; JSON file contains no executable content; Colab root access is expected for system config |
| T-02-02 | Information Disclosure | ~/.libero/config.yaml with repo path | low | accept | Research-only env; no secrets; paths are local Colab drive paths |
| T-02-03 | Denial of Service | OffScreenRenderEnv BDDL path misconfigured | medium | mitigate | BDDL_FILE derived from REPO_ROOT in Cell 1; cell 13 writes config.yaml pointing to same root; consistent derivation prevents path mismatch |
</threat_model>

<verification>
After both tasks complete, run the Task 2 automated check. To verify ENV-02 intent:

grep check: python3 -c "import json; src=''.join(json.load(open('LIBERO/notebooks/01-colab-env-setup.ipynb'))['cells'][16]['source']); print('matplotlib.use Agg:', 'matplotlib.use' in src and 'Agg' in src); print('image flip:', '[::-1]' in src); print('mean check:', 'mean()' in src); print('PASS/FAIL:', 'ENV-02' in src)"
</verification>

<success_criteria>
ENV-01 cell prints package versions and PASS/FAIL conflict status. ENV-02 cell renders a LIBERO Panda frame, asserts mean pixel > 5.0, saves to LIBERO/notebooks/outputs/libero_render_check.png with "Saved → " print, and displays inline. Both cells run without import errors on a correctly installed post-restart runtime.
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Description |
|----------|------|-------------|
| Notebook (Block B, ENV-01/02) | LIBERO/notebooks/01-colab-env-setup.ipynb | Cells 11-17 appended: EGL bootstrap, LIBERO config, sys.path, ENV-01/02 verification |
| Render check image (runtime) | LIBERO/notebooks/outputs/libero_render_check.png | Saved by ENV-02 cell at runtime; non-black Panda LIBERO frame |

<output>
Create .planning/phases/01-colab-environment-setup/01-02-SUMMARY.md when done
</output>
