---
phase: 01-colab-environment-setup
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - LIBERO/notebooks/01-colab-env-setup.ipynb
autonomous: true
requirements:
  - ENV-03
must_haves:
  truths:
    - ENV-03 verification cell loads OpenVLA-OFT via AutoModelForVision2Seq.from_pretrained with torch_dtype=torch.bfloat16 and low_cpu_mem_usage=True (per D-05 — bf16, no 4-bit quant)
    - ENV-03 cell passes a dummy 256x256 RGB image and prompt through model.predict_action() and asserts action.shape == (7,)
    - ENV-03 cell prints "ENV-03: PASS — action shape (7,)" on success (per D-07)
    - The unnorm_key diagnostic is included — if "libero_spatial" raises KeyError, print(list(model.norm_stats.keys())) before raising
    - A summary table cell is the final cell — lists ENV-01, ENV-02, ENV-03 with PASS/FAIL status references
    - No 4-bit quantization (bitsandbytes) appears anywhere in the notebook (per D-05, deferred)
  artifacts:
    - LIBERO/notebooks/01-colab-env-setup.ipynb (cells 18-20 appended — final state)
  key_links:
    - CHECKPOINT = "moojink/openvla-7b-oft-finetuned-libero-spatial" — specific model ID from RESEARCH.md
    - trust_remote_code=True required for AutoModelForVision2Seq.from_pretrained on this checkpoint
    - torch.bfloat16 dtype — A100 supports bf16; prevents OOM on 40GB VRAM (per D-05)
    - action.shape == (7,) — the only contract that matters for ENV-03; action values are not validated
---

<objective>
Append the ENV-03 VLA loading verification cell and the final summary table cell to complete the notebook. After this plan, the notebook is the complete Phase 1 deliverable: 20 cells covering the full install-to-verify pipeline. A researcher running all cells top-to-bottom (with the one required restart) will see ENV-01, ENV-02, and ENV-03 all print PASS.

Purpose: ENV-03 is the highest-stakes verification — loading a 7B model in bf16 on A100. The cell must handle the unnorm_key uncertainty from the Assumptions Log (A1) and provide a diagnostic path if the key name differs.

Output: Completed notebook with cells 18-20 (ENV-03 cell, summary markdown); Phase 1 is fully planned and ready for execution.
</objective>

<execution_context>
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/workflows/execute-plan.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.planning/ROADMAP.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.planning/phases/01-colab-environment-setup/01-CONTEXT.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.planning/phases/01-colab-environment-setup/01-RESEARCH.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.planning/phases/01-colab-environment-setup/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append ENV-03 OpenVLA-OFT load verification cell and summary (cells 18-20)</name>
  <files>LIBERO/notebooks/01-colab-env-setup.ipynb</files>

  <read_first>
    - LIBERO/notebooks/01-colab-env-setup.ipynb — read current notebook JSON to confirm cells 0-17 are present and find the correct append index (should be 18)
    - .planning/phases/01-colab-environment-setup/01-RESEARCH.md §Code Examples §ENV-03 VLA Load Check — the exact from_pretrained() kwargs (trust_remote_code=True, torch_dtype=torch.bfloat16, low_cpu_mem_usage=True), processor pattern, dummy image construction, predict_action call, and unnorm_key diagnostic
    - .planning/phases/01-colab-environment-setup/01-RESEARCH.md §Assumptions Log A1 — unnorm_key="libero_spatial" is ASSUMED; the fallback diagnostic (print model.norm_stats.keys()) must be included in the cell
    - .planning/phases/01-colab-environment-setup/01-RESEARCH.md §Common Pitfalls Pitfall 7 — OOM prevention: torch_dtype=torch.bfloat16 and low_cpu_mem_usage=True are both required; check GPU assertion passed
    - .planning/phases/01-colab-environment-setup/01-CONTEXT.md §Decisions D-05 (bf16, no bitsandbytes), D-07 (PASS/FAIL output format)
  </read_first>

  <action>
  Read the current notebook JSON (after Plans 01 and 02), confirm 18 cells (0-17) exist, then append cells 18, 19, 20.

  Cell 18 [markdown]: "## ENV-03: OpenVLA-OFT Model Load Check\n\nLoads `moojink/openvla-7b-oft-finetuned-libero-spatial` in bf16 on A100. Requires ~16GB VRAM. Verify GPU assertion passed (Cell 2) before running."

  Cell 19 [code] — ENV-03 verification cell (per D-05, D-07, RESEARCH.md Code Examples ENV-03). Implement in this exact step order:
  (a) Import torch, numpy as np, and PIL.Image.
  (b) Import AutoModelForVision2Seq and AutoProcessor from transformers.
  (c) Define CHECKPOINT = "moojink/openvla-7b-oft-finetuned-libero-spatial" (UPPER_SNAKE_CASE per CLAUDE.md).
  (d) Print "Loading processor from {CHECKPOINT}..." then load: processor = AutoProcessor.from_pretrained(CHECKPOINT, trust_remote_code=True).
  (e) Print "Loading model in bf16 on GPU..." then load: model = AutoModelForVision2Seq.from_pretrained(CHECKPOINT, trust_remote_code=True, torch_dtype=torch.bfloat16, low_cpu_mem_usage=True).to("cuda"). Both trust_remote_code=True and torch_dtype=torch.bfloat16 are required — omitting either will cause load failure or OOM respectively (per RESEARCH.md Pitfall 7, D-05).
  (f) Print model dtype: next(model.parameters()).dtype and device: next(model.parameters()).device.
  (g) Build dummy input: dummy_image = Image.fromarray(np.random.randint(0, 255, (256, 256, 3), dtype=np.uint8)) and prompt = "In: What action should the robot take to pick up the black bowl?\nOut:". These are the exact values from RESEARCH.md Code Examples.
  (h) Process inputs: inputs = processor(prompt, dummy_image).to("cuda:0", dtype=torch.bfloat16).
  (i) Run inference inside torch.no_grad(): action = model.predict_action(**inputs, unnorm_key="libero_spatial", do_sample=False). Wrap in a try/except KeyError: on KeyError, print "KeyError on unnorm_key='libero_spatial'. Available keys: " + str(list(model.norm_stats.keys())) + ". Re-run with correct key." then re-raise. This handles Assumption A1 from RESEARCH.md.
  (j) Assert and report: status = "PASS" if action.shape == (7,) else "FAIL". Print "ENV-03: {status} — action shape: {action.shape}, dtype: {action.dtype}". Print "Action values: {action.cpu().numpy()}".

  Cell 20 [markdown]: Final summary table. Title "## Phase 1 Summary". Include a markdown table with columns: Requirement | Check | Status | Notes. Three rows: ENV-01 (version check + pip check | run Cell 15 | placeholder), ENV-02 (LIBERO render | run Cell 16 | placeholder), ENV-03 (VLA load + action shape | run Cell 19 | placeholder). Add a note: "Update Status column after running verification cells. All three must show PASS before proceeding to Phase 2." Also add "**Phase 1 deliverable:** LIBERO/notebooks/01-colab-env-setup.ipynb — run cells 0-20 (with restart after cell 10)."
  </action>

  <verify>
    <automated>python3 -c "
import json
nb = json.load(open('LIBERO/notebooks/01-colab-env-setup.ipynb'))
cells = nb['cells']
assert len(cells) == 21, f'Expected 21 cells (0-20), got {len(cells)}'
cell19 = ''.join(cells[19]['source'])
assert 'AutoModelForVision2Seq' in cell19, 'AutoModelForVision2Seq missing from ENV-03 cell'
assert 'trust_remote_code=True' in cell19, 'trust_remote_code missing from ENV-03 cell'
assert 'torch.bfloat16' in cell19, 'bfloat16 dtype missing from ENV-03 cell'
assert 'low_cpu_mem_usage=True' in cell19, 'low_cpu_mem_usage missing from ENV-03 cell'
assert 'predict_action' in cell19, 'predict_action call missing from ENV-03 cell'
assert 'action.shape' in cell19 and '(7,)' in cell19, 'shape assertion missing from ENV-03 cell'
assert 'ENV-03' in cell19 and 'PASS' in cell19 and 'FAIL' in cell19, 'PASS/FAIL output missing from ENV-03 cell'
assert 'norm_stats' in cell19 or 'norm_stats.keys' in cell19, 'unnorm_key diagnostic fallback missing'
assert 'bitsandbytes' not in cell19 and 'load_in_4bit' not in cell19, '4-bit quant must not appear (deferred per D-05)'
cell20 = ''.join(cells[20]['source'])
assert 'ENV-01' in cell20 and 'ENV-02' in cell20 and 'ENV-03' in cell20, 'Summary table missing requirements'
print('ENV-03 cell and summary: OK')
print(f'Total cells: {len(cells)}')
"</automated>
  </verify>

  <acceptance_criteria>
  - Cell 19 source contains "AutoModelForVision2Seq", "AutoProcessor", "trust_remote_code=True", "torch.bfloat16", "low_cpu_mem_usage=True"
  - Cell 19 source contains "predict_action" and "unnorm_key"
  - Cell 19 source contains a try/except KeyError block that prints model.norm_stats.keys() (Assumption A1 diagnostic)
  - Cell 19 source contains "action.shape == (7,)" and prints "ENV-03: PASS" or "ENV-03: FAIL"
  - Cell 19 source does NOT contain "bitsandbytes", "load_in_4bit", or "quantization" (per D-05, deferred)
  - Cell 20 is a markdown summary table listing all three requirements
  - Total notebook cell count is exactly 21 (cells 0-20)
  - CHECKPOINT variable uses UPPER_SNAKE_CASE as required by CLAUDE.md
  </acceptance_criteria>

  <done>
  The notebook has exactly 21 cells. The automated check passes with no assertion errors. The ENV-03 cell loads the model in bf16, runs predict_action with a dummy image, and prints PASS/FAIL based on action.shape == (7,). The summary table closes the notebook.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Notebook → HuggingFace Hub | Model weights downloaded from moojink/ org; trust_remote_code=True executes remote model code |
| Colab GPU → model weights | 7B bf16 model loads ~14GB onto A100; incorrect dtype risks OOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Spoofing | moojink/openvla-7b-oft-finetuned-libero-spatial HuggingFace checkpoint | medium | mitigate | Use exact checkpoint ID from RESEARCH.md (cited from official openvla-oft LIBERO.md); trust_remote_code=True is required and acceptable for published research model; verify checkpoint SHA in note comment |
| T-03-02 | Tampering | trust_remote_code=True in AutoModelForVision2Seq.from_pretrained | medium | accept | Required by model architecture; moojink is the published OFT paper author; research context; acceptable for academic pipeline |
| T-03-03 | Denial of Service | OOM if bf16 not set or T4 used | high | mitigate | torch_dtype=torch.bfloat16 and low_cpu_mem_usage=True enforced in cell; GPU assertion in Cell 2 warns if not A100 before this cell runs |
</threat_model>

<verification>
Run automated check from the task verify block. Full notebook structure check:

python3 -c "import json; nb = json.load(open('LIBERO/notebooks/01-colab-env-setup.ipynb')); print('Total cells:', len(nb['cells'])); [print(i, nb['cells'][i]['cell_type'], repr(''.join(nb['cells'][i]['source'])[:60])) for i in range(len(nb['cells']))]"

This prints a cell-by-cell summary for visual inspection.
</verification>

<success_criteria>
The complete notebook (21 cells) covers the full pipeline from install through VLA inference verification. A researcher on a fresh A100 Colab runtime can run all cells (with one restart after cell 10) and see all three ENV checks print PASS. The notebook is committed to git at LIBERO/notebooks/01-colab-env-setup.ipynb and Phase 1 is ready for /gsd-verify-work.
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Description |
|----------|------|-------------|
| Notebook (complete) | LIBERO/notebooks/01-colab-env-setup.ipynb | Final 21-cell notebook; all install, bootstrap, and verification cells |
| Render check image (runtime) | LIBERO/notebooks/outputs/libero_render_check.png | Saved by ENV-02 cell (Cell 16) at runtime; proves EGL rendering works |

<output>
Create .planning/phases/01-colab-environment-setup/01-03-SUMMARY.md when done
</output>
