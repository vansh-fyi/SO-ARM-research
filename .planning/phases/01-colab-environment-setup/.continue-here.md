---
context: phase
phase: 01-colab-environment-setup
task: 9
total_tasks: 12
status: paused
last_updated: 2026-07-09T04:41:52.112Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [x] CONSTRAINT: sys.modules numpy caching — After torch imports numpy 2.0.2 in Cell 2, `import numpy` in later cells returns the cached version even after pip reinstall. Always use `subprocess pip show numpy` to check on-disk version, never `import numpy; print(np.__version__)`.
- [x] CONSTRAINT: numpy pin ordering — numpy<2 MUST be installed LAST in Step 3. Any package installed after it (e.g. opencv 4.10+) will upgrade numpy back to 2.x. Also pin opencv<4.10 to prevent the conflict entirely.
- [x] CONSTRAINT: flash-attn cp312 wheel — flash-attn 2.5.x has NO cp312 wheel. Use 2.6.3+cu118torch2.2+cp312 pre-built wheel. No cu121 wheel exists in 2.6.3; cu118 runs on CUDA 12.x via forward compatibility.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Import-based version check | Using `import numpy; np.__version__` after pip reinstall returns cached sys.modules version, not on-disk | blocking | Use `subprocess pip show numpy` — always checks disk |
| numpy pin before opencv | Installing numpy<2 before opencv causes opencv 4.10+ to upgrade numpy back to 2.x | blocking | numpy<2 pin must be the FINAL pip command in Step 3 |
| flash-attn source compile | `pip install flash-attn --no-build-isolation` compiles CUDA kernels for 60-90 min then OOMs T4 | blocking | Use pre-built wheel URL from GitHub releases |
| conda on Colab | Colab has no conda; conda commands fail silently | advisory | Use pip only; check binary wheel availability first |

<current_state>
Step 3 of Block A now PASSES: mujoco 3.3.2 installed, numpy 1.26.4 on disk confirmed.
User is pausing mid-session. Steps 4, 5, 6 not yet run in current Colab session.
Step 3 fix just committed (3a3d283). Ready for Steps 4-6 then Block B.
</current_state>

<completed_work>

Completed Tasks (this phase):
- Task 1: Notebook skeleton + Block A install cells (plan 01-01) — Done — `3fb1d31`
- Task 2: Fix libosmesa6 apt + LIBERO path + cell text (round 1) — Done — `ef50d7f`
- Task 3: Fix 4 T4 runtime bugs (mujoco build isolation, LIBERO clone fallback, HF hub pin, flash-attn) — Done — `05c9450`
- Task 4: Rewrite Step 3: mujoco 3.3.2 binary wheel + mj_step1→mj_kinematics shim — Done — `489c22a`
- Task 5: flash-attn pre-built wheel (cu118+torch2.2+cp312, avoid 90-min OOM) — Done — `c4b0a9b`
- Task 6: Restore numpy<2 + correct flash-attn wheel (no cu121 in 2.6.3) — Done — `336afa1`
- Task 7: Pin opencv<4.10 + move numpy pin last in Step 3 — Done — `8fb3e2e`
- Task 8: Fix numpy version check: subprocess pip-show vs import (sys.modules caching) — Done — `3a3d283`

Step 3 now passes on Colab T4 (confirmed by user this session).
</completed_work>

<remaining_work>

- Task 9: User completes Steps 4, 5, 6 on Colab T4 (Step 3 confirmed ✓)
  - Step 4: LIBERO editable install (~1 min)
  - Step 5: transformers fork + HF deps (~3 min) — expect 2 resolver warnings (gradio, sentence-transformers) — safe to ignore
  - Step 6: flash-attn 2.6.3+cu118 pre-built wheel (~30 sec)
  - Then: Runtime > Restart session
- Task 10: Execute Wave 2 (plan 01-02) — Block B cells: EGL bootstrap, LIBERO config, sys.path, ENV-01/02 verification
- Task 11: Execute Wave 3 (plan 01-03) — ENV-03 OpenVLA-OFT load cell + summary table
- Task 12: Phase 1 verification (gsd-verifier) + ROADMAP/STATE update
</remaining_work>

<decisions_made>

- mujoco 3.3.2 chosen over 2.3.7: no cp312 binary wheel for 2.3.7; conda not on Colab; 3.3.2 confirmed stable for LIBERO
- bddl_base_domain.py patched: mj_step1 → hasattr shim → mj_kinematics (mujoco 3.x equivalent for forward-kinematics-only reset step)
- numpy<2 pinned LAST in Step 3: torch 2.2.0 compiled against numpy 1.x; numpy 2.x breaks torch.from_numpy() needed for LIBERO image pipeline
- opencv pinned <4.10: 4.10+ requires numpy>=2 which causes automatic upgrade
- flash-attn 2.6.3+cu118torch2.2+cp312: only pre-built cp312 wheel available; cu118 works on CUDA 12.x via forward compat
- numpy version check via subprocess: sys.modules caches old numpy version; subprocess reads fresh disk state
- huggingface_hub 0.36.2: tokenizers==0.19.1 requires hub<1.0.0; gradio warning is non-blocking
</decisions_made>

<blockers>
- Human action: User needs to run Steps 4-6 on Colab T4, then restart runtime
- After restart: Block B (ENV-01/02/03 verification) needs to be executed
</blockers>

## Required Reading (in order)
1. `LIBERO/notebooks/01-colab-env-setup.ipynb` — current state of all cells
2. `LIBERO/libero/libero/envs/bddl_base_domain.py` L754-759 — mj_step1 shim
3. `.planning/phases/01-colab-environment-setup/01-02-PLAN.md` — Wave 2 plan (Block B)

## Critical Anti-Patterns (do NOT repeat these)
- ANTI-PATTERN: source-compile flash-attn on T4 → crashes after 60-90 min OOM → use pre-built wheel URL
- ANTI-PATTERN: check numpy version with `import numpy` after pip reinstall → sys.modules cache → use subprocess pip show
- ANTI-PATTERN: install numpy<2 before opencv in Step 3 → opencv 4.10+ upgrades it back → pin numpy last AND pin opencv<4.10

## Infrastructure State
- Colab T4 runtime: fresh restart needed (user ran Steps 1-3 this session; runtime may still be live)
- Step 3: PASSING — numpy 1.26.4 on disk, mujoco 3.3.2 installed
- Steps 4-6: not yet run in current session (Step 5 passing from earlier session, before Step 3 was fixed)
- Git: commit 3a3d283 is latest; ~8 uncommitted files in working tree (planning files, config, notebook symlinks)

<context>
We're in an iterative Colab compatibility debug loop for Block A. 8 rounds of fixes total. The core challenge: Colab Python 3.12 has pre-installed packages that conflict with LIBERO's older dependency requirements. Each fix revealed a new layer of the onion. Now Step 3 is confirmed working. Steps 4-6 should be cleaner (Step 5 already passed, Step 6 now uses pre-built wheel). After Block A + restart, Wave 2 (Block B) builds the verification cells (EGL rendering check, LIBERO env init, OpenVLA-OFT model load).
</context>

<next_action>
Start with: Ask user "Did Steps 4, 5, 6 all pass? Did you restart the runtime?" — if yes: spawn gsd-executor for plan 01-02 (Wave 2 Block B cells). If Step 6 fails: check flash-attn output (should show 'flash-attn 2.6.3 installed'), fall back to cu123 wheel if cu118 fails.
</next_action>
