---
context: phase
phase: 01-colab-environment-setup
task: 5
total_tasks: 8
status: blocked_on_human
last_updated: 2026-07-08T11:55:17.982Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: version-pins-predate-colab-python — LIBERO's requirements.txt pins target Python 3.8-3.10; Colab is now on Python 3.11/3.12 where many packages lack binary wheels and source builds fail — Check Python version first (`!python --version`) before assuming any exact pin will install cleanly.
- [ ] CONSTRAINT: mujoco-no-cp312-pip-wheel — mujoco 2.3.7 has no cp311/cp312 binary wheel on PyPI; Python 3.12 also removed `distutils` which mujoco's setup.py requires; pip source builds always fail — Use `conda install -c conda-forge mujoco=2.3.7` (now in Step 3/Cell 6).
- [ ] CONSTRAINT: libero-nested-git-not-synced — LIBERO is a nested git repo inside SoARM-Research; Google Drive doesn't sync nested `.git/` directories; `setup.py` is missing on Colab even when LIBERO/ dir exists — Cell 7 (Step 4) now has auto-clone fallback from GitHub.
- [ ] CONSTRAINT: tokenizers-huggingface-conflict — `tokenizers==0.19.1` hard-requires `huggingface_hub<1.0.0`; adding `>=1.2.0` causes `ResolutionImpossible` — Keep `huggingface_hub` unpinned in Cell 8 (Step 5).

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| silent-q-masking | Using `-q` on pip installs hides source-build failures; three fix rounds were needed because the real errors were invisible | advisory | Remove `-q` from any install that is failing; add it back only after the install passes |
| adding-version-pins-that-conflict | Added `huggingface_hub>=1.2.0` to resolve an advisory Colab gradio warning; this turned a non-blocking advisory conflict into a hard `ResolutionImpossible` | blocking | Never add version constraints to resolve advisory warnings; only add constraints when a package won't import |
| assuming-exact-pins-work-on-new-python | Assumed LIBERO's `requirements.txt` pins would install clean on modern Colab; numpy 1.22.4 and opencv 4.6.0.66 have no cp311/cp312 wheels | blocking | Check PyPI wheel availability for the target Python version before pinning; use `--prefer-binary` and relaxed bounds where needed |

<current_state>
Phase 1, Bug-fix loop on Block A (plan 01-01 cells). Wave 1 (01-01) executor ran and created the notebook skeleton. Three rounds of Colab tests have revealed source-build failures on what is likely Python 3.11/3.12. The latest fix (commit 1eb3073) rewrites Step 3 (Cell 6 / JSON index 6) to:
- Use `conda install -c conda-forge mujoco=2.3.7` instead of pip source build
- Relax numpy to `>=1.24,<2` (has cp312 wheels)
- Relax opencv to `>=4.7,<5` (has cp312 wheels)  
- Use `--prefer-binary` for gym

This fix has NOT yet been tested on Colab. We are waiting for the user to re-run Step 3 and report the result before proceeding to Wave 2.

Notebook: `LIBERO/notebooks/01-colab-env-setup.ipynb` (git path: `libero/notebooks/01-colab-env-setup.ipynb`)
Current cells: 11 (indices 0-10), Block A only.
Wave 2 will append Block B cells (11-17). Wave 3 will append ENV-03 cells (18-20).
</current_state>

<completed_work>

- Task 1: Wave 1 executor — created notebook cells 0-10 (Block A skeleton) — commit 3fb1d31
- Task 2: Fix round 1 — libosmesa6 apt error, LIBERO_PKG wrong path, Cell 10 text — commit ef50d7f
- Task 3: Fix round 2 — mujoco --no-build-isolation + cmake, LIBERO clone fallback, huggingface_hub pin revert, flash-attn timing comment — commit 05c9450
- Task 4: Fix round 3 — rewrote Step 3 (Cell 6) entirely: conda for mujoco, relaxed numpy/opencv/gym pins, added Python version print and mujoco import guard — commit 1eb3073
</completed_work>

<remaining_work>

- Task 5: Await Colab test of Step 3 (conda mujoco). If pass: proceed to Wave 2. If fail: diagnose and fix round 4.
- Task 6: Wave 2 — spawn gsd-executor for plan 01-02 (Block B: EGL bootstrap, LIBERO config.yaml, sys.path setup, ENV-01/02 verification cells 11-17)
- Task 7: Wave 3 — spawn gsd-executor for plan 01-03 (ENV-03: OpenVLA-OFT load + 7D action check + summary table, cells 18-20)
- Task 8: Phase 1 verification (gsd-verifier) + ROADMAP/STATE mark complete
</remaining_work>

<decisions_made>

- conda-forge for mujoco 2.3.7: pip source build fails on Python 3.12 (distutils gone + no cp312 wheel); conda-forge has pre-compiled binaries for all Python versions
- numpy relaxed to >=1.24,<2: 1.22.4 has no cp311/cp312 wheel; 1.24+ does; LIBERO-compatible
- opencv relaxed to >=4.7,<5: 4.6.0.66 has no cp312 wheel; 4.7+ does
- gym relaxed to >=0.21,<=0.26 --prefer-binary: avoids source build on modern Python
- LIBERO auto-clone to /content/libero: Drive doesn't sync nested git; clone from GitHub as fallback; updates LIBERO_PKG, LIBERO_ROOT, BDDL_FILE in-kernel
- huggingface_hub kept unpinned: tokenizers==0.19.1 requires <1.0.0; any >=1.2.0 pin causes ResolutionImpossible
- Wave 2 gated on Block A Colab pass: Block B verification cells depend on Block A installs succeeding
</decisions_made>

<blockers>

- Human action required: Re-run Step 3 (Cell 6) on Colab T4 GPU, confirm conda install of mujoco=2.3.7 succeeds. Then run Steps 4-6 and report any remaining failures before Wave 2 proceeds.
</blockers>

## Required Reading (in order)

1. `LIBERO/notebooks/01-colab-env-setup.ipynb` — the primary artifact; current state is 11 cells (0-10), Block A only
2. `.planning/phases/01-colab-environment-setup/01-02-PLAN.md` — Wave 2 plan (Block B cells)
3. `.planning/phases/01-colab-environment-setup/01-03-PLAN.md` — Wave 3 plan (ENV-03)

## Critical Anti-Patterns (do NOT repeat these)

- **ADDING-VERSION-CONSTRAINTS-FOR-ADVISORY-WARNINGS**: Adding `huggingface_hub>=1.2.0` to address a non-blocking advisory conflict turned it into a hard pip `ResolutionImpossible`. Never add lower/upper bounds to packages already resolving unless they fail to import. → Before adding any version constraint, verify: (a) the current install actually fails at runtime, (b) the constraint doesn't conflict with another pinned package's requirements.
- **ASSUMING-PIP-PINS-WORK-ON-NEW-PYTHON**: LIBERO's requirements.txt was written for Python 3.8-3.10. Don't assume any exact pin installs cleanly on Colab without checking PyPI wheel availability for the actual Python version. → Run `!python --version` at the start of every Block A troubleshooting session; check `https://pypi.org/project/<pkg>/#files` for cp3xx wheels before using exact pins.

## Infrastructure State

- Colab runtime: T4 GPU (user's tier); Python version unknown but likely 3.11 or 3.12 based on source build failures
- Google Drive: mounted at `/content/drive/MyDrive/SoARM-Research`; LIBERO/ nested git dir present but setup.py missing (Drive doesn't sync nested git)
- Notebook kernel: must be restarted after Block A install (Cell 10 STOP cell instructs this)
- flash-attn (Step 6): compiles CUDA kernels — 30-60+ min on T4, ~5-10 min on A100. User previously ran for 55 min without completion — this is expected on T4.

<context>
We're in a tight iterative loop: write cell → user tests on Colab → failure surfaces → diagnose → fix → repeat. The root cause of all Block A failures has been the same: LIBERO's version pins predate Python 3.11/3.12. The pattern is now understood. The latest fix (conda for mujoco + relaxed numpy/opencv) should close the last source-build failure. If Step 3 passes, Steps 4-6 should be clean (LIBERO clone fallback handles Step 4; huggingface_hub fix handles Step 5; flash-attn just takes a long time on T4). Once Block A fully passes a Colab run, spawn Wave 2 immediately.

Cell numbering note: when communicating with the user, use Step numbers ("Step 3 of 6") not JSON cell indices ("Cell 6"). The user counts differently (they called JSON cell 6 "cell 5"). The Step N of 6 labels in the cell comments are the reliable reference.
</context>

<next_action>
Start with: Ask user "Did Step 3 (conda mujoco install) pass on Colab?" — if yes, ask them to run Steps 4-6 and confirm all pass. If any step fails, get the error output (without -q so errors are visible) and diagnose. Once all 6 steps pass: spawn gsd-executor for plan 01-02 (Wave 2).
</next_action>
