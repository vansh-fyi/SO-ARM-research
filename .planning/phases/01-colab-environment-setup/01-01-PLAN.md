---
phase: 01-colab-environment-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - LIBERO/notebooks/01-colab-env-setup.ipynb
autonomous: false
requirements:
  - ENV-01
must_haves:
  truths:
    - Running cells 0-9 top-to-bottom on a fresh Colab runtime completes without pip resolver errors
    - Block A install order is correct — apt before pip, torch before flash-attn, transformers fork installed last (never from PyPI)
    - A markdown restart cell clearly tells the researcher to stop and restart runtime before proceeding to Block B
    - GPU assertion cell checks torch.cuda.get_device_name(0) and warns loudly if not A100 (per D-06)
    - All UPPER_SNAKE_CASE path constants (REPO_ROOT, LIBERO_ROOT) are in a single config cell at the top of Block A
  artifacts:
    - LIBERO/notebooks/01-colab-env-setup.ipynb (exists, has cells 0-9)
    - LIBERO/notebooks/outputs/ (directory created by notebook)
  key_links:
    - transformers fork (git+https://github.com/moojink/transformers-openvla-oft.git) installed LAST — pip resolver cannot downgrade to PyPI version
    - flash-attn installed AFTER torch==2.2.0 — compiles against installed torch CUDA headers
    - apt-get EGL system packages installed BEFORE pip mujoco — C EGL libraries must exist when mujoco Python extension builds
---

## Phase Goal

**As a** VLA researcher, **I want to** run a single Colab notebook to install the full SOARM+LIBERO+OpenVLA-OFT stack, **so that** I can verify my environment is correctly configured before running inference.

<objective>
Create the notebook file with Block A: all install cells (apt + pip) in the correct dependency order, a GPU assertion cell (D-06), a REPO_ROOT config cell, and a clearly-marked runtime restart cell. After completing this plan, a researcher can open the notebook, run cells 0-9, and restart the runtime to trigger Block B.

Purpose: ENV-01 requires conflict-free installation. The install ordering is the most fragile part of the entire pipeline — eight ordering constraints must be respected or subsequent verification cells will fail silently.

Output: LIBERO/notebooks/01-colab-env-setup.ipynb with cells 0-9; LIBERO/notebooks/outputs/ directory placeholder.
</objective>

<execution_context>
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/workflows/execute-plan.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.planning/PROJECT.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.planning/ROADMAP.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.planning/phases/01-colab-environment-setup/01-CONTEXT.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.planning/phases/01-colab-environment-setup/01-RESEARCH.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.planning/phases/01-colab-environment-setup/01-PATTERNS.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/LIBERO/requirements.txt
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/explorations/create_scene.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create notebook skeleton and Block A install cells (cells 0-9)</name>
  <files>LIBERO/notebooks/01-colab-env-setup.ipynb</files>

  <read_first>
    - LIBERO/requirements.txt — all pinned LIBERO deps that the install cells must satisfy; every version listed there that is NOT overridden by decisions D-03/D-05 must be preserved exactly
    - .planning/phases/01-colab-environment-setup/01-RESEARCH.md §Standard Stack and §Installation Order — exact pip install command structure and critical ordering rationale
    - .planning/phases/01-colab-environment-setup/01-PATTERNS.md §Cell Block A — pip/apt Install Cells — full install sequence with version pins and ordering constraints table
    - .planning/phases/01-colab-environment-setup/01-CONTEXT.md §Decisions — D-01 (single notebook), D-02 (path), D-05 (bf16, no bitsandbytes), D-06 (GPU assertion cell)
    - explorations/create_scene.py lines 13-14 — sys.path insert pattern to replicate in config cell (LIBERO_PKG)
    - LIBERO/notebooks/quick_walkthrough.ipynb — existing notebook structure to understand .ipynb cell format
  </read_first>

  <action>
  Create LIBERO/notebooks/01-colab-env-setup.ipynb as a valid Jupyter notebook (nbformat 4.4, Python 3 kernel) with exactly the following cells in order. All path constants must use UPPER_SNAKE_CASE per CLAUDE.md convention.

  Cell 0 [markdown]: Title block — "# Phase 1: Colab Environment Setup" with the three ENV requirements listed as a checklist (- [ ] ENV-01, - [ ] ENV-02, - [ ] ENV-03), and a note that Block A must run once before restart.

  Cell 1 [code] — REPO_ROOT config (per D-01 single restart-aware notebook): Set REPO_ROOT = "/content/drive/MyDrive/SoARM-Research" as a user-editable constant. Derive LIBERO_ROOT = f"{REPO_ROOT}/LIBERO/libero/libero", LIBERO_PKG = f"{REPO_ROOT}/LIBERO/libero", OUT_DIR = f"{REPO_ROOT}/LIBERO/notebooks/outputs", and BDDL_FILE pointing to the libero_spatial bowl task. Print all paths so the researcher can confirm mount point before continuing.

  Cell 2 [code] — GPU assertion (per D-06): Import torch, assert torch.cuda.is_available(), get torch.cuda.get_device_name(0) and total VRAM via torch.cuda.get_device_properties(0).total_memory / 1e9. If "A100" not in gpu_name, print a loud WARNING block (not raise — researcher may want to proceed on T4 for install-only). Print "GPU: {name}, VRAM: {vram:.1f} GB" and "A100 confirmed. Proceeding." or the WARNING text. This is the D-06 check.

  Cell 3 [markdown]: "## BLOCK A: Install (run all cells in this block, then restart runtime)"

  Cell 4 [code] — apt-get EGL system packages: Run apt-get install -y -q for libglfw3, libglew-dev, libosmesa6-dev, libgles2, libglvnd0, libegl-dev, libegl1, libgl1-mesa-glx. These must be first per ordering constraint 1 from PATTERNS.md.

  Cell 5 [code] — PyTorch install: pip install torch==2.2.0 torchvision==0.17.0 torchaudio==2.2.0 with --index-url https://download.pytorch.org/whl/cu121. This must run before flash-attn per ordering constraint 2.

  Cell 6 [code] — MuJoCo + simulation stack: pip install mujoco==2.3.7 gym==0.25.2, then pip install robosuite==1.4.0 bddl==1.0.1 easydict==1.9 cloudpickle==2.1.0 einops==0.4.1 numpy==1.22.4 opencv-python==4.6.0.66 imageio[ffmpeg]. Exact versions from LIBERO/requirements.txt and RESEARCH.md Standard Stack. Add -q flag.

  Cell 7 [code] — LIBERO editable install: pip install -e {LIBERO_PKG}. Note: LIBERO_PKG variable is used here so the researcher's REPO_ROOT setting flows through. Add a comment "# editable install — LIBERO package changes are live without reinstall".

  Cell 8 [code] — OpenVLA-OFT supporting packages: pip install timm==0.9.10 tokenizers==0.19.1 sentencepiece==0.1.99 peft==0.11.1 accelerate huggingface_hub. Then pip install git+https://github.com/moojink/transformers-openvla-oft.git. The git fork must be installed LAST in this cell — after all other packages — per D-03 and PATTERNS.md ordering constraint 3. Do NOT install transformers from PyPI.

  Cell 9 [code] — flash-attn (last install, per ordering constraint 8 from PATTERNS.md): First pip install packaging ninja -q, then pip install "flash-attn==2.5.5" --no-build-isolation -q. Add a comment "# This cell takes ~10 min to compile CUDA kernels. Do not interrupt."

  Cell 10 [markdown]: "## *** STOP — Restart runtime now (Runtime > Restart session), then continue from BLOCK B below ***". Make this visually prominent with a separator line above and below.

  The notebook must NOT include any MUJOCO_GL setting, any import of mujoco/robosuite/libero, or any verification logic — those belong in Plan 02. Block A is install-only. Create LIBERO/notebooks/outputs/ as a directory by including an `os.makedirs` call in Cell 1 alongside the path constant definitions.
  </action>

  <verify>
    <automated>python3 -c "import json; nb = json.load(open('LIBERO/notebooks/01-colab-env-setup.ipynb')); cells = nb['cells']; assert len(cells) == 11, f'Expected 11 cells (0-10), got {len(cells)}'; assert all(c['cell_type'] in ('markdown','code') for c in cells), 'All cells must be markdown or code'; torch_cell = cells[5]['source']; assert 'torch==2.2.0' in ''.join(torch_cell), 'torch version pin missing'; flash_cell = cells[9]['source']; assert 'flash-attn==2.5.5' in ''.join(flash_cell), 'flash-attn version pin missing'; fork_cell = cells[8]['source']; assert 'moojink/transformers-openvla-oft' in ''.join(fork_cell), 'transformers fork URL missing'; print('Notebook structure: OK')"</automated>
  </verify>

  <acceptance_criteria>
  - Notebook file exists at LIBERO/notebooks/01-colab-env-setup.ipynb and is valid JSON (nbformat 4)
  - Cell 1 contains REPO_ROOT, LIBERO_ROOT, LIBERO_PKG, OUT_DIR, BDDL_FILE as UPPER_SNAKE_CASE constants
  - Cell 2 contains torch.cuda.get_device_name(0) check and "A100" string comparison (D-06)
  - Cell 5 installs torch==2.2.0 with --index-url https://download.pytorch.org/whl/cu121
  - Cell 6 installs mujoco==2.3.7 and robosuite==1.4.0 (exact versions from LIBERO/requirements.txt)
  - Cell 8 installs git+https://github.com/moojink/transformers-openvla-oft.git — no PyPI transformers pin
  - Cell 9 installs flash-attn==2.5.5 with --no-build-isolation and has comment about 10-min compile time
  - Cell 10 is a markdown cell with "STOP" and "Restart runtime" instruction
  - No cell in 0-10 contains import mujoco, import robosuite, import libero, or MUJOCO_GL
  - LIBERO/notebooks/outputs/ directory creation is present in Cell 1 via os.makedirs
  </acceptance_criteria>

  <done>
  Notebook exists with 11 cells (0-10), passes the automated JSON structure check, contains all required install commands in the correct dependency order, and has the restart stop cell as cell 10. No MuJoCo imports appear in Block A.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Colab runtime → PyPI/GitHub | pip installs pull from external registries; packages run with full runtime privileges |
| Researcher → Notebook | Researcher edits REPO_ROOT config cell; incorrect paths cause FileNotFoundError at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | git+https://github.com/moojink/transformers-openvla-oft.git | medium | mitigate | Install from named branch/tag in URL; pin commit SHA in comment alongside install command for reproducibility |
| T-01-02 | Tampering | robosuite, mujoco, bddl (SUS packages from Package Legitimacy Audit) | medium | mitigate | All approved by legitimacy audit (ARISE-Initiative, Google DeepMind, StanfordVL); install with exact version pins; no version ranges |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | Package Legitimacy Audit completed in RESEARCH.md; all SUS packages approved; no SLOP packages; install exact pinned versions only |
| T-01-03 | Elevation of Privilege | os.makedirs /usr/share/glvnd/egl_vendor.d/ write | low | accept | Required for EGL GPU rendering; JSON config file contains no executable content; Colab runtime already has root-equivalent access |
</threat_model>

<verification>
Run the automated cell structure check from the verify block. Manually confirm no MuJoCo imports appear in cells 0-10 by grepping:

grep -c "import mujoco\|import robosuite\|import libero\|MUJOCO_GL" in the notebook source strings should return 0 for cells 0-10.
</verification>

<success_criteria>
A researcher can open LIBERO/notebooks/01-colab-env-setup.ipynb on a fresh Colab A100 runtime, run cells 0-10 sequentially, and the runtime restarts cleanly. After restart, the install environment is ready for Block B verification.
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Description |
|----------|------|-------------|
| Notebook (Block A) | LIBERO/notebooks/01-colab-env-setup.ipynb | Jupyter notebook with cells 0-10: config, GPU check, install block, restart cell |
| Output directory | LIBERO/notebooks/outputs/ | Created by Cell 1; receives libero_render_check.png in Plan 02 |

<output>
Create .planning/phases/01-colab-environment-setup/01-01-SUMMARY.md when done
</output>
