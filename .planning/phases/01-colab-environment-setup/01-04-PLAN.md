---
phase: 01-colab-environment-setup
plan: 04
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - libero/notebooks/01-colab-env-setup.ipynb
autonomous: true
gap_closure: true
requirements: [ENV-01, ENV-02, ENV-03]

must_haves:
  truths:
    - "On a fresh Colab runtime, Block A ends with a 'numpy ABI gate: PASS' line printed BEFORE the restart instruction, and the gate raises RuntimeError (telling the user NOT to restart) when the on-disk numpy is binary-incoherent"
    - "After restart, the ENV-02 cell prints 'ENV-02: PASS' with a non-black LIBERO render — the gym -> gym.utils.seeding -> numpy.random.mtrand import chain no longer raises the numpy dtype-size ValueError (UAT Test 3 truth)"
    - "After restart, the ENV-03 cell prints 'ENV-03: PASS' with a 7-D per-step action — transformers/utils/generic.py never executes its jax.numpy or tensorflow imports because USE_FLAX/USE_TF backend guards are set before any transformers import (UAT Test 4 truth)"
    - "The ENV-01 cell verifies numpy==1.26.4 and runs an in-kernel numpy.random ABI canary, and ENV-01 prints FAIL when any version row mismatches (not only on pip conflicts)"
  artifacts:
    - "libero/notebooks/01-colab-env-setup.ipynb — 22 cells total: one new Block A final-gate code cell inserted between the flash-attn cell and the STOP restart markdown"
  key_links:
    - "USE_TORCH/USE_TF/USE_FLAX env vars are set in the EGL bootstrap cell AND re-set at the top of the ENV-03 cell, textually before any transformers import"
    - "The numpy ABI gate cell is the LAST pip-invoking cell in Block A — no pip operation exists between it and the STOP restart markdown"
---

<objective>
Close the two UAT blocker gaps from Phase 01 (Tests 3 and 4 in 01-UAT.md), which share one root cause: numpy ABI incompatibility between the notebook's `numpy<2` pin and C extensions compiled against numpy 2.x headers.

- **Gap 1 (ENV-02):** `import gym` -> `gym.utils.seeding` -> `numpy.random.mtrand` raises `ValueError: numpy.dtype size changed... Expected 96 from C header, got 88 from PyObject`. The 96-vs-88 signature means a numpy-2.x-compiled extension loaded under a numpy 1.26.4 core — the on-disk numpy install ends Block A in a mixed state. The Cell 6 pin runs at Step 3 of 6, and Steps 4-6 run more pip operations after it with no final re-verification.
- **Gap 2 (ENV-03):** `prismatic` -> `transformers/utils/generic.py` line 42 module-level `import jax.numpy` -> Colab system jaxlib (numpy-2.x-compiled) -> same ValueError, followed by an IPython ultratb infinite loop (a symptom that disappears once the crash is eliminated).

Fix strategy (honors established facts from UAT: mujoco 3.3.2 + mj_kinematics shim, LIBERO clone fallback at /content/libero, flash-attn uninstalled, torch 2.2.0 pin retained per openvla-oft — so `numpy<2` stays):
1. Make numpy enforcement the FINAL Block A step: purge numpy completely (uninstall loop + removal of leftover `numpy*` directories in site-packages, which is the direct repair for the observed mixed install), cleanly reinstall exactly `numpy==1.26.4` with `--no-cache-dir`, then run a fresh-subprocess ABI probe that replicates the exact ENV-02 crash path (`import numpy.random`, `numpy.random.rand`, `import gym`, construct a `gym.spaces.Box`). A fresh subprocess reads the same on-disk state a post-restart kernel will read, so a passing probe guarantees the post-restart import chain survives — the "restart, then crash" loop is broken before the restart.
2. Sever the jax/tensorflow import path inside transformers via its official backend env guards (`USE_TORCH=1`, `USE_TF=0`, `USE_FLAX=0`, read by `transformers/utils/import_utils.py` in the 4.40.1-based fork per D-03) — set in the EGL bootstrap cell (first post-restart cell) and defensively re-set at the top of the ENV-03 cell. No uninstall of Colab system jax/flax/tensorflow needed.
3. Extend ENV-01 (per D-07 structured PASS/FAIL) with a numpy==1.26.4 row and an in-kernel numpy.random ABI canary so a broken environment is diagnosed at the first Block B verification cell, not deep inside ENV-02.

Purpose: ENV-02 and ENV-03 print PASS on a fresh Colab runtime following the notebook's Block A -> restart -> Block B flow.
Output: Modified `libero/notebooks/01-colab-env-setup.ipynb` (22 cells; one new gate cell, three edited cells, two edited markdown cells).
</objective>

## Phase Goal

**As a** researcher, **I want to** run the setup notebook top-to-bottom on a fresh Colab A100 runtime and see ENV-01, ENV-02, and ENV-03 all print PASS, **so that** the language-to-embodied-action pipeline has a verified working environment to build on.

> Derived from the ROADMAP Phase 1 goal for this gap-closure run (phase already executed; goal text predates user-story formatting). Vertical slices: Task 1 restores the ENV-02 user-visible outcome, Task 2 restores the ENV-03 user-visible outcome, Task 3 refines diagnosis of both.

## Multi-Source Coverage Audit

| Source | Item | Status |
|--------|------|--------|
| GOAL (ROADMAP Phase 1) | Deps install conflict-free on fresh runtime | COVERED — Task 1 (final numpy gate makes the install outcome deterministic and verified pre-restart) |
| GOAL | EGL headless render produces non-black frames | COVERED — Task 1 (removes the crash blocking ENV-02) |
| GOAL | OpenVLA-OFT loads and returns 7-D action | COVERED — Task 2 (removes the jax crash blocking ENV-03) |
| REQ ENV-01 | Install without conflicts, version check | COVERED — Task 3 (numpy row + ABI canary + version-mismatch now fails ENV-01) |
| REQ ENV-02 | Non-black render, MUJOCO_GL=egl ordering | COVERED — Task 1 (ordering untouched; crash path repaired) |
| REQ ENV-03 | VLA loads on GPU, 7-D action | COVERED — Task 2 |
| RESEARCH | EGL ICD before MUJOCO_GL, config.yaml before import libero, fork-last install order | COVERED — unchanged; all edits preserve ordering constraints 1-8 from 01-PATTERNS.md |
| CONTEXT D-01 | Single notebook, restart-aware ordering | COVERED — gate cell sits last in Block A, before the STOP markdown |
| CONTEXT D-03 | transformers 4.40.1 fork | COVERED — guard semantics target 4.40.1 `import_utils.py`; fork untouched |
| CONTEXT D-05 | A100 bf16, no bitsandbytes | COVERED — untouched |
| CONTEXT D-07 | Structured PASS/FAIL per ENV check | COVERED — gate and canary use the same PASS/FAIL print convention |
| CONTEXT deferred (4-bit/T4, Drive persistence, pi0) | Out of scope | EXCLUDED — no task touches these |

## Artifacts This Phase Produces

- New notebook code cell: **Block A final gate — numpy ABI enforcement** (inserted after the flash-attn cell, before the STOP markdown), containing identifiers `PURELIB`, `NUMPY_PIN`, `_ABI_PROBE`
- Env guard assignments `os.environ["USE_TORCH"]`, `os.environ["USE_TF"]`, `os.environ["USE_FLAX"]` in the EGL bootstrap cell and at the top of the ENV-03 cell
- `EXPECTED["numpy"] = "1.26.4"` row in the ENV-01 cell
- In-kernel **numpy ABI canary** block in the ENV-01 cell (prints `numpy ABI canary: OK` / `numpy ABI canary: FAIL`)
- Gate PASS line: `numpy ABI gate: PASS — numpy 1.26.4 coherent on disk; safe to restart runtime`
- Updated Block A header markdown (ordering rule 4: numpy gate last) and Block B header markdown (descriptive cell names, guard note)

<execution_context>
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/workflows/execute-plan.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-colab-environment-setup/01-UAT.md
@.planning/phases/01-colab-environment-setup/01-RESEARCH.md
@.planning/phases/01-colab-environment-setup/01-02-SUMMARY.md
@.planning/phases/01-colab-environment-setup/01-03-SUMMARY.md
@libero/notebooks/01-colab-env-setup.ipynb

**Git path casing (macOS trap from 01-02):** git tracks the notebook as `libero/notebooks/01-colab-env-setup.ipynb` (lowercase `libero/`). Always `git add libero/notebooks/...` — `git add LIBERO/notebooks/...` silently stages nothing on this case-insensitive filesystem.

**Current notebook state (21 cells, indices 0-20):** Cell 4-9 = Block A Steps 1-6 (apt EGL, torch, sim stack + numpy pin, LIBERO editable, OFT + fork, flash-attn). Cell 10 = STOP restart markdown. Cell 11 = Block B header markdown (its numeric cell references are already stale). Cell 12 = EGL bootstrap. Cell 15 = ENV-01. Cell 16 = ENV-02 (contains the numba stub shim — keep it; it is orthogonal to this fix). Cell 19 = ENV-03 (prismatic guard + model load). Locate cells by their unique leading comment/heading strings, not by index — Task 1 inserts a cell and shifts all subsequent indices by +1.

**Notebook editing pattern:** edit the `.ipynb` JSON with a Python script (json.load / json.dump with `indent=1`, preserving `nbformat: 4` / `nbformat_minor: 5` and each cell's existing `id`/`metadata`; new cell needs a unique `id`, `metadata: {}`, `outputs: []`, `execution_count: null`, and `source` as a list of newline-terminated strings). Strip `outputs` only on cells you modify if convenient; do not reformat untouched cells.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Block A final numpy ABI gate cell and fix stale Step 3 restart hint (Gap 1 / ENV-02, per D-01 + D-07)</name>
  <files>libero/notebooks/01-colab-env-setup.ipynb</files>
  <read_first>
    - libero/notebooks/01-colab-env-setup.ipynb (Cells 3, 6, 9, 10 — current Block A header, Step 3 sim stack, flash-attn cell, STOP markdown)
    - .planning/phases/01-colab-environment-setup/01-UAT.md (## Gaps + Cell 13 crash chain in the Cell Execution Log)
  </read_first>
  <action>
    Three edits, all in Block A:

    1. **Insert one new code cell** immediately AFTER the cell whose source starts with `# Step 6 of 6 — flash-attn` and immediately BEFORE the markdown cell containing `STOP — Restart runtime now`. Leading comment line: `# Block A final gate — numpy ABI enforcement (run LAST, after Steps 1-6, before restart)`. The cell (per D-07 PASS/FAIL convention) must:
       - Explain in a short comment block WHY it exists: Steps 4-6 run pip operations after the Step 3 numpy pin; UAT observed the on-disk numpy ending Block A in a mixed state (numpy-2.x-compiled `random/mtrand` extension under a 1.26.4 core -> dtype-size ValueError at ENV-02's gym import), so numpy must be purged, exact-pinned, and ABI-probed as the final Block A operation.
       - Define `PURELIB` from `sysconfig.get_paths()["purelib"]` and `NUMPY_PIN = "numpy==1.26.4"` (exact pin: deterministic, cp312 wheel exists, matches the ENV-01 expected row Task 3 adds).
       - **Purge loop:** while `importlib.metadata.version("numpy")` resolves (wrap in try/except `PackageNotFoundError`; max 3 iterations), run `subprocess.run([sys.executable, "-m", "pip", "uninstall", "-y", "numpy"], capture_output=True)`.
       - **Leftover sweep (the direct repair for the mixed install):** using `glob` over `PURELIB`, `shutil.rmtree` (files: `os.remove`) any remaining entries matching exactly `numpy`, `numpy-*.dist-info`, `numpy.libs` — print each removed path with the project `Saved →`-style arrow convention adapted as `Removed → {path}`. Restrict removal to those three glob patterns only.
       - **Clean reinstall:** `subprocess.run([sys.executable, "-m", "pip", "install", NUMPY_PIN, "--no-cache-dir", "-q"], ...)`; raise RuntimeError on nonzero returncode with captured stderr.
       - **Fresh-subprocess ABI probe:** define `_ABI_PROBE` as a Python source string executed via `subprocess.run([sys.executable, "-c", _ABI_PROBE], capture_output=True, text=True)`. The probe must: import numpy; print `numpy.__version__` and `numpy.__file__`; `import numpy.random`; call `numpy.random.rand(3)` and `numpy.random.default_rng().random(3)`; print `numpy.random.mtrand.__file__`; then `import gym` and `import gym.spaces` and construct `gym.spaces.Box(low=-1.0, high=1.0, shape=(1,), dtype=numpy.float32)` — this walks gym.utils.seeding's `RandomNumberGenerator(np.random.Generator)` definition, the exact ENV-02 crash path from the UAT log; finally print a probe-ok sentinel line. A fresh subprocess loads the on-disk numpy (not the kernel's cached 2.x from Cell 2's torch import), so it sees exactly what the post-restart kernel will see. The probe must NOT import mujoco, robosuite, or libero (MUJOCO_GL is not configured pre-restart).
       - **Result handling:** if the probe subprocess exits nonzero, print its full stdout and stderr, then raise RuntimeError telling the user: do NOT restart; the environment is incoherent; re-run this gate cell and report the diagnostic output. If it exits zero, print the probe stdout followed by `numpy ABI gate: PASS — numpy 1.26.4 coherent on disk; safe to restart runtime`.
    2. **Fix the misleading trailing print in the Step 3 cell** (source starts with `# Step 3 of 6 — MuJoCo + simulation stack`): the final print currently instructs an immediate restart even though Steps 4-6 follow. Replace that print's message with `✓ simulation stack installed — continue with Step 4 (LIBERO editable install)`. Also append one sentence to the existing numpy-pin comment in that cell stating that final authoritative numpy enforcement happens in the Block A gate cell after Step 6. Leave everything else in the cell (mujoco 3.3.2 pin, numba pin, opencv bound, subprocess version check) unchanged — these are established facts from UAT.
    3. **Update the Block A header markdown cell** (contains `## BLOCK A: Install`): add ordering rule 4: the numpy ABI gate must run last, after all other installs, and must print PASS before restarting.

    Preserve nbformat 4/minor 5, existing cell ids/metadata; give the new cell a fresh unique `id`, `metadata: {}`, `outputs: []`, `execution_count: null`. Resulting notebook: 22 cells.
  </action>
  <acceptance_criteria>
    - `python3 -c "import json; nb=json.load(open('libero/notebooks/01-colab-env-setup.ipynb')); assert len(nb['cells'])==22, len(nb['cells'])"` passes
    - Exactly one cell contains both `numpy ABI gate` and `--no-cache-dir`; that cell also contains `NUMPY_PIN`, `numpy==1.26.4`, `_ABI_PROBE`, `sysconfig.get_paths`, `numpy.random.rand(3)`, `mtrand.__file__`, `gym.spaces.Box`, and `numpy ABI gate: PASS`
    - The gate cell's index is exactly one greater than the flash-attn cell's index and exactly one less than the index of the markdown cell containing the restart-stop heading
    - Structural region check passes: among Block A cells strictly between the gate cell and the restart markdown cell there are zero code cells (gate is the last executable Block A cell), and no cell after the gate and before the restart markdown contains the substring `pip install`
    - The Step 3 cell's source contains `continue with Step 4` and no longer ends by instructing a restart
    - The Block A header markdown lists a 4th ordering rule mentioning the numpy gate
    - `json.load` succeeds and every cell retains `cell_type`, `source`; every code cell has `outputs` and `execution_count` keys (nbformat validity)
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "
import json
nb=json.load(open('libero/notebooks/01-colab-env-setup.ipynb'))
cells=nb['cells']; srcs=[''.join(c['source']) for c in cells]
assert len(cells)==22, f'expected 22 cells, got {len(cells)}'
gate=[i for i,s in enumerate(srcs) if 'numpy ABI gate' in s and '--no-cache-dir' in s]
assert len(gate)==1, gate
g=gate[0]
for tok in ['NUMPY_PIN','numpy==1.26.4','_ABI_PROBE','sysconfig.get_paths','numpy.random.rand(3)','mtrand.__file__','gym.spaces.Box','numpy ABI gate: PASS']:
    assert tok in srcs[g], tok
fa=[i for i,s in enumerate(srcs) if s.startswith('# Step 6 of 6')][0]
stop=[i for i,s in enumerate(srcs) if 'Restart runtime now' in s and cells[i]['cell_type']=='markdown'][0]
assert g==fa+1 and stop==g+1, (fa,g,stop)
s3=[i for i,s in enumerate(srcs) if s.startswith('# Step 3 of 6')][0]
assert 'continue with Step 4' in srcs[s3]
hdr=[i for i,s in enumerate(srcs) if '## BLOCK A: Install' in s][0]
assert '4.' in srcs[hdr] and 'gate' in srcs[hdr].lower()
print('Task 1 OK')"</automated>
  </verify>
  <done>Notebook has 22 cells; the numpy ABI gate is the final executable Block A cell, purging and exact-pinning numpy 1.26.4 and probing the gym/numpy.random crash path in a fresh subprocess before the user is told to restart; Step 3 no longer instructs a premature restart.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Sever the transformers -> jax/tensorflow import path with backend env guards (Gap 2 / ENV-03, per D-03 + D-07)</name>
  <files>libero/notebooks/01-colab-env-setup.ipynb</files>
  <read_first>
    - libero/notebooks/01-colab-env-setup.ipynb (EGL bootstrap cell starting `# EGL bootstrap — FIRST POST-RESTART CELL`; ENV-03 cell starting `# prismatic guard`; Block B header markdown cell containing `## BLOCK B: Verification`)
    - .planning/phases/01-colab-environment-setup/01-UAT.md (Test 4 gap + Cell 14 crash chain: transformers/utils/generic.py line 42 `import jax.numpy` -> jax._src.clusters.k8s_cluster -> np.random.rand(5) -> dtype-size ValueError -> IPython ultratb infinite loop)
  </read_first>
  <action>
    Three edits, all in Block B, run after Task 1 (cell indices have shifted +1 past the new gate cell — locate by content):

    1. **EGL bootstrap cell** (source starts with `# EGL bootstrap — FIRST POST-RESTART CELL`): after the existing Step (b) env-var block (`MUJOCO_GL`, `PYOPENGL_PLATFORM`, `TOKENIZERS_PARALLELISM`), add a Step (b2) block setting `os.environ["USE_TORCH"] = "1"`, `os.environ["USE_TF"] = "0"`, `os.environ["USE_FLAX"] = "0"`. Comment must explain: the transformers 4.40.1 fork's `utils/import_utils.py` reads these (`USE_FLAX` maps to its internal `USE_JAX`; value "0" forces `_flax_available = False`, and `USE_TF="0"` plus `USE_TORCH="1"` forces `_tf_available = False`), which prevents the module-level `import jax.numpy` / tensorflow imports in `transformers/utils/generic.py` — Colab's system jaxlib and tensorflow are compiled against numpy 2.x and crash with the dtype-size ValueError under our numpy 1.26.4 (UAT Test 4). Update the cell's final confirmation print to also mention that transformers backend guards are set (torch only; TF/Flax disabled).
    2. **ENV-03 cell** (source starts with `# prismatic guard`): insert at the very top — before the existing `import importlib, subprocess, sys as _sys` line and therefore textually before any transformers import in the cell — an `import os` plus the same three assignments (`USE_TORCH="1"`, `USE_TF="0"`, `USE_FLAX="0"`), with a one-line comment: defensive re-set so ENV-03 survives out-of-order Block B execution; must precede any transformers import (transformers caches backend availability at first import). Leave the prismatic guard, model load, norm_stats overlay, UNNORM_KEY resolution, and (8,7)-chunk handling untouched — those are confirmed working per STATE decisions.
    3. **Block B header markdown cell** (contains `## BLOCK B: Verification`): its hard-coded cell numbers (Cell 14-18) are stale and now doubly wrong after Task 1's insertion. Replace the numbered bullet list with a descriptive ordered list without cell indices (EGL bootstrap + transformers backend guards FIRST; LIBERO config.yaml bootstrap; sys.path setup; ENV-01 version + ABI canary; ENV-02 render; ENV-03 VLA load), and add one sentence: the EGL bootstrap cell also disables transformers' TF/Flax backends so the ENV-03 cell never imports Colab's numpy-2.x-compiled jax/tensorflow.

    Preserve nbformat structure and untouched cells exactly as in Task 1.
  </action>
  <acceptance_criteria>
    - The EGL bootstrap cell contains all three assignments `os.environ["USE_TORCH"] = "1"`, `os.environ["USE_TF"] = "0"`, `os.environ["USE_FLAX"] = "0"`
    - In the ENV-03 cell, the string `USE_FLAX` appears at a lower character offset than the string `import importlib, subprocess, sys as _sys` (guards precede the prismatic importlib probe — `importlib.import_module('prismatic.training.train_utils')` imports transformers first, so this line, not the later `from transformers import`, is the earliest crash trigger) AND at a lower offset than `from transformers import`
    - The ENV-03 cell contains all three guard assignments
    - The Block B header markdown contains no stale `Cell 14`-style numeric references and mentions the TF/Flax backend guard
    - Notebook still has 22 cells and parses as JSON
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "
import json
nb=json.load(open('libero/notebooks/01-colab-env-setup.ipynb'))
srcs=[''.join(c['source']) for c in nb['cells']]
assert len(srcs)==22
egl=[s for s in srcs if s.startswith('# EGL bootstrap')][0]
env3=[s for s in srcs if s.startswith('# prismatic guard') or 'prismatic guard' in s.splitlines()[0]][0]
for s in (egl, env3):
    for tok in ['USE_TORCH','USE_TF','USE_FLAX']:
        assert tok in s, tok
assert env3.index('USE_FLAX') < env3.index('import importlib, subprocess, sys as _sys'), 'guards must precede the prismatic importlib probe (earliest transformers-importing statement)'
assert env3.index('USE_FLAX') < env3.index('from transformers import'), 'guards must precede transformers import'
hdr=[s for s in srcs if '## BLOCK B: Verification' in s][0]
assert 'Cell 14' not in hdr and 'Cell 18' not in hdr
low=hdr.lower(); assert 'flax' in low or 'guard' in low
print('Task 2 OK')"</automated>
  </verify>
  <done>USE_TORCH/USE_TF/USE_FLAX guards are set in the first post-restart cell and re-set defensively at the top of the ENV-03 cell before any transformers import, so transformers/utils/generic.py never imports Colab's numpy-2.x-compiled jax or tensorflow; Block B docs reflect the flow.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: ENV-01 numpy row + in-kernel numpy.random ABI canary; version mismatches now fail ENV-01 (ENV-01, per D-07)</name>
  <files>libero/notebooks/01-colab-env-setup.ipynb</files>
  <read_first>
    - libero/notebooks/01-colab-env-setup.ipynb (ENV-01 cell — source starts with `# ENV-01: Package version check + pip conflict scan`; note its `EXPECTED` dict, `all_ok` flag, `OUR_PACKAGES` set, and that the final PASS/FAIL print currently keys off `our_conflicts` only)
  </read_first>
  <action>
    Single-cell edit to the ENV-01 cell (run after Tasks 1-2; locate by leading comment):

    1. Add `"numpy": "1.26.4"` to the `EXPECTED` dict so the version table verifies the gate's pin survived the restart.
    2. After the version table and before the pip-check section, add a **numpy ABI canary** block: try `import numpy.random` then `numpy.random.rand(3)` inside try/except. On success print `numpy ABI canary: OK — numpy.random C extensions load cleanly under numpy {numpy.__version__}`. On any exception, print `numpy ABI canary: FAIL — {type name}: {message}`, print guidance to re-run the Block A final numpy gate cell, restart the runtime, and retry Block B, and set `all_ok = False`.
    3. Fix the latent gate bug: the final ENV-01 verdict currently prints PASS whenever `our_conflicts` is empty, ignoring `all_ok` — so a version MISMATCH (e.g. numpy silently back on 2.x) would not fail ENV-01. Change the verdict logic so ENV-01 prints PASS only when `all_ok` is true AND `our_conflicts` is empty; otherwise print FAIL listing the failing condition(s) (version mismatch/missing rows, ABI canary failure, and/or our-package conflicts). Keep the existing OUR_PACKAGES first-token conflict filtering and build-tag stripping exactly as-is (established 01-02/01-03 decisions).
  </action>
  <acceptance_criteria>
    - The ENV-01 cell's `EXPECTED` dict contains a `numpy` key with value `1.26.4`
    - The ENV-01 cell contains `numpy ABI canary` and both outcome literals `numpy ABI canary: OK` and `numpy ABI canary: FAIL`, and the canary failure branch assigns `all_ok = False`
    - The final verdict expression references `all_ok` together with `our_conflicts` (source contains a conditional combining both), and the cell still contains the existing `split('+')[0]` build-tag strip and `OUR_PKG_KEYS` first-token filter
    - Notebook still has 22 cells and parses as JSON
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "
import json, re
nb=json.load(open('libero/notebooks/01-colab-env-setup.ipynb'))
srcs=[''.join(c['source']) for c in nb['cells']]
assert len(srcs)==22
env1=[s for s in srcs if s.startswith('# ENV-01: Package version check')][0]
assert re.search(r'\"numpy\"\s*:\s*\"1\.26\.4\"', env1)
for tok in ['numpy ABI canary: OK','numpy ABI canary: FAIL',\"split('+')[0]\",'OUR_PKG_KEYS']:
    assert tok in env1, tok
assert any(('all_ok' in ln and 'our_conflicts' in ln) for ln in env1.splitlines()), 'verdict must combine all_ok and our_conflicts on one conditional line'
print('Task 3 OK')"</automated>
  </verify>
  <done>ENV-01 verifies numpy 1.26.4, exercises numpy.random in-kernel as the first post-restart ABI signal, and fails loudly on any version mismatch, canary failure, or our-package conflict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Colab runtime -> PyPI | pip install of numpy==1.26.4 in the gate cell |
| Notebook -> site-packages filesystem | gate cell removes leftover numpy directories under sysconfig purelib |
| Notebook -> HuggingFace Hub | unchanged from 01-03 (moojink checkpoint, trust_remote_code) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04-01 | Tampering | Leftover-sweep rmtree in gate cell | low | mitigate | Path computed from `sysconfig.get_paths()["purelib"]`; glob restricted to exactly `numpy`, `numpy-*.dist-info`, `numpy.libs`; each removal printed |
| T-01-04-02 | Denial of Service | USE_TF/USE_FLAX guards disable transformers backends | low | accept | Guards are session-scoped env vars; torch backend explicitly enabled (USE_TORCH=1); no package uninstalled |
| T-01-04-SC | Tampering | pip install (numpy==1.26.4) | low | mitigate | numpy is foundational, exact-pinned, already part of the audited Block A install set (Cell 6 `numpy>=1.24,<2`); no new packages introduced; `--no-cache-dir` avoids stale local artifacts |
</threat_model>

<verification>
Automated (local, per task): the three JSON-assertion commands above; all must print `Task N OK`.

Whole-notebook structural check: `python3 -c "import json; nb=json.load(open('libero/notebooks/01-colab-env-setup.ipynb')); assert nb['nbformat']==4; assert all(('outputs' in c and 'execution_count' in c) for c in nb['cells'] if c['cell_type']=='code'); print('nb OK', len(nb['cells']))"` prints `nb OK 22`.

<human-check>
Runtime confirmation (fresh Colab A100, via re-run UAT / /gsd-verify-work — the executor cannot run Colab):
1. Run Block A top-to-bottom. The final gate cell prints the probe output ending in `numpy ABI gate: PASS — numpy 1.26.4 coherent on disk; safe to restart runtime`. If it raises instead, do NOT restart — capture the diagnostic (numpy file paths, mtrand path) and report.
2. Restart runtime; run Block B in order. ENV-01 prints the numpy 1.26.4 row OK, `numpy ABI canary: OK`, and `ENV-01: PASS`.
3. ENV-02 prints `ENV-02: PASS` with a non-black render and no `numpy.dtype size changed` ValueError (UAT Test 3 truth closed).
4. ENV-03 prints `ENV-03: PASS` with a 7-D per-step action (chunk (8,7) accepted) and no jax/tensorflow import in the traceback path; no IPython ultratb loop (UAT Test 4 truth closed).
</human-check>
</verification>

<success_criteria>
- Both UAT blocker gaps have a concrete repair in the notebook: mixed-numpy purge + exact pin + pre-restart fresh-subprocess probe of the gym/numpy.random path (Gap 1), and transformers backend guards severing the jax/tensorflow import path (Gap 2)
- All three automated task verifications pass locally; notebook is valid nbformat-4 JSON with 22 cells
- ENV-01 now covers numpy and fails on version mismatch — regression detection for this exact failure class
- No executed plan (01-01..01-03) modified; no established fact (mujoco 3.3.2 shim, /content/libero clone fallback, flash-attn fallback, torch 2.2.0 pin) reverted
</success_criteria>

<output>
Create `.planning/phases/01-colab-environment-setup/01-04-SUMMARY.md` when done.
</output>
