---
phase: 05-spatial-awareness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - LIBERO/libero/libero/vla/eval_loop.py
  - LIBERO/libero/libero/vla/interface.py
  - LIBERO/libero/libero/vla/test_eval_loop.py
  - LIBERO/libero/libero/vla/pi0_backend.py
  - LIBERO/libero/libero/vla/test_pi0_backend.py
  - LIBERO/libero/libero/vla/oft_backend.py
autonomous: true
requirements: [SPAT-02]

must_haves:
  truths:
    - "Both camera views (agentview + eye_in_hand) are present in the images dict passed to backend.predict() on every eval_loop episode step (D-01)."
    - "Pi0Backend sends genuinely distinct base/wrist images to the openpi policy server -- no duplication of a single view into both obs keys (D-02)."
    - "OFTBackend packs both views into a single multi-image forward pass using the checkpoint's documented num_images_in_input=2 pattern (D-02)."
  artifacts:
    - LIBERO/libero/libero/vla/eval_loop.py
    - LIBERO/libero/libero/vla/interface.py
    - LIBERO/libero/libero/vla/pi0_backend.py
    - LIBERO/libero/libero/vla/oft_backend.py
    - LIBERO/libero/libero/vla/test_eval_loop.py
    - LIBERO/libero/libero/vla/test_pi0_backend.py
  key_links:
    - "eval_loop.run_episode's images dict -> backend.predict(images, language) -> each backend's internal dual-image consumption (Pi0Backend.predict / OFTBackend.predict)"
    - "VLABackend Protocol docstring (interface.py) documents the images dict contract every backend/eval_loop caller relies on"
---

<objective>
Wire both SOARM camera views (wrist `robot0_eye_in_hand` + overhead `agentview`) all the way from `eval_loop.py`'s observation dict through to each VLA backend's real multi-image inference call, replacing Phase 3's single-eye_in_hand-image baseline (D-01, D-02). `Pi0Backend` currently duplicates the eye_in_hand frame into both openpi obs keys as a documented placeholder; `OFTBackend` currently sends only one image. Both target checkpoints already have native multi-image support at the API level (RESEARCH.md-verified) -- this plan stops duplicating/omitting and starts sending the real second view.

Purpose: Satisfy SPAT-02 ("all camera views are passed as input to the VLA during inference") as a literal behavioral fact, not just "captured/logged" -- per D-01's explicit framing in 05-CONTEXT.md.
Output: `eval_loop.py`'s images dict includes both views; `Pi0Backend.predict()` sends real distinct base/wrist images; `OFTBackend.predict()` concatenates both views' processed pixel_values before calling `predict_action`. Local mock-based tests prove the dict-plumbing and non-duplication behavior; the OFT torch.cat packing is source-verified locally (no local GPU) and flagged for Colab confirmation per VALIDATION.md's Manual-Only Verifications.
</objective>

<execution_context>
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/workflows/execute-plan.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-spatial-awareness/05-CONTEXT.md
@.planning/phases/05-spatial-awareness/05-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire both camera views into eval_loop.py's images dict (D-01)</name>
  <files>LIBERO/libero/libero/vla/eval_loop.py, LIBERO/libero/libero/vla/interface.py, LIBERO/libero/libero/vla/test_eval_loop.py</files>
  <read_first>
    LIBERO/libero/libero/vla/eval_loop.py (current: line 26 camera_name param default "robot0_eye_in_hand_image", line 63 images = {"eye_in_hand": obs[camera_name]}, line 69 vw.append_obs(obs, done, camera_name=camera_name) -- camera_name is ALSO the video-writer's camera, do not repurpose it)
    LIBERO/libero/libero/vla/interface.py (VLABackend.predict() docstring, lines 24-43, currently states "Phase 3 populates only the eye_in_hand key")
    LIBERO/libero/libero/vla/test_eval_loop.py (MockEnv at line 25, _obs dict at line 31; existing 4 tests, mock-based, no MuJoCo/GPU dependency)
  </read_first>
  <behavior>
    - Test: run_episode's images dict passed to backend.predict() contains keys "eye_in_hand" AND "agentview".
    - Test: images["eye_in_hand"] equals obs["robot0_eye_in_hand_image"]; images["agentview"] equals obs["agentview_image"] (values sourced correctly, not swapped).
    - Test: all 4 pre-existing test_eval_loop.py tests still pass unmodified in behavior (only MockEnv's fixture data gains a key).
  </behavior>
  <action>
    Add a new agentview_camera_name: str = "agentview_image" parameter to run_episode() (eval_loop.py, after the existing camera_name param). Change images = {"eye_in_hand": obs[camera_name]} to images = {"eye_in_hand": obs[camera_name], "agentview": obs[agentview_camera_name]}. Do NOT touch the camera_name kwarg's other use as the video-writer camera (line 69) -- it stays exactly as-is. Update run_episode's docstring Args block to document the new parameter. Update interface.py's VLABackend.predict() docstring (currently: "Phase 3 populates only the eye_in_hand key; later phases may add more keys without changing this signature") to state Phase 5 (SPAT-01/SPAT-02, D-01) now populates both "eye_in_hand" and "agentview" keys, matching this plan's actual change -- do not alter the method signature itself. In test_eval_loop.py's MockEnv.__init__, add a second key to self._obs: "agentview_image": np.zeros((4, 4, 3), dtype=np.uint8). Add a new test function test_images_dict_includes_both_camera_views_spatial (name contains "spatial" to match VALIDATION.md's -k spatial filter) using a small RecordingBackend class whose predict(self, images, language) stores the received images dict on self.seen_images and returns np.zeros((8, 7)); call run_episode(env, backend, "pick up the bowl", "/tmp/unused_video"), then assert set(backend.seen_images.keys()) >= {"eye_in_hand", "agentview"} and use np.testing.assert_array_equal to confirm each value matches the corresponding MockEnv._obs entry (not swapped).
  </action>
  <verify>
    <automated>conda run -n libero pytest LIBERO/libero/libero/vla/test_eval_loop.py -x -q -k spatial</automated>
  </verify>
  <acceptance_criteria>
    - conda run -n libero pytest LIBERO/libero/libero/vla/test_eval_loop.py -x -q exits 0 (5/5 tests pass: 4 pre-existing + 1 new)
    - grep -q 'obs\[agentview_camera_name\]' LIBERO/libero/libero/vla/eval_loop.py matches (source assertion: the dict construction sources agentview from a parameter, not a hardcoded literal)
    - interface.py's VLABackend.predict docstring no longer states only the eye_in_hand key is populated
  </acceptance_criteria>
  <done>images dict passed to backend.predict() during run_episode contains both "eye_in_hand" and "agentview" keys sourced from obs["robot0_eye_in_hand_image"] and obs["agentview_image"] respectively; all 5 test_eval_loop.py tests pass; interface.py's docstring reflects the Phase 5 contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pi0Backend sends real distinct base/wrist images, no duplication (D-02)</name>
  <files>LIBERO/libero/libero/vla/pi0_backend.py, LIBERO/libero/libero/vla/test_pi0_backend.py</files>
  <read_first>
    LIBERO/libero/libero/vla/pi0_backend.py (Pi0Backend.predict(), lines 113-138 -- current single-image duplication: img built from images["eye_in_hand"] only; obs = dict with "observation/image" and "observation/wrist_image" both set to the same img variable)
    LIBERO/libero/libero/vla/test_pi0_backend.py (all 6 existing tests call backend.predict({"eye_in_hand": fake_image}, ...) with only ONE key; tests 2-6 will KeyError once predict() reads images["agentview"])
  </read_first>
  <behavior>
    - Test: predict() builds obs["observation/image"] from images["agentview"] (resized 224x224, uint8) and obs["observation/wrist_image"] from images["eye_in_hand"] (resized 224x224, uint8) -- independently, not from a shared variable.
    - Test: when images["eye_in_hand"] and images["agentview"] are different arrays, obs["observation/image"] != obs["observation/wrist_image"] (proves no duplication).
    - Test: all 6 pre-existing tests (init, obs-dict-keys, returns-actions, retry-reconnect, retry-exhausted, fail-fast-dead-port) still pass once their predict() call sites include both required keys.
  </behavior>
  <action>
    Per D-02 and RESEARCH.md Pattern 1 (matching openpi's native libero_policy.py obs-key contract, CITED github.com/Physical-Intelligence/openpi), replace Pi0Backend.predict()'s current single-image duplication with two independently-built images: base_img = image_tools.convert_to_uint8(image_tools.resize_with_pad(np.asarray(images["agentview"]), 224, 224)) assigned to obs["observation/image"], and wrist_img = image_tools.convert_to_uint8(image_tools.resize_with_pad(np.asarray(images["eye_in_hand"]), 224, 224)) assigned to obs["observation/wrist_image"]. Update the method's docstring (currently: "this backend only consumes the eye_in_hand key... both openpi obs image keys below reuse it since no separate wrist camera exists in this phase's scope") to state Phase 5/D-02 now sends genuinely distinct base and wrist views, matching openpi's native dual-image contract, no duplication. In test_pi0_backend.py, widen every backend.predict({"eye_in_hand": fake_image}, ...) call site (tests 2 through 6, ~5 occurrences) to backend.predict({"eye_in_hand": fake_image, "agentview": fake_image}, ...) so the retry/error-path tests aren't broken by the new required "agentview" key (predict() will now raise KeyError on a dict missing it). Update test_predict_builds_obs_dict_with_required_keys's docstring to reflect the dual-source behavior. Add one new test test_predict_uses_distinct_base_and_wrist_images_spatial (name contains "spatial") that calls predict() with two DIFFERENT arrays (e.g. np.zeros((4,4,3), dtype=np.uint8) for eye_in_hand vs np.full((4,4,3), 255, dtype=np.uint8) for agentview) and asserts the captured obs["observation/image"] array-equals the agentview input while obs["observation/wrist_image"] array-equals the eye_in_hand input (not swapped, and not equal to each other via a not np.array_equal(...) check).
  </action>
  <verify>
    <automated>conda run -n libero pytest LIBERO/libero/libero/vla/test_pi0_backend.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - conda run -n libero pytest LIBERO/libero/libero/vla/test_pi0_backend.py -x -q exits 0 (7/7 tests pass: 6 pre-existing + 1 new)
    - grep -q 'images\["agentview"\]' LIBERO/libero/libero/vla/pi0_backend.py matches
    - test_predict_uses_distinct_base_and_wrist_images_spatial explicitly proves non-duplication (asserts the two obs image values differ)
  </acceptance_criteria>
  <done>Pi0Backend.predict() sources observation/image from images["agentview"] and observation/wrist_image from images["eye_in_hand"] independently (no shared duplicated array); all 7 test_pi0_backend.py tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: OFTBackend dual-image pixel_values packing (D-02)</name>
  <files>LIBERO/libero/libero/vla/oft_backend.py</files>
  <read_first>
    LIBERO/libero/libero/vla/oft_backend.py (OFTBackend.predict(), lines 121-150 -- current single-image call: pil_image = Image.fromarray(images["eye_in_hand"]); inputs = self.processor(prompt, pil_image)...; result = self.model.predict_action(**inputs, ...))
  </read_first>
  <action>
    Per D-02 and RESEARCH.md Pattern 1 (CITED github.com/moojink/openvla-oft experiments/robot/openvla_utils.py get_vla_action, treat the exact packing as Colab-verify-before-trust per RESEARCH.md Pitfall 2 / Assumption A1), replace OFTBackend.predict()'s single-image call with the checkpoint's documented multi-image pattern: build primary = Image.fromarray(images["eye_in_hand"]) and extra_views = [Image.fromarray(images["agentview"])]; process primary through self.processor(prompt, primary) into primary_inputs (kept as .to(self.device, dtype=torch.bfloat16) exactly as today); process each of extra_views the same way into a list extra_inputs; concatenate all pixel_values tensors via primary_inputs["pixel_values"] = torch.cat([primary_inputs["pixel_values"]] + [e["pixel_values"] for e in extra_inputs], dim=1); call self.model.predict_action(**primary_inputs, unnorm_key=self.unnorm_key, do_sample=False) (unchanged kwargs otherwise). Keep the existing assert actions.shape == (8, 7) line unchanged. Update the method docstring to document the dual-image packing and flag it as Colab-verification-pending -- this project has no local GPU/torch (torch and transformers are NOT importable in the local libero conda env; conda run -n libero python -c "import torch" raises ModuleNotFoundError, confirmed this session). This matches the existing project convention: OFTBackend's GPU-backed behavior is verified on Colab, not locally (03-RESEARCH.md Environment Availability, 05-VALIDATION.md Manual-Only Verifications table) -- this file has zero local pytest coverage today and that does not change this task. Do NOT attempt to add a local mock-based test for this file -- VALIDATION.md's Wave 0 Gaps list only requires extending test_eval_loop.py and test_pi0_backend.py (Tasks 1-2 above), not a new oft_backend test file.
  </action>
  <verify>
    <automated>grep -q 'images\["agentview"\]' LIBERO/libero/libero/vla/oft_backend.py && grep -q 'torch.cat' LIBERO/libero/libero/vla/oft_backend.py && grep -q 'dim=1' LIBERO/libero/libero/vla/oft_backend.py && echo SOURCE_OK</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'images\["agentview"\]' LIBERO/libero/libero/vla/oft_backend.py matches
    - grep -q 'torch.cat' LIBERO/libero/libero/vla/oft_backend.py matches
    - grep -q 'primary_inputs\["pixel_values"\]' LIBERO/libero/libero/vla/oft_backend.py matches (source assertion the concatenated tensor is fed back into the call, not discarded)
    - `assert actions.shape == (8, 7)` line is unchanged (still present verbatim)
  </acceptance_criteria>
  <done>oft_backend.py's predict() builds primary_inputs from images["eye_in_hand"] and concatenates images["agentview"]'s processed pixel_values along dim=1 before calling predict_action; source-level assertions confirm the pattern is present (local torch-free verification, matching this file's pre-existing zero-local-test convention); real GPU-backed behavior (does the checkpoint actually consume 2 images correctly) remains a documented Colab-only manual verification per VALIDATION.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval_loop.py -> Pi0Backend | obs dict crosses a localhost websocket to a separate serve_policy.py process (per Phase 3's kernel-separation decision, unrelated to this phase's D-06) |
| eval_loop.py -> OFTBackend | images dict crosses into HF trust_remote_code=True model code, in-process |
| VLA action output -> env.step() | model-predicted actions directly drive the simulated robot; not a new boundary this plan, unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | pi0_backend.py obs dict (observation/image vs observation/wrist_image) | medium | mitigate | D-02 assigns agentview->observation/image and eye_in_hand->observation/wrist_image explicitly from two independently-built variables (not a shared one); test_predict_uses_distinct_base_and_wrist_images_spatial asserts the two obs values are NOT equal, preventing a silent view-swap/duplication regression. |
| T-05-02 | Information Disclosure | openpi websocket server binding | low | accept | Unchanged this plan -- already mitigated in Phase 3 (serve_policy.py bound to localhost only, 03-RESEARCH.md); this plan only changes obs payload content, not server binding. |
| T-05-03 | Tampering | oft_backend.py multi-image pixel_values concat order | medium | mitigate | torch.cat order fixed as [eye_in_hand, agentview] per the checkpoint's documented quick-start pattern; source-assertion verify checks torch.cat + dim=1 + pixel_values reassignment presence; Colab-side real-inference spot-check (VALIDATION.md Manual-Only Verifications) is the full behavioral mitigation given no local GPU. |
| T-05-SC | Tampering (supply chain) | N/A -- no new package installs this plan | -- | n/a | Package Legitimacy Audit not applicable (RESEARCH.md: "No new packages are installed by this phase"). |

</threat_model>

<verification>
Run all three vla test commands and confirm green:
- `conda run -n libero pytest LIBERO/libero/libero/vla/test_eval_loop.py -x -q` (5/5, SPAT-02 dict-plumbing)
- `conda run -n libero pytest LIBERO/libero/libero/vla/test_pi0_backend.py -x -q` (7/7, D-02 non-duplication)
- `grep` source assertions on oft_backend.py (SPAT-02/D-02, Colab-manual behavioral confirmation deferred per VALIDATION.md)

Full per-module run for the wave: `conda run -n libero pytest LIBERO/libero/libero/vla -x -q`.
</verification>

<success_criteria>
- eval_loop.py's images dict includes both "eye_in_hand" and "agentview" keys on every episode step (SPAT-02).
- Pi0Backend sends two genuinely distinct images to the policy server (D-02, verified via non-duplication test).
- OFTBackend's source shows the documented dual-image torch.cat packing (D-02, source-verified; GPU behavior Colab-pending).
- All local pytest suites for touched files pass (12/12 across test_eval_loop.py + test_pi0_backend.py).
</success_criteria>

<output>
Create `.planning/phases/05-spatial-awareness/05-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- Modified `run_episode()` signature: new parameter `agentview_camera_name: str = "agentview_image"` (eval_loop.py)
- Modified `images` dict construction in `run_episode()`: now includes `"agentview"` key (eval_loop.py)
- Modified `Pi0Backend.predict()` internal logic: `base_img`/`wrist_img` locals replace the single shared `img` local (pi0_backend.py)
- Modified `OFTBackend.predict()` internal logic: `primary`, `extra_views`, `primary_inputs`, `extra_inputs` locals; `torch.cat(..., dim=1)` packing (oft_backend.py)
- New test functions: `test_images_dict_includes_both_camera_views_spatial` (test_eval_loop.py), `test_predict_uses_distinct_base_and_wrist_images_spatial` (test_pi0_backend.py)
- No new files, no new classes, no new CLI flags.
