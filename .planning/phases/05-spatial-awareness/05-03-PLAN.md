---
phase: 05-spatial-awareness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - LIBERO/libero/libero/envs/predicates/base_predicates.py
  - LIBERO/libero/libero/envs/predicates/__init__.py
  - LIBERO/libero/libero/envs/test_spatial_predicates.py
  - LIBERO/libero/libero/bddl_files/libero_spatial_soarm/put_the_cream_cheese_to_the_right_of_the_bowl.bddl
  - LIBERO/libero/libero/bddl_files/libero_spatial_soarm/put_the_cream_cheese_near_the_bowl.bddl
  - LIBERO/libero/libero/bddl_files/libero_spatial_soarm/put_the_butter_between_the_bowl_and_the_cream_cheese.bddl
autonomous: true
requirements: [SPAT-05]

must_haves:
  truths:
    - "At least 3 BDDL tasks exist whose goal predicates evaluate genuine spatial relations (left/right, near, between) via real position logic, not just plain On/In checks."
    - "Each new spatial predicate correctly distinguishes true and false spatial configurations (unit-level)."
    - "Each new BDDL task's init-region placement satisfies its own goal predicate at reset, for every point in the randomized init range -- no ambiguous boundary cases (D-09)."
    - "Task success/failure scoring uses MuJoCo's privileged sim-state ground truth (D-05), matching every other existing LIBERO task's scoring mechanism."
    - "The new 3rd-object region (butter_1, for the 'between' task) is empirically validated collision-free at rest, not assumed safe (D-06, Pitfall 4)."
  artifacts:
    - LIBERO/libero/libero/envs/predicates/base_predicates.py
    - LIBERO/libero/libero/envs/predicates/__init__.py
    - LIBERO/libero/libero/envs/test_spatial_predicates.py
    - LIBERO/libero/libero/bddl_files/libero_spatial_soarm/put_the_cream_cheese_to_the_right_of_the_bowl.bddl
    - LIBERO/libero/libero/bddl_files/libero_spatial_soarm/put_the_cream_cheese_near_the_bowl.bddl
    - LIBERO/libero/libero/bddl_files/libero_spatial_soarm/put_the_butter_between_the_bowl_and_the_cream_cheese.bddl
  key_links:
    - "BDDL :goal predicate names (leftofx/rightofx/nearto) -> VALIDATE_PREDICATE_FN_DICT -> _eval_predicate() dispatch (libero_tabletop_manipulation.py, unchanged) -> ObjectState.get_geom_state() (ground truth, satisfies D-05 by construction, no new plumbing)"
    - "'Between' goal = (And (RightOfX butter_1 akita_black_bowl_1) (LeftOfX butter_1 cream_cheese_1)) -- decomposition into 2 binary predicates, since _eval_predicate only dispatches 2/3-token tuples (RESEARCH.md-confirmed dispatcher-arity limitation, no ternary predicate added)"
---

<objective>
Add 4 new binary spatial predicate classes (`LeftOfX`, `RightOfX`, `NearTo`, `FarFrom`) to LIBERO's existing predicate registry, and author 3 new BDDL tasks under a new `libero_spatial_soarm/` suite that use them with genuinely spatial `:goal` blocks -- covering left/right, near, and between (D-07). Two tasks reuse Phase 4's already-collision-validated 2-object regions verbatim (zero new embodiment risk); the third ("between") introduces a new 3rd object region that gets its own empirical collision-safety check, following Phase 4's established `sim.data.contact` methodology (D-06, Pitfall 4) rather than assuming safety from coordinates alone.

Purpose: Satisfy SPAT-05 ("at least 3 BDDL tasks use spatial language prompts... and success predicates correctly evaluate spatial conditions") -- LIBERO's own existing "spatial" task suite only disambiguates WHICH object via language, its goal predicates are plain On/In checks (RESEARCH.md-confirmed by reading an existing libero_spatial BDDL file); this plan authors genuinely relation-evaluating predicates, a new capability this project's benchmark needs that doesn't exist upstream.
Output: 4 new predicate classes registered in `VALIDATE_PREDICATE_FN_DICT`; 3 new BDDL task files; unit tests for predicate logic + integration tests proving each task's goal is reset-satisfiable across the full randomized init range and is directionally correct (not trivially always-true).
</objective>

<execution_context>
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/workflows/execute-plan.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-spatial-awareness/05-CONTEXT.md
@.planning/phases/05-spatial-awareness/05-RESEARCH.md
@.planning/phases/04-dataset-collection/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LeftOfX/RightOfX/NearTo/FarFrom predicate classes + registry (D-05, D-07, D-08)</name>
  <files>LIBERO/libero/libero/envs/predicates/base_predicates.py, LIBERO/libero/libero/envs/predicates/__init__.py, LIBERO/libero/libero/envs/test_spatial_predicates.py</files>
  <read_first>
    LIBERO/libero/libero/envs/predicates/base_predicates.py (whole file, 119 lines -- only `from typing import List` imported today, no numpy; On class lines 62-64, Stack class lines 84-90 are the exact analog pattern to follow)
    LIBERO/libero/libero/envs/predicates/__init__.py (whole file, 36 lines -- VALIDATE_PREDICATE_FN_DICT currently has exactly these 10 keys: true, false, in, on, up, printjointstate, open, close, turnon, turnoff)
    LIBERO/libero/libero/envs/object_states/base_object_states.py (ObjectState.get_geom_state, lines 47-52 -- returns {"pos": ..., "quat": ...} via sim.data.body_xpos/body_xquat; check_ontop, lines 78-94, uses a `< 0.03` XY-distance tolerance band -- the precedent for this task's MARGIN constant, D-08)
  </read_first>
  <behavior>
    - LeftOfX(arg1, arg2): True iff (arg2.pos.x - arg1.pos.x) > MARGIN (arg1 is left of arg2 along the table's X axis, beyond a margin).
    - RightOfX(arg1, arg2): True iff (arg1.pos.x - arg2.pos.x) > MARGIN (mirror of LeftOfX).
    - NearTo(arg1, arg2): True iff planar (XY) distance between arg1.pos and arg2.pos is < THRESHOLD.
    - FarFrom(arg1, arg2): True iff planar (XY) distance between arg1.pos and arg2.pos is > THRESHOLD.
    - Cases (stub objects with a fixed get_geom_state()["pos"]): clear left pair -> LeftOfX True, RightOfX False; clear right pair -> mirror; same-X pair (within MARGIN) -> both LeftOfX and RightOfX False (D-09 ambiguity avoided by construction, not by a tie-break); close pair (< THRESHOLD) -> NearTo True, FarFrom False; far pair (> THRESHOLD) -> mirror.
  </behavior>
  <action>
    Add `import numpy as np` to base_predicates.py (there is currently no numpy import in this file -- only `from typing import List`). Add 4 new BinaryAtomic subclasses immediately after the existing Stack class (lines 84-90): LeftOfX with class attribute MARGIN = 0.03 and __call__(self, arg1, arg2) returning (arg2.get_geom_state()["pos"][0] - arg1.get_geom_state()["pos"][0]) > self.MARGIN; RightOfX with MARGIN = 0.03 and the mirror-image comparison (arg1...[0] - arg2...[0]) > self.MARGIN; NearTo with THRESHOLD = 0.22 and __call__ returning np.linalg.norm(arg1.get_geom_state()["pos"][:2] - arg2.get_geom_state()["pos"][:2]) < self.THRESHOLD; FarFrom with THRESHOLD = 0.20 and the mirror comparison using `>`. MARGIN=0.03 matches base_object_states.py::ObjectState.check_ontop's existing `< 0.03` XY-tolerance precedent (D-08). NearTo.THRESHOLD=0.22 and FarFrom.THRESHOLD=0.20 are this plan's D-08 starting defaults -- Task 2 verifies NearTo=0.22 against the actual authored region geometry and documents the exact margin computation; FarFrom=0.20 is not exercised by any of this plan's 3 authored BDDL tasks (kept for benchmark-surface completeness per D-07's "broadest coverage" framing, matching RESEARCH.md Pattern 4's full 4-class recommendation) but is available for future spatial task authoring. Register all 4 in LIBERO/libero/libero/envs/predicates/__init__.py's VALIDATE_PREDICATE_FN_DICT via .update({"leftofx": LeftOfX(), "rightofx": RightOfX(), "nearto": NearTo(), "farfrom": FarFrom()}) placed after the existing dict literal -- confirm none of these 4 keys collide with the 10 pre-existing keys (true/false/in/on/up/printjointstate/open/close/turnon/turnoff). Create LIBERO/libero/libero/envs/test_spatial_predicates.py with a `class _StubObj` helper (`def __init__(self, pos): self._pos = np.array(pos, dtype=float)`; `def get_geom_state(self): return {"pos": self._pos}`) and unit tests covering every case in the behavior block above for all 4 predicate classes, called directly (no BDDL/env construction needed for this task -- that is Tasks 2-3).
  </action>
  <verify>
    <automated>conda run -n libero pytest LIBERO/libero/libero/envs/test_spatial_predicates.py -x -q -k "not integration"</automated>
  </verify>
  <acceptance_criteria>
    - conda run -n libero pytest LIBERO/libero/libero/envs/test_spatial_predicates.py -x -q -k "not integration" exits 0
    - grep -c '"leftofx"\|"rightofx"\|"nearto"\|"farfrom"' LIBERO/libero/libero/envs/predicates/__init__.py returns 4 (all 4 registered exactly once)
    - grep -q 'import numpy as np' LIBERO/libero/libero/envs/predicates/base_predicates.py matches
  </acceptance_criteria>
  <done>LeftOfX/RightOfX/NearTo/FarFrom correctly classify every stub-object True/False case per the behavior block; all 4 registered in VALIDATE_PREDICATE_FN_DICT under lowercase keys with zero collisions against the 10 pre-existing keys.</done>
</task>

<task type="auto">
  <name>Task 2: Left/right + near BDDL tasks -- reused, already-validated regions (D-06 fast path, D-09)</name>
  <files>LIBERO/libero/libero/bddl_files/libero_spatial_soarm/put_the_cream_cheese_to_the_right_of_the_bowl.bddl, LIBERO/libero/libero/bddl_files/libero_spatial_soarm/put_the_cream_cheese_near_the_bowl.bddl, LIBERO/libero/libero/envs/test_spatial_predicates.py</files>
  <read_first>
    LIBERO/libero/libero/bddl_files/libero_goal/put_the_cream_cheese_in_the_bowl.bddl (whole file, 45 lines -- the EXACT source of regions/fixtures/objects/init to copy verbatim: akita_black_bowl_region ranges (-0.21 -0.04 -0.19 -0.02), cream_cheese_region ranges (-0.05 0.02 -0.03 0.04))
    .planning/phases/04-dataset-collection/04-02-SUMMARY.md ("RESOLVED, commit 6ecd160" section -- confirms these exact 2 regions are the already-collision-validated, in-reach layout; do not re-derive new coordinates)
    LIBERO/libero/libero/datasets/test_hdf5_writer.py (import-path preamble, lines 1-45 -- reuse for this task's integration tests)
    LIBERO/libero/libero/envs/env_wrapper.py (ControlEnv.check_success, line 103 -- proxies to self.env._check_success(), the method to call in integration tests)
  </read_first>
  <action>
    Create directory LIBERO/libero/libero/bddl_files/libero_spatial_soarm/. Author put_the_cream_cheese_to_the_right_of_the_bowl.bddl by copying put_the_cream_cheese_in_the_bowl.bddl's :regions/:fixtures/:objects/:init blocks VERBATIM (same 2 already-collision-validated regions -- Phase 4 already proved these placements are collision-free and within the ~0.45m reach envelope, commit 6ecd160), changing :language to "Put the cream cheese to the right of the bowl" and :goal to (And (RightOfX cream_cheese_1 akita_black_bowl_1)). This holds for EVERY point in the randomized init range with margin: cream_cheese_region's minimum x (-0.05) minus akita_black_bowl_region's maximum x (-0.19) = 0.14, which is > MARGIN=0.03 (D-09's no-ambiguity requirement satisfied by construction, not just at the nominal centroid). Author put_the_cream_cheese_near_the_bowl.bddl identically (same 2 regions/init verbatim), :language "Put the cream cheese near the bowl", :goal (And (NearTo cream_cheese_1 akita_black_bowl_1)). Verify NearTo.THRESHOLD=0.22 (set in Task 1) clears the worst-case MAXIMUM distance across both regions' full ranges: farthest-corner distance is cream_cheese's farthest corner from the bowl region (-0.03, 0.04) vs bowl's farthest corner (-0.21, -0.04), giving sqrt(0.18^2 + 0.08^2) = sqrt(0.0388) ~= 0.197m, which clears THRESHOLD=0.22 with a ~0.023m margin (D-09). If the executor's actual measured resting-position distance (post-physics-settle, sampled via env.reset()) differs meaningfully from this static init-range estimate, adjust NearTo.THRESHOLD in base_predicates.py accordingly and re-verify this task's satisfaction (the right-of task uses a different predicate, unaffected). In test_spatial_predicates.py, add integration tests (matching test_hdf5_writer.py's import-path preamble) that construct a real OffScreenRenderEnv(bddl_file_name=<resolved path to each new file, via the same os.path.dirname/normpath convention as collector.py's BDDL_DIR but pointed at libero_spatial_soarm>, robots=["Soarm101"], camera_heights=128, camera_widths=128, has_renderer=False, has_offscreen_renderer=True) for EACH of these 2 new BDDL files, loop env.reset() 20 times, and assert env.check_success() is True on every reset (proves the goal holds across the full randomized init range, not just one sample) -- name these tests to contain "right_of" and "near" respectively for VALIDATION.md's -k filtering. Add a directional-correctness check per file using the already-registered dispatcher (import eval_predicate_fn from libero.envs.predicates): call eval_predicate_fn("rightofx", env.env.object_states_dict["akita_black_bowl_1"], env.env.object_states_dict["cream_cheese_1"]) (arguments REVERSED from the BDDL goal's actual order) and assert it returns False, proving RightOfX isn't trivially always-True regardless of argument order.
  </action>
  <verify>
    <automated>conda run -n libero pytest LIBERO/libero/libero/envs/test_spatial_predicates.py -x -q -k "right_of or near"</automated>
  </verify>
  <acceptance_criteria>
    - conda run -n libero pytest LIBERO/libero/libero/envs/test_spatial_predicates.py -x -q -k right_of exits 0, and the test's internal 20-reset loop reports 20/20 successes (asserted, not just printed)
    - conda run -n libero pytest LIBERO/libero/libero/envs/test_spatial_predicates.py -x -q -k near exits 0, and the test's internal 20-reset loop reports 20/20 successes
    - the :regions and :init blocks in both new BDDL files are byte-for-byte identical to put_the_cream_cheese_in_the_bowl.bddl's corresponding blocks (source assertion: only :language and :goal differ)
  </acceptance_criteria>
  <done>Both new BDDL tasks parse, construct a real SOARM env, and reset() into a goal-satisfying state on 20/20 trials; the reversed-argument directional check proves the predicates aren't trivially true; zero new collision risk introduced (regions byte-for-byte identical to Phase 4's already-validated layout).</done>
</task>

<task type="auto">
  <name>Task 3: "Between" BDDL task -- new 3rd-object region + empirical collision-safety validation (D-06, D-07, Pitfall 4)</name>
  <files>LIBERO/libero/libero/bddl_files/libero_spatial_soarm/put_the_butter_between_the_bowl_and_the_cream_cheese.bddl, LIBERO/libero/libero/envs/test_spatial_predicates.py</files>
  <read_first>
    LIBERO/libero/libero/bddl_files/libero_goal/put_the_cream_cheese_in_the_bowl.bddl (regions to reuse verbatim for akita_black_bowl_1 / cream_cheese_1, same as Task 2)
    .planning/phases/04-dataset-collection/04-02-SUMMARY.md ("CORRECTION" section -- the exact empirical methodology to replicate: direct sim.data.contact / actuator telemetry inspection, not distance-based assumptions; the robot base sits at world (-0.38, 0, 0.90))
    /opt/homebrew/Caskroom/miniconda/base/envs/libero/lib/python3.9/site-packages/robosuite/environments/base.py (check_contact, lines 565-577 -- `self.check_contact(geoms_1, geoms_2)` accepts individual geom names, lists, OR MujocoModel instances; env.env.get_object(name) returns a MujocoModel, env.env.robots[0].robot_model is the robot's MujocoModel -- this is the sanctioned, already-used-in-this-codebase API (base_object_states.py::ObjectState.check_contact calls exactly this), prefer it over hand-parsing sim.data.contact arrays)
  </read_first>
  <action>
    Author put_the_butter_between_the_bowl_and_the_cream_cheese.bddl in libero_spatial_soarm/: reuse the SAME akita_black_bowl_region and cream_cheese_region ranges verbatim (as Task 2), add a NEW butter_region with ranges (-0.13 -0.08 -0.11 -0.06) (x in [-0.13,-0.11], y in [-0.08,-0.06] -- region-centroid planar distance from the robot base at world (-0.38, 0) is sqrt(0.26^2 + 0.07^2) ~= 0.269m, comfortably within the ~0.45m reach envelope measured in 04-02-SUMMARY.md, and offset in y away from both existing regions' y-range to reduce forward-corridor risk per Pitfall 4). Add butter_1 - butter to :objects (the butter asset category already exists in this LIBERO fork's stable_hope_objects set, same graspable size class as cream_cheese per STATE.md's Phase 4 gripper-testing notes -- confirmed smaller than the 84mm jaw ceiling). Add (On butter_1 main_table_butter_region) to :init. Set :language to "Put the butter between the bowl and the cream cheese", :goal to (And (RightOfX butter_1 akita_black_bowl_1) (LeftOfX butter_1 cream_cheese_1)) -- D-07's decomposition pattern: "between" expressed as two binary predicates, since _eval_predicate only dispatches 2/3-token tuples (RESEARCH.md-confirmed dispatcher-arity finding, libero_tabletop_manipulation.py, do not modify that dispatcher). This holds for every point across all 3 randomized ranges with margin: butter_region's minimum x (-0.13) minus akita_black_bowl_region's maximum x (-0.19) = 0.06 > MARGIN=0.03 for RightOfX(butter_1, akita_black_bowl_1); cream_cheese_region's minimum x (-0.05) minus butter_region's maximum x (-0.11) = 0.06 > MARGIN=0.03 for LeftOfX(butter_1, cream_cheese_1). Per D-06/Pitfall 4, this is a NEW region (unlike Task 2's verbatim reuse) and needs its own empirical embodiment-safety check, not an assumption from coordinates alone: in test_spatial_predicates.py, add an integration test (name contains "between") that constructs the real env against this new BDDL file, loops env.reset() 20 times, and after each reset calls env.env.check_contact(env.env.robots[0].robot_model, env.env.get_object("butter_1")) (robosuite's sanctioned contact-check API, matching base_object_states.py::ObjectState.check_contact's own usage pattern -- prefer this over hand-parsing sim.data.contact), asserting it is False on every one of the 20 resets (the arm should not be touching the new object merely by existing at its rest pose -- Phase 4's exact lesson: "always check... sim.data.contact before concluding a hardware/torque ceiling", 04-02-SUMMARY.md). Also assert env.check_success() is True on all 20 resets, plus a reversed-argument directional check for both leftofx and rightofx against butter_1 (same pattern as Task 2). If the 20-reset contact check finds ANY unexpected robot-vs-butter_1 contact, adjust butter_region's ranges (shift further from the robot's forward corridor, mirroring Phase 4's actual resolution of moving objects away from near-base/near-centerline placement) and re-run -- cap iteration at 3 focused adjustments before reporting the geometry as a blocker, matching this project's established Phase 4 escalation convention (04-02-SUMMARY.md's explicit 5-iteration cap precedent).
  </action>
  <verify>
    <automated>conda run -n libero pytest LIBERO/libero/libero/envs/test_spatial_predicates.py -x -q -k between</automated>
  </verify>
  <acceptance_criteria>
    - conda run -n libero pytest LIBERO/libero/libero/envs/test_spatial_predicates.py -x -q -k between exits 0
    - the between test's 20-reset contact-inspection loop asserts (not just prints) zero robot-vs-butter_1 contacts across all 20 resets
    - ls LIBERO/libero/libero/bddl_files/libero_spatial_soarm/*.bddl lists exactly 3 files (this task's + Task 2's 2 files = SPAT-05's "at least 3" satisfied)
  </acceptance_criteria>
  <done>put_the_butter_between_the_bowl_and_the_cream_cheese.bddl parses, constructs a real SOARM env, resets into a goal-satisfying state on 20/20 trials, and produces ZERO robot-vs-butter_1 contacts at rest across all 20 resets (embodiment-safety empirically validated per D-06/Pitfall 4's methodology, not assumed); SPAT-05's "at least 3 BDDL tasks" requirement is met by this file plus Task 2's 2 files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BDDL task file (static, developer-authored data) -> predicate dispatcher | :goal predicate names parsed and looked up in VALIDATE_PREDICATE_FN_DICT; not user-supplied at runtime, so standard web-app injection concerns don't apply (RESEARCH.md Security Domain V5) |
| New object region -> robot physical rest pose | untested placement could cause an unintended physical collision at spawn |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Tampering | New BDDL task regions define ambiguous/overlapping spatial zones (predicate could pass/fail unpredictably depending on random init sampling) | medium | mitigate | Tasks 2/3 explicitly compute worst-case region-pair margins at plan-time (>=0.06m against a 0.03m MARGIN for left/right and between; ~0.023m margin below the NearTo threshold) and verify with a 20-reset check_success() loop at execution-time (D-09). |
| T-05-08 | Tampering (sim-state integrity) | New butter_1 object placement causes an undetected physical interpenetration/collision at spawn | medium | mitigate | Task 3's dedicated 20-reset env.check_contact(robot_model, butter_1) inspection (Phase 4's established methodology, reusing robosuite's sanctioned API) before accepting the layout, per D-06/Pitfall 4. |
| T-05-09 | Repudiation | Predicate registry key collision silently overwrites an existing LIBERO predicate | low | accept | Verified novel: none of leftofx/rightofx/nearto/farfrom exist in the current VALIDATE_PREDICATE_FN_DICT (grep-confirmed against the 10 pre-existing keys, Task 1 acceptance criteria). |
| T-05-10 | Tampering | A spatial predicate is accidentally used as the pass/fail referee via depth-derived XYZ instead of ground truth | low | accept | Not possible by construction -- these predicate classes only ever call get_geom_state() (ObjectState's sim.data.body_xpos-backed method); depth_xyz.py (Plan 02) is a separate module never imported here, satisfying D-05. |
| T-05-SC | Tampering (supply chain) | N/A -- no new package installs this plan | -- | n/a | Package Legitimacy Audit not applicable (RESEARCH.md: "No new packages are installed by this phase"). |

</threat_model>

<verification>
Run the full new test file and confirm green:
- `conda run -n libero pytest LIBERO/libero/libero/envs/test_spatial_predicates.py -x -q` (all unit + integration tests: predicate logic, right_of, near, between)

Confirm exactly 3 new BDDL files exist: `ls LIBERO/libero/libero/bddl_files/libero_spatial_soarm/*.bddl | wc -l` equals 3.
</verification>

<success_criteria>
- 4 new spatial predicate classes exist, correctly registered, with zero key collisions (SPAT-05, D-05, D-07, D-08).
- 3 new BDDL tasks exist covering left/right, near, and between (D-07), each reset()-satisfying its own goal on 20/20 trials with directional correctness proven.
- The new "between" task's 3rd-object region is empirically collision-validated (D-06, Pitfall 4), not assumed safe.
- Predicates use ground-truth get_geom_state() exclusively (D-05) -- no depth-derived XYZ ever enters a pass/fail check.
</success_criteria>

<output>
Create `.planning/phases/05-spatial-awareness/05-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- New classes in `base_predicates.py`: `LeftOfX`, `RightOfX`, `NearTo`, `FarFrom` (each `BinaryAtomic` subclasses)
- New registry keys in `predicates/__init__.py`'s `VALIDATE_PREDICATE_FN_DICT`: `"leftofx"`, `"rightofx"`, `"nearto"`, `"farfrom"`
- New directory + 3 new BDDL files in `LIBERO/libero/libero/bddl_files/libero_spatial_soarm/`: `put_the_cream_cheese_to_the_right_of_the_bowl.bddl`, `put_the_cream_cheese_near_the_bowl.bddl`, `put_the_butter_between_the_bowl_and_the_cream_cheese.bddl`
- New file `LIBERO/libero/libero/envs/test_spatial_predicates.py`
- No changes to any existing file (base_predicates.py and predicates/__init__.py gain additions only, no existing lines removed/altered).
