---
phase: 06-fine-tuning-evaluation
plan: 04
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - LIBERO/notebooks/06a-finetune.ipynb
autonomous: true
gap_closure: true
requirements: [TUNE-01, TUNE-02]

must_haves:
  truths:
    - "The RLDS-conversion cell imports hdf5_to_rlds via the same libero.libero.datasets package prefix every other notebook in this repo already uses correctly (06b-eval.ipynb, 03a/03b, 01, 02, quick_walkthrough.ipynb), matching the real on-disk layout LIBERO/libero/libero/datasets/rlds_converter.py."
    - "The OXE-registration cell imports apply_soarm_spatial_registration via the identical libero.libero.datasets prefix, matching LIBERO/libero/libero/datasets/oxe_register.py's real on-disk location."
    - "Neither edited cell still carries the stale ModuleNotFoundError output/traceback captured before this fix -- both cells' cached outputs and execution_count are cleared so the notebook's committed state doesn't misrepresent post-fix behavior."
  artifacts:
    - LIBERO/notebooks/06a-finetune.ipynb
  key_links:
    - "06a-finetune.ipynb's RLDS-conversion cell -> LIBERO/libero/libero/datasets/rlds_converter.hdf5_to_rlds (Plan 06-01)"
    - "06a-finetune.ipynb's OXE-registration cell -> LIBERO/libero/libero/datasets/oxe_register.apply_soarm_spatial_registration (Plan 06-02)"
---

## Multi-Source Coverage Audit

| Source | ID | Item | Plan | Status | Notes |
|--------|-----|------|------|--------|-------|
| GOAL | -- | Phase 6 success criterion 1: "HDF5 dataset converts to RLDS format and OpenVLA-OFT LoRA fine-tuning (r=32) runs to completion on Colab A100 without crashing" | 06-04 | COVERED | 06-UAT.md test 1 shows this criterion is entirely blocked by the two-line import bug this plan fixes; `finetune.py`'s torchrun cell was never even reached |
| REQ | TUNE-01 | RLDS conversion | 06-04 | COVERED | RLDS-conversion cell's import path corrected so `hdf5_to_rlds` actually resolves |
| REQ | TUNE-02 | LoRA fine-tuning runs to completion | 06-04 | COVERED | OXE-registration cell (hard prerequisite to the `finetune.py` invocation, per Pitfall 1) import path corrected |
| RESEARCH | -- | n/a | -- | N/A | This is a diagnosed regression fix against an already-planned/executed deliverable, not new research-derived scope |
| CONTEXT | -- | n/a | -- | N/A | No CONTEXT.md decision is touched; purely a bugfix to a wrong module path |
| -- | -- | TUNE-03, TUNE-04 (06-03 scope; UAT tests 2-5, all `blocked_by: prior-phase`) | 06-03 (already merged, complete) | OUT OF SCOPE | Blocked only *transitively* through this same import bug (no trained adapter exists yet to evaluate); no code changes needed for TUNE-03/TUNE-04 themselves -- re-verification of tests 2-5 happens via the standard end-of-phase UAT re-run once this fix lands and a live Colab training run produces a real `HF_ADAPTER_REPO_ID` |

<objective>
Close the sole diagnosed 06-UAT.md gap (severity: blocker, test 1): `LIBERO/notebooks/06a-finetune.ipynb`'s RLDS-conversion cell and OXE-registration cell both import from `libero.datasets.*` instead of the real on-disk `libero.libero.datasets.*` package path, so both cells crash with `ModuleNotFoundError: No module named 'libero.datasets'` before `finetune.py`'s torchrun cell is ever reached. Root cause and exact fix locations are already fully diagnosed in 06-UAT.md's Gaps section (confirmed by direct comparison against every other notebook in the repo and against the real package layout on disk) -- no further investigation needed.

Purpose: Unblock the entire Phase 6 human verification chain (UAT tests 1-5 all currently `issue`/`blocked`) at its single root cause, so the researcher's next live Colab A100 run can actually reach `finetune.py`, produce a trained LoRA adapter, and unblock the before/after evaluation benchmark (06-03) and the shared WandB training+eval dashboard.
Output: `LIBERO/notebooks/06a-finetune.ipynb` with both import cells corrected to the real `libero.libero.datasets` package path, and their stale pre-fix error outputs cleared.
</objective>

<execution_context>
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/workflows/execute-plan.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-fine-tuning-evaluation/06-UAT.md
@.planning/phases/06-fine-tuning-evaluation/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the two wrong-package-path imports in 06a-finetune.ipynb and clear their stale error outputs</name>
  <files>LIBERO/notebooks/06a-finetune.ipynb</files>
  <read_first>
    LIBERO/libero/libero/datasets/rlds_converter.py and LIBERO/libero/libero/datasets/oxe_register.py -- confirms both modules genuinely live under the nested `LIBERO/libero/libero/datasets/` directory on disk (two `libero` path segments before `datasets`), NOT `LIBERO/libero/datasets/` (which is a separate, unrelated directory holding the actual HDF5 demo data, e.g. `soarm_spatial/*_demo.hdf5` -- a filesystem path, not a Python package).
    LIBERO/notebooks/06b-eval.ipynb -- confirms the correct, already-working import convention this phase's sibling notebook uses for the same nested package (search for any `from libero.libero...` import in that notebook to see the pattern this fix must match).
    .planning/phases/06-fine-tuning-evaluation/06-UAT.md (Gaps section) -- full diagnosis: exact cell locations, root cause, and confirmation that every other notebook in the repo already uses the correct two-segment prefix.
  </read_first>
  <action>
    In `LIBERO/notebooks/06a-finetune.ipynb`, locate the RLDS-conversion cell (its source imports `hdf5_to_rlds`) and the OXE-registration cell (its source imports `apply_soarm_spatial_registration`). Both currently resolve one package segment too shallow -- per 06-UAT.md's diagnosis, they're missing the nested second `libero` segment that the real on-disk package path requires and that every other notebook in this repo (06b-eval.ipynb, 03a/03b, 01, 02, quick_walkthrough.ipynb) already uses correctly for this submodule.

    Insert the missing `libero` segment into both import lines so the RLDS-conversion cell's import becomes `from libero.libero.datasets.rlds_converter import hdf5_to_rlds` and the OXE-registration cell's import becomes `from libero.libero.datasets.oxe_register import apply_soarm_spatial_registration`. Do not touch anything else in either cell -- specifically, leave the `RLDS_OUT_DIR` / hdf5-glob filesystem path strings (`f"{REPO_ROOT}/LIBERO/libero/datasets/soarm_spatial/..."`) exactly as they are: those are correct, already-working references to the separate on-disk data directory (`LIBERO/libero/datasets/soarm_spatial/`), not import statements, and sit one directory level shallower than the Python package by design.

    Both edited cells currently carry a cached error output (execution result + traceback) from before this fix, captured when the import genuinely failed. Clear each edited cell's `outputs` array to empty and reset its `execution_count` to `null`, so the committed notebook doesn't misleadingly show a stale failure for code that is now fixed. Do not alter any other cell's outputs.

    Preserve the notebook's overall JSON structure (cell ids, cell ordering, all other cells' source and outputs) exactly as-is -- this is a two-line source fix plus a two-cell output clear, nothing else.
  </action>
  <verify>
    <automated>python3 -c "import json; json.load(open('LIBERO/notebooks/06a-finetune.ipynb'))" && echo "JSON_VALID"
grep -c 'from libero\.libero\.datasets\.rlds_converter import hdf5_to_rlds' LIBERO/notebooks/06a-finetune.ipynb
grep -c 'from libero\.libero\.datasets\.oxe_register import apply_soarm_spatial_registration' LIBERO/notebooks/06a-finetune.ipynb
grep -c 'from libero\.datasets\.' LIBERO/notebooks/06a-finetune.ipynb</automated>
    <human-check>
      Per this project's `human_verify_mode: end-of-phase` config, the real Colab confirmation is not a blocking mid-plan checkpoint. Before closing this gap in the phase's UAT record, re-run `06a-finetune.ipynb` end-to-end on a Colab A100 runtime through Block A, restart, Block B, the RLDS-conversion cell, and the OXE-registration smoke-test cell. Confirm both cells now complete without `ModuleNotFoundError` and the notebook proceeds to the `finetune.py` torchrun cell (06-UAT.md test 1's original expectation).
    </human-check>
  </verify>
  <done>
    `python3 -c "import json; json.load(...)"` confirms the notebook is still valid JSON. Both fixed import lines are present exactly once each (grep count 1). No import in the file still resolves one package segment shallow (grep count 0 for the incorrect shallow prefix). The two edited cells' cached outputs and execution_count no longer reflect the pre-fix failure state.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Notebook source -> Colab kernel | The notebook's Python source is trusted, executor-authored code (no untrusted external input crosses this boundary in this plan's change) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04-01 | Tampering | LIBERO/notebooks/06a-finetune.ipynb (JSON structure) | low | mitigate | Automated verify includes a JSON-validity parse (`json.load`) before/after the edit, so a malformed edit that corrupts the notebook's cell structure is caught immediately rather than silently shipped |
| T-06-04-02 | Repudiation | Stale cached error outputs left in committed notebook | low | accept | Clearing the two edited cells' outputs/execution_count removes the only stale/misleading artifact this change touches; no new repudiation risk is introduced since no new package installs or external calls are added by this plan |
</threat_model>

<verification>
Both edited import lines resolve against the real on-disk `LIBERO/libero/libero/datasets/{rlds_converter,oxe_register}.py` package layout. Notebook JSON remains valid and no other cell's content changes. Full closure of 06-UAT.md test 1 (and consequent unblocking of tests 2-5, all `blocked_by: prior-phase`) requires the `<human-check>` live Colab A100 re-run described above -- this plan's automated verify proves the source-level fix is correct and complete; it cannot execute the notebook itself (no local GPU/Colab kernel available in this environment, per this project's established convention for GPU-only verification steps).
</verification>

<success_criteria>
`06a-finetune.ipynb`'s RLDS-conversion and OXE-registration cells both import from `libero.libero.datasets.*` (matching the real package layout and every other notebook's established convention). The notebook remains valid JSON with no unrelated changes. The researcher can proceed to re-run the notebook end-to-end on Colab A100 without hitting the `ModuleNotFoundError` that blocked 06-UAT.md test 1.
</success_criteria>

<output>
Create `.planning/phases/06-fine-tuning-evaluation/06-04-SUMMARY.md` when done
</output>
