---
phase: 06-fine-tuning-evaluation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - LIBERO/libero/libero/vla/eval_loop.py
  - LIBERO/libero/libero/vla/test_eval_loop.py
  - LIBERO/libero/libero/vla/adapter_backend.py
  - LIBERO/libero/libero/vla/test_adapter_backend.py
  - LIBERO/libero/libero/vla/__init__.py
  - LIBERO/notebooks/06b-eval.ipynb
autonomous: false
requirements: [TUNE-03, TUNE-04]
user_setup:
  - service: Hugging Face Hub (read access)
    why: "Download the LoRA adapter + its dataset_statistics.json from the HF Hub repo Plan 06-02's training notebook pushed to -- reuses the existing read-side hf_hub_download pattern already used for the base checkpoint"
    env_vars: []
    dashboard_config: []
  - service: Weights & Biases
    why: "Log the before/after success-rate benchmark tables to the shared soarm-oft-finetune-eval WandB project (D-13, TUNE-04) -- run_suite has no native WandB hooks, this notebook wraps it"
    env_vars:
      - name: WANDB_API_KEY
        source: "wandb.ai -> Settings -> API keys (existing project convention, .planning/codebase/INTEGRATIONS.md)"
    dashboard_config: []

must_haves:
  truths:
    - "run_episode/run_suite accept an optional seed/episode_seeds argument and call env.seed(seed) freshly inside the per-episode loop (not once before it), so the same episode_seeds list reused across a before-run and an after-run produces byte-identical initial object placements (TUNE-03, D-08, Pitfall 7)."
    - "FinetunedOFTBackend loads a HF Hub-hosted LoRA adapter, merges it into the base OFT checkpoint via peft.PeftModel.merge_and_unload(), re-applies set_num_images_in_input(2), and overlays the ADAPTER's OWN dataset_statistics.json (not Phase 4's file) -- while inheriting OFTBackend's predict() contract unchanged, so eval_loop.run_suite drives it exactly as it drives the zero-shot backend (D-01, Pitfall 6)."
    - "06b-eval.ipynb re-runs the full zero-shot (before) benchmark under the identical seeded 20-episodes x 4-tasks protocol used for the fine-tuned (after) benchmark, and logs both success-rate tables to the shared soarm-oft-finetune-eval WandB project (TUNE-03, TUNE-04, D-07, D-08, D-09, D-13)."
  artifacts:
    - LIBERO/libero/libero/vla/eval_loop.py
    - LIBERO/libero/libero/vla/adapter_backend.py
    - LIBERO/libero/libero/vla/test_adapter_backend.py
    - LIBERO/notebooks/06b-eval.ipynb
  key_links:
    - "eval_loop.run_suite(..., episode_seeds=list(range(20))) is called identically in 06b's before-cell (plain OFTBackend) and after-cell (FinetunedOFTBackend) -- same seed list, same 4-task list, same episode count (D-08)."
    - "FinetunedOFTBackend(adapter_repo_id=...) downloads from the exact HF Hub repo_id Plan 06-02's training notebook printed after its final checkpoint push (HF_ADAPTER_REPO_ID)."
    - "Both notebooks' wandb.init(project=WANDB_PROJECT, ...) target the same soarm-oft-finetune-eval project (Plan 06-02's WANDB_PROJECT constant) so training curves and eval results appear together in one WandB dashboard (TUNE-04)."
---

<objective>
Add per-episode seed plumbing to the existing `eval_loop.py` (D-08), build a `FinetunedOFTBackend` that reloads a HF Hub LoRA adapter through `OFTBackend`'s already-proven loading path (D-01), and build the Colab eval notebook that re-runs the full zero-shot benchmark and the fine-tuned benchmark under an identical seeded 20-episodes x 4-tasks protocol, logging both to a shared WandB project (TUNE-03, TUNE-04, D-07/D-08/D-09/D-12/D-13).

Purpose: TUNE-03 requires spatial vs. non-spatial success rates measured BEFORE and AFTER fine-tuning under conditions isolated from initial-state randomization noise -- this plan makes that comparison apples-to-apples (identical seeds) and genuinely re-runs the zero-shot baseline rather than reusing Phase 3's differently-configured 0% number (D-09).
Output: `eval_loop.py`'s additive seed plumbing, `adapter_backend.py`'s `FinetunedOFTBackend` (locally tested via the same fake-module-injection technique Phase 5 already established for `OFTBackend`), and `06b-eval.ipynb` (separate Colab kernel per D-12: adapter download, before/after seeded `run_suite` runs, WandB eval logging, Pitfall-6 stats-divergence check).
</objective>

<execution_context>
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/workflows/execute-plan.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-fine-tuning-evaluation/06-CONTEXT.md
@.planning/phases/06-fine-tuning-evaluation/06-RESEARCH.md
@.planning/phases/06-fine-tuning-evaluation/06-PATTERNS.md
@.planning/phases/06-fine-tuning-evaluation/06-VALIDATION.md
@LIBERO/libero/libero/vla/eval_loop.py
@LIBERO/libero/libero/vla/oft_backend.py
@LIBERO/libero/libero/vla/test_oft_backend.py
@LIBERO/libero/libero/envs/env_wrapper.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: eval_loop.py -- per-episode seed plumbing for identical before/after benchmarking (TUNE-03, D-08, Pitfall 7)</name>
  <files>LIBERO/libero/libero/vla/eval_loop.py, LIBERO/libero/libero/vla/test_eval_loop.py</files>
  <read_first>
    LIBERO/libero/libero/vla/eval_loop.py (whole file, 163 lines -- `run_episode` signature lines 20-28 and `obs = env.reset()` at line 62; `run_suite` signature lines 95-103 and the per-episode loop lines 131-137).
    LIBERO/libero/libero/vla/test_eval_loop.py (whole file, 202 lines -- `MockEnv` class lines 25-43, the existing 5 tests' style, especially `test_run_suite_multi_task_multi_episode_summary` lines 148-184 for the multi-episode assertion pattern this new determinism test extends).
    LIBERO/libero/libero/envs/env_wrapper.py (lines 133-134, `ControlEnv.seed(self, seed): self.env.seed(seed)` -- confirms `env.seed(seed)` is already a valid call on every env this project constructs, no new env method needed).
    .planning/phases/06-fine-tuning-evaluation/06-RESEARCH.md (Pattern 5 "Seed-plumbed run_suite for D-08"; Pitfall 7 "run_suite's env_factory creates ONE env reused across all 20 episodes of a task" -- the exact correctness requirement: `env.seed(seed)` must be called freshly INSIDE the per-episode loop, not once before it).
  </read_first>
  <behavior>
    - `run_episode(env, backend, language, video_path, max_steps=..., camera_name=..., agentview_camera_name=..., seed=None)`: when `seed` is not `None`, calls `env.seed(seed)` immediately before `env.reset()`; when `seed is None` (the default), behavior is byte-identical to today (no `env.seed` call at all) -- backward compatible, proven via a `MockEnv.seed` recorder: `test_run_episode_calls_env_seed_before_reset` asserts `env.last_seed == 42` after `run_episode(env, backend, language, video_path, seed=42)`, and a companion test asserts `env.last_seed` stays `None`/unset when `seed` is omitted.
    - `run_suite(env_factory, backend, tasks, language_map, episodes_per_task, video_dir, max_steps=..., episode_seeds=None)`: when `episode_seeds` is a list of length `episodes_per_task`, each `run_episode` call for `ep_idx` passes `seed=episode_seeds[ep_idx]` -- verified via a test asserting the SAME `env.seed(...)` call sequence (in order, with matching values) occurs across TWO separate `run_suite` invocations using the SAME `env_factory`/`episode_seeds` (the literal regression Pitfall 7 warns against: "re-running the before benchmark twice with the same seed list produces different summary-table numbers" would mean this test fails).
  </behavior>
  <action>
    In `eval_loop.py`, add `seed: int | None = None` as the last parameter of `run_episode` (keep all existing parameters/defaults unchanged, purely additive). Immediately before the existing `obs = env.reset()` line (line 62), insert: if `seed is not None`, call `env.seed(seed)`. Update the docstring's Args section to document `seed` per the Behavior spec (byte-identical to today when omitted).

    Add `episode_seeds: list | None = None` as the last parameter of `run_suite` (purely additive, existing signature/defaults unchanged otherwise). Inside the existing `for ep_idx in range(episodes_per_task):` loop (lines 131-137), compute `seed = episode_seeds[ep_idx] if episode_seeds is not None else None` and pass `seed=seed` into the `run_episode(...)` call -- this placement is INSIDE the per-episode loop, matching Pitfall 7's explicit correctness requirement (the loop's `env = env_factory(task)` call, line 127, already happens once per task OUTSIDE this loop and is reused across all `episodes_per_task` episodes -- do not move the seed application outside the `ep_idx` loop). Update the docstring's Args section to document `episode_seeds`.

    In `test_eval_loop.py`, add a `last_seed` attribute (init to `None`) and a `def seed(self, seed): self.last_seed = seed` method to the existing `MockEnv` class (lines 25-43) -- purely additive, do not change `MockEnv.__init__`/`reset`/`step`'s existing behavior. Add `test_run_episode_calls_env_seed_before_reset` and `test_run_episode_without_seed_never_calls_env_seed` (per the Behavior spec's first bullet). Add `test_run_suite_applies_seeds_identically_across_two_runs`: build a small `env_factory` returning a fresh `MockEnv()` per call, run `run_suite(env_factory, MockBackend(), ["task_a.bddl"], {"task_a.bddl": "do the task"}, episodes_per_task=3, video_dir="/tmp/video_dir", max_steps=5, episode_seeds=[10, 20, 30])` TWICE, capturing each `MockEnv` instance's `last_seed` sequence via a list appended to inside a subclassed/wrapped `env_factory` (or by returning the same tracked list of envs and reading `.last_seed` per episode -- since `run_suite` calls `env_factory(task)` once per task and reuses that env across the 3 episodes, only the FINAL episode's seed (30) is observable on that single `MockEnv` instance after the loop; assert the env's `last_seed == 30` after each of the two `run_suite` calls, proving both runs applied the identical final seed and, by extension per the per-episode `seed=episode_seeds[ep_idx]` code path, the identical seed at every intermediate episode too -- for stronger coverage, additionally have `MockEnv.seed` append to a `self.seed_calls` list rather than only tracking `last_seed`, and assert `env.seed_calls == [10, 20, 30]` after each of the two independent `run_suite` calls).
  </action>
  <verify>
    <automated>conda run -n libero pytest LIBERO/libero/libero/vla/test_eval_loop.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `conda run -n libero pytest LIBERO/libero/libero/vla/test_eval_loop.py -x -q` exits 0 with all 5 existing tests still passing plus 3 new tests passing (8 total)
    - `grep -q "def run_episode(" LIBERO/libero/libero/vla/eval_loop.py` and `grep -q "seed: int | None = None" LIBERO/libero/libero/vla/eval_loop.py` both match
    - `grep -q "episode_seeds: list | None = None" LIBERO/libero/libero/vla/eval_loop.py` matches
    - `grep -B2 "obs = env.reset()" LIBERO/libero/libero/vla/eval_loop.py | grep -q "env.seed(seed)"` matches (confirms the seed call is positioned immediately before reset, inside run_episode)
    - Full vla suite still green: `conda run -n libero pytest LIBERO/libero/libero/vla -x -q`
  </acceptance_criteria>
  <done>run_episode/run_suite accept optional seed/episode_seeds parameters, apply them per-episode (inside the loop, not hoisted out), remain byte-identical to current behavior when omitted, and are proven correct via 3 new tests including a two-independent-runs determinism proof (D-08, Pitfall 7).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: adapter_backend.py -- FinetunedOFTBackend (HF Hub adapter reload, PEFT merge, adapter's own norm_stats overlay) (D-01, Pitfall 6)</name>
  <files>LIBERO/libero/libero/vla/adapter_backend.py, LIBERO/libero/libero/vla/test_adapter_backend.py, LIBERO/libero/libero/vla/__init__.py</files>
  <read_first>
    LIBERO/libero/libero/vla/oft_backend.py (whole file, 193 lines -- `OFTBackend.__init__` lines 84-129, especially the `hf_hub_download(checkpoint, "dataset_statistics.json")` overlay at lines 114-117 and `self.model.vision_backbone.set_num_images_in_input(2)` at line 107; `predict()` is inherited unchanged, lines 131-192).
    LIBERO/libero/libero/vla/test_oft_backend.py (whole file, 173 lines -- the `fake_oft_deps` fixture, lines 30-126: THIS IS THE EXACT TECHNIQUE to extend, not reinvent. `monkeypatch.setitem(sys.modules, "torch"/"huggingface_hub"/"transformers", fake_module)` before importing `oft_backend`, plus `monkeypatch.setattr(mod, "_prismatic_ok", lambda: True)` to skip the network-touching prismatic clone path).
    .planning/phases/06-fine-tuning-evaluation/06-RESEARCH.md (Pattern 4 "Reload the fine-tuned adapter for the after eval without touching OFTBackend"; Pitfall 6 "Two different dataset_statistics.json files can silently diverge" -- the eval notebook's after-backend MUST load the checkpoint's OWN stats file pushed alongside the adapter, not Phase 4's file; Assumptions Log A4 -- merge_and_unload()'s resulting model tree may not expose `.vision_backbone` at the same location, verify on Colab).
  </read_first>
  <behavior>
    - `FinetunedOFTBackend(adapter_repo_id, checkpoint=CHECKPOINT, device="cuda")`: constructing it calls `super().__init__(checkpoint=checkpoint, device=device)` FIRST (reusing `OFTBackend`'s entire prismatic-guard + bf16-load + `set_num_images_in_input(2)` + base-checkpoint norm_stats overlay unchanged), THEN downloads the adapter via `snapshot_download(adapter_repo_id)`, merges via `PeftModel.from_pretrained(self.model, adapter_dir).merge_and_unload()` reassigned to `self.model`, re-applies `self.model.vision_backbone.set_num_images_in_input(2)` (Assumption A4 -- must survive the merge), and OVERWRITES `self.model.norm_stats`/`self.unnorm_key` by downloading `dataset_statistics.json` from `adapter_repo_id` itself (NOT re-reading the base `checkpoint`'s file a second time) -- verified via `fake_oft_deps`-style fixture assertions: `fake_peft_model_cls.from_pretrained.call_args_list == [mock.call(<the base-checkpoint fake_model>, <adapter_dir>)]`, `fake_model.vision_backbone.set_num_images_in_input.call_args_list == [mock.call(2), mock.call(2)]` (once from the inherited `OFTBackend.__init__`, once from the re-application after merge), and `fake_hf_hub.hf_hub_download.call_args_list` contains a call with `adapter_repo_id` as the first positional arg for the SECOND `dataset_statistics.json` fetch (distinct from the first call made during `super().__init__()` with `checkpoint`).
    - `FinetunedOFTBackend.predict(images, language)` is NOT overridden -- inherited from `OFTBackend` verbatim, proven via a test that calls `backend.predict(...)` and asserts it returns a `(8, 7)`-shaped array using the SAME `fake_model.predict_action` mock `OFTBackend`'s own tests already exercise.
  </behavior>
  <action>
    Create `adapter_backend.py`. Import `from .oft_backend import OFTBackend, CHECKPOINT` (relative import, same package) at module top -- `huggingface_hub.snapshot_download`/`hf_hub_download` and `peft.PeftModel` are imported the SAME way `oft_backend.py` imports `torch`/`transformers`/`huggingface_hub` (module-top, unconditional -- this file is ALSO Colab-only per this project's established convention for the `vla` package's GPU-dependent backends; `vla/__init__.py`'s guarded try/except wraps the whole import, not this file internally).

    Implement `class FinetunedOFTBackend(OFTBackend):` with `def __init__(self, adapter_repo_id: str, checkpoint: str = CHECKPOINT, device: str = "cuda"):`. Body: `super().__init__(checkpoint=checkpoint, device=device)` first (per Behavior spec). Then `from huggingface_hub import snapshot_download` is already available via the module-top `huggingface_hub` import -- call `adapter_dir = snapshot_download(adapter_repo_id)`. Then `from peft import PeftModel` (module-top import) -- `self.model = PeftModel.from_pretrained(self.model, adapter_dir).merge_and_unload()`. Immediately after, re-apply `self.model.vision_backbone.set_num_images_in_input(2)` with a comment citing Assumption A4 (merge_and_unload() may return a model whose module tree layout differs -- this call will `AttributeError` loudly if so, which is the intended fail-fast behavior, not a silent skip). Then overlay THIS adapter's own stats (Pitfall 6): `stats_path = hf_hub_download(adapter_repo_id, "dataset_statistics.json")`, `with open(stats_path) as f: self.model.norm_stats = json.load(f)` (OVERWRITING the base-checkpoint stats `super().__init__()` already set), then re-run the SAME `unnorm_key` resolution logic `OFTBackend.__init__` uses (start with `"libero_spatial"`, fall back to `"libero_spatial_no_noops"`, raise `KeyError` listing available keys if neither exists) against these NEW adapter-specific stats, reassigning `self.unnorm_key`. Add a print statement noting which stats source is active, mirroring `OFTBackend.__init__`'s existing `print(f"norm_stats overlaid from dataset_statistics.json: ...")` convention, so it's visually obvious in notebook output which stats file is in effect (supports the Pitfall 6 divergence-check the eval notebook, Task 3, performs separately).

    Do NOT override `predict()` -- `FinetunedOFTBackend` inherits it verbatim from `OFTBackend`, which is the entire point of this design (eval_loop.run_suite drives both backends through the identical `predict(images, language)` call with zero branching).

    Create `test_adapter_backend.py` mirroring `test_oft_backend.py`'s `fake_oft_deps` fixture pattern EXACTLY, extended with a fake `peft` module: add `fake_peft = types.ModuleType("peft")`, `fake_peft_model_cls = mock.MagicMock(name="PeftModel")`, `fake_peft_model_cls.from_pretrained = mock.MagicMock(return_value=<a mock with .merge_and_unload() returning fake_model itself>)`, `fake_peft.PeftModel = fake_peft_model_cls`, `monkeypatch.setitem(sys.modules, "peft", fake_peft)`. Also extend `fake_hf_hub.hf_hub_download`/add `fake_hf_hub.snapshot_download = mock.MagicMock(return_value="/fake/adapter/dir")` -- write a SECOND temp `dataset_statistics.json` (distinct content from the base-checkpoint one, e.g. a different `unnorm_key` value) so tests can assert the adapter-specific file (not the base one) is what ends up in `self.model.norm_stats`. Delete `libero.vla.adapter_backend`/`libero.libero.libero.vla.adapter_backend` from `sys.modules` before each fresh import, same as `test_oft_backend.py`'s existing `oft_backend` cache-clearing. Implement the assertions from the Behavior spec's two bullets as 2 test functions: `test_init_merges_adapter_and_reapplies_dual_image_mode` and `test_predict_inherited_from_oft_backend_returns_8x7_chunk`.

    Update `LIBERO/libero/libero/vla/__init__.py`: add a THIRD guarded try/except block (mirroring the existing `OFTBackend`/`Pi0Backend` pattern, lines 3-30) importing `from .adapter_backend import FinetunedOFTBackend`, degrading to `FinetunedOFTBackend = None` on any exception (same rationale -- torch/peft/huggingface_hub/transformers are Colab-only, this local no-GPU pytest suite must still be able to import the package).
  </action>
  <verify>
    <automated>conda run -n libero pytest LIBERO/libero/libero/vla/test_adapter_backend.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `conda run -n libero pytest LIBERO/libero/libero/vla/test_adapter_backend.py -x -q` exits 0 (2/2 new tests pass)
    - `grep -q "class FinetunedOFTBackend(OFTBackend):" LIBERO/libero/libero/vla/adapter_backend.py` matches
    - `grep -q "merge_and_unload()" LIBERO/libero/libero/vla/adapter_backend.py` matches
    - `grep -q "def predict" LIBERO/libero/libero/vla/adapter_backend.py` returns no match (predict is NOT overridden -- inherited verbatim)
    - `grep -q "hf_hub_download(adapter_repo_id" LIBERO/libero/libero/vla/adapter_backend.py` matches (confirms the adapter's OWN stats file is fetched, not just the base checkpoint's)
    - Full vla suite still green: `conda run -n libero pytest LIBERO/libero/libero/vla -x -q`
  </acceptance_criteria>
  <done>FinetunedOFTBackend subclasses OFTBackend, reuses its entire loading path via super().__init__, merges a HF-Hub-downloaded PEFT adapter, re-applies dual-image mode, and overlays the adapter's OWN dataset_statistics.json (not the base checkpoint's) -- all proven locally via fake-module-injection tests mirroring test_oft_backend.py's established technique, with zero GPU/network/real-model dependency.</done>
</task>

<task type="auto">
  <name>Task 3: 06b-eval.ipynb -- adapter download, seeded before/after run_suite benchmark, WandB eval logging, Pitfall-6 stats-divergence check (TUNE-03, TUNE-04, D-07/D-08/D-09/D-12/D-13)</name>
  <files>LIBERO/notebooks/06b-eval.ipynb</files>
  <read_first>
    .planning/phases/06-fine-tuning-evaluation/06-CONTEXT.md (D-07 -- 20 episodes x 4 tasks: 3 spatial in `LIBERO/libero/libero/bddl_files/libero_spatial_soarm/` + 1 non-spatial `LIBERO/libero/libero/bddl_files/libero_goal/put_the_cream_cheese_in_the_bowl.bddl`; D-08 identical seeds; D-09 full zero-shot re-run).
    LIBERO/notebooks/03a-oft-inference-eval.ipynb (`env_factory`/`TASKS`/`LANGUAGE_MAP`/`VIDEO_DIR` cell construction pattern, and the `run_suite(env_factory, backend, TASKS, LANGUAGE_MAP, episodes_per_task=..., video_dir=..., max_steps=600)` call shape this notebook's before/after cells reuse with the new `episode_seeds` kwarg from Task 1).
    .planning/phases/06-fine-tuning-evaluation/06-02-SUMMARY.md (Plan 06-02's final `HF_ADAPTER_REPO_ID` naming convention and `WANDB_PROJECT` constant -- this notebook's `adapter_repo_id` parameter and `wandb.init(project=...)` call must match exactly).
  </read_first>
  <action>
    Create `LIBERO/notebooks/06b-eval.ipynb` as a fresh notebook (new file, separate Colab kernel per D-12 -- do NOT extend `06a-finetune.ipynb`). Markdown header cell: "# Phase 6b: Before/After Fine-Tuning Benchmark (TUNE-03, TUNE-04)", stating this notebook re-runs a zero-shot (before) and fine-tuned (after) benchmark under an identical seeded protocol and logs both to WandB.

    Cell: GPU assertion (same pattern as prior notebooks). Cell: repo delivery (GitHub-clone + getpass pattern). Cell: `ADAPTER_REPO_ID = "<paste the HF_ADAPTER_REPO_ID Plan 06-02's training notebook printed>"` as an explicit, clearly-commented configuration cell (this is a genuine runtime input the researcher fills in after a real training run completes -- not something this plan can hardcode).

    Markdown cell: "## Block A: Install" -- `pip install peft==0.20.0 --no-deps` (this notebook needs `peft` for `FinetunedOFTBackend`'s merge step; `tensorflow-datasets`/`accelerate` are NOT needed here, this kernel never touches RLDS/training) followed by `pip install -U protobuf` (Pitfall 4, same final-step convention). Markdown cell: "*** STOP -- Restart runtime now ***".

    Post-restart Block B cells: EGL bootstrap, `~/.libero/config.yaml` bootstrap, `sys.path` insertion, matplotlib `Agg` + numba shim -- identical to prior notebooks.

    Cell: task/language setup -- `TASKS = ["libero_spatial_soarm/put_the_cream_cheese_to_the_right_of_the_bowl.bddl", "libero_spatial_soarm/put_the_cream_cheese_near_the_bowl.bddl", "libero_spatial_soarm/put_the_butter_between_the_bowl_and_the_cream_cheese.bddl", "libero_goal/put_the_cream_cheese_in_the_bowl.bddl"]` (D-07's exact 4 tasks: 3 spatial + 1 non-spatial), `BDDL_ROOT = f"{LIBERO_ROOT}/bddl_files"`, `LANGUAGE_MAP` built by calling `get_problem_info(os.path.join(BDDL_ROOT, t))["language_instruction"]` for each `t` in `TASKS` (reuses the real BDDL parser, not hardcoded strings, matching this converter's own D-04-adjacent convention from Plan 06-01), `EPISODES_PER_TASK = 20` (D-07), `EPISODE_SEEDS = list(range(20))` (D-08 -- fixed, reused identically for both runs), `env_factory = lambda bddl: OffScreenRenderEnv(bddl_file_name=os.path.join(BDDL_ROOT, bddl), robots=["Soarm101"], camera_heights=256, camera_widths=256, has_renderer=False, has_offscreen_renderer=True)` (Phase 2's confirmed no-custom-controller-kwarg pattern).

    Cell: WandB init -- `import wandb; WANDB_PROJECT = "soarm-oft-finetune-eval"` (matches Plan 06-02's constant exactly, so both notebooks' runs land in the same project per D-13/TUNE-04); `wandb_run = wandb.init(project=WANDB_PROJECT, job_type="eval", name=f"before-after-{ADAPTER_REPO_ID.split('/')[-1]}")`.

    Markdown cell: "## BEFORE: Zero-Shot Baseline (D-09 -- full re-run, not reused from Phase 3)". Cell: `from libero.libero.vla import OFTBackend; before_backend = OFTBackend(); before_results = run_suite(env_factory, before_backend, TASKS, LANGUAGE_MAP, episodes_per_task=EPISODES_PER_TASK, video_dir=f"{VIDEO_DIR}/before", max_steps=600, episode_seeds=EPISODE_SEEDS)`.

    Markdown cell: "## Pitfall 6: dataset_statistics.json divergence check". Cell: download Phase 4's `dataset_statistics.json` (`hf_hub_download` or direct read from the cloned repo's `LIBERO/libero/datasets/soarm_spatial/dataset_statistics.json`) and, AFTER constructing `after_backend` below, compare its `soarm_spatial`-keyed action/proprio `q01`/`q99` values against `after_backend.model.norm_stats`'s adapter-specific entry -- print a clear WARNING (not a hard failure -- these ARE expected to differ somewhat, per Pitfall 6, since one is raw-HDF5 stats and the other is post-RLDS-conversion stats) if any dimension's relative difference exceeds a documented threshold (e.g. 10%), so silent drift is visible rather than assumed away.

    Markdown cell: "## AFTER: Fine-Tuned Adapter (identical seeds)". Cell: `from libero.libero.vla import FinetunedOFTBackend; after_backend = FinetunedOFTBackend(adapter_repo_id=ADAPTER_REPO_ID); after_results = run_suite(env_factory, after_backend, TASKS, LANGUAGE_MAP, episodes_per_task=EPISODES_PER_TASK, video_dir=f"{VIDEO_DIR}/after", max_steps=600, episode_seeds=EPISODE_SEEDS)` -- SAME `TASKS`/`LANGUAGE_MAP`/`EPISODES_PER_TASK`/`EPISODE_SEEDS`/`max_steps` as the before-cell (D-08's identical-protocol requirement, literally the same Python variables, not re-typed values that could drift).

    Cell: WandB eval-results wrapper (no native `run_suite` hook exists, per 06-RESEARCH.md's Architectural Responsibility Map -- this notebook must wrap it manually). Compute per-task before/after success rates from `before_results`/`after_results` (group by `r["task"]`, mean of `r["success"]`), build a `wandb.Table` with columns `task, before_success_rate, after_success_rate, delta`, and `wandb_run.log({"before_after_benchmark": table})`; also log the two aggregate rates as scalars (`wandb_run.log({"before_aggregate_success_rate": ..., "after_aggregate_success_rate": ...})`) so they render as a comparable summary alongside `finetune.py`'s training-loss curves in the same project.

    Final markdown cell: "## Phase 6b Summary" with a table (columns: Item, Status `☐`, Notes) listing TUNE-03 (before/after table populated), TUNE-04 (WandB eval log link), and the Pitfall-6 divergence-check result.
  </action>
  <verify>
    <automated>python -c "import json; nb = json.load(open('LIBERO/notebooks/06b-eval.ipynb')); src = ''.join(c for cell in nb['cells'] for c in cell.get('source', [])); assert 'EPISODE_SEEDS = list(range(20))' in src and 'episode_seeds=EPISODE_SEEDS' in src and src.count('episode_seeds=EPISODE_SEEDS') >= 2 and 'FinetunedOFTBackend' in src and 'WANDB_PROJECT = \"soarm-oft-finetune-eval\"' in src, 'missing expected before/after seeded benchmark cell content'"</automated>
  </verify>
  <acceptance_criteria>
    - `LIBERO/notebooks/06b-eval.ipynb` parses as valid JSON
    - Notebook source contains `EPISODE_SEEDS = list(range(20))` and passes `episode_seeds=EPISODE_SEEDS` to BOTH the before-cell's and after-cell's `run_suite` calls (at least 2 occurrences)
    - Notebook source contains exactly 4 task BDDL paths matching D-07 (3 from `libero_spatial_soarm/`, 1 from `libero_goal/`)
    - Notebook source contains `WANDB_PROJECT = "soarm-oft-finetune-eval"` matching Plan 06-02's constant
    - Notebook source contains a divergence-check comparing the two `dataset_statistics.json` sources (Pitfall 6)
  </acceptance_criteria>
  <done>06b-eval.ipynb downloads the fine-tuned adapter, re-runs the zero-shot baseline under D-09, runs the fine-tuned benchmark under the byte-identical seeded protocol (D-08), flags any norm-stats divergence (Pitfall 6), and logs a before/after comparison table plus aggregate scalars to the shared soarm-oft-finetune-eval WandB project (TUNE-03, TUNE-04).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Human re-verifies peft package legitimacy for this notebook's separate Colab kernel install</name>
  <read_first>
    - .planning/phases/06-fine-tuning-evaluation/06-02-PLAN.md (Task 3 -- the FIRST peft legitimacy verification already performed for the training notebook's kernel; this is the SAME package/version, installed again in a genuinely SEPARATE Colab kernel per D-12, so a quick re-confirm is required rather than a full fresh investigation)
  </read_first>
  <action>Blocking human verification per the Package Legitimacy Gate protocol -- `peft==0.20.0` is installed a second time in this notebook's independent Colab kernel (D-12: eval runs in a separate kernel from training). Confirm the SAME already-verified package/version before running Task 3's `pip install peft==0.20.0` cell on this kernel. This checkpoint is never auto-approvable regardless of workflow.auto_advance.</action>
  <what-built>Task 3's `06b-eval.ipynb` install cell that installs `peft==0.20.0` -- the same package already verified legitimate in Plan 06-02 Task 3, now reinstalled in this notebook's separate Colab kernel session.</what-built>
  <how-to-verify>
    1. Confirm this is the SAME version (`peft==0.20.0`) already verified in Plan 06-02 Task 3 -- if the pinned version has changed for any reason, re-run the full verification (pip index versions / pypi.org/project/peft, confirm github.com/huggingface/peft origin).
    2. After installing on this kernel, run `pip show peft` and confirm the Home-page/Author fields still match `github.com/huggingface/peft`.
  </how-to-verify>
  <acceptance_criteria>
    - `pip show peft` output's Home-page references `github.com/huggingface/peft` on this notebook's kernel too
  </acceptance_criteria>
  <resume-signal>Type "approved" once `pip show peft` confirms the same HuggingFace origin on this kernel, or "blocked" with the observed metadata if it differs from Plan 06-02's verification.</resume-signal>
  <done>Human re-confirmed peft's legitimacy for this notebook's independent Colab kernel install before Task 3's install cell ran -- Package Legitimacy Gate satisfied for both notebooks' separate install sites.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| Source | ID | Item | Plan | Status | Notes |
|--------|-----|------|------|--------|-------|
| GOAL | -- | Phase 6 goal: "OpenVLA-OFT is fine-tuned on SOARM demonstrations via LoRA on Colab A100 and evaluated against a spatial benchmark, with before/after results tracked in WandB" | 06-01, 06-02, 06-03 | COVERED | Data conversion (06-01) -> training (06-02) -> benchmark+tracking (06-03) |
| REQ | TUNE-01 | HDF5 -> RLDS conversion | 06-01 | COVERED | `hdf5_to_rlds` + tests |
| REQ | TUNE-02 | LoRA r=32 fine-tuning on Colab A100 | 06-02 | COVERED | `finetune.py` torchrun invocation, Task 4 |
| REQ | TUNE-03 | Spatial vs non-spatial before/after benchmark | 06-03 | COVERED | Seeded `run_suite` before/after cells, Task 3 |
| REQ | TUNE-04 | Training + eval metrics tracked in WandB | 06-02, 06-03 | COVERED | Training curves (06-02 Task 4, native finetune.py hooks) + eval results (06-03 Task 3, manual wandb wrapper), same `soarm-oft-finetune-eval` project |
| RESEARCH | -- | Pattern 1: RLDS conversion as direct TFDS write | 06-01 | COVERED | Task 1 |
| RESEARCH | -- | Pattern 2: OXE registration via runtime monkeypatch | 06-02 | COVERED | Task 1 (oxe_register.py) |
| RESEARCH | -- | Pattern 3: resumable finetune.py + HF Hub checkpoint push | 06-02 | COVERED | Task 4 |
| RESEARCH | -- | Pattern 4: adapter reload without touching OFTBackend | 06-03 | COVERED | Task 2 (adapter_backend.py) |
| RESEARCH | -- | Pattern 5: seed-plumbed run_suite | 06-03 | COVERED | Task 1 |
| RESEARCH | -- | Pitfalls 1-7 | 06-01, 06-02, 06-03 | COVERED | Distributed per pitfall as cited in each task's read_first/action |
| RESEARCH | -- | Package Legitimacy Audit (peft/tensorflow-datasets/accelerate SUS) | 06-02, 06-03 | COVERED | Blocking checkpoints in both plans |
| CONTEXT | D-00 | LoRA r=32 locked upstream | 06-02 | COVERED | `--lora_rank 32 --use_lora True` |
| CONTEXT | D-01 | HF Hub private repo for adapter | 06-02, 06-03 | COVERED | `create_repo(..., private=True)` + assertion (06-02); adapter download (06-03) |
| CONTEXT | D-02 | Periodic intermediate checkpoints pushed | 06-02 | COVERED | `push_checkpoint_to_hub` polling loop |
| CONTEXT | D-03 | Timestamped HF Hub repo ids | 06-02 | COVERED | `HF_ADAPTER_REPO_ID` naming convention |
| CONTEXT | D-04 | Custom converter, not upstream builder tooling | 06-01 | COVERED | `rlds_converter.py`, direct TFDS write |
| CONTEXT | D-05 | One-time conversion script, committed output, not regenerated every run | 06-01, 06-02 | COVERED | CLI + skip-if-exists check in 06a's bootstrap |
| CONTEXT | D-06 | Testable RLDS converter module with unit tests | 06-01 | COVERED | 8 tests in test_rlds_converter.py |
| CONTEXT | D-07 | 20 episodes x 4 tasks (3 spatial + 1 non-spatial) | 06-03 | COVERED | `TASKS`/`EPISODES_PER_TASK` cell |
| CONTEXT | D-08 | Identical episode seeds before/after | 06-03 | COVERED | `EPISODE_SEEDS` reused in both cells; eval_loop.py seed plumbing (Task 1) |
| CONTEXT | D-09 | Full zero-shot re-run, not reused Phase 3 number | 06-03 | COVERED | BEFORE cell uses plain `OFTBackend`, fresh run |
| CONTEXT | D-10 | Resumability designed in from the start | 06-02 | COVERED | `--resume`/`--resume_step` documented relaunch procedure |
| CONTEXT | D-11 | Call finetune.py, not a custom training loop | 06-02 | COVERED | `torchrun ... vla-scripts/finetune.py` |
| CONTEXT | D-12 | Separate training and eval notebooks | 06-02, 06-03 | COVERED | `06a-finetune.ipynb` / `06b-eval.ipynb` |
| CONTEXT | D-13 | New dedicated WandB project | 06-02, 06-03 | COVERED | Shared `WANDB_PROJECT = "soarm-oft-finetune-eval"` constant |
| CONTEXT | (discretion) | RLDS output dir naming | 06-01 | COVERED | `LIBERO/libero/datasets/soarm_spatial/rlds/` |
| CONTEXT | (discretion) | finetune.py hyperparameters beyond r=32 | 06-02 | COVERED | batch_size=8, learning_rate=5e-4, save_freq=1000, per 06-RESEARCH.md's cited LIBERO.md example |
| CONTEXT | (discretion) | run_suite seed-plumbing approach | 06-03 | COVERED | Additive `seed`/`episode_seeds` kwargs (Pattern 5) |
| CONTEXT | (discretion) | WandB project naming | 06-02, 06-03 | COVERED | `soarm-oft-finetune-eval` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| pip/PyPI install (peft) -> this notebook's Colab kernel | Second independent install site for an already-verified SUS package (D-12: separate kernel from training) |
| HF Hub (read) -> Colab kernel | Downloads a real adapter + its stats file the researcher's own training run produced -- lower risk than the write-side (Plan 06-02), but still an external-content trust boundary |
| eval_loop.py's new seed plumbing -> env.seed() delegation | Additive change to an existing, previously-proven-correct eval path; incorrect placement would silently corrupt the before/after comparison's validity, not crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03-SC | Tampering (supply chain) | `pip install peft` (this notebook's separate kernel) | medium | mitigate | Task 4 is a blocking `checkpoint:human-verify` re-confirming the same already-verified package for this notebook's independent Colab kernel session (D-12); severity is medium (not high, like 06-02's first verification) since the package/version/origin were already fully investigated once this same phase. |
| T-06-03-01 | Tampering | `eval_loop.py`'s seed application placement | high | mitigate | Per Pitfall 7, applying `env.seed(seed)` once BEFORE the per-episode loop (instead of inside it) would silently make only episode 0 seeded correctly, invalidating D-08's identical-seed comparison without any crash or error. Mitigated by placing the call inside `run_episode` (called fresh per episode) and by `test_run_suite_applies_seeds_identically_across_two_runs` (Task 1) explicitly proving two independent `run_suite` calls with the same `episode_seeds` produce the identical `env.seed()` call sequence. |
| T-06-03-02 | Tampering | `FinetunedOFTBackend`'s norm_stats source (Pitfall 6) | high | mitigate | Silently reusing Phase 4's `dataset_statistics.json` (raw-HDF5 stats) instead of the adapter's own post-RLDS-conversion stats would produce a working-looking but numerically wrong "after" benchmark. Mitigated by `adapter_backend.py` fetching `dataset_statistics.json` from `adapter_repo_id` specifically (not `checkpoint`), proven via `test_init_merges_adapter_and_reapplies_dual_image_mode`'s distinct-fixture-content assertion, PLUS an explicit notebook-level divergence-check cell (Task 3) that surfaces (not silently assumes-away) any drift between the two stats sources. |
| T-06-03-03 | Information Disclosure | HF Hub adapter download (read-only) | low | accept | Read-only `snapshot_download`/`hf_hub_download` of the researcher's own private repo -- same established, already-audited pattern as the base checkpoint download (Phase 1/3); no new write capability, no new secret handling beyond the existing HF token read pattern. |

</threat_model>

<verification>
Run the local test suites and confirm green:
- `conda run -n libero pytest LIBERO/libero/libero/vla/test_eval_loop.py -x -q` (8/8, including 3 new seed-plumbing tests)
- `conda run -n libero pytest LIBERO/libero/libero/vla/test_adapter_backend.py -x -q` (2/2, FinetunedOFTBackend fake-injection tests)
- `conda run -n libero pytest LIBERO/libero/libero/vla -x -q` (full package regression: test_eval_loop + test_oft_backend + test_pi0_backend + test_adapter_backend, no new failures)
- Notebook JSON-validity + expected-cell-content check (automated `python -c` command above)

<human-check>
Colab GPU before/after benchmark run (TUNE-03, TUNE-04 -- manual-only per 06-VALIDATION.md, this project has no local GPU):
1. After Plan 06-02's training run completes and prints its final `HF_ADAPTER_REPO_ID`, paste it into `06b-eval.ipynb`'s `ADAPTER_REPO_ID` cell.
2. Run `06b-eval.ipynb` end-to-end on a Colab A100 (or T4, if bf16 inference-only fits) runtime through Block A, restart, Block B, task/language setup, WandB init, the BEFORE cell, the Pitfall-6 divergence-check cell, and the AFTER cell.
3. Confirm the before/after `wandb.Table` and aggregate scalars appear in `https://wandb.ai/<entity>/soarm-oft-finetune-eval` alongside Plan 06-02's training-loss curves (same project, per D-13) -- this is the specific manual check 06-VALIDATION.md flags as "WandB dashboard shows both training-loss curves AND eval success-rate results".
4. Visually confirm the divergence-check cell printed either a clean pass or a clearly-flagged WARNING (not a silent skip) for the two `dataset_statistics.json` sources.
This is the real-Colab confirmation source-level review and local tests cannot provide -- run it via the next `/gsd-verify-work`/UAT pass on this phase.
</human-check>
</verification>

<success_criteria>
- `eval_loop.py`'s seed plumbing is additive, backward-compatible, and proven correct (including the exact-placement requirement from Pitfall 7) via 3 new local tests.
- `FinetunedOFTBackend` reuses `OFTBackend`'s entire loading path unchanged, merges the adapter, and overlays the adapter's OWN norm_stats -- proven via 2 new local tests using the same fake-injection technique this project already established for `OFTBackend`.
- `06b-eval.ipynb` runs the before and after benchmarks under a byte-identical seeded protocol (same `TASKS`/`LANGUAGE_MAP`/`EPISODES_PER_TASK`/`EPISODE_SEEDS` Python variables, not re-typed values) and logs both to the shared `soarm-oft-finetune-eval` WandB project.
- The peft package-legitimacy re-verification checkpoint is completed before this notebook's install cell runs.
- The `<human-check>` Colab run (before/after tables in WandB, divergence-check result) is completed via the next verify-work/UAT pass.
</success_criteria>

<output>
Create `.planning/phases/06-fine-tuning-evaluation/06-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- Modified `LIBERO/libero/libero/vla/eval_loop.py`: `run_episode` gains `seed: int | None = None`; `run_suite` gains `episode_seeds: list | None = None`; both purely additive, backward-compatible.
- New file `LIBERO/libero/libero/vla/adapter_backend.py`: class `FinetunedOFTBackend(OFTBackend)` with `__init__(self, adapter_repo_id: str, checkpoint: str = CHECKPOINT, device: str = "cuda")`; `predict()` inherited unchanged.
- New file `LIBERO/libero/libero/vla/test_adapter_backend.py`.
- Modified `LIBERO/libero/libero/vla/__init__.py`: adds a third guarded `from .adapter_backend import FinetunedOFTBackend` block (degrades to `None` on import failure, same as `OFTBackend`/`Pi0Backend`).
- New file `LIBERO/notebooks/06b-eval.ipynb`: eval notebook (bootstrap, adapter download, seeded before/after `run_suite` benchmark, Pitfall-6 divergence check, WandB eval-results wrapper).
- New runtime constants: `EPISODE_SEEDS = list(range(20))`, `WANDB_PROJECT = "soarm-oft-finetune-eval"` (matches Plan 06-02's constant), `TASKS` (4-element D-07 task list).
