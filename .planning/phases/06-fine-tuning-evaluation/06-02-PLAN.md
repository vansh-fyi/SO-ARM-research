---
phase: 06-fine-tuning-evaluation
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - LIBERO/libero/libero/datasets/oxe_register.py
  - LIBERO/libero/libero/datasets/test_oxe_register.py
  - LIBERO/libero/libero/datasets/__init__.py
  - LIBERO/notebooks/06a-finetune.ipynb
autonomous: false
requirements: [TUNE-02, TUNE-04]
user_setup:
  - service: Hugging Face Hub (write access)
    why: "Push the LoRA adapter + periodic training checkpoints to a new private, timestamped HF Hub repo (D-01/D-02/D-03) -- reuses the existing HF token bootstrap (Drive file -> getpass, STATE.md 01-close), now used for WRITE not just read"
    env_vars: []
    dashboard_config: []
  - service: Weights & Biases
    why: "Log finetune.py's native training-loss curves to a new dedicated soarm-oft-finetune-eval project (D-13, TUNE-04), separate from the existing lifelong-learning wandb project"
    env_vars:
      - name: WANDB_API_KEY
        source: "wandb.ai -> Settings -> API keys (existing project convention, .planning/codebase/INTEGRATIONS.md)"
    dashboard_config: []

must_haves:
  truths:
    - "register_soarm_spatial() (and its apply_soarm_spatial_registration() Colab wrapper) inject a soarm_spatial entry into the installed openvla-oft package's OXE_DATASET_CONFIGS/OXE_NAMED_MIXTURES dicts before finetune.py's config machinery is imported, closing 06-RESEARCH.md's single highest-risk unknown (dataset registration) without editing the cloned package's files on disk (D-04, Pitfall 1)."
    - "06a-finetune.ipynb runs openvla-oft's own finetune.py via torchrun with --lora_rank 32 --use_lora True --merge_lora_during_training False (TUNE-02, D-00, D-11), and periodically pushes timestamped, private, resumable checkpoints to a new HF Hub repo during training (D-01/D-02/D-03/D-10)."
    - "finetune.py's native WandB hooks log training-loss curves to a new dedicated soarm-oft-finetune-eval project, not the existing lifelong-learning project (TUNE-04, D-13)."
    - "SUS-flagged packages (peft, tensorflow-datasets, accelerate) are never pip-installed without an explicit human legitimacy verification first (Package Legitimacy Gate)."
  artifacts:
    - LIBERO/libero/libero/datasets/oxe_register.py
    - LIBERO/libero/libero/datasets/test_oxe_register.py
    - LIBERO/notebooks/06a-finetune.ipynb
  key_links:
    - "06a-finetune.ipynb's bootstrap cell calls libero.datasets.rlds_converter.hdf5_to_rlds(...) (Plan 06-01) only if LIBERO/libero/datasets/soarm_spatial/rlds/soarm_spatial doesn't already exist on disk (D-05: one-time conversion, not regenerated every run)."
    - "06a-finetune.ipynb calls libero.datasets.oxe_register.apply_soarm_spatial_registration() in the SAME kernel, BEFORE any vla-scripts/finetune.py config import (Pitfall 1)."
    - "The HF Hub checkpoint-push helper's create_repo(..., private=True) call is the exact repo the eval notebook's FinetunedOFTBackend (Plan 06-03) downloads from via adapter_repo_id."
---

<objective>
Register the SOARM RLDS dataset into openvla-oft's OXE dataset registry via a runtime monkeypatch, then build the Colab A100 training notebook that runs `finetune.py`'s LoRA (r=32) fine-tuning loop to completion with resumability and periodic private HF Hub checkpoint pushes, with training-loss curves logged to a new dedicated WandB project (TUNE-02, TUNE-04, D-00/D-01/D-02/D-03/D-10/D-11/D-12/D-13).

Purpose: `finetune.py --dataset_name soarm_spatial` resolves through the installed package's `OXE_DATASET_CONFIGS`/`OXE_NAMED_MIXTURES` dicts -- there is no CLI flag to point at an unregistered dataset (06-RESEARCH.md's single highest-risk finding). This plan closes that gap and wires the rest of the resumable, checkpoint-persisted, WandB-tracked training loop around the already-tested RLDS converter from Plan 06-01.
Output: `oxe_register.py` (dict-injection registration, locally unit-tested without prismatic installed) and `06a-finetune.ipynb` (Colab bootstrap + RLDS-conversion-if-needed + registration + finetune.py torchrun invocation + resumable HF Hub checkpoint push + WandB training config).
</objective>

<execution_context>
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/workflows/execute-plan.md
@/Users/hp/Desktop/Work/Repositories/SoARM-Research/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-fine-tuning-evaluation/06-CONTEXT.md
@.planning/phases/06-fine-tuning-evaluation/06-RESEARCH.md
@.planning/phases/06-fine-tuning-evaluation/06-PATTERNS.md
@.planning/phases/06-fine-tuning-evaluation/06-VALIDATION.md
@.planning/phases/06-fine-tuning-evaluation/06-01-SUMMARY.md
@LIBERO/libero/libero/vla/oft_backend.py
@LIBERO/notebooks/01-colab-env-setup.ipynb
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: oxe_register.py -- injectable OXE dict registration, unit-testable without prismatic installed (D-04, Pitfall 1)</name>
  <files>LIBERO/libero/libero/datasets/oxe_register.py, LIBERO/libero/libero/datasets/test_oxe_register.py, LIBERO/libero/libero/datasets/__init__.py</files>
  <read_first>
    LIBERO/libero/libero/vla/oft_backend.py (lines 32-78, `_prismatic_ok`/`_ensure_prismatic` -- the guarded external-package import + fail-loudly-with-traceback pattern this module's Colab-facing wrapper must mirror).
    .planning/phases/06-fine-tuning-evaluation/06-RESEARCH.md (Pattern 2 "Dataset registration via runtime monkeypatch, not a source edit" -- the exact `OXE_DATASET_CONFIGS`/`OXE_NAMED_MIXTURES` entry shape to inject; Pitfall 1 "silently fails to find an unregistered dataset"; Assumptions Log A1, `state_encoding: StateEncoding.POS_EULER` flagged as an unverified placeholder for this project's joint-space proprio).
    .planning/phases/06-fine-tuning-evaluation/06-01-SUMMARY.md (rlds_converter.py's final `hdf5_to_rlds` signature and output dataset_name convention -- oxe_register.py's registered dataset_name must match exactly).
    LIBERO/libero/libero/datasets/test_normalization.py (whole file, 143 lines) -- import-path bootstrap lines 21-28 (`_REPO_ROOT`/`_LIBERO_LIBERO` sys.path insertion, copy verbatim at the same directory depth; this project's test_rlds_converter.py, Plan 06-01 Task 2, already reused this same bootstrap).
  </read_first>
  <behavior>
    - `register_soarm_spatial(oxe_dataset_configs: dict, oxe_named_mixtures: dict, state_encoding, action_encoding, dataset_name: str = "soarm_spatial") -> None` is pure dict mutation with NO external imports -- callable and testable with plain empty dicts and sentinel string values for `state_encoding`/`action_encoding`, no `prismatic` package required. After calling it: `oxe_dataset_configs[dataset_name]["image_obs_keys"] == {"primary": "agentview_rgb", "secondary": None, "wrist": "eye_in_hand_rgb"}`, `oxe_dataset_configs[dataset_name]["state_obs_keys"] == ["state", None, None]`, `oxe_dataset_configs[dataset_name]["state_encoding"] is state_encoding` (the exact object passed in, not a copy), `oxe_dataset_configs[dataset_name]["action_encoding"] is action_encoding`, and `oxe_named_mixtures[dataset_name] == [(dataset_name, 1.0)]`.
    - `apply_soarm_spatial_registration(dataset_name: str = "soarm_spatial") -> dict` imports `prismatic.vla.datasets.rlds.oxe.configs.{OXE_DATASET_CONFIGS, StateEncoding, ActionEncoding}` and `prismatic.vla.datasets.rlds.oxe.mixtures.OXE_NAMED_MIXTURES` from the INSTALLED package, calls `register_soarm_spatial` on the real dicts using `StateEncoding.POS_EULER`/`ActionEncoding.EEF_POS`, and returns the registered config dict. Since `prismatic` genuinely is not installed in this project's local libero conda env, calling this function locally raises `RuntimeError` with a traceback already printed (mirrors `_ensure_prismatic`'s fail-loud contract) -- this is itself a real, unmocked local test (no monkeypatching required to prove the guard fires).
  </behavior>
  <action>
    Create `oxe_register.py`. At module top: only stdlib (`from __future__ import annotations`; no `prismatic`/`tensorflow` import at column 0 -- matches this package's other modules' local-import convention). Implement `register_soarm_spatial(oxe_dataset_configs, oxe_named_mixtures, state_encoding, action_encoding, dataset_name="soarm_spatial") -> None` exactly per the Behavior spec's dict shape -- this function takes the two registry dicts AND the two encoding enum values as plain parameters (dependency injection) specifically so it has zero import-time coupling to `prismatic` and is fully unit-testable with bare dicts/sentinels.

    Implement `apply_soarm_spatial_registration(dataset_name="soarm_spatial") -> dict` as the Colab-facing wrapper: `try: from prismatic.vla.datasets.rlds.oxe.configs import OXE_DATASET_CONFIGS, StateEncoding, ActionEncoding; from prismatic.vla.datasets.rlds.oxe.mixtures import OXE_NAMED_MIXTURES` -- on any exception, `import traceback; traceback.print_exc()` then `raise RuntimeError("prismatic OXE registry import failed -- see traceback above; run this only inside the training notebook's Colab kernel after openvla-oft/prismatic is installed")`, mirroring `_ensure_prismatic`'s exact fail-loud shape (print full traceback, then raise a clear, actionable RuntimeError -- never swallow). On success, call `register_soarm_spatial(OXE_DATASET_CONFIGS, OXE_NAMED_MIXTURES, StateEncoding.POS_EULER, ActionEncoding.EEF_POS, dataset_name=dataset_name)` with an inline comment flagging `StateEncoding.POS_EULER` as an ASSUMED placeholder for this project's joint-space (not EEF-pose) proprio per 06-RESEARCH.md Assumption A1 and Open Question 1 -- the training notebook's own smoke-test cell (Task 3) is what actually confirms this live. Return `OXE_DATASET_CONFIGS[dataset_name]`.

    Update `LIBERO/libero/libero/datasets/__init__.py`: append `from .oxe_register import register_soarm_spatial, apply_soarm_spatial_registration` to the SAME unconditional-import section as `normalization`/`rlds_converter` (Plan 06-01) -- `oxe_register.py` has zero import-time dependency on prismatic/tensorflow either, so it must always import cleanly for the local no-sim pytest suite too.

    Create `test_oxe_register.py` in the same task (self-contained per-task verification, mirroring Plan 06-01 Task 2's and Plan 06-03's TDD tasks) with the same `_REPO_ROOT`/`_LIBERO_LIBERO` sys.path bootstrap as `test_normalization.py` lines 21-28 (identical relative depth, this file lives in the same directory). Import `from libero.datasets.oxe_register import register_soarm_spatial, apply_soarm_spatial_registration`. Implement `test_register_soarm_spatial_injects_expected_dict_shape`: build two distinct sentinel objects via plain `object()` instances (standing in for `state_encoding`/`action_encoding` with no `prismatic` import needed), call `register_soarm_spatial({}, {}, state_encoding=<sentinel_1>, action_encoding=<sentinel_2>)` against fresh empty dicts, then assert every dict-shape expectation from the Behavior spec: `oxe_dataset_configs["soarm_spatial"]["image_obs_keys"] == {"primary": "agentview_rgb", "secondary": None, "wrist": "eye_in_hand_rgb"}`, `["state_obs_keys"] == ["state", None, None]`, `["state_encoding"] is <sentinel_1>` (exact object identity, not equality), `["action_encoding"] is <sentinel_2>`, and `oxe_named_mixtures["soarm_spatial"] == [("soarm_spatial", 1.0)]`. Implement `test_register_soarm_spatial_uses_custom_dataset_name`: call with `dataset_name="custom_name"` against fresh empty dicts and assert the injected key is `"custom_name"` (not the `"soarm_spatial"` default) in both `oxe_dataset_configs` and `oxe_named_mixtures`. Implement `test_apply_soarm_spatial_registration_fails_loud_without_prismatic`: since this project's local `libero` conda env genuinely has no `prismatic` installed (confirmed, no mocking needed), call `apply_soarm_spatial_registration()` directly inside `pytest.raises(RuntimeError, match="prismatic OXE registry import failed")`, and use `capsys.readouterr()` to assert the captured output contains `"Traceback"`, proving the `traceback.print_exc()`-then-`raise` fail-loud contract fires end-to-end (mirrors `_ensure_prismatic`'s real, unmocked local-failure test).
  </action>
  <verify>
    <automated>conda run -n libero pytest LIBERO/libero/libero/datasets/test_oxe_register.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `conda run -n libero pytest LIBERO/libero/libero/datasets/test_oxe_register.py -x -q` exits 0
    - `grep -q "def register_soarm_spatial" LIBERO/libero/libero/datasets/oxe_register.py` matches
    - `grep -q "def apply_soarm_spatial_registration" LIBERO/libero/libero/datasets/oxe_register.py` matches
    - `grep -qc "^import prismatic\|^from prismatic" LIBERO/libero/libero/datasets/oxe_register.py` returns no match at column 0 (prismatic import is confined inside `apply_soarm_spatial_registration`'s try block, not at module top)
    - `conda run -n libero python -c "from libero.datasets.oxe_register import register_soarm_spatial, apply_soarm_spatial_registration"` (run from `LIBERO/libero/`) succeeds with no import error
  </acceptance_criteria>
  <done>oxe_register.py provides register_soarm_spatial (pure, injectable, unit-tested with plain dicts) and apply_soarm_spatial_registration (Colab-facing guarded wrapper that fails loudly with a traceback when prismatic isn't installed -- proven by a real local test, not a mock); __init__.py exports both unconditionally.</done>
</task>

<task type="auto">
  <name>Task 2: 06a-finetune.ipynb Part A -- Colab bootstrap: GPU check, package installs, RLDS conversion, OXE registration, HF Hub + WandB auth (D-05, Pitfall 1, Pitfall 4)</name>
  <files>LIBERO/notebooks/06a-finetune.ipynb</files>
  <read_first>
    LIBERO/notebooks/01-colab-env-setup.ipynb (Block A cell structure -- GPU check cell, apt/pip install cell ordering, restart-runtime warning cell, EGL bootstrap pattern; the established "protobuf pinned last" install-order convention from Pitfall 4 / STATE.md 01-close).
    .planning/phases/06-fine-tuning-evaluation/06-RESEARCH.md (Standard Stack Installation block -- exact `peft==0.20.0 tensorflow-datasets==4.9.10 accelerate` install command; Pitfall 4 "protobuf pin dance"; Package Legitimacy Audit table -- peft/tensorflow-datasets/accelerate all SUS).
    .planning/phases/06-fine-tuning-evaluation/06-01-SUMMARY.md (rlds_converter.py's CLI flags and default `--out-dir`/`--dataset-name` values -- this notebook's conversion-if-needed cell must call it with matching defaults).
    STATE.md (01-close accumulated-context notes -- HF token bootstrap: Drive file `MyDrive/SoARM-Research/.hf_token` -> `getpass()`, reusable verbatim for this notebook's HF Hub auth cell).
  </read_first>
  <action>
    Create `LIBERO/notebooks/06a-finetune.ipynb` as a fresh notebook (new file). Markdown header cell: "# Phase 6a: OpenVLA-OFT LoRA Fine-Tuning (TUNE-02)", stating this notebook converts (if needed) the Phase 4 dataset to RLDS, registers it with openvla-oft's OXE registry, and runs `finetune.py`'s LoRA r=32 training loop with resumable HF Hub checkpoint pushes and WandB logging -- requires a Colab A100 runtime (bf16, no quantization per D-00's locked LoRA-not-QLoRA choice).

    Cell: GPU assertion mirroring Notebook 01's pattern -- `import torch; assert torch.cuda.is_available()`, print `torch.cuda.get_device_name(0)`, and print (not raise, since Colab tier availability varies) a loud warning if the device name does not contain `"A100"`, stating this phase's roadmap-locked A100 requirement.

    Cell: repo delivery -- reuse this project's established GitHub-clone + getpass(GITHUB_TOKEN) delivery cell (identical to Notebooks 01-03's/05's delivery cells, per `260802-ijb`/`260802-itm` STATE.md quick-task notes), setting `REPO_ROOT`.

    Markdown cell: "## Block A: Install" stating this notebook needs Phase 1's baked environment PLUS three new packages this phase introduces: `peft` (LoRA), `tensorflow-datasets` (RLDS loading), and (conditionally) `accelerate`. Cell: `pip install peft==0.20.0 tensorflow-datasets==4.9.10 --no-deps` (per 06-RESEARCH.md's Standard Stack install command; verify `--no-deps` necessity live -- `peft`/`tensorflow-datasets` have normal dependency trees unlike `dlimp`/`openvla-oft`, so drop `--no-deps` if it causes missing transitive imports) followed by an attempted `import accelerate` check; only `pip install accelerate==1.14.0` if that import fails (06-RESEARCH.md Open Question 2's recommendation -- avoid an unnecessary install for a possibly-unused package). Final cell in this install sequence: `pip install -U protobuf` as the LAST pip operation (Pitfall 4 -- `tensorflow-datasets` is another protobuf consumer, same class of risk as Phase 1's `tensorflow_graphics`/openvla-oft chain; mirror Phase 1 Block A/5b's exact ordering). Markdown cell: "*** STOP -- Restart runtime now ***" identical wording/warning to prior notebooks' restart-not-delete distinction.

    Post-restart cells (Block B, mirroring Notebook 01/02/03's established order): EGL bootstrap (`MUJOCO_GL=egl` env var set before any MuJoCo import, NVIDIA ICD JSON creation), `~/.libero/config.yaml` bootstrap, `sys.path` insertion of `REPO_ROOT` and the LIBERO package root, matplotlib `Agg` backend + numba shim cell -- identical to prior notebooks' Block B.

    Markdown cell: "## RLDS Conversion (TUNE-01, D-05 -- one-time, skip if already converted)". Cell: `import os; from libero.datasets.rlds_converter import hdf5_to_rlds; RLDS_OUT_DIR = "LIBERO/libero/datasets/soarm_spatial/rlds/"; RLDS_DATASET_NAME = "soarm_spatial"; if not os.path.isdir(os.path.join(RLDS_OUT_DIR, RLDS_DATASET_NAME)):` then glob `LIBERO/libero/datasets/soarm_spatial/*_demo.hdf5`, call `hdf5_to_rlds(paths, RLDS_OUT_DIR, dataset_name=RLDS_DATASET_NAME)`, print `f"converted -> {RLDS_OUT_DIR}{RLDS_DATASET_NAME}/"` (arrow-notation) -- else print a "already converted, skipping" message (D-05's explicit "not regenerated inline every run" requirement).

    Markdown cell: "## OXE Dataset Registration (Pitfall 1 -- must run BEFORE any finetune.py config import)". Cell: `from libero.datasets.oxe_register import apply_soarm_spatial_registration; cfg = apply_soarm_spatial_registration(RLDS_DATASET_NAME); print(f"registered {RLDS_DATASET_NAME}: {cfg}")` -- this IS Pitfall 1's recommended smoke-test cell (resolves and prints the config before the full `torchrun` launch).

    Cell: HF Hub auth -- reuse the exact Drive-file-bootstrap-then-getpass pattern from STATE.md's 01-close note (`MyDrive/SoARM-Research/.hf_token` if present, else `getpass()` prompt), then `from huggingface_hub import login; login(token=hf_token)`. Cell: WandB auth -- `import os, wandb; wandb.login(key=os.environ.get("WANDB_API_KEY") or getpass.getpass("WandB API key: "))`, matching the `WANDB_API_KEY` env var convention from `.planning/codebase/INTEGRATIONS.md`.
  </action>
  <verify>
    <automated>python -c "import json; nb = json.load(open('LIBERO/notebooks/06a-finetune.ipynb')); assert nb['cells'], 'notebook has no cells'; src = ''.join(c for cell in nb['cells'] for c in cell.get('source', [])); assert 'hdf5_to_rlds' in src and 'apply_soarm_spatial_registration' in src and 'peft==0.20.0' in src and 'protobuf' in src, 'missing expected cell content'"</automated>
  </verify>
  <acceptance_criteria>
    - `LIBERO/notebooks/06a-finetune.ipynb` parses as valid JSON (`json.load` succeeds)
    - Notebook source contains a cell calling `hdf5_to_rlds` guarded by an `os.path.isdir` check (D-05 skip-if-already-converted)
    - Notebook source contains a cell calling `apply_soarm_spatial_registration` positioned before any `finetune.py`/`vla-scripts` reference
    - Notebook source contains the `pip install -U protobuf` cell as the last install operation in Block A
  </acceptance_criteria>
  <done>06a-finetune.ipynb's bootstrap sequence (GPU check, delivery, package installs with protobuf-last ordering, restart, Block B, RLDS-conversion-if-needed, OXE registration smoke test, HF Hub + WandB auth) is complete and self-consistent, ready for Task 4's finetune.py invocation cells.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Human verifies peft/tensorflow-datasets/accelerate package legitimacy before Task 2's install cell runs</name>
  <read_first>
    - .planning/phases/06-fine-tuning-evaluation/06-RESEARCH.md (Package Legitimacy Audit table -- peft/tensorflow-datasets/accelerate SUS disposition and rationale: "too-new"/"unknown-downloads" legitimacy-seam heuristics, not any structural red flag)
  </read_first>
  <action>Blocking human verification per the Package Legitimacy Gate protocol (peft, tensorflow-datasets, and accelerate all flagged SUS by 06-RESEARCH.md's audit). Present the how-to-verify steps below to the user, wait for the resume signal, and do not run Task 2's `pip install peft==0.20.0 tensorflow-datasets==4.9.10 --no-deps` cell (or the conditional `accelerate` install) on Colab until the user confirms each package's origin. This checkpoint is never auto-approvable regardless of workflow.auto_advance.</action>
  <what-built>Task 2's notebook cell that installs `peft==0.20.0`, `tensorflow-datasets==4.9.10`, and (conditionally) `accelerate==1.14.0` from PyPI -- all three flagged SUS by the Package Legitimacy Audit (unknown download counts, no structural red flags found).</what-built>
  <how-to-verify>
    1. Run `pip index versions peft` / check `https://pypi.org/project/peft/` and confirm the project-url/homepage metadata points to `github.com/huggingface/peft`.
    2. Run `pip index versions tensorflow-datasets` / check `https://pypi.org/project/tensorflow-datasets/` and confirm it points to `github.com/tensorflow/datasets`.
    3. If the `accelerate` conditional install fires, check `https://pypi.org/project/accelerate/` and confirm it points to `github.com/huggingface/accelerate`.
    4. After installing on Colab, run `pip show peft tensorflow-datasets` (and `accelerate` if installed) and confirm the `Home-page`/`Author` fields match the official HuggingFace/TensorFlow orgs, not an impostor publisher.
  </how-to-verify>
  <acceptance_criteria>
    - `pip show peft` output's Home-page references `github.com/huggingface/peft`
    - `pip show tensorflow-datasets` output's Home-page references `github.com/tensorflow/datasets`
  </acceptance_criteria>
  <resume-signal>Type "approved" once all installed packages' metadata confirms the official HuggingFace/TensorFlow origin, or "blocked" with the observed metadata if any looks like an impostor package.</resume-signal>
  <done>Human confirmed peft/tensorflow-datasets(/accelerate)'s PyPI metadata matches their official upstream orgs before Task 2's install cell ran on Colab -- Package Legitimacy Gate satisfied for all SUS-flagged dependencies this plan introduces.</done>
</task>

<task type="auto">
  <name>Task 4: 06a-finetune.ipynb Part B -- finetune.py torchrun invocation, resumability, periodic private HF Hub checkpoint push, WandB training config (TUNE-02, TUNE-04, D-00/D-01/D-02/D-03/D-10/D-11/D-13)</name>
  <files>LIBERO/notebooks/06a-finetune.ipynb</files>
  <read_first>
    .planning/phases/06-fine-tuning-evaluation/06-RESEARCH.md (Pattern 3 "Resumable finetune.py invocation with lightweight checkpoint pushes" -- the exact `torchrun`/flag list and the `create_repo`/`upload_folder` push snippet; Pitfall 5 "save_latest_checkpoint_only=True loses more than expected"; Security Domain -- HF Hub write-token leakage risk + `private=True` must-verify requirement).
  </read_first>
  <action>
    Markdown cell: "## finetune.py: LoRA r=32 Fine-Tuning (TUNE-02, D-00, D-11)" stating the exact locked hyperparameter (`--lora_rank 32`) and Claude's-discretion hyperparameters below are informed by openvla-oft's own documented LIBERO.md example.

    Cell: define `RUN_TIMESTAMP = datetime.now().strftime("%Y%m%d-%H%M%S")` (import `datetime`), `RUN_ROOT_DIR = f"/content/soarm_ft_runs/{RUN_TIMESTAMP}"`, `HF_ADAPTER_REPO_ID = f"<hf-username>/soarm-oft-lora-{RUN_TIMESTAMP}"` (D-03: timestamped, not overwritten), `WANDB_PROJECT = "soarm-oft-finetune-eval"` (D-13: new dedicated project, shared with Plan 06-03's eval notebook so both training curves and eval results land in the same dashboard project).

    Cell: build and run the `torchrun` command exactly per 06-RESEARCH.md Pattern 3: `torchrun --standalone --nnodes 1 --nproc-per-node 1 vla-scripts/finetune.py --vla_path moojink/openvla-7b-oft-finetuned-libero-spatial --data_root_dir {RLDS_OUT_DIR} --dataset_name {RLDS_DATASET_NAME} --run_root_dir {RUN_ROOT_DIR} --use_l1_regression True --use_diffusion False --use_film False --num_images_in_input 2 --use_proprio True --lora_rank 32 --use_lora True --merge_lora_during_training False --save_freq 1000 --save_latest_checkpoint_only False --batch_size 8 --learning_rate 5e-4 --wandb_entity <entity> --wandb_project {WANDB_PROJECT}` -- run via `subprocess.run` or a `!torchrun ...` shell-magic cell (match this project's existing notebook convention for long-running subprocess launches, e.g. `serve_policy.py`'s handling in Notebook 03b). `--merge_lora_during_training False` is a deliberate override of the library's `True` default (06-RESEARCH.md's key optimization -- avoids re-saving a ~14-16GB merged model at every checkpoint; D-01 only needs the small adapter). `--save_latest_checkpoint_only False` is likewise a deliberate override (Pitfall 5 -- keeps timestamped `--{step}_chkpt` directories so a mid-write disconnect corruption still leaves a prior complete checkpoint to resume from, directly supporting D-02/D-10). Add a markdown note documenting the relaunch-after-disconnect procedure: re-run this cell with `--resume True --resume_step <last_completed_step>` appended (D-10), where `<last_completed_step>` is read from the last successfully HF-Hub-pushed checkpoint directory name.

    Cell: define `push_checkpoint_to_hub(checkpoint_dir: str, repo_id: str = HF_ADAPTER_REPO_ID) -> None` -- `from huggingface_hub import create_repo, upload_folder, HfApi`; `create_repo(repo_id, private=True, exist_ok=True)`; immediately after, `assert HfApi().repo_info(repo_id).private is True, "HF Hub repo is not private -- aborting push"` (explicit code-level verification per ASVS Security Domain -- `create_repo` defaults to public if the flag is ever omitted or misapplied, so this assertion must be a hard gate, not just a call-site comment); then `upload_folder(repo_id=repo_id, folder_path=checkpoint_dir, run_as_future=True)` (non-blocking per 06-RESEARCH.md Pattern 3); print `f"pushed {checkpoint_dir} -> hf.co/{repo_id}"` (arrow-notation). Cell: a simple polling loop (runs after/alongside the `torchrun` launch, or as a periodic cell the researcher re-runs) that lists `RUN_ROOT_DIR` for new `*_chkpt` directories not yet pushed (track pushed dirs in a local `set()`), calls `push_checkpoint_to_hub` on each new one, and after a successful push deletes all but the 2 most-recently-pushed local checkpoint directories to manage Colab disk usage (06-RESEARCH.md Open Question 3's recommendation).

    Final markdown cell: "## Phase 6a Summary" with a table (columns: Item, Status `☐`, Notes) listing: TUNE-02 (finetune.py completed without crashing -- fill in after running), Resume-from-checkpoint verified (kill+relaunch mid-run, WandB step continuity -- fill in after running), TUNE-04 training curves (WandB project link: `f"https://wandb.ai/<entity>/{WANDB_PROJECT}"`), Final adapter HF Hub repo id (`HF_ADAPTER_REPO_ID` -- this is the exact value Plan 06-03's eval notebook needs as `adapter_repo_id`).
  </action>
  <verify>
    <automated>python -c "import json; nb = json.load(open('LIBERO/notebooks/06a-finetune.ipynb')); src = ''.join(c for cell in nb['cells'] for c in cell.get('source', [])); assert '--lora_rank 32' in src and '--merge_lora_during_training False' in src and '--save_latest_checkpoint_only False' in src and 'private=True' in src and 'repo_info(repo_id).private is True' in src, 'missing expected training/push cell content'"</automated>
  </verify>
  <acceptance_criteria>
    - `LIBERO/notebooks/06a-finetune.ipynb` parses as valid JSON
    - Notebook source contains the exact flags `--lora_rank 32`, `--use_lora True`, `--merge_lora_during_training False`, `--save_latest_checkpoint_only False`
    - Notebook source contains `create_repo(` with `private=True` AND a following `assert ... .private is True` verification line (not just the create_repo call alone)
    - Notebook source contains `WANDB_PROJECT = "soarm-oft-finetune-eval"` (or an equivalent single dedicated-project constant reused by both the torchrun `--wandb_project` flag and the summary cell's dashboard link)
  </acceptance_criteria>
  <done>06a-finetune.ipynb's finetune.py invocation cell runs LoRA r=32 training with merge-during-training disabled and timestamped non-overwritten checkpoints; a push helper uploads each new checkpoint to a private, verified HF Hub repo non-blockingly; a documented resume procedure exists for relaunch after a Colab disconnect; WandB training-loss curves target the new soarm-oft-finetune-eval project.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| pip/PyPI install (peft, tensorflow-datasets, accelerate) -> Colab kernel | Untrusted third-party code execution surface; SUS-flagged by the Package Legitimacy Audit |
| Colab kernel -> HF Hub (write) | New WRITE-capable token usage (prior phases only ever read); a leaked write token has a materially higher blast radius (can push/delete repos) than the prior read-only usage |
| oxe_register.py -> installed openvla-oft package's in-memory OXE registry dicts | In-process monkeypatch of a third-party package's module-level state; incorrect values (Assumption A1) could silently corrupt what the model trains on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02-SC | Tampering (supply chain) | `pip install peft/tensorflow-datasets/accelerate` | high | mitigate | All three SUS-flagged by the Package Legitimacy Audit. Task 3 is a blocking `checkpoint:human-verify` (never auto-approvable) requiring the human to confirm each package's PyPI metadata matches its official HuggingFace/TensorFlow org before Task 2's install cell is ever run on Colab. |
| T-06-02-01 | Information Disclosure | HF Hub token (write-capable, Task 2's auth cell) | high | mitigate | Reuses the existing `getpass()`-only bootstrap pattern (never `userdata.get()`-persisted, never hardcoded in notebook source, per `260802-itm`) -- now write-capable, so a leaked token could push/delete HF Hub repos, not just read them. No change to the delivery mechanism itself; risk is explicitly re-flagged here since the blast radius changed. |
| T-06-02-02 | Information Disclosure | HF Hub repo visibility (Task 4's `create_repo`) | high | mitigate | `create_repo(..., private=True)` is followed by an explicit `assert HfApi().repo_info(repo_id).private is True` code-level check (ASVS V6/Security Domain) -- `create_repo` silently defaults to public if the flag is ever omitted or dropped in a future edit; the assertion turns that class of regression into an immediate crash instead of a silent data leak. |
| T-06-02-03 | Tampering | `oxe_register.py`'s injected `state_encoding`/`action_encoding` values (Assumption A1) | medium | mitigate | `apply_soarm_spatial_registration`'s smoke-test print (Task 2) surfaces the registered config before the full `torchrun` launch, and Task 1's unit tests prove the dict-mutation logic itself is correct given ANY encoding values -- the specific enum choice (`StateEncoding.POS_EULER`) remains a flagged, Colab-verification-pending assumption per 06-RESEARCH.md Open Question 1, not silently treated as certain. |
| T-06-02-04 | Tampering | Checkpoint corruption on Colab disconnect (Pitfall 5) | medium | mitigate | `--save_latest_checkpoint_only False` (Task 4) keeps timestamped checkpoint directories instead of overwriting a single one in place, so a mid-write disconnect leaves a prior complete checkpoint available to resume from. |

</threat_model>

<verification>
Run the local test suite and confirm green:
- `conda run -n libero pytest LIBERO/libero/libero/datasets/test_oxe_register.py -x -q` (register_soarm_spatial dict-injection tests + the real fail-loud-without-prismatic test)
- `conda run -n libero pytest LIBERO/libero/libero/datasets -x -q` (full package regression, no new failures)
- Notebook JSON-validity + expected-cell-content checks (both automated `python -c` commands above)

<human-check>
Colab GPU training run (TUNE-02, TUNE-04, D-10 -- manual-only per 06-VALIDATION.md, this project has no local GPU):
1. Run `06a-finetune.ipynb` end-to-end on a Colab A100 runtime through Block A, restart, Block B, RLDS conversion, OXE registration smoke test, HF Hub/WandB auth, and the `finetune.py` torchrun cell.
2. Confirm `finetune.py` runs to completion without crashing (exit code / final log line check).
3. Confirm at least one checkpoint push succeeds: `HfApi().repo_info(HF_ADAPTER_REPO_ID).private is True` and the repo is visible under the researcher's HF Hub account.
4. Simulate a disconnect: interrupt the kernel mid-training, relaunch the torchrun cell with `--resume True --resume_step <N>`, and confirm the WandB run's step counter continues from `<N>` rather than resetting to 0.
5. Confirm the WandB dashboard (`https://wandb.ai/<entity>/soarm-oft-finetune-eval`) shows training-loss curves for this run.
This is the real-Colab confirmation source-level review and local tests cannot provide -- run it via the next `/gsd-verify-work`/UAT pass on this phase.
</human-check>
</verification>

<success_criteria>
- `oxe_register.py`'s dict-injection logic is proven correct via local unit tests (no prismatic needed), and its Colab-facing wrapper is proven to fail loudly (not silently) when prismatic is absent.
- `06a-finetune.ipynb` implements the full bootstrap -> conversion-if-needed -> registration -> LoRA r=32 training -> resumable checkpoint push -> WandB pipeline with all flags/values specified above present in its source.
- All SUS-flagged package installs (peft, tensorflow-datasets, accelerate) are gated behind a blocking human-verify checkpoint that never auto-approves.
- HF Hub repo privacy (`private=True`) is enforced with an explicit code-level assertion, not just a call-site argument.
- The `<human-check>` Colab run (training completion, resume-from-checkpoint, WandB training curves) is completed via the next verify-work/UAT pass.
</success_criteria>

<output>
Create `.planning/phases/06-fine-tuning-evaluation/06-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- New file `LIBERO/libero/libero/datasets/oxe_register.py`: functions `register_soarm_spatial(oxe_dataset_configs, oxe_named_mixtures, state_encoding, action_encoding, dataset_name="soarm_spatial") -> None`, `apply_soarm_spatial_registration(dataset_name="soarm_spatial") -> dict`.
- New file `LIBERO/libero/libero/datasets/test_oxe_register.py`.
- Modified `LIBERO/libero/libero/datasets/__init__.py`: adds `from .oxe_register import register_soarm_spatial, apply_soarm_spatial_registration`.
- New file `LIBERO/notebooks/06a-finetune.ipynb`: full training notebook (bootstrap, RLDS conversion-if-needed, OXE registration, `finetune.py` torchrun invocation, `push_checkpoint_to_hub` helper, WandB config).
- New runtime constants used across this notebook and consumed by Plan 06-03: `WANDB_PROJECT = "soarm-oft-finetune-eval"`, `HF_ADAPTER_REPO_ID` naming convention `f"<hf-username>/soarm-oft-lora-{RUN_TIMESTAMP}"`.